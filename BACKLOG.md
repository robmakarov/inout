# BACKLOG — bugs, evidence, ideas. For agents. The roadmap lives in `.ai/TASKS`; done items are deleted on sight.
Dump anything at the top of Inbox. Whoever picks it up triages it (lane, severity) or promotes it to a task id and leaves a one-line pointer here.

## Inbox
- [2026-09-05, ROBERT: "90 minute max take render so fucking long, longer than video itself" — **J9 SHIPPED THE TIME ON TOP; THE PHYSICS BELOW IT IS UNCHANGED AND CLOSED**] The 81 minutes are real: max60 is 3024x1964 at 60 = 356 Mpx/s of output against an engine measured at 440-470, every axis closed 2026-09-04 (seven measurements, one of them native ffmpeg at HALF Chrome's speed). What J9 found and fixed is the time ABOVE it — three ways the export made the same picture twice, all length-dependent, which is why every rig run and every short take was green. (a) A chunk vanishing between the missing-set and the concatenation came out as OPFS's **NotFoundError**, which is not a ChunkedRenderUnavailable, so the press did not fall back to anything — it FAILED after the whole render. (b) Every other refusal fell back to `renderExport` from frame zero: a second ~81-minute generation over one lost 2.5 s piece. `renderChunkedResuming` re-enters once instead. (c) `App.tsx` sweeps with NO keep set (a boot has no plan to pass), so a second window, a recovery check or an agent reading a take could delete the chunks a live export was concatenating; a render now leaves a heartbeat claim and the sweep stands down. PROVED ON THE ENCODER, not argued: `exp chunkrender` lane j9 — same theft staged mid-concat, **pre-J9 FAILED, now 1 of 12 chunks re-rendered, 900 video packets of 900**, and a boot sweep landing mid-export **STOOD DOWN**. Two earlier writings of that lane staged nothing and the rig said so (deleting a chunk BEFORE the export is an ordinary cache miss; sorting keys drew the audio artifact) — kept in the lane's header. **WHAT IS STILL OPEN AND IS THE SAME DEFECT CLASS**: `sweepPrerenderBlobs` (prerender.ts) still claims by module memory, so a second tab still wipes a finished PRE-RENDER — on a long take that is a whole wasted generation, exactly like (c) was. It wants the same treatment (claim by record) and is the next hour to find. Also unmeasured: whether Chrome GRANTS durable storage in Robert's own profile — J9 asks (`navigator.storage.persist()`), prod answered "best-effort" in a fresh temp profile, which is Chrome's documented behaviour with no engagement signals and says nothing about his installed one.
- [P0 — B15 TOOK THE FIRST HALF 2026-09-05; THE DEATH ITSELF IS CHROME'S AND THE LAB CANNOT REACH IT] **TAB/SYSTEM AUDIO DIES MID-TAKE ON A WHOLE-SCREEN SHARE, AND UNTIL NOW THE PRODUCT SAID NOTHING WHILE IT HAPPENED.** Robert 2026-09-04: "i made max record, 5:51, tab audio died after 2:36, it was working before for some seconds although nothing at all showed in beatrate, and died excatly in moment i switched youtube video from one to another - that happened before". **THE AUTOPSY, OFF HIS OWN DISK** (`rec_tcjr3v2cskgd`, 350,872 ms, quality `max`, four channels all full length — screen 3024x1964 avg 55.1 fps · camera 1280x720 avg 29.5 fps · mic mono opus · system-audio stereo opus 398,748 B): pure digital zeros 0.007 → 145.187 s (max_volume −91.0 dB) · REAL AUDIO 145.187 → 155.752 s, all 10.57 s of it (mean −26.3 dB) · zeros again 155.752 → 350.861 s (195.108 s). The death lands at the instant he clicked one YouTube video to another, and Chrome's own speaker icon stays lit on that tab at 240 s and 330 s while we write silence. RULED OUT BY MEASUREMENT, do not re-chase: the screen (no delivery gap at the death — 53/54/47 fps across seconds 155-157; his ~1 s freeze is YouTube's own navigation, recorded faithfully), our tap (no packet gap >0.1 s in 351 s), the main thread (lateness p50 0.5 ms over 1,403 samples), elastic/door (one background shed, four decisions, none audio), and the revive (12 attempts, 0 recovered — by design it cannot: it re-taps a clone of a source that is itself silent). **B15 MEASURED WHY EVERY LAB CELL WAS NEGATIVE AND IT IS THE SURFACE** (docs/qa/b15-surface.json, this machine, Chrome 152): a MONITOR share answered by `--auto-select-desktop-capture-source`, by the newer `--auto-select-screen-capture-source`, with `MacCatapLoopbackAudioForScreenShare` forced on, and with `systemAudio: 'include'` asked for, returns 3024x1964 video and **NO AUDIO TRACK AT ALL** — four routes, four times. Only the NATIVE macOS picker has the box that turns it on, and no switch can answer it (`PICKER NEVER ANSWERED`, 30 s, every run). The tab control gets audio in 157 ms. **SO THE TWO PATHS ARE DIFFERENT SOURCES AND THE LAB HAS ONLY EVER RUN ONE**: tab audio is Chrome's per-renderer mix (label "Tab audio", reported latency 10 ms — 2.9 ms at 44.1 kHz in the rig); a monitor share's audio on macOS is the machine's own loopback (Chrome 152 ships `CatapAudioInputStream`, a Core Audio process tap) and Robert's take reported **20 ms**. That number was in the take all along with nothing naming what it belonged to. Every negative cell in `tabAudioDeath.ts` (tone → teardown → 45 s silent → new element, both topologies, plus a 35 s occlusion cell) captured a TAB, and `memory-slope --tab` ran two 60-minute TAB-audio takes green today: the tab path does not die and it is not the path that does. **WHAT IS STILL UNTESTED IS UNTESTABLE FROM HERE** — his surface, and the YouTube SPA switch on it. One Accessibility tick on this machine (System Settings → Privacy & Security → Accessibility) would let System Events click the native picker and open the whole lane; without it osascript answers -25211 and CGEvent posting is refused the same way. That tick is Robert's. **THE HALF THAT WAS OURS IS FIXED (B15, 2026-09-05).** "nothing at all showed in beatrate" was true of the whole product: the level ring is fed `mic ?? system-audio` (CaptureScreen.tsx) so a take with a mic draws THE MIC, the stage badge renders only when `screenStream && !cameraStream` so a camera take has no ring at all, and the U1 panel's ring is that same single stream — there was no live indicator for this channel anywhere. Now: the revive ladder's own knowledge is surfaced. A channel that was HEARD and is writing pure zeros on a live, unmuted track raises `channel-silent` after the second failed rescue and a band says so, cleared by `channel-audible`. Measured twice, both on real getDisplayMedia tracks: the ENGINE (`npm run exp -- tabaudio --keep-audio`) silent at +18.06 s after 10.008 s of zeros (attempt 2), audible at +38.19 s, 186 ms after the sound came back; and THE SCREEN on the deployed build (`node scripts/b15-band.mjs` — records a real tab, silences it, reads the band off the DOM): band absent while sound played, **appeared 11.0 s after the sound stopped** ("Tab Audio stopped making sound — the source is still connected and sending nothing. Tap its chip off and on to reconnect it."), **cleared 0.5 s after it returned**, take `rec_2aigv4skanda`. The take also now records WHICH SURFACE AND WHICH AUDIO PATH it captured (`Recording.capturedSurface`), the card names it beside the `audio-continuity` verdict, and `channelLabel` reads the surface instead of the engine×OS table — a monitor share is "System Audio", and Robert will stop being told his "Tab Audio" died when what died was his machine's. **WHAT IS LEFT AND IS ROBERT'S**: the sentence (B4's gate — wording is his), whether the band should also offer a one-tap reconnect, and the Accessibility tick that opens his surface to the lab. AUTOPSY METHOD, kept because it needs nothing from him: his takes live in `~/Library/Application Support/Google/Chrome/Default/File System/105/t/19` (origin 105 = `https_inout-kappa.vercel.app_0`, mapped in `File System/Origins`), one numbered file per channel — ffmpeg reads them in place (`silencedetect=n=-80dB`, `volumedetect`, `ffprobe -show_entries packet=pts_time`); the take record comes from copying `Default/IndexedDB/https_inout-kappa.vercel.app_0.indexeddb.*` into a throwaway profile and launching Chrome on it (`scripts/lib/chrome.mjs`). His own profile is never opened and never written.
- [P0, MEASURED IN ROBERT'S OWN SAFARI 26.6 2026-09-04 — `node scripts/safari.mjs`, no artifact needed] **THE v2 COMPOSITE WORKER NEVER STARTS ON WEBKIT: `start()` IS SENT AND NEVER REPLIES.** A 25 s take on prod with `?synthetic=1`: the seam picks its rung and says so at +433 ms, then nothing — `[capture] composite painter: ...` (printed only once the worker's start reply lands) NEVER appears, and at stop the take says `live composite stop failed TimeoutError: composite start timed out after 4000ms`. IT IS NOT P9's SEAM: the same hang, identically, with `?intake=element`, which never touches the worker-side code (+293 ms, then nothing). IT IS NOT THE PLATFORM: every primitive that `start()` uses was timed inside a Safari module worker and all are fast — OffscreenCanvas 2d 2 ms · webgl2 38 ms · WebGPU adapter 4 ms + device 3 ms · `VideoEncoder.isConfigSupported` 8 ms and `configure` → "configured" 53 ms (avc1.42E01E 1920x1080) · `AudioEncoder.isConfigSupported` AAC **and** Opus both supported · OPFS subdirectory + `createSyncAccessHandle` write/read 59 ms. ~170 ms of work that hangs past 25 s. STANDING SUSPECT, and P8 named it first: mediabunny's fragmented-MP4 writer on WebKit. TWO THINGS IT COSTS BEYOND SAFARI: (1) a composite start that never returns is INVISIBLE for the whole take — the 4 s budget is only read at STOP (session.ts COMPOSITE_START_BUDGET_MS), so nothing says the composite is missing while there is still time to fall back; (2) the take is not lost — the raw channels recorded fine (+49.9 KB camera, +390.7 KB screen over 25 s) — but every Safari take silently has no composite, so no instant export.
- [P0, SAME RUNS — **P8's BLOCKER IS GONE, IT REPRODUCES WITHOUT AN ARTIFACT**] **SAFARI'S MIC DELIVERS NOTHING, ON `?synthetic=1`, EVERY RUN.** Four consecutive 7-25 s takes in real Safari 26.6 on prod, all RED on the same dimension and nothing else: `channels — mic was requested and never delivered a byte`. P8 has been blocked since it was written on "ONE Robert artifact: the exported file, or a Safari take with console" — it does not need one. `node scripts/safari.mjs <url> <probe.js>` reproduces it in ~40 s with the console in hand, and the take's own card names it. Note the shape differs from the field report ("couple second in middle and nothing else"): here it is ZERO bytes from the start, which may be the same defect earlier or a second one. Retriage P8 against this.
- [P0, READ OUT OF ROBERT'S OWN CHROME 2026-09-04 — deeper than the '1080p' fence below] **A TAKE WITH A CAMERA HAS NO COPY SOURCE AT ALL, SO ITS EXPORT ALWAYS RENDERS.** Robert 2026-09-04: "i edited nothing and export is not fucking instant at all". Take `rec_yx4mi1or851p`: 71.7 min, step 1440p, channels = screen 3024x1964@30 mp4 1389 MB · camera 1280x720@30 mp4 1316 MB · mic · tab audio, **`composite: null`**. `singleGenerationSource` refuses on "2 video channels"; `chooseCopySource` then finds no composite → `source: null` → render at EVERY tier, edited or not. Job `job_ycgbewvvrimk` (2560x1662@30, allowPacketCopy false): phase `rendering`, 11.2 % at 2.0 min → 19.3 % at 3.6 → 42.2 % at 9.2, i.e. **~21.8 min of render for a 71.7-min take**, pressed 8.4 min after the stop. FIXING THE '1080p' FENCE CHANGES NOTHING HERE — there is no composite to offer. Native-res capture stops writing one (quality.ts `sourceVideoRate` header: "NO COMPOSITE IS NOW A NORMAL TAKE"), which is right for screen-only takes (single generation copies the raw channel: 452 KB instant) and wrong for every take with a camera, where the composite was the ONLY file holding the output picture. **ROBERT ANSWERED THIS 2026-09-04 AND CHOSE (b). LEVER (a) IS DEAD** — robert (27), "kill the glued copy encoding": J6 stopped recording the composite on EVERY take, so "record it whenever there is a camera" is the opposite of the ruling and must not be re-proposed. (b) IS WHAT SHIPPED, in two halves: J5 renders the edit he made while he edits, and F16b's AT-STOP job now fires for every camera take (their exports all render now, which is what `exportWouldRender` asks). Measured on prod at default flags, screen+camera, 8.9 s take: `pre-render started AT STOP` → `pre-render ready after 3.0s — 1.2 MB waiting before it was asked for`, and in the rig the press then costs **0.3 ms** against 74.5 ms cold. WHAT IS STILL OPEN IS ONLY THE LENGTH: this take was 71.7 min and would need ~21.8 min of render against the 8.4 idle minutes he left, so the at-stop job would still have been running when he pressed. The open question is no longer "which lever" but "does the pre-render finish, and what does the dock show while it has not" — measure it at 60+ minutes before claiming this closed. Related but separate: the `allowPacketCopy` string fence, below.
- [P0, same take, 2026-09-04] **TAB AUDIO DIED AT 52.5 MIN, 86 RESCUE ATTEMPTS RECOVERED NOTHING, NOBODY TOLD HIM, AND THE CARD GRADED IT PASS.** Robert: "on 53 minute tab audio died again". `rec_yx4mi1or851p` card: `rescue` PASS — "tab audio 20 revive bursts (86 attempts), 0 recovered sound — runs began 0.1s, 995.2s, 2788.7s, **3148.1s**, 3249.2s, 3469.9s, then every ~60 s to 4184.5s; nothing was recovered, so the source itself was silent and no audio was lost". 3148.1 s = 52.5 min = his 53. Mic beside it: silent tail 0 ms, 71 MB. THE RESCUE CANNOT FIX THIS SHAPE: it clones the SAME track and re-taps it (measuredAudio.ts, revive branch) while Chrome keeps that track `live` and unmuted delivering pure zeros — a clone of a dead source is dead. Only a NEW getDisplayMedia can re-acquire, which needs a gesture (and the picker, i.e. the wedge). TWO REPORTING HOLES BEHIND IT: (1) ~~`audio-continuity` measures only the silent TAIL~~ **FIXED 2026-09-05**: the take now carries `silentTotalMs` — every zero after the channel was first HEARD, not the open run — and the dimension convicts on it, saying "spread through the take, not only at the end" and quoting the unbroken tail beside it. Silence BEFORE any sound still convicts nobody (that is `Recording.missing`). A take made before the counter is graded on the tail exactly as it was. Pinned by four tests built on this take's own numbers (tail 1840 ms, total 1,150,000 ms, 4,300,000 ms take → FAIL). (2) G6(g) traded a gate that could not pass for one that cannot FAIL — "nothing recovered ⇒ the source was genuinely quiet" is exactly backwards for a channel that had signal for 52 min and then never again. **THE LIVE HALF SHIPPED 2026-09-05 (B15)**: a channel that was heard and is now writing zeros on a live, unmuted track raises `channel-silent` after the second failed rescue and the capture screen says so — measured firing at +18.06 s after 10.008 s of zeros, cleared 186 ms after sound returned. G6(g)'s backwards `rescue` gate is NOT fixed by that and is still open: "nothing recovered ⇒ the source was genuinely quiet" remains exactly wrong for a channel that had signal for 52 min and then never again. SAME DEFECT, THIRD INSTANCE, WITH THE TRIGGER WITNESSED: top of this Inbox.
- [P1, 2026-09-04] ~~**THE BLACK BOX DOES NOT RECORD WHICH BUILD THE TAKE WAS MADE ON**~~ **FIXED 2026-09-05** — `Recording.buildId` is stamped at capture from vite's `__INOUT_BUILD__` (Vercel's sha, git locally, `dev` in dev) and the report card's HEADLINE carries it; a take made before the field shows `?`. The original entry, kept because it is the evidence: `rec_yx4mi1or851p` ran a 20 %-complete render with **zero `rchunk-*` files in OPFS** — J1's chunk cache never engaged, and no `plan.chunkable` refusal explains it (has video, 30 fps, whole-frame grid). It is the PWA trap: J1 merged 09:52:54, J3 09:57:00, the take STARTED 10:00:21 — the tab was loaded before that deploy landed and ran the pre-J1 build for the next 80 minutes. Every field report from a long take is a report about the build the tab was loaded with, and nothing in the take says which. Stamp the build hash into the take record and print it on the card; one line, and it retires a whole class of wrong attribution.
- [P2, 2026-09-04] **A SECOND APP TAB WIPES THE FIRST TAB'S PRE-RENDER.** `sweepPrerenderBlobs` (prerender.ts:394-398) deletes every `prerender-*` blob except the one claimed by THIS page's in-memory `job`, and `job` is module state — so any second tab's boot (a recovery check, a second window, an agent reading a take) deletes a finished pre-render another tab is about to hand to an export. Found while reading the take above from a second tab; it did NOT cost that export (a ~22-min render cannot have finished in the 8.4 min available). Fix: claim by record, not by memory — or scope the sweep by age the way the chunk cache does.
- [G lane, FOUND AND INSTRUMENTED 2026-09-03 — the rig was charging the product for its own cost] **`?synthetic=1` IS NOT A VALID max60 THROUGHPUT INSTRUMENT, AND EVERY max60 ARRIVAL NUMBER TAKEN THROUGH IT IS THE RIG'S.** The synthetic screen is a canvas the harness repaints on every rAF tick: at 3024x1964 that is 5.9 Mpx of 2D drawing per frame, ~713 Mpx/s at 120 Hz, on the same GPU as the encoder under test. Measured on prod, screen-only at `qstep=max`, ENCODER DROPPING ZERO in every cell — so the loss is upstream of the product entirely: 1920x1080 synthetic delivered 56.8 fps arrived, 3024x1964 delivered 43.3. It scales with the canvas the HARNESS paints. The same build on the REAL capture path (`fps-check --real-tab`, a genuine getDisplayMedia of a browser tab, no canvas the rig owns) delivers **715/715, 725/725 and 728/729 frames at 3024x1788@60 — 58.6-58.9 fps, 0 dropped, card GREEN 13/13 on three runs**. WHAT THIS INVALIDATES: any past or future reading of 'max cannot hold 60' taken through `?synthetic=1` at a large screensize. Use `--real-tab`. WHAT IS STILL NOT MEASURED: whole-SCREEN capture, because macOS drives its own ScreenCaptureKit picker that no Chrome switch can answer (it hangs the full 30 s), and forcing Chrome's own picker back with `--disable-features=ScreenCaptureKitPickerScreen,...` gets 'Permission denied by system' in 641 ms instead. A tab is a real capture and is Robert's own game-tab case, so it is the instrument for now; whole-screen needs a TCC grant nobody can script.
- [P0 — ~~FIXED 2026-09-05~~, the fence now asks the geometry question its own contract always stated: `tierIsComposite(recording, tier)` in quality.ts, used by the editor's export press, the editor's pre-render decision and TakesList's save. Four tests pin it on the 2560x1662 case that re-rendered, on a differing geometry, on the 1080p case that must not regress, and on a take with no composite. Kept as the evidence:] THE INSTANT PATH WAS FENCED TO THE LITERAL STRING '1080p', SO THE EDITOR'S OWN DEFAULT TIER TURNED IT OFF. Robert 2026-09-03: "i moved nothing in 5 minutes video and nothing was instant stil" — his unedited `inout-03-09-2026-043451.mp4` (2560x1662, 5.1 min) certifies `"path":"render"`. CHAIN: EditorScreen.tsx:303 passes `allowPacketCopy: isDefaultTier(chosen)`; quality.ts:344 `isDefaultTier(t) = t.id === DEFAULT_TIER_ID` and DEFAULT_TIER_ID is the constant `'1080p'` (quality.ts:138); but `defaultTierForTake` (quality.ts:226) correctly returns the step the take was RECORDED at — `'source'` for a `max` take, `'1440p'` for a 1440 one. Neither is `'1080p'`. So `allowPacketCopy` is FALSE for every take not exported at exactly 1080p, the composite is never offered to `chooseCopySource`, and an unedited take re-renders. choose.ts:75 states the intent correctly — "True only when the requested output geometry IS the composite's" — and then the implementation asks a different question: is the tier NAMED 1080p, not does the geometry MATCH. Same bug, second site: TakesList.tsx:196 hardcodes `top.id === '1080p'`. FIX: ask the geometry question the comment already states (requested width/height === the composite's), which also makes it correct when the composite follows the source (F13). NOTE the fence is composite-only — O3c consults single-generation on every tier — so screen-only takes whose raw channel already matches still copy (STATE's "max screen-only: 452 KB instant"); it is the COMPOSITE cases that all fall to render. **SINCE J6 (2026-09-04) THERE IS NO COMPOSITE TO FENCE**: `chooseCopySource` reaches "the take has no composite" before `allowComposite` is ever consulted, so on the shipped rung this bug can no longer cost anything. It is still WRONG, and it still bites with `?glue=record` — fix it when something else is open in choose.ts rather than as its own P0. Whether single-gen would otherwise have rescued this particular take depends on its channel geometry — check it, but the fence is wrong either way. COST OF THE BUG: 72 min of rebuild for a 124.8-min take that had nothing to rebuild.
- [P1, HALF ANSWERED BY B14 2026-09-03 — the CAPTURE half is measured and shipped; the RENDER half is still open] THE TWO ENCODER-THROUGHPUT MEASUREMENTS IN THIS REPO DISAGREE 3.5×, AND NEITHER MEASURED THE SIZE THE ARGUMENT TURNS ON. **B14 measured the capture side properly and the product now does it every launch**: the meter runs at the display's own size with the take's own codec, and prod reads 369-396 Mpx/s at 3024x1964 against 330 at 1920x1080 (rate.ts:98's 416 was the right SHAPE — throughput rises with frame size — and 5-11 % optimistic for this machine today). A max take now records 3024x1964@60 live, 0 dropped, card GREEN. What is STILL unmeasured is SUSTAINED RENDER at that output, which is a different encoder doing decode+encode. rate.ts:98 said 416 Mpx/s at 3024x1964 (idle, one hardware AVC encoder) — it is what `rateForSurface` lets 60 fps through on. scripts/encode-cost.mjs's header says a real render at the 1080p step sustained 58 fps at qp20 = 120 Mpx/s, and 91 fps on the bitrate target = 188 Mpx/s. Robert's 124.8-min take rendered at ~52 fps of 3024x1964@30 = 309 Mpx/s of decode+encode, between them. NOBODY HAS MEASURED SUSTAINED ENCODE AT max/60 OUTPUT (3024x1964@60 = 356 Mpx/s), which is the number that decides whether a background render can ever keep up with a take: at 416 it is 1.17× realtime, at 120 it is 0.34×, i.e. 107 min vs 6 h for a 2-hour take. The machine is a BASE M3 (8 cores, 8 GB) with ONE media engine, so parallel encoders/workers cannot raise the aggregate — the whole "render it faster" family is bounded by this one number. Run encode-cost.mjs at --width=3024 --height=1964 with the 60 fps duty cycle, idle machine, before any scheduling work is planned. SECOND, ALREADY-MEASURED LEVER SITTING UNUSED in the same header: qp20 costs 1.57× the wall clock of bitrate mode (58 vs 91 fps) — 36 % of export time is being spent on the constant-quality path, and per SIZE-CODEC the 8 Mbps constant it is measured against was never derived.
- [P2 — ~~DID NOT REPRODUCE 2026-09-05, SIX RUNS OUT OF SIX~~, and the likely cure is E3, which landed
  after the observation] THE EDITOR'S FIRST FRAME STALLED THE MAIN THREAD ~150 ms, INTERMITTENTLY.
  Re-measured with `node scripts/g7-lateness.mjs --lanes=take,editor --take=12`, headed, document
  visible 100 % of the window, six consecutive runs: **worst second 9.2 / 5.5 / 2.9 / 3.2 / 1.5 /
  3.1 ms, and ZERO of ~5,600 samples over one frame** — against the 30 ms Phase-1 claim and the
  150 ms that was seen. The original read (`RED — the main thread was 150 ms late at 0.0s`, 819.9 ms
  of lateness over 59 samples, 9 of 934 samples over a frame) is not reproducible on this build.
  WHAT PROBABLY FIXED IT, and it is a hypothesis and not a measurement: E3 made the editor-ahead
  window refcounted, so the pre-render AND the export size probe both hold the render back while the
  editor opens — its own row records `RED, worst second 51 ms at 10.0s` before and `GREEN at 10.8 ms`
  after, which is the same instrument finding the same class of stall. Left here rather than deleted
  because "intermittent" is exactly what six runs cannot disprove; delete it if it stays quiet.
  **THE REPRODUCE LINE IN THIS ENTRY WAS WRONG AND COST A RUN**: `--lanes=editor` alone measures
  nothing, because that lane only polls `__inoutEditorReport()` and nothing has recorded a take for it
  to open — it needs `take,editor` (or the `drag` lane, which calls it).

- [M1's own audit, three items never closed — LIFTED OUT OF THE M1 SPEC 2026-09-04 so they stop living inside a task marked done] **(a) THE AUDIO ANCHOR ASSUMES ZERO LATENCY WHEN THE PLATFORM WON'T SAY.** `measuredAudio.ts` uses the track's reported `latency`, bounded at 200 ms, and **falls back to 0** when there is none — so a Bluetooth mic (100-300 ms in the signal path) is anchored as if it had no delay at all, and the export places its sound that much late. The bound itself is underived. X14 is where this is repaired (anchor on `AudioData.timestamp`, which tracks a delay the reported constant cannot); until then it is a known, unmeasured error on one device class nobody here owns. **(e) CAN A MACHINE RE-EARN ITS RESOLUTION?** `encoderBudget.ts` keeps `sustained` and `collapsed`; its header says `sustained` outranks `collapsed` when they disagree, which is the un-ratcheting half — but nobody has MEASURED whether a machine that collapsed once, and now runs only reduced takes, can ever raise `sustained` again, or whether the reduction pins it. A one-way ratchet would quietly cap a machine forever. **(g) UNDERIVED CONSTANTS**: audio forced to stereo 48 kHz 128 kbps (codecs.ts), the 200 ms latency cap above, the 2000 ms drain budget (recorderDrain.ts), displayRelease.ts's 100 ms. Each earns a measurement or goes.
- [M4 follow-up, measured 2026-09-04 → G lane, no task yet] **THE ENCODER DROPS FRAMES IN QUANTIZER MODE AND SAYS NOTHING.** `bitrateMode: 'quantizer'` — what the product ships for the export — silently discards a variable share of submitted frames on this machine's hardware AVC encoder, and the loss grows with how close the timestamps are: 60 of 60 kept at 15 fps spacing, 59 then 41 at 30 (the same cell, two runs), 44 at 60, 21-44 at 120. A **bitrate target keeps every frame at every rate**. NOT the pixel format (an NV12 ring round-tripped through a decoder drops the same 43 of 60 as an RGBA canvas), NOT the backlog (draining the queue every frame and feeding at real time both still read 44), NOT a callback race (the count is the same at flush, after a task and after 300 ms), and NOT throughput (640x360 drops MORE than 1280x720: 27 of 60). Rig `scripts/m4-temporal.mjs` refuses to compare bytes across cells that kept different counts and walks the spacing down until nothing drops. **ONE CONSEQUENCE CHECKED, ONE STILL OPEN.** (a) CHECKED AND CLEAR: `encode-cost.mjs` computes `fps` from frames SUBMITTED, so its quantizer cells could have been counting frames the encoder never made — they were not. It spaces its timestamps at 30 fps, where the drop barely bites, and a full sweep at 1920x1080 kept every frame in every one of its nine cells. Its published numbers stand. It now reports `submitted`/`out`/`outFps` and prints a loud line when they differ, so a future run on a machine or a rate where this DOES bite says so itself instead of quietly halving. (b) ANSWERED 2026-09-04 AND IT IS A CLEAN NEGATIVE — **the shipped export does not lose frames at 60**. A max take recorded and exported through the product on prod (2560x1440, `?qstep=max&screenfps=60`, served from the at-stop pre-render, i.e. the same render the press uses) came out **741 frames for 12.350 s at 60/1 — exactly 60 fps, nothing missing**, read with ffprobe from outside the product. So the drop is real in a rig that drives `VideoEncoder` directly and does NOT reach the export, which goes through mediabunny's CanvasSource. What is still not known is WHY the two differ, and that is worth knowing before anyone writes another encoder rig: the difference is in how the frames are fed, not in the codec or the mode. J5's 900-of-900 at 30 fps was never evidence about 60; this is.
- [M4 follow-up, measured 2026-09-04 → a SIZE lead for the export, no task yet] **THE DECLARED FRAMERATE MOVES THE BYTES FOR IDENTICAL PICTURES.** Same 30 pictures, same timestamps, same qp20, one field different: declaring half the framerate cost **9168 B/frame against 4992** — 1.84x — at 1280x720, and 32628 against 25646 at 3024x1964. It was found as a confound (M4's first cost line compared a half-rate arm that declared half the rate, and read 0.554x when the like-for-like answer is 1.018x), and it is worth chasing on its own: if the export declares its OUTPUT rate while re-encoding a 60 fps take down to 30, this says the file is paying for that word. Nobody has looked at what the shipped export actually declares.
- [X11a follow-up, measured 2026-09-04 → X14; a FACT, not a defect] **THE AUDIO ANCHOR IS DECIDED BY WHETHER THE BROWSER HAD ALREADY MADE A SOUND.** `AudioData.timestamp` off a `MediaStreamTrackProcessor` is the system monotonic clock rebased per BROWSER PROCESS (107,423 s read against a 107,400 s machine uptime), so the `ctx=` in the product's first-sample line is how long that browser's audio device had been running when the take's first quantum arrived. A take whose device was ALREADY RUNNING anchors **~110 ms later** (median ~148 ms against ~36 ms) — bigger than the whole 40-66 ms sync X14 is trying to close. Consistent with the new track joining a running stream as a catch-up burst that the anchor's own guard (`sincePrevArrivalMs >= quantumMs/2`) then rejects. AND THE WARNING THAT COMES WITH IT: `rawAnchorMs` spans **19-350 ms** across 24 readings on ONE build on an idle machine, and the same arm against itself pairs at **−50 ms** (`scripts/x11a-anchor.mjs --arms=main,main`), so no anchor difference under ~100 ms can be read off takes at all — measure the mechanism instead. Evidence docs/qa/x11a-worker-tap.json.
- [2026-09-04, ROBERT: "renedring is 4 time slower than acceptable" → the arithmetic, off his own last export] **HIS EXPORT WAS `max`: 3024x1964 AT 60 fps, qp20** — read out of the file's own certificate (`~/Downloads/inout-04-09-2026-143252.mp4`, `"path":"render"`, `"fps":60`, `"gopSec":5`, so it also predates J5/J6/the 2.5 s grid). That take is 351 s = **21,060 frames of 5.94 Mpx = 125 Gpx**, and it asks **356 Mpx/s of OUTPUT**. THE CEILING, measured two ways on this machine: the production render sustained **84-99 fps of 3024x1964 = ~529 Mpx/s** (R1, through the real export worker), and the encoder alone, with the rig's painting finally OUT of the loop (`encode-cost.mjs` 2026-09-04, RING), sustains **74.1 fps = 440 Mpx/s** at the shipped qp20 config. **So a max60 export runs at ~1.0-1.5x realtime and 4x faster means 1.4-2.1 Gpx/s — 3-4x this engine's measured ceiling.** Every config axis is now dead: HEVC 10 % SLOWER (O11d row), qp20 costs 6 % over a bitrate target at the encoder (the 57 % in the end-to-end wall is bytes through the muxer), queue 16 no move, a second encoder 1.04-1.07x through the product (R1). THE ONLY MULTIPLIERS LEFT ARE WORK NOT DONE: **max60 → max30 = exactly 2.00x** (rate first, resolution after — DECISIONS robert (22)) · **max60 → the 1080p30 default = 5.73x** (356 → 62.2 Mpx/s) · and the wait itself, which J5+F16b removed FOUR HOURS AFTER this export (17:31/18:29 against 14:38) — the file for a max+camera take is now rendered at stop and while he edits, so the press serves a file that already exists. HE HAS NOT SEEN THAT BUILD YET. **EVERY AXIS AT THE ENCODER IS NOW CLOSED, MEASURED FOUR WAYS THE SAME NIGHT** (`encode-cost.mjs`, 3024x1964, ring): config ±6 % · codec HEVC **−10 %** · sessions **+6 % aggregate** (78.8 fps at two, 77.7 at four, against 74.1 at one — one lane already saturates the engine, which is the mechanism R1's 1.04-1.07x never had) · input format **−2 %** (GPU-backed NV12 straight off a decoder is 72.3 fps against the canvas's RGBA 74.1, so the RGBA→NV12 conversion the render pays every frame is free). This machine encodes AVC at **440-470 Mpx/s** and max60 asks 356. **A SECOND GENERATION OF THE PICTURE CANNOT BE MADE CHEAP — the only lever left is not making one.** **SEVEN MEASUREMENTS, ONE NIGHT, AND THE SEVENTH IS OUTSIDE THE BROWSER.** Native ffmpeg through VideoToolbox on the same file runs **39 fps** with or without `-hwaccel videotoolbox` zero-copy — HALF what Chrome's WebCodecs reaches, so the browser is not the tax. And run beside the Chrome rig, in its own process: Chrome fell 74.1 → **38.1** while the native lane held **32**, **70 fps aggregate — below the 74 one Chrome process reaches alone.** The encode silicon is shared and saturated across process boundaries; there is no share of it Chrome is failing to reach. **THE REPEATED-FRAME LEVER IS DEAD, NOT DEFERRED — and the first number here was wrong.** mpdecimate at its DEFAULT thresholds calls 37.7 % of his frames duplicates, but those are NEAR-duplicates; at strict identity (`hi=1:lo=1:frac=0.001`) **21,015 of 21,060 frames survive — 45 frames, 0.2 %**. The mechanism is plain once seen: the screen sample repeats (55.1 fps source into a 60 fps grid) but the CAMERA sample changes underneath it, so the composed picture is new almost every instant. Exact elision pays only on a screen-only take or a genuinely still screen, and NOTHING on the case he named. Robert refused it before this was measured ("i dont trust this shit with repeated frames yet") and was right. **WHAT IS LEFT IS NOT THE ENCODER AT ALL: never encode the same picture TWICE.** Three live defects put a second full render on the same engine — the pre-render key carries no render flag (evidence entry below), a second app tab wipes the first tab's pre-render (Inbox P2), and J1's chunk cache did not engage on his 71.7-min take (Inbox P1). Each one is a whole wasted generation on a machine that has exactly one. REFUSED BY ROBERT 2026-09-04, do not re-propose: a capture-time composite in any form ("no fucking composite"), and anything that moves resolution, rate or quality ("nothing except render speed must change").
- [G lane, found 2026-09-04 building O9(b); **FIXED 2026-09-04, one half still unmeasured**] **THE PRE-RENDER'S KEY CARRIED NO RENDER FLAG.** `prerenderKey` was `[recording.id, edit, settings]`, so a file rendered BEFORE a flag was flipped was served for an export made AFTER it and the flag silently did nothing — live for `?cq=` and F13's `?sourceframe=`, and the reason O9(b) had to refuse the pre-render outright whenever `?colour=all` was on. FIXED: `currentRenderFlags()` moved from chunkedRender.ts to chunkPlan.ts (which imports only flag readers, so prerender.ts can ask it — chunkedRender.ts cannot be imported there) and now goes into BOTH `prerenderKey` and `prerenderShape`; choose.ts's `fullColour ? null : takePrerender(...)` workaround is deleted, since the key does that job for all four flags instead of one. It invalidates every pre-render made before the change, which is what a version bump is for. PINNED per flag (`prerender.test.ts`, 'a render flag changes the key — every one of them'), and writing that test found two more instances of the same family: `setConstantQuality` and `setFullColourEnabled` write localStorage, which a worker and a test environment both lack, so the seams that actually work there are `setConstantQualityOverride` and `setFullColourOverride` — the exact hole constantQuality.ts's own header describes. **STILL UNMEASURED, and it is now one command's worth of work:** nobody has watched a flag flip between a stop and an export END TO END. The discriminator is J5's — press-to-file is 9 ms when a pre-render is served and seconds when it is not — so the probe is a camera take (its export must render), stop, flip `?cq=` in `/?test`, press export, and time the press. It needs a HEADED rig: a hidden pane paces the pre-render at 1 Hz and the served arm never becomes ready.
- [O4 follow-up, found 2026-09-04 reading compositorWGPU.ts → no task yet; a DEFECT, so it ships on] A LOST WEBGPU DEVICE MID-TAKE PAINTS NOTHING UNTIL THE NEXT SHAPE CHANGE: `device.lost` only clears `devicePromise`, and `makePainter` (compositor.worker.ts) runs on `adoptShape`. Rebuild on the NEXT FRAME onto the WebGL2 rung and print it in the report card. Unmeasured: no rig forces a device loss; the probe is `device.destroy()` behind a /?test row, and the worker is frame-driven so a hidden pane can run it.
- [O4 follow-up → O9's chroma rig, measurement first] MACHINES WITHOUT WEBGPU GET WEBGL2's COLOUR, less saturated than the WebGPU look Robert chose (PSNR 55.9 dB apart on a real NV12 frame: `texImage2D` and `importExternalTexture` convert NV12→RGB differently). Run both painters on the chroma rig against the SOURCE; if WebGL2 keeps less, own the conversion on that rung only (`copyTo` the planes → R8/RG8 textures → BT.709 in GLSL; ~1-2 ms/frame there, unmeasured). The WebGPU rung is never touched. Chromium Linux outside Intel Gen12+ and every blocklisted GPU is this rung.
- [P1] THE LONG-TAKE EXPORT IS THE FRAME COUNT, and the machine may render it twice. Measured 2026-09-02 (`exp nativerender`, 60 s 3024x1964@60 source, 1080p step, real production render): 122-138 fps sustained — so a 124-minute take is 27 min of render at 30 fps out and ~57 min at 60 fps (`max`). RULED OUT as causes: the background frame + a zoom (128 ms over 3600 frames, 0.036 ms/frame — Robert's "must not make it slower" is already true), the decoder's `optimizeForLatency` hint (6 %, inside run noise, and it is what bounds GPU memory), constant quality (its cost tracks output BYTES: −11 % on still text = faster, not slower). Encoder alone does 187 fps at 1080p qp20 hardware (`scripts/encode-cost.mjs`), decode alone ~207 fps at 3024x1964, and they run SERIAL on one worker thread — overlapping them WAS named here at ~1.4x AND IS DEAD — X4 measured the decode farm at 0.99-1.08x against a >=2x gate, because decode and encode CONTEND FOR ONE MEDIA ENGINE (the same decode takes 4.7 s alone and 17-23 s with the encoder beside it; a second JS thread does not create a second engine). See .ai/PROPOSALS D. A session offered to build this on 2026-09-04 from this stale line before reading D — do not repeat that (render.ts's header says a prefetch pump "bought nothing", measured on a 12 s take where the shape was different). THE POLICY HALF OF THIS IS CLOSED: J1 (2026-09-03) made the render resumable content-keyed chunks, so a cancelled job keeps everything it finished, and J5 (2026-09-04) starts one for the edit he MADE — on a 30 s take a second small edit re-rendered 3 chunks of 12 and the press a minute later cost 9 ms. What is left here is the throughput, which is the frame count above.
- [P1 → B11] smart cut refuses a 120 s trim on `Timestamps must be non-negative (got -0.0003333333333337407s)` = −1/3000 s in `outAt()` (smartCut.ts:456), one cold run in five; the user silently gets a full render.
- [P1 → B12, MECHANISM NAMED AND THE DROP FIXED 2026-09-04; ONE HALF STILL OPEN] THE AUDIO TAP THREW CAPTURED SOUND AWAY UNDER A STARVED MAIN THREAD. `MediaStreamTrackProcessor` does not wait for a starved reader — it DROPS — and its default buffer holds **~87 ms** (measured three ways: stall minus the largest gap the tap left in its own chunk timestamps = 84 / 90 / 87 ms). On 45 s takes with a dosed main-thread block (`scripts/b12-audiostarve.mjs`): **20.1 / 28.6 / 32.5 s of real audio gone**, which WallClockHold then repaid as SILENCE — so the channel read FULL LENGTH and the card's `channels` dimension said "all full length" while 42 % of the take was nothing (the card reds on `audio-clock`, which is the only dimension that saw it). Fixed by `TRACK_TAP_BUFFER_MS = 4000` as `maxBufferSize` (docs/FLAGS.md `?audiobuf=`): the same three cells lose **0.00 / 0.00 / 0.07 s** over an undosed control and pad **0**, largest gap 316→3, 1410→5, 2913→5 ms. STILL OPEN, and it is the half that produces H2's actual symptom: when a stall is long enough that every arrival drains in one burst, `wallClockHold.ts:171` (`coveredMs < settleMs * 0.75`) refuses EVERY correction, so nothing is repaid and the channel ends short instead of padding. NOT REPRODUCED here — every dosed cell padded — so it is a lead with a line, not a finding. Whoever takes X11a owns it.
- [P2 → G6] THE REPORT CARD CANNOT SEE A HOLE BETWEEN TWO SEGMENTS OF ONE KIND. Measured 2026-09-03 on the M1 floor rig: a take whose screen was segmented mid-take (three screen rows, a 5,047 ms gap between the first and second) graded `channels: 4 channels, all full length` and `picture: 4 video channels, none stalled`. The dimension counts rows against the take's duration and never asks whether consecutive rows of the SAME kind are contiguous. Any segmenting path hits this — O16's `?resstep=1`, H1's containment, the M1 floor's size rung — and the hole is exactly what a user would notice. The fix is one comparison per kind (next.startOffsetMs vs prev.startOffsetMs + prev.durationMs) and a band, and it belongs to whoever owns the card next.
- [P2 → G6/M1] AFTER M1 THE CARD CONTRADICTS ITSELF ON A RESCUED TAKE. The `rate` dimension reds any take that stepped ("this take degraded to keep up"), which is now the SUCCESS case of the emergency floor: the same take grades `elastic: order held` and `decisions: 0 refused, 0 failed` and still comes out RED overall. Measured on the rig, both floor lanes. Somebody has to decide which of the two is the verdict — grading the STEPPING is what G6(g) already calls a defect in the `rescue` dimension, and this is the same shape.
- [P0 → W2] the screen wedge: rung 3 (bare request) wedged three in a row 2026-09-02; page has no move left; Robert's levers and the second-origin test in docs/SCREEN_WEDGE.md.
- ~~[P1 → B1b] the size probe over-prices still text 1.50×~~ **CLOSED 2026-09-04**: it was the ten seconds the probe picked, not its encode (the probe charges 0.98-1.10× what the render charges for the very same seconds). The window is now chosen from the take's own activity profile instead of being taken from the middle. What is left is a take with TWO contents in it — still while you read, busy while you scroll — where one window cannot price both; the probe now under-promises there (0.54× on a deliberately bimodal fixture) instead of promising 6× the file. Numbers in `src/core/compose/sizeProbe.ts` note 7, rig `npm run exp -- b1b`.
- [P2 → B1b/F16] THE SIZE PROBE'S WALL IS THE HARDWARE ENCODER, and the <2 s budget is not reachable while it encodes a whole GOP at every step: 300 frames × (0.52 + 0.92 + 2.07 + 3.69 Mpx) = 2.16 Gpx against this machine's own ~385 Mpx/s (B14), a 5.6 s floor. Measured 13-22 s on a four-lane fixture and 1,928 ms on a 1080p take whose default step packet-copies. The lever is ONE GOP instead of two (`warmPass: false`): wall 13.5 → 3.4 s for 5 points of accuracy. Not taken — the budget was never derived from anything the user feels, and the panel opens instantly with the number landing late.
- [P2 → B10] the size probe encodes 300 frames on the main thread ~11 s after the editor opens; drag stalls 35-201 ms land inside that window.
- ~~[P1/P2 → G6] instrument flakes~~ **G6(a)-(f) DONE 2026-09-03** (branch task/g6): both oracles take a bounded quiet-machine preflight, print the load beside every number, and CONFIRM a red with a second reading — red in both = FAIL, red in one = INCONCLUSIVE and exit nonzero. Under the gate.sh lock none of the four flakes reproduce: v1 throughput 8/8 over the band (1.00-1.46×), spur −51.3 to −59.0 dB across 14 cells, fidelity render 0.03 dB / THD −68.8, 120 s cell 3/3 clean — the CDP death's CAUSE is still unnamed, but it now arrives with chrome stderr + page console instead of `cdp-run exited 1`. `?slow=` was never dead code (183 ms vs 6079 ms on prod); the parser had tests and the delivery had none, and the wiring is now pinned by two. The screen encoder is NOT starved: a requested bitrate is a CEILING — still content achieves 31.6 % of an 8 Mbps request, the most-changing content 55.4 %, and neither reaches it (DECISIONS 2026-09-03). Still open under G6: (g) `rescue` grades attempts not loss [G7 owns it] · (h) the black box does not record which tap ran [B13 owns it].
- [P2 → B10] the size probe encodes 300 frames on the main thread ~11 s after the editor opens; drag stalls 35-201 ms land inside that window. G7 2026-09-03 NARROWS THE SHAPE: on a 1080p `?synthetic=1` take the probe runs (300 frames in 1928 ms) and a 13 s drag through the whole editor window reads GREEN, worst second 7.8 ms — the probe encodes asynchronously and on small frames each chunk fits inside a frame budget. The 35-201 ms readings are from `qstep=max&screensize=3024x1964&prerender=1`: NATIVE-RES probe frames beside an at-stop pre-render. B10's gate must be read on that query or it proves nothing.
- [P1/P2 → G6] instrument flakes: v1 export-throughput coin flip (0.46-0.94× loaded, 0.51-0.82× once idle) · fidelity render lane red under load (8.69 dB, THD −56.8) · spur gate moves 25 dB with load (−35 vs −50/−60) · 120 s cell dies on CDP ~1/3 · `?slow=` is dead code (parseSlowChannels has no caller) · the 720p camera PiP wrote MORE bytes than the 3024x1964 screen on rec_78ogcw052vdn (camera 900 MB at 2.49 Mbps, screen 814 MB at 2.25 of 8 requested) — starved encoder or honest still content?
- [P2] Robert 2026-08-29 "at some points video got slows down and lag too" on a long take — UNDIAGNOSED; pin the stage first: capture (CompositorStats.framesDropped / maxEncodeGapMs / peakQueue are in the take), preview playback, or the file.
- [P2 → SIZE-CODEC] files too large for the quality on motion-heavy takes: an unedited export packet-copies the composite's realtime encode at a flat ceiling; the levers are the codec lane and idea 11.
- [P1 → SIZE-CODEC/O9] coloured text loses ~30 % of its colour, all at capture (source arrives NV12: ≈20 % Chrome's, ≈10 % ours); Robert 2026-08-29: "I WANT 100% COLORS". Single generation recovers to 80 % green at no cost; 4:4:4 AV1 keeps 99.3 %, software only.
- [P2 → X14] sync reads 40-66 ms audio-late on the oracle; Robert can feel it; target is 0 (ruling 2026-09-02). Levers: idea 4 (capture stamps, echo ruler). CONTRADICTED IN THE FIELD 2026-09-02 → B13: on rec_gpsoujs2sydf tab audio is 46.4 ms EARLY against the screen (screen rawAnchor +36.4, system-audio effective −10.0), the opposite direction from the oracle's typical. Either the oracle does not measure the tab-audio-vs-screen pair, or the two channels are anchored differently — settle which before X14 retunes anything.
- [P1 → B13] TAB AUDIO IS ANCHORED 46.4 ms AHEAD OF THE PICTURE, and 10 ms of that is arithmetic on a channel that has no microphone: `reportedInputLatencyMs: 10` is subtracted from the system-audio anchor, exactly as it is from the mic. Tab audio is an internal loopback — there is no input path and no input latency to remove. Chrome reports 10 ms for every device (B7) and measuredAudio subtracts it blind. The other 36.4 ms is the picture landing late because video is anchored at frame READ (X14a's subject). Robert hears the sum: "tab audio part of second faster than screen video". A lead is perceptually ~3× harsher than a lag (~45 ms tolerance vs ~125), which is why 46 ms is audible.
- [P1 → B13] tab audio sounds thin and has small noises (Robert 2026-09-02, "less bass or whatever … some small noises"). Voice processing is the shape of it (AEC/NS/AGC high-pass the bottom, gate quiet passages, downmix to mono — he heard exactly this 2026-08-26 as "mono warble") and the take was recorded 177 s after a level-3 wedge, i.e. on the floor rung. But it is NOT established: acquire.ts sends RAW_DISPLAY_AUDIO on every rung including the floor, and `repairDisplayAudio` re-applies the flags to a track that arrives processed. Exclude the export mix's makeup + u/(1+u) limiter, opus 128 kbps stereo, and any stereo→mono collapse before blaming the tap. UNADJUDICABLE RETROACTIVELY: the delivered `ec/ns/agc` are logged to the console and never stored → G6(h).
- [P1 → G6(g)] A GATE THAT CANNOT PASS. The report card's `rescue` dimension graded rec_gpsoujs2sydf RED on 63 revive attempts across 15 silent runs, every one of which was a quiet tab (Robert: "nothing was there"). A revive fires after 5 s of pure silence on a LIVE track and cannot see whether the source was silent, so any honest long take with a quiet tab grades red. It cost A1's gate 2 a day of ambiguity. Grade LOSS (silentTail, silence contradicting a source known to be playing), not attempts.
- [DOC BUG, fixed 2026-09-02] displayWedge.ts's ladder comment said "rung 3 moves our three raw-audio flags onto the delivered track instead of sending them in the request". acquire.ts (the owner) sends RAW_DISPLAY_AUDIO on every rung, floor included, and `repairDisplayAudio` is a belt for a track that arrives processed anyway. One fact, one home: the owner wins.
- [P2] the oracle's arrival probes stamp `performance.now()` at READ, not the frame's own timestamp — the 6.5 ms per-run term G5 left; record both stampings (small rig change).
- [P2] the MediaRecorder fallback is not a fallback for a frame the platform's AVC refuses (both paths ask the same encoder); wanted: a rung that changes the ASK (even the size, drop the level, accept VP9).
- [P2] `setChannelActive(kind, false)` stops through onTrackEnded without draining → a switched-off channel loses 200-1300 ms under load; needs async onTrackEnded (throttle, drain, stop); own session.
- [P1 → O6] the 4K game tab: keep-alive/cap fixes shipped, "not all the time" remains; Robert's re-verify with native res is the only instrument (a synthetic 4K source paints at ~209 Mpx/s).
- [P1] wedge recovery under game load: the boot notice is fixed (sticky banner, owed-flag); whether the reload itself ran under load is unproven — needs the arming timeline from a repro.
- [P1] camera light at app load (Robert, once, unreproduced) — if real it violates "no idle device access, ever"; the light DURING the picker is the approved concurrent arm.
- [Robert] July/August field reports (07-23 stops/desync/noises · 08-06 frozen frame · 08-22 4K freeze · 08-25/26 desync, audio quality, tab audio dying) each had fixes shipped; his recheck on the current build is what closes them. THE AUDIO HALF IS NOW ANSWERED (2026-09-02, rec_gpsoujs2sydf, 124.8 min): tab audio no longer dies — the 08-25/26 "tab audio dying" report does not reproduce (silentTail 1,970 ms against 1,650,144 on the 09-01 failure) — and the surviving complaints are B13's three, which are quality, not loss. The desync report survives in B13(1) with a number and a direction for the first time. Still open for his recheck: the video half (08-06 frozen frame, 08-22 4K freeze → O6).
- [P2] a starved take's padded silence can land in the fidelity oracle's fixed 2.5 s window (a consequence of the wall-clock hold, not a regression) → G6(b)'s guard.
- [P2] the audio-only size fallback is 8 Mbps × duration labelled rough; do not invent a constant.
- [P1 → M1] SEVEN ELASTIC SYSTEMS, NOT ONE (found 2026-09-02 answering Robert's "so what is about that we have two elastics"). Ours: the frame-rate ladder 60↔30 on pressure/delivery (captureLadder.ts:105) · the encoder budget cutting screen resolution before the take (encoderBudget.ts:250) · the composite watchdog DROPPING THE WHOLE screen+camera mix after 5 s under 12 fps, no flag, fully automatic (liveCompositeV2.ts, compositorWatchdog.ts) — since J6 it grades PAINTED frames, not encoded ones, so it can no longer fire on a busy encoder; what it now watches is the paint, which is what the preview and the liveness detector ride on · the background-work broker throttling the pre-render (backgroundWork.ts:90) · the export QP governor (below). NOT ours: the capture encoders run in BITRATE mode (rawVideo.worker.ts:238, and compositor.worker.ts only under `?glue=record` since J6) so the browser's own rate control decides what detail to discard under strain — this is the mechanism that eats a ticking tab clock, per robert (13) · Chrome's own adaptation, which may ignore applyConstraints and downscales getDisplayMedia surfaces itself (acquire.ts:467, acquire.ts:614). At least four can lower quality with no decision anyone can point at, they can stack on one take, and no single place records that quality moved or why. M1's "ONE elastic system" must be read as all seven, not just max's floor plus E1.
- [P1 → M1/SIZE-CODEC] "CONSTANT QUALITY" IS NOT CONSTANT: constantQuality.ts:338 floats QP up one step whenever output runs >1.15× a bitrate ceiling, to MAX_GOVERNED_QP 32 from a target of 20 — a large quality drop, taken automatically, to hit a SIZE target. A hidden size governor living inside the thing named for not doing that; against robert (8)/(13) it is a defect, not a setting. Decide it deliberately or bound it so fine detail is never the thing it spends.
- [P1 → SIZE-CODEC] WHERE THE BYTE BUDGET CAME FROM (traced 2026-09-02 on Robert's "where the fuck byte budget came from"): `VIDEO_BITRATE = 8_000_000` at codecs.ts:11 — a bare constant, NO comment, NO measurement, NO derivation, in a repo where every other number carries a paragraph about its origin. Everything hangs off it: quality.ts scales it per tier as a bits-per-pixel ratio of 1440p's (quality.ts:64, resolveTier), and the export QP governor's ceiling is that scaled number (constantQuality.ts:338). Extrapolated to 3024x1964@60 it asks 45 Mbps = a 645 MB ceiling (quality.ts:430). It is also mostly inert on still content — measured 1.84 Mbps achieved against the 8 Mbps ceiling on still text (quality.ts:101) — so the governor does NOT fire on a still screen with a ticking clock; it fires on MOTION, where it makes the picture worse to hold a limit nobody derived. Either measure the number or delete the ceiling; do not inherit it. SEPARATE DEFECT in the same governor, independent of the constant: it governs on a CUMULATIVE average since the first packet (`bytesOut / elapsedSec`, constantQuality.ts:336) — not a recent window — so it answers the whole history instead of what is on screen now: late in a long take it barely moves, and at the very start, where `elapsedSec` is floored at 0.5, a keyframe burst can read as wildly over budget and walk QP up within the first fraction of a second (UNVERIFIED — check whether the OPENING of a motion render is its worst part). Deleting the ceiling changes NOTHING on still content (1.84 of 8 Mbps achieved) and affects only the full-render path — instant copies and smart cuts never reach this code, but max+camera does.

## UI asked for, on the UI, not wired → tasks
F19 continue from the playhead (button says "roadmap") · F20 the timeline closing over a cut · "Show in folder" cannot exist on the web (it explains the Downloads folder instead) · Send / Copy link on take cards blank by ruling 2026-08-30 · pause/resume → U2 · device picker → U5.

## Later — approved shapes, deliberately inactive
- Two devices, one take (phase 5; DECISIONS 2026-09-02 (3)/(4)): one account pairs paid users, p2p/manual pairing for free (v2, "not only QR"); a monitor stream each way over a direct WebRTC link (relay only as a paid fallback); each side records locally at full quality; fragments ship on J1's journal; the remote camera is a PiP input to the live compositor; clock = pings + room-audio correlation with drift fit; the take certifies its own sync bound; the inviter moderates; users may let another user save their recording into that user's cloud. Streaming out = the same link to a WHIP ingest.
- Cursor-excluded capture + vector cursor re-render, click ripples, auto-zoom (P4's metadata track).
- Local quality models, zero tokens, per instance (camera background blur; denoise = F10).
- Server-side auth-user deletion hook (client already deletes user-owned rows/objects).
- Image/screenshot capture channel.
- Instant link mode (progressive upload + cloud assembly; OFF by default, consent copy) — rides J1's journal.
- Design-later pack: brand templates on F3 · link-unfurl page · library search · keyboard-first editing.
- Pre-cut capture (keyframes at speech onsets; Robert "idk, later") — superseded by idea 3, which costs no capture bytes.

## Project
Phase 2 (TASKS): provision Supabase + Google OAuth and prove login → upload → link in a second browser · domain + product email · public deploy · first users + feedback channel. Robert records daily; friction becomes bounded decisions, UX changes are his.

## Ideas — the 45 of 2026-09-02. REVIEWED 2026-09-02 (Robert: "yes, tidy it"); 23 of them were already tasks.
Numbers are PERMANENT ids (.ai/TASKS cites them). Rule for this block: an idea whose work has a home
elsewhere is ONE POINTER LINE here, never a second copy of the spec. Tags: UX = a user sees or hears a
change → Robert's yes · eng = invisible.

### Owned elsewhere — do not re-plan here
4 → X14 · 5 → X11 (idea 5 IS its light first step, now X11a) · 11 → SIZE-CODEC lane · 15 → AI1's delta
detector, measured by X17 · 17 → P4 · 20 → E2 · 28 → J1 · 29 → C1 · 30 → C2 · 31 → C1 · 32 → C5 ·
33 → C3/C8 signalling · 34 → C4 · 35 → the FROZEN RULE constraints in .ai/TASKS · 36-42 → CONTINUOUS
LANE + T1 (40 also ruled: the report card is an agent/dev surface, never a user one) · 43 · 44 · 45 → C7.

### Promoted to Phase 1 by this review — specs and gates in .ai/TASKS
2 → H7 self-manifesting media files · 21 → E3 deadline-paced background work ·
22 → G7 main-thread lateness on the report card · 25 → M4 temporal-layers probe. Plus two probes that were
buried inside heavy tasks and each answer an OPEN Phase-1 bug: X14a (`VideoFrame.metadata().captureTime` —
the string appears NOWHERE in src/; the anchor is still read-time `performance.now()`, measuredVideo.ts:229)
and X11a (transfer the track processor's readable into the worker — the standing suspect for B12).

### The one measurement — ideas 1, 15, 16 and AI1's known miss are ONE number, not four projects
X17 the changed-tile census (.ai/TASKS, Phase 5): how much of Robert's REAL work screen changes per second,
at what tile size, measured BEFORE anything is built. It decides 1 (lossless changed-tile master for work
screens; perfect text, far smaller, frees the screen encoder; hybrid with the encoder for motion · UX ·
heavy), 16 (the lossy sibling: an atlas of changed rectangles through the hardware encoder · eng · heavy),
15 (zoom proposals from pixel change, like Tighten · UX proposals · medium) and why AI1's keyframe picker
dropped frames whose only change was small (cursor, text fields). Robert on 1: "try it but skeptical" —
the census IS the cheap half of trying it.

### Still only ideas — phase-gated, nothing owed until their gate opens
6 constant-quality capture: quantizer mode + the export's governor on the raw screen encoder · UX (file sizes) · medium · measure the size delta first, then Robert's call. SETTLED 2026-09-02 (Robert: "constant quality is must be anyway isnt it? unless is elastic is on, so offering me to turn off elastic?"): they are NOT alternatives and elastic is never turned off. Elastic decides HOW MUCH WORK WE ASK FOR (resolution, rate, what is composited); the quantizer decides HOW FAITHFULLY the encoder renders what it was asked for. Today's bitrate ceiling is a SECOND, hidden elastic system nobody controls — under pressure it silently discards the finest detail, which is exactly the ticking tab clock of robert (13). Constant quality removes that hidden system, so the visible one (E2/M1, "like water") must be good enough to be the only brake. Still needed: an upper bitrate bound so a pathological screen cannot fill the disk (disk-guard territory), and that bound may never be met by trading the clock away.
7 cloud player composites screen+camera from two packet copies + the camera track as data → instant max+camera SHARE · UX · medium · needs C8
9 the record IS the export — **J2 KILLED 2026-09-03 (DECISIONS robert (23)); the research is parked in TASKS Phase 5 and is not a task.** What survives of the idea and is live: `.ai/PROPOSALS` A (an instant unedited export at max by recording the finished picture beside the clean screen, nothing removed) — Robert's call. B14's number is in: max records 3024x1964@60 on prod (09-03). Idea 7 stays the share-side twin.
14 continue after a crash: recovery + R-CONT continue = one take · UX control · medium-heavy · after F19
18 installed core serves the browser: PWA gains native capture when the core is installed · UX install · medium · after P4
24 heat as a signal: thermal state from the native shell, battery-aware ceilings in the browser · eng · light · after P4
26 two devices, one take — the full approved shape is in "Later — approved shapes" above · UX new mode · heavy · needs C1-C3 + J1
27 streaming out: composed stage as a track to any WHIP ingest, same link as 26 · UX new mode · medium · after 26

### DEAD by Robert's word 2026-09-02 — do not re-propose
3 speculative seams (pre-cut at silences while idle) — "i dont feel like its good, triming goes back and forth all the time". A pre-cut guess is worthless when the trim keeps moving. What he actually wants instead: R1, review the render path, it was too slow on his last take.
11 name the size (type an exact target file size) — "bullshit, user only use quality slider".
15 zoom proposals from pixel change — "bullshit, excatly the shit that must not go unaproved, features for users". The delta detector itself survives ONLY as X17's measurement and AI1's index, never as a user-facing suggestion.
8 idle consolidation · 12 the player is the editor · 13 every take at 60 by interpolation · 23 UI complexity dial · 32 device-hosted sharing for free users — Robert 2026-09-02 on the whole cut list: "other to cut - fuck them".

### THE INTERACTIVE SCREEN — a direction Robert named 2026-09-02, bigger than any single idea
"select text is important, clicking links on screen too, also i think this is important direction, we can make screen on video almost interactive." Text in a recorded screen is selectable, links are clickable — the recording stops being a flat picture. Robert 2026-09-02 asked how far it goes: "i pause video of guy on some page and i can scroll that page?" — THREE LEVELS, and the middle one is new: (a) OCR the paused frame → select/copy what is VISIBLE, recognise visible URLs; no scrolling, what was off-screen was never recorded. (b) SCROLLING IS SEEKING IN TIME — if he scrolled past that content at any point in the take, those pixels exist somewhere in the timeline, so scrolling a PAUSED frame = jumping to the moment that region was on screen and stitching it in. Needs no extension and no DOM: it needs the change-tile index that idea 1/16 produces anyway, plus a scroll-offset estimate (derivable from tile motion). This is the strongest argument yet for idea 1. (c) the page's own description (idea 10) → true scrolling and real links even for what was never shown; needs a vehicle and breaks on canvas/video/protected content. D-OCR is the cheap half; the full half needs idea 10 — the reason 10 survived the cuts. Same data serves AI collaboration. Also on his mind and already logged in "Later — approved shapes": the screenshot / image capture channel.
10 record the page not the pixels — Robert 2026-09-02: "10 is way to experiment later, put it somwhere for coloboration with ai experimints in v2 to consider". NOT cut. Phase 5 / v2, beside idea 1, as an AI-collaboration experiment; still needs AI2's vehicle (extension/snippet/P4), still breaks on canvas/video/protected content.
19 shared math in Rust → WASM + native — UN-DEMOTED by the ceiling ruling (DECISIONS robert (7)): it belongs INSIDE P4, not nowhere.
NOTE on 1 and 16 after the ceiling ruling and the every-movement rule: idea 1 is a v2 experiment with a second reason to exist — Robert 2026-09-02: "feels like something that can be used for collaboration with ai too" (a tile stream already states WHAT CHANGED, which is what an agent needs and what a video hides). Idea 16 stays alive but is now bounded: any tile that changed AT ALL is kept, cursor twitches included; no threshold is permitted, so its saving must come from the encoder, never from discarding movement.

### Idea outside the 45, kept
- OCR text selection in the paused player (Robert 2026-08-29: "select text from video" while paused): OCR the paused frame locally (zero tokens), transparent selectable spans over the stage scaled with its transform, cached per frame; degrades to today. Open: WASM OCR size vs the PWA budget · the cloud player · feeding the For-AI index. → Phase 4 as D-OCR.
