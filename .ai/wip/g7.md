# G7 — main-thread lateness as a report-card dimension (branch task/g7, worktree /tmp/inout-g7)

## DONE
- read: .ai/TASKS G7 + B10 + operating rules, reportCard.ts, session.ts stop stats, pressure.ts, scripts/editor-drag-cost.mjs.
- BUILT: `core/lateness.ts` (worker clock + main-thread stamp, O(1) tally, LoAF/longtask owners),
  `core/latenessBeat.worker.ts`, `LatenessSummary` in `core/types.ts`, `stopStats.lateness` written at
  the stop (session.ts `clearTick`, so a cancelled take takes its worker with it too), the `lateness`
  dimension + `buildEditorCard()` in reportCard.ts, `startEditorLateness()` on the editor's mount,
  and `__inoutEditorReport()` / `__inoutLateness(ms)` / `__inoutLatenessStart()` in main.tsx.
- TESTS: `core/lateness.test.ts` (11) + 7 new report-card cases; typecheck + `npm test` green.
- RIG: `scripts/g7-lateness.mjs` (lanes clamp/cost/take/editor); `scripts/lib/chrome.mjs` grew ONE
  opt-in option, `throttled: true`, because every rig here disables background throttling and the
  clamp question cannot be asked on a Chrome that has it off.

## MEASURED
- **CLAMP LANE — 2026-09-02, headed Chrome with background throttling LEFT ON, page HIDDEN, 20 s,
  both clocks in the same page** (`node scripts/g7-lateness.mjs --lanes=clamp`):
  - main-thread `setInterval(16)`: **1.2 Hz · p50 983.6 ms late · p95 986.9 · max 989.8** (24 ticks).
    Chrome's ~1 Hz clamp, confirming E1's 984 ms to within 0.4 ms on this machine.
  - worker beat, stamped on that SAME hidden main thread: **63.3 Hz · p50 0.0 · p95 0.6 · max
    153.5 ms · 13 beats missed** (1,266 arrivals).
  → THE DESIGN HOLDS. worker→main postMessage is NOT throttled where a timer is clamped 60x, so the
    take's card can carry a real number. The 153.5 ms max is a genuine main-thread stall in a hidden
    page — a timer would have buried it under 984 ms of throttle.

- **THE TAKE'S CARD CARRIES THE DIMENSION** (two runs, headed, visible page, `?synthetic=1`, 20 s
  synthetic takes): `rec_hz9ymbn7bcq8 · 19.9s · GREEN — 12 of 12 dimensions measured and inside band`
  and `rec_tzv1xjar3af2 · 19.4s · GREEN`. Readings: worst second **16.2 ms at 1.0s** (p50 0.5, p95 1.0,
  max 16.2 at 1.3s, 0 of 1,240 samples over one frame) and worst second **2.0 ms at 0.0s** (max 2.0,
  0 of 1,240 over a frame). So a clean take passes with room — the band does not cry wolf.
- **THE EDITOR'S CARD, NO DRAG, 1080p take**: `editor · first 14.9s · RED — the main thread was 150 ms
  late at 0.0s` (9 of 934 samples over a frame). The stall is the editor's FIRST FRAME, not the size
  probe. A second run of the same shape read GREEN at 7.8 ms worst — so the 150 ms is the mount, and
  it is intermittent. NOT MY TASK, but it is a real Phase-1 defect: file it in BACKLOG.
- **B10 DOES NOT REPRODUCE ON THE DEFAULT SHAPE, and that is a finding about B10.** A 1080p
  `?synthetic=1` take + a 13 s drag (767 pointermoves) through the editor's whole window read GREEN,
  worst second 7.8 ms — while the size probe DID run inside it (`300 frames from 4.7s of 19.4s in
  1928 ms`). The probe encodes asynchronously, so on small frames each chunk fits inside a frame
  budget. editor-drag-cost.mjs found 35-201 ms on `qstep=max&screensize=3024x1964&prerender=1` — the
  probe encoding NATIVE-RES frames beside an at-stop pre-render. That query is what the B10 gate must
  be read on, and it is the run now queued.
- **COST — the first two methods were the wrong instrument, and said so.** A throughput A/B (fixed
  work, then fixed wall) read **209.5 → 210.9 work units/s, i.e. the SAMPLED run faster**: the
  workload's own spread is ±0.7 % where the quantity is ~0.03 % of a second. Replaced with CDP
  `Performance.getMetrics` TaskDuration over two equal idle windows — the renderer's own main-thread
  busy time. The sampler's SELF-report meanwhile: **0.783 ms/s over 499 samples at 16 ms** (and 0.625
  over 1,240 on a take) — that is an upper bound dominated by `performance.now()` quantisation on a
  ~2 µs body, which is why the external number is the gate's.

## NEXT
- QUEUED BEHIND J1's 900 s cell: `scripts/gate.sh node scripts/g7-lateness.mjs --lanes=cost,take,drag
  --take=25 --query='synthetic=1&qstep=max&screensize=3024x1964&prerender=1'` — the B10 shape (the
  1080p default does not stall; see MEASURED). Then: fill the FLAGS row's COST_ROW_PLACEHOLDER with
  the A/B number, write the .ai/TASKS handoff, run `npm run oracle` + `--engine=v1` +
  `npm run exp -- armcancel` (capture/* and the stop path were touched), delete this file on land.

## THE DESIGN DECISION THAT COSTS A SESSION IF IT IS RE-DERIVED
A TAKE RUNS WITH THIS DOCUMENT HIDDEN (Robert records his screen and switches away),
and a hidden Chrome tab clamps setInterval to ~1 Hz — E1 measured it: a 16 ms main-thread
ticker read 984 ms late AT IDLE and 3568 ms under a real 3 s burn (core/pressure.ts header).
So a main-thread timer CANNOT be the clock for capture-time lateness: it would report the
clamp as a stall on every take. The clock has to live on a thread that is not throttled
(E1: a worker ticker in the same hidden page ran 59 Hz, max 27 ms late), and the main thread
only STAMPS ARRIVALS. That is the instrument: worker beats, main-thread arrival, lateness =
arrival − due, both converted through performance.timeOrigin.

## LOST ONCE, DO NOT REPEAT
- the first worktree was cut in `$TMPDIR` (`/private/var/folders/...`) and macOS PURGED it between
  sessions: every uncommitted file went with it (the rig, rewritten from scratch). This worktree is
  `~/.inout-worktrees/inout-g7`, where the other live sessions moved for the same reason. COMMIT
  AFTER EVERY WORKING PIECE — a task branch commit is local and costs nothing.

- **B10 REPRODUCED AS A NUMBER — 2026-09-03, built bundle, `qstep=max&screensize=3024x1964&prerender=1`,
  25 s take, 30 s drag (1,739 pointermoves) through the editor, the size probe running inside it
  (300 frames in 9,952 ms):**
  - the product's instrument: **worst second 229.3 ms at 3.0s** (1,794.6 ms of lateness over 63
    samples), second worst 53.8 ms at 2.0s, 18 of 1,874 samples over one frame, 0 missed.
  - editor-drag-cost.mjs's own interval ticker, SAME page, SAME drag: max **230.2 ms**, spikes at
    2,982 ms (54 ms) and 3,302 ms (230 ms). THE TWO INSTRUMENTS AGREE TO 1 ms, which is the answer to
    "is the new one measuring the same thing" — it is.
  - IT NAMES AN OWNER, AND IT IS NOT THE ONE B10 NAMES: `output() via AudioDataOutputCallback`,
    **192.7 ms of blocking inside a 269 ms frame at 3.03 s** (plus 12.8 ms blocking at 2.92 s). The
    size probe's own callback is in the list at 442.7 ms duration and **0 ms blocking**. So the stall
    a person feels while dragging is AUDIO DECODE OUTPUT on the main thread, and B10's fix (move the
    size probe off the main thread) would leave it exactly where it is.
  - A 13 s DRAG DOES NOT REPRODUCE IT (max 16.4 ms, both instruments) — the window has to be ~30 s.
- **COST — the gate is NOT met at a 16 ms period, and the number is the wake-up, not the code.**
  Built bundle, CDP TaskDuration: idle page 0.17 ms/s busy → **+7.6 ms/s at 16 ms, +5.0 at 32, +2.3 at
  64**, and +7.5 with attribution on (so the observer is ~0.1-0.3 of it). Inside a running take, at
  16 ms: main thread busy **119.1 → 127.6 ms/s (+8.5)**, of which SCRIPT is only +1.0 — the other
  7.5 is task overhead per wake-up. Per beat that is ~120 us at 16 ms and ~150 us at 64 ms, i.e. a
  fixed price per wake-up rather than anything my handler does. The controls that decide what to do
  about it (bare setInterval, bare worker beat with an empty handler, 128/256 ms periods) are running.

## BLOCKED
- the gate: J1 holds it with a 900 s chunkrender cell (`--timeout=7200`). The B10 run is queued behind
  it; `scripts/gate.sh --status` says who has it.

## DO NOT
- do not sample the main thread with setInterval/rAF during capture (clamp, above).
- do not touch src/core/report/reportCard.ts from another session — G7 owns it.
- do not run anything heavy without scripts/gate.sh; never merge to main from here.
- do not measure this sampler's cost with a throughput A/B — 0.03 % of a second under ±0.7 % of noise.
  CDP `Performance.getMetrics` TaskDuration is the instrument; the lane already uses it.
- do not rank long-animation-frame owners by `duration`: a 487.4 ms frame with `blockingDuration: 0`
  outranked everything on a take whose worst sample was 2.0 ms. Blocking time is what a stall is.
- do not look for B10 on a 1080p synthetic take (above).
