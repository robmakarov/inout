# P9 — the frame-source seam (v2 on Safari and Firefox)

## DONE
- The seam contract in `src/core/types.ts`: `FrameIntakeKind` (main-processor · worker-processor ·
  element-sampler) + `FrameIntakeDeclaration` (maxFps · frameClock · liveness · mainThreadPixels).
  `CompositeRecording` now carries `intake` and `painter`.
- `src/core/capture/frameIntake.ts` — the probes, the order, the switch (`?intake=`,
  `inout.capture.intake`). Probes, never names.
- `src/core/capture/frameIntakeElement.ts` — the Gecko rung: hidden `<video>` per source, its own
  AudioWorklet tick at 125 Hz (3 quanta), `new VideoFrame(el)` at most once per output period and never
  while the element's clock has not advanced.
- `compositor.worker.ts` — `cmd:'probe'` (does this worker have a processor?) and `cmd:'source'`
  (a CLONE of the track is transferred in and read here). One `ingestFrame()` for every rung. The worker
  branches on the MESSAGE, never on the browser. `mainTimeOrigin` aligns the worker's clock to the
  main thread's so the audio and the video stay on one clock.
- `liveCompositeV2.ts` — the walk, the state line before the press, the liveness beat for the rung that
  gave its track away, teardown.
- `reportCard.ts` — the `picture` dimension now names the intake and the painter per take.
- The oracle carries the rung: `OracleReport.compositeIntake/compositePainter` → `oracle-gate.mjs` →
  `made=<intake>/<painter>` on every cell line.
- `/?test` panel row "How frames get in" + docs/FLAGS.md.
- `npm run typecheck` green · `npm test` 1194/1194 green (12 new in frameIntake.test.ts).

## NEXT
Land it: re-read `.ai/TASKS` against `git log` (main moved 3 commits), rewrite the P9 row to what is
proven and what is not, `git merge --ff-only task/p9` FROM THE MAIN CHECKOUT.

## MEASURED
THE ONE THAT CHANGES THE TASK — Chrome 152, this Mac, headless AND headed (scripts/see.mjs):
  main thread  MediaStreamTrackProcessor  YES
  worker       MediaStreamTrackProcessor  NO       VideoEncoder YES
  MediaStreamTrack transfer to a worker   DataCloneError ("does not have a transferable type")
CHROMIUM AND WEBKIT ARE EXACT COMPLEMENTS ON THIS API. The `worker-processor` rung therefore CANNOT be
exercised on Chromium at all — the task row assumed it could. What IS proven on Chromium is that its
probe refuses correctly and the walk falls to the sampler.

Oracle cells, `scripts/gate.sh npm run oracle`, each naming what it measured:
  intake=main     PASS made=main-processor/webgpu  sync=56.6/67.3ms compT0=0ms   export=3.89x
  intake=worker   PASS made=element-sampler/webgpu sync=65.1/79.7ms compT0=33ms  export=3.73x
                       (the fall-through, working — NOT evidence for the worker rung)
  intake=element  PASS made=element-sampler/webgpu sync=56.3/70.9ms compT0=233ms export=4.08x
Earlier runs before `made=` existed (auto/worker/element, 39.8-53.5 ms sync, all PASS) are NOT evidence
for any particular rung and are not quoted.
typecheck green · 1194 tests green (13 new).

## BLOCKED
- The `worker-processor` rung has no evidence anywhere and cannot get any on this machine's Chrome.
  Needs Playwright WebKit or Robert's own Safari. Robert 2026-09-04: "No, not now" on the download.
- Which engine a Safari/Firefox user gets by DEFAULT has not moved and must not until those cells exist
  (`intakeArmed()` carries the rule and the reason).

## DO NOT
- Do NOT call it "frame source" in code or flags: `inout.frame.source` already means the SOURCE the
  composite's SHAPE follows (src/core/frame.ts, F13). This seam is the INTAKE.
- Do NOT add ticks to `TAP_SOURCE` in liveCompositeV2.ts — `sampleLiveness()` runs on every tap message,
  so an extra tick moves the SHIPPED Chromium path's liveness cadence.
- Do NOT transfer the take's own track to the worker: the same MediaStream feeds the raw channel
  recorder and the preview, and a transferred track is detached from the page. Transfer a CLONE.
- Do NOT read an oracle cell as a rung's evidence unless its `made=` says that rung. Three cells were
  nearly quoted for a rung that had not run.
- Do NOT try to measure the sampler with a bare off-DOM `<video>` on a canvas captureStream in a fresh
  tab: it sits at readyState 0 / videoWidth 0 forever there (measured, track live, canvas painting).
  It works inside the oracle and in v1. The instrument is the odd one out; use the oracle.
