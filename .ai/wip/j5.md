# J5 — background render while editing (task/j5, worktree ~/.inout-worktrees/inout-j5)

DONE
- `core/compose/editRender.ts` — the ONE door from an edit to a background render: baseline = the take
  as it opened, 1.2 s settle, no render for a packet copy / smart cut, `?bgrender=0` = J3 exactly.
- `core/compose/editRenderFlag.ts` (`?bgrender=`, default ON, sticky key `inout.compose.bgrender`),
  `/?test` row "Make the export while you edit", compose/index.ts exports.
- `EditorScreen.tsx` calls `noteEditorEdit` in the effect that already ran `editBindsPrerender`, and
  `cancelEditRender` on unmount. J3's comment block replaced.
- `editRender.test.ts` (10 tests) — BORN RED proven: with the baseline rule removed, 2 fail.
- `prerenderTrigger.test.ts` rewritten from J3's absence to J5's one-door source gate (6 tests).
- `experimental/perf/backgroundEditRender.ts` + harness cell `j5` — drives the product's door and
  answers untouched / premade press / byte-identity / chunk invalidation.
- typecheck + 1228 unit tests green.

NEXT
- `node scripts/exp.mjs j5 --timeout=1800` through `scripts/gate.sh` and record the four numbers here.

MEASURED
- (nothing from the rigs yet — this line is rewritten as each lands)

BLOCKED
- nothing.

DO NOT
- Do not widen this into cutting the export's fixed cost: J7 measured that floor at 16.7 ms + 0.5 ms
  per second of take with every chunk pre-made, and ruled CUT NOTHING HERE.
- Do not ship it off. robert (27) is the yes; a fix landed disabled is doing nothing (CLAUDE.md).
- Do not use the browser pane for any lateness number — hidden document, timers clamped to ~1 Hz.
- Do not run `editor-drag-cost.mjs` at its default `screensize=3024x1964&qstep=max`: G8's synthetic
  source kills the tab 14-28 s in, and its takes sit inside that window. 1920x1080 / 1080p.
