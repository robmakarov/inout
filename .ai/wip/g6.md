# G6 instrument flakes — parts (a)-(f) ONLY [worktree ~/.inout-worktrees/inout-g6, branch task/g6]
(g) is G7's (reportCard.ts) · (h) is B13's · (i) landed on main (3260d09). DO NOT touch g/h.
WORKTREES MOVED OFF TMPDIR: macOS wiped /tmp/inout-g6 mid-session and took an hour of uncommitted
work with it. Cut worktrees with INOUT_WORKTREE_DIR=$HOME/.inout-worktrees and COMMIT OFTEN.

## DONE
- (e) RESOLVED, commit d117909. `?slow=` is NOT dead code. Prod: `?synthetic=1` armed in 183 ms,
  `?synthetic=1&slow=mic:6000` in 6079 ms with "Waiting for microphone…" from t=30 ms.
  parseSlowChannels always had a caller (synthetic.ts -> createSyntheticChannelsProgressive ->
  session.ts:632). The PARSER had 3 tests, the DELIVERY had none — that is why the belief survived.
  Fix: `deliverSyntheticProgressively(rig, config, handlers, delays = syntheticSlowDelays())` seam
  + 2 tests (fake rig, fake timers). RED PROOF: default -> `new Map()` fails exactly those 2.
  Docs corrected: docs/FLAGS.md, CLAUDE.md, BACKLOG.md, .ai/TASKS.
- (a)(b)(c)(d) MECHANISM BUILT, commit b8be439. scripts/lib/machine.mjs + both oracles:
  bounded loud preflight (waitForQuiet, band 0.35), per-run load sampler on every printed number,
  and CONFIRM-BEFORE-BELIEVING: a single cell re-measures a red and compares BY DIMENSION —
  red in both = FAIL, red in one = INCONCLUSIVE (exit nonzero, never green, never a regression).
  A cold matrix counts its own disagreements for free. CDP retries 3x and passes --logDir=,
  so a death finally arrives with chrome stderr + page console (that is G6d's real defect:
  the diagnosis was written and discarded).

## NEXT
- (f) IN FLIGHT: `scripts/gate.sh node scripts/exp.mjs o11 '{"takeMs":10000}'` -> /tmp/g6-o11.json.
  NOTE: TASKS said `npm run exp -- bits`; there is NO `bits` experiment — the id is `o11`.
  Read it with scratchpad/o11read.py: capture achievedMbps for content=screen vs content=motion at the
  SAME 8 Mbps request. screen far below + motion near request + full packet counts = honest content,
  not a starved encoder. Then write the verdict into .ai/DECISIONS and close TASKS (f).

## MEASURED (do not re-run these)
- (e) prod arm latency: 183 ms (no slow=) vs 6079 ms (slow=mic:6000, request 6000).
- machine idle baseline, six 1 s windows: busy 0.1554 0.1568 0.1629 0.1585 0.1554 0.1654,
  load/core 0.37-0.39. A 6 s oracle cell by itself runs the machine at 42-57 % mean busy —
  which is why load can NEVER adjudicate a red; only a second reading can.
- v1 oracle, 8 cold cells, lock held: export throughput 1.07 1.19 1.42 1.46 1.00 1.32 1.39 1.43
  (8/8 over the 1.0x band; task opened at 0.46-0.94x loaded). Band sits AT the measured minimum.
  Same matrix: `tail: last flash` red in 1/8 -> correctly INCONCLUSIVE, no finding.
- v2 oracle 6 s: PASS, export 4.66x, spur -56.2 dB, sync 50.7/70.7 ms.
- v1 oracle 6 s single: PASS, export 1.2x, spur -59.0 dB.
- fidelity, quiet: render toneErr 0.03 dB / THD -68.8 / sep 59.8; instant residual 0.04 dB.
  (Task opened at 8.69 dB / THD -56.8 under load — that red was the machine.)
- fidelity --no-composite red proof: CONFIRMED on the second take (structural red still fails).
- typecheck clean; 1068 tests green (94 files).
- (d) 3 x 120 s cells under the lock: 3/3 PASS, 178.9/179.9/194.6 s, export 6.21/6.17/5.48x,
  spur -56.0/-52.9/-51.3 dB, tail 79/71/67 ms, preflight 9.9 % busy, per-cell load mean 27.5/31/38 %.
  The ~1/3 CDP death did NOT reproduce -> its CAUSE IS STILL UNNAMED. Do not claim it fixed.

## BLOCKED
- nothing.

## DO NOT
- never merge to main, never move the main ref, never checkout another branch.
- every browser/render/encode run goes through `scripts/gate.sh` (one at a time, all sessions).
- do not touch src/core/report/reportCard.ts (G7) or the black box's audiotap field (B13).
- do not read a single red as a regression — that is the whole point of this task.
