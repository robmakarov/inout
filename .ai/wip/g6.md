# G6 instrument flakes — parts (a)-(f) ONLY [worktree /tmp/inout-g6, branch task/g6]
(g) is G7's (reportCard.ts) · (h) is B13's · (i) landed on main (3260d09). DO NOT touch g/h.

## DONE
- (e) RESOLVED. `?slow=` is NOT dead code. Measured on prod (headed Chrome, fresh profile, SW busted):
  `?synthetic=1` armed in 183 ms · `?synthetic=1&slow=mic:6000` armed in 6079 ms with
  "Waiting for microphone…" from t=30 ms. parseSlowChannels HAS a caller and always did
  (synthetic.ts -> createSyntheticChannelsProgressive -> session.ts:632, every synthetic arm).
  The belief survived because the PARSER had 3 unit tests and the DELIVERY had none.
  Fix: split `deliverSyntheticProgressively(rig, config, handlers, delays = syntheticSlowDelays())`
  out of createSyntheticChannelsProgressive; 2 new tests drive it with a fake rig + fake timers.
  RED PROOF: replacing the default with `new Map()` fails exactly those 2 (30 pass / 2 fail).
  Docs corrected: docs/FLAGS.md, CLAUDE.md, BACKLOG.md, .ai/TASKS.

## NEXT
- (a)-(d): build `scripts/lib/machine.mjs` (contention probe) and make the three rigs
  INCONCLUSIVE-or-retry instead of flipping. Baseline the contention probe idle first.

## MEASURED
- (e) prod arm latency: 183 ms (no slow=) · 6079 ms (slow=mic:6000), request 6000 ms.
- typecheck clean; instantArm.test.ts 32 pass (was 30).

## BLOCKED
- nothing.

## DO NOT
- never merge to main, never move the main ref, never checkout another branch.
- every browser/render/encode run goes through `scripts/gate.sh` (one at a time, all sessions).
- do not touch src/core/report/reportCard.ts (G7) or the black box's audiotap field (B13).
