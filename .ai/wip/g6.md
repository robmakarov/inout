# G6 (a)-(f) — DONE, on branch task/g6, NOT merged [worktree ~/.inout-worktrees/inout-g6]
(g) is G7's (reportCard.ts) · (h) is B13's · (i) landed on main (3260d09). DO NOT touch g/h.
WORKTREES MUST NOT LIVE IN TMPDIR: macOS wiped /tmp/inout-g6 mid-session and took an hour of
uncommitted work with it. Cut with INOUT_WORKTREE_DIR=$HOME/.inout-worktrees and COMMIT OFTEN.

## DONE
- (e) `?slow=` was NEVER dead code. Prod: 183 ms armed without it, 6079 ms with `slow=mic:6000`,
  "Waiting for microphone…" from t=30 ms. The parser had 3 tests, the DELIVERY had none — that is the
  whole reason four documents said "no caller". Seam `deliverSyntheticProgressively(rig, config,
  handlers, delays = syntheticSlowDelays())` + 2 tests; RED PROOF: default -> `new Map()` fails
  exactly those 2. FLAGS/CLAUDE.md/BACKLOG/TASKS corrected.
- (a)(b)(c)(d) ONE READING IS NOT A VERDICT. scripts/lib/machine.mjs + both oracles: bounded loud
  quiet-machine preflight (band 0.35), load sampler printed beside every gate number, and a red is
  RE-MEASURED and compared BY DIMENSION — red in both = FAIL (both readings printed), red in one =
  INCONCLUSIVE, exit nonzero, "this run says nothing about the engine". Cold matrices count their own
  disagreements free. CDP retries 3x (confirming cell too) and passes `--logDir=`.
- (f) THE SCREEN ENCODER IS NOT STARVED — verdict in .ai/DECISIONS 2026-09-03.

## NEXT
- Nothing in (a)-(f). The branch is ready for whoever lands G6; (g)/(h) are other sessions'.
- Landing note for the merger: CLAUDE.md and .ai/STATE moved on main after this branch was cut
  (B13's resampler commits), so expect context conflicts on those two lines, not content ones.

## MEASURED (do not re-run these)
- (e) prod arm: 183 ms (no slow=) vs 6079 ms (slow=mic:6000, request 6000).
- machine idle baseline, six 1 s windows: busy 0.1554-0.1654, load/core 0.37-0.39. A 6 s oracle cell
  alone runs the machine at 42-79 % mean busy -> LOAD CAN NEVER ADJUDICATE A RED, only a second reading.
- (a) v1, 8 cold 6 s cells under the lock: export throughput 1.07 1.19 1.42 1.46 1.00 1.32 1.39 1.43.
  8/8 over the 1.0x band; the task opened at 0.46-0.94x loaded. MINIMUM IS 1.00 — no headroom left on
  v1 (v2 reads 1.91-4.66x). Noted at the band in oracle-gate.mjs. Same matrix went INCONCLUSIVE on
  `tail: last flash` red in 1/8 — correctly refused as a finding.
- (c) spur across 14 cells: -51.3 to -59.0 dB against a -40 band (it read -35 loaded). Zero reds.
- (b) fidelity quiet: render toneErr 0.03 dB / THD -68.8 / sep 59.8; instant residual 0.04 dB.
  Task opened at 8.69 dB / THD -56.8. NOTE: this branch is based on 03a83dd, BEFORE B13's resampler
  commits, so that comparison is like-for-like code. Red proof `--no-composite`: CONFIRMED, still fails.
- (d) 3 x 120 s cells: 3/3 PASS, 178.9/179.9/194.6 s, export 6.21/6.17/5.48x, spur -56.0/-52.9/-51.3,
  tail 79/71/67 ms, preflight 9.9 % busy, per-cell load mean 27.5/31/38 %. The ~1/3 CDP death did NOT
  reproduce -> ITS CAUSE IS STILL UNNAMED. Do not claim it fixed.
- (f) o11, one machine, one 8 Mbps request: CAPTURE still 2.53 Mbps (31.6 %) vs motion 4.43 Mbps
  (55.4 %); RENDER motion 2.400-2.409 Mbps (30.0-30.1 %) across 5 GOP rungs, screen 1.25-2.63
  (15.6-32.8 %); PSNR 66.1-93.0 dB. Camera rungs: 4 Mbps req -> 1.25 Mbps, 2.5 -> 0.89.
- typecheck clean; 1068 tests green (94 files, 7 new). v2 oracle green on final code (export 1.91x).

## BLOCKED
- v1 oracle re-run on FINAL code is QUEUED behind session 2942857d's chunkrender on the gate lock.
  v1 was already green on this branch's capture change (8-cell matrix + single cell); the only oracle
  change since is the confirming-cell retry, which is dead code on a green run.

## DO NOT
- never merge to main, never move the main ref, never checkout another branch.
- every browser/render/encode run goes through `scripts/gate.sh`.
- do not run `npx prettier --write` here: the repo has NO prettier config, so it reformats whole files
  to the wrong style. It cost one commit to undo.
- do not read a single red as a regression — that is the whole point of this task.
