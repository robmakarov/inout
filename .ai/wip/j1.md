# J1 — the render remembers. Resume snapshot (rewritten, never appended).

## DONE — built, shipped ON, every gate met with numbers
On `task/j1` (worktree `~/.inout-worktrees/inout-j1`). typecheck + 1,088 unit tests + `npm run build` green.
- `chunkPlan.ts` (PURE) — the GOP-aligned grid in exact output FRAME indices (chunk i = frames
  [i·gop·fps, (i+1)·gop·fps); gop·fps is whole at 30 and 60, so the concatenated file's timestamps
  are the unbroken render's own k/fps and there is no drift to bound). The content descriptor IS
  the edit restricted to that chunk's output span.
- `chunkStore.ts` — content-addressed OPFS (`rchunk-<sha256>`), published by a RENAME after
  finalize, so a key that exists means bytes that are complete. That one property makes the cache
  the render cursor, the resume cursor AND the shipping cursor with no bookkeeping. Capped at a
  quarter of the origin's quota, LRU, 24 h expiry; boot sweep in App.tsx; `reclaim.ts` leaves it.
- `chunkedRender.ts` — one reader set per contiguous RUN of misses; one continuous audio artifact;
  concatenation by packet copy with the avcC byte-compared across chunks (mismatch => the unbroken
  render). `shippableChunks()` names the shipping cursor.
- `render.ts` FACTORED, not duplicated: `window`, `tracks`, `sink`, `readers`, `targetNeedsAudio`
  are additive options that default to exactly today's behaviour.
- `chunkedFlag.ts`, a ROW IN `/?test`, `docs/FLAGS.md`, `urlOverrides`.
- `chunkPlan.test.ts` — 27 gates, incl. the grep-pinned no-length-heuristic rule, born red against
  the deleted `jobPixels > PACE_ABOVE_PIXELS`.
- Rigs: `exp chunkrender` (six lanes + simulated kill) and `oracle --query=` (new).

## MEASURED — every J1 gate
900 s take, 1920x1080@30 -> 1080p30, 180 chunks (`exp chunkrender`):
- ONE SPAN: a zoom => **2 of 180 chunks, 2.2 s against a cold 166.0 s** = 163.7 s saved. The
  "difference is minutes" gate, as a measurement. Audio artifact REUSED (zero audio work).
- RE-EXPORT 0 chunks **474 ms** (0.003x) · UNDO 0 chunks 442 ms · BACKGROUND **180 of 180**.
- PACKET COPY: 27,000 video + 42,191 audio packets copied, **0 frames re-encoded**; concat 513 ms
  of 165,983 ms (0.3 %).
- RESUME killed at 50 %: 90 survived -> 90 more in **85.2 s** (exactly half) -> file VALID.
- EVERY LANE demuxes to **900.075 s / 27,000 packets / 180 keyframes / certification present**.
- DISK: 181 files = 88.8 MB beside an 88.67 MB output (the cache costs ~one output).
40 s take, three kill points: valid at 20/50/85 %; a stray part file is never a hit.
COST, through the PRODUCTION EXPORT WORKER, arm verified to have actually chunked:
- 40 s take, `exp nativerender --query=chunked=`, n=3 a side: **9,501 -> 10,232 ms = 1.08x**.
- 6 s take, `oracle --query=chunked=`, n=3 a side, ALL SIX PASS: export **1.70x -> 1.56x** = the
  same ~8 %. So there is NO disproportionate short-take penalty — the hypothesis was tested and
  refuted, not assumed away.
- Output byte-identical in every comparison: same packets, keyframes, timestamps, size.

## THE TRAP THAT COST THE MOST TIME, so nobody pays it twice
A LOADED LAPTOP READS LIKE A REGRESSION. One run showed `export=4.79x -> 1.38x` and looked like a
3.5x disaster. It was the machine: 7 Chrome instances, 65 % CPU, 21 % free memory, 62k pageouts.
`gate.sh` serializes COMMANDS, not the Chromes earlier runs leave behind, so readings drift down
all night. The control arm read **1.70x** under that same load where it reads 4.78x quiet. ONLY an
A/B on ONE BUILD can tell code from machine — which is why `oracle` now takes `--query=`.
Three INSTRUMENT bugs also fired tonight, and every one first appeared as EMPTY OUTPUT, which is
indistinguishable from "ran fine, found nothing": an f-string quote (cost six renders), a variable
scoped to `parseArgs`, and the same variable missing from the retry wrapper `runOracleOnceGated`.
Loops now print `NO VERDICT` with the tail of the output rather than nothing.

## DEFAULT IS ON
CLAUDE.md 2026-09-03: "A DEFECT FIX SHIPS ON ... the thing being replaced is what carries the
switch". The defect the task names is that the at-stop pre-render renders the whole take and every
edit throws it away. `?chunked=0` (and the `/?test` row) is the unbroken render, which also stays
the automatic fallback for a take with no video, a chunk cache that will not open, or an avcC that
disagrees between chunks.
CONSEQUENCE THE MERGER MUST KNOW: with the default ON, ~a dozen `exp` rigs that call
`exportRecording` now measure the CHUNKED engine. `getLastChunkedRenderStats()` returning non-null
is the discriminator, and `nativerender` reports it per output.

## NEXT
`oracle --engine=v1 --query=chunked=` A/B (running) — v1 is the one lane that actually RENDERS
(smart cut refuses a MediaRecorder composite), and its throughput is a known G6(a) coin flip, so
it needs both arms on one build. Then the TASKS handoff block and the READY-map update.

## BLOCKED
Nothing.

## DO NOT
- Do not key chunks by index or by anything summarised (prerender.ts's key comment says why).
- Do not record raw KEYFRAMES in the descriptor — record the RESOLVED value (`viewportAt`,
  `cameraPoseAt`) and collapse the constant case. Listing keyframes made one zoom at 40:00
  invalidate all 24 chunks of a 120 s take while drawing identical pixels.
- Do not chunk the audio: AAC and opus carry encoder priming, so a splice clicks every boundary.
- Do not spawn a worker per chunk (worker start + first VideoEncoder ~ 3.2 s, H6).
- Do not reuse one VideoChannelReader across a gap of cache hits — it decodes the gap.
- Do not reintroduce a length-conditional anything; `chunkPlan.test.ts` greps for it.
- Do not trust `exp chunkrender`'s `speed` verdict: both lanes run in ONE page and the first pays
  every one-time cost, which handed the chunked lane a 25 % "win" it had not earned. The rig says
  so itself now. Use `nativerender --query=chunked=` or `oracle --query=chunked=`.
- Do not expect exact reuse across a TIME SHIFT. A cut moves later chunks onto different source
  instants and the shift is not generally a whole number of frames, so those chunks' pixels
  genuinely differ at sub-frame phase. Pure time edits never reach the render anyway (smart cut),
  and where a shift IS whole-frame the content key hits for free.
