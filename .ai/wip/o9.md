# O9 — 100 % COLOURS. Resume snapshot (rewritten, never appended).

## DONE
- (a) BUILT, DEFAULT OFF: `src/core/compose/supersample.ts` — `?ss=1|1.5|2`, storage
  `inout.compose.ss`, /?test row "Draw bigger, then shrink", docs/FLAGS.md row.
  render.ts draws the composition into an aligned N× canvas and reduces it into the
  delivery canvas once per frame (`reduce()`, counted in `stats.drawMs`). Factor is used
  only when `w*f` and `h*f` are whole and even; steps 2 → 1.5 → 1 on alignment, an 8192 px
  side or 16 Mpx. Flag read on the MAIN thread and forwarded (pipeline.ts → export.worker.ts),
  the seam workerFlags.test.ts exists for.
- Chunk key: `RenderFlagPrint.ss` in chunkPlan.ts + settingsPrint, NOT a KEY_VERSION bump —
  the default draws exactly what shipped, so bumping would junk every valid cached chunk on
  every machine for a change that has not happened. The bump belongs to the DEFAULT FLIP.
- typecheck green · 1212 unit tests green (10 new in supersample.test.ts).

## NEXT
Run the chroma rig control and the A/B: `scripts/gate.sh npm run exp -- x15c` then the same
with `--query=ss=2`, and read `3-export-render` green keptPct either side.

## MEASURED
Nothing yet in this task. Standing numbers it is measured against (.ai/TASKS):
chain green raw 80.0 → composite 70.3 → render 67.3 · ladder 1x 80.0 · 1.5x 90.1 · 2x 94.7 ·
4:4:4 99.3 at 1.11x bytes · fringe 9.15 → 3.03 · draw is 535 of 27,370 ms (1.95 %).

## BLOCKED
Nothing. Robert's eye is needed only at the finished A/B, per the READY map.

## DO NOT
- Do not read `?ss=` inside the export worker: no localStorage, and `location` is the worker
  script's URL. It is forwarded or it is dead.
- Do not expect the x15c fixture to show (a) working: it is 1920x1080 END TO END
  (`setSyntheticScreenSize({width: W, height: H})`, W=1920), so the export draw is 1:1 and
  there is nothing to supersample. (a) only has something to average when the source is
  BIGGER than the file.
