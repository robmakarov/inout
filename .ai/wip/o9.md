# O9 — 100 % COLOURS. Resume snapshot (rewritten, never appended).

## DONE
- (a) SUPERSAMPLED DRAW: built, measured, REMOVED the same day. The export's draw is byte-for-byte
  what it was. The pure function survives as `src/experimental/perf/supersampleDraw.ts` ONLY so the
  refusal can be re-run. Do not rebuild it — the reason is in render.ts at the frame canvas.
- (b) THE 4:4:4 RUNG SHIPS, OFF: `src/core/compose/fullColour.ts` (`?colour=all`, storage
  `inout.compose.fullcolour`), pinned in render.ts beside constant quality, /?test row
  "Keep every colour", docs/FLAGS.md. Probed per frame size, `prefer-software`, declines into
  today's rung THROUGH THE DOOR. The instant, smart-cut (choose.ts) and chunked (chunkedRender.ts)
  paths decline BY NAME when it is on; all three fall through to the unbroken render.
- Chunk key carries `RenderFlagPrint.fullColour`. No `KEY_VERSION` bump: the default renders
  exactly what shipped, so bumping would junk every valid cached chunk for a change that has not
  happened. The bump belongs to a DEFAULT FLIP.
- Rig + driver: `exp o9draw` (src/experimental/perf/drawCeiling.ts) ·
  `sh scripts/gate.sh node scripts/o9-colour.mjs --ab` · evidence docs/qa/o9-colour.json ·
  A/B in `~/Downloads/inout-o9`.
- typecheck green · 1,213 unit tests green · oracle v2 PASS.

## NEXT
Robert looks at `~/Downloads/inout-o9` (open the two `-4x.png` first — that is where it is visible)
and says whether the default moves, and for which destinations. Nothing else here is engineering.

## MEASURED
- The DRAW loses nothing: a 1:1 draw of a 3024-wide source into 1080p keeps 99.8 % green with no
  encoder anywhere (fringe 3.21); at an exact 2:1 minification 100 % and fringe 0.19.
- Supersampling the draw: finished 4:2:0 file 78.2 % → 78.2-78.7 % (run variance), fringe 3.21 →
  3.59 (worse), +8 % export wall clock. `imageSmoothingQuality:'high'` at 1x moves nothing either.
- 4:2:0 vs 4:4:4 on one still code page at 1080p: green 77.8 → 99.3 %, fringe 8.60 → 3.61,
  268 → 296 KB (1.10x).
- End to end through the product (`exp x15c --query=colour=all`): render green 60.9 → 77.8,
  blue 71.4 → 86.1; it lands on the raw channel's own 78.2, so the export stops costing a
  colour generation. Control run same day: raw 78.2 · composite 70.3 · instant 70.3 · render 60.9.
- (b) on MOVING content: AV1 4:4:4 software 72.8-84.7 fps vs AVC hardware 147-154 (1.8-2.1x slower),
  1.87x the bytes. On the still page +7.5 % wall clock.
- oracle v2 PASS: sync 65.4/77.6 ms, spur -54.6 dB, tail 71 ms, inst=instant/61.2, trim=smartcut/59.3,
  export 3.46x, made=main-processor/webgpu.

## BLOCKED
Robert's eye on the A/B, and his yes for any default. Nothing technical.

## DO NOT
- Do not read `?colour=` inside the export worker: no localStorage, and `location` is the worker
  script's URL. It is forwarded (pipeline.ts → export.worker.ts) or it is dead.
- Do not let the chunked render take a 4:4:4 export: its concatenation muxes ONE AVC track and the
  first attempt died on "Couldn't extract an AVCDecoderConfigurationRecord from the AVC packet".
- Do not expect `exp x15c` to show a DRAW lever: it is 1920x1080 end to end, so the export draw is
  1:1. Use `exp o9draw`, whose source is bigger than the file.
- Do not read X15(e)'s 1x/1.5x/2x ladder as a claim about the draw. Those rungs ENCODE at 1620p and
  2160p and cost 1.68x and 2.29x the bytes. That misreading is the whole of (a).
