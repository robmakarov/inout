# M1 — the combine: ONE elastic system, ONE DOOR (resume snapshot, rewritten in place)

## DONE — on branch `task/m1` (worktree now at ~/.inout-worktrees/inout-m1, see BLOCKED)
THE DOOR (structural half):
- `src/core/door.ts` — `passDoor(request, apply)` writes the decision and hands `apply` the only
  `DoorTicket` that reaches a dial-moving primitive. The ticket is branded with a MODULE-PRIVATE
  unique symbol: a bypassing path cannot compile. Outcomes applied / refused / failed.
  Bounded ring (400) + dropped count, never throws, one object per DECISION (never per frame).
- Contract: `DoorDecision`/`CaptureDial`/`DoorDecider`/`DoorOutcome` in core/types.ts, persisted as
  `TakeStopStats.decisions`. ONE WRITE, TWO VIEWS: a decision carrying a `layer` mirrors into E2's
  elastic ledger ON ITS OUTCOME, so `stopStats.elastic` still means what E2's grader reads.
- Routed: ladder rate step · the step max REFUSES · encoder-budget composite drop · encoder-budget
  screen narrowing · composite watchdog drop (audit d) · acquire export-ceiling cap · F15 rate budget ·
  CAPTURE_MAX cap (audit h) · odd-side evening · raw tab-audio re-ask · P0-tail throttle + timed-out
  drain (audit b) · background broker (unseen) · burst absorber (burst) · capture codec rung ·
  export codec rung skips (audit f) · Chrome's own adaptation (detected at 1 Hz, decider `chrome`).
- ENFORCEMENT `src/core/doorOnly.test.ts`: `applyConstraints` in ONE shipped file (door.ts);
  `noteElastic` in two (definition + the door's mirror); every `encoder.configure` an allowlist entry
  with a reason. PROVEN TO BITE (added a bypass to deviceGuard.ts → red, names the file).
- `decisions` dimension on the report card; `__inoutDoor()` reads the ledger mid-take.

THE EMERGENCY FLOOR (behavioural half, `?floor=1`, OFF):
- `pressureSampler.ts` — the compositor's sampler EXTRACTED; both workers use it (equivalence test
  carries the shipped formulas). `rawVideo.worker.ts` samples only when asked (max + flag).
- `emergencyFloor.ts` — the ORDER: camera fps → screen fps 60→30 → resolution LAST (one step, ¾ long
  edge, floor 1280). Audio has no rung and no branch. Recovery = the list backwards.
- `floorController.ts` — re-decides nothing: asks `ladderDecision` and turns its DIRECTION into a rung;
  where the floor's ladder is wider than the rate ladder, the SAME function is asked again with the
  rate moved to the end that leaves room.
- session.ts wires it: screen channel only, `floorArmed()` = max && flag. Also gives max its layer one
  (`noteTakePressure` had no caller at max).
- docs/FLAGS.md carries the row.

## NEXT
The product-level rig: a headed max60 take with `?floor=1` + an induced load spike, reading
`__inoutDoor()` live and the report card at stop — audio silentTail 0, picture loss < 5 s, recovery
automatic, every event in the ledger; then the same take with the flag OFF as the byte-identical
control. Then `npm run oracle:long` quoted.

## MEASURED (this branch, 2026-09-03)
- typecheck green · unit tests 1097 pass / 99 files (baseline was 1061 / 93).
- `npm run drill` 29 checks pass.
- oracle v2 PASS: sync=62.4/76.8ms spur=-56.7dB jump=0.059 compT0=0ms compOff=100ms
  inst=instant/62.4ms trim=smartcut/63.8ms tail=65ms export=4.76x.
- oracle v1 PASS: sync=67.1/82.0ms spur=-50.8dB jump=0.061 inst=instant/52.7ms trim=render/54.0ms
  tail=83ms export=1.33x.
- oracle:load PASS (stop path touched): source ALIVE, fps PASS, stopMs 25/28, tailGap 16 ms.
- `npm run exp -- armcancel` green: cancel rejected in 140 ms, pre-aborted 0 ms, normal take armed 8 ms,
  4 channels, every preview track released.
- The enforcement scan fails the build on a bypass (proven both directions).
- AUDIT (b) ANSWERED: the 2 s DRAIN_BUDGET_MS runs ONLY on the MediaRecorder lanes (the drain's filter
  requires `ch.recorder`) and on composite v1; the default raw/measured path flushes its own encoder
  with no budget. A fallback-lane defect, and now it says so in the take.
- DEFECT FOUND AND FIXED BY THE DOOR: liveCompositeV2 wrote the picture step into the elastic ledger at
  the VERDICT and session.ts then refused to step in max — every loaded max take carried a shed that
  never happened. The record now belongs to the act, with its outcome.

## BLOCKED
- M1(a) audio input latency: NOT MINE. B13 owns it.
- 2026-09-03 03:39 the system wiped $TMPDIR and EVERY session's worktree with it (all five registered
  worktrees became prunable at once; nothing was lost but uncommitted work). This worktree was re-cut
  at `~/.inout-worktrees/inout-m1` via `INOUT_WORKTREE_DIR`, which survives that cleanup. Commit often.

## DO NOT
- Do not re-fix (a). Do not move a default: the floor ships OFF, the flip is Robert's.
- Do not judge rate/fps/elastic through the agent browser pane (1 Hz timer clamp) — headed scripts.
- Do not write the elastic ledger from anywhere but the door (the scan refuses the commit).
- Do not re-run oracle:load to "check" a capture change on this machine — G6 already ruled it
  machine-bound; it passed here and that is what is quoted.
