# M1 — the combine: ONE elastic system, ONE DOOR (resume snapshot, rewritten in place)

## DONE — branch `task/m1`, worktree ~/.inout-worktrees/inout-m1. FULL EVIDENCE: the M1 block in .ai/TASKS.
- THE DOOR: `core/door.ts` (ticket + passDoor + bounded ring + mirror into E2's ledger on the OUTCOME),
  contract types in core/types.ts, persisted as `stopStats.decisions`, graded by a `decisions`
  dimension, readable mid-take with `__inoutDoor()`, and carried into every export's certification
  as `capture.gaveUp`.
- 17 sites routed, including all of the audit's (b) (c) (d) (f) (h) and Chrome's own adaptation.
- ENFORCED by `core/doorOnly.test.ts` (proven to bite) AND by the type: the ticket cannot be minted
  outside the module.
- THE FLOOR: `?floor=1` OFF · `?floorres=1` OFF (the size rung, gated on its own 5,047 ms seam).
  One instrument (`pressureSampler.ts` shared by both workers), one policy (`ladderDecision`),
  one order (`emergencyFloor.ts`), thin bookkeeping (`floorController.ts`).
- RIG: `node scripts/exp.mjs floor --headed --query='screenfps=60&camfps=60'` — four lanes
  (calm · floor · off · composite-drop) through the PRODUCTION session.
- Two defects found and fixed (requestedRate never set at max; the floor's reason named the wrong
  cause) and one removed by the door itself (the ledger recorded a max shed that never happened).
- Two instrument findings filed in BACKLOG for G6.

## NEXT
Nothing is half-done. The next session's move is EITHER land it (from the MAIN checkout:
`git merge --ff-only task/m1`, gates are green and quoted in the TASKS block) OR, if Robert wants the
seam fixed before the size rung ships at all, open the drain: the 5,047 ms is `stepScreenSegment`
waiting for an encoder that is behind, and O16's own 30 ms is what the same code does idle.

## MEASURED (2026-09-03, this branch)
- typecheck green · 1099 unit tests / 101 files (baseline 1061 / 93) · drill 29 checks.
- oracle v2 PASS sync=35.6/48.7ms inst=51.6 trim=smartcut/52.8 tail=76 export=4.95x.
- oracle v1 PASS sync=56.4/73.3ms inst=48.3 trim=render/42.3 tail=77 export=1.51x.
- oracle:long PASS (120 s) sync=46.8/69.1ms inst=55.1 trim=smartcut/54.2 tail=68 export=6.13x.
- oracle:load PASS (source ALIVE, fps PASS, stopMs 25/28, tailGap 16 ms) · armcancel green.
- FLOOR, shipping shape: engaged +841 ms · camera 60→30 · screen 60→30 · back +895 ms / +3,975 ms ·
  ZERO seams · audio silentTail 0 · 13 decisions, 0 refused, 0 failed · order held.
- FLOOR with the size rung: order camera→rate→size held; size seam DOWN 5,047 ms, UP 30 ms.
- CALM lane: 0 rungs. OFF lane (positive control): 0 decisions, no sampler.
- COMPOSITE DROP: recorded through the door at 5.6 s in the product.
- Audio padding under load varied 0 / 0 / 2,543 ms across three loaded takes — one run each, so it is
  noise until repeated; B12 owns that question.

## BLOCKED
- Robert's two flips (`?floor=`, `?floorres=`). Nothing else waits on anything.
- Entry (a) is B13's.

## DO NOT
- Do not re-fix (a). Do not move a default.
- Do not judge rate/fps/elastic through the agent browser pane (1 Hz clamp) — the rig is headed.
- Do not write the elastic ledger from anywhere but the door (the scan refuses the commit).
- Do not run the floor rig without `--query='screenfps=60&camfps=60'`: createSyntheticChannels
  re-applies the URL fps on every take, so a rig-set 60 is silently undone and the lane records 30.
- Do not read `oracle:load` as a capture verdict on this machine (G6 ruled it machine-bound).
