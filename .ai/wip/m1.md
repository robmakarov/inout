# M1 — the combine: ONE elastic system, ONE DOOR (resume snapshot, rewritten in place)

## DONE
- Worktree claimed: /private/var/.../T/inout-m1 on `task/m1` (the session it was cut for died on 401s
  with zero work; owner marker re-stamped).
- Read: TASKS M1 spec, ARCH, STATE, DECISIONS robert (12)(15)(16)(17), elasticLog.ts (E2's ledger,
  which says in its own header that M1's door writes into it), captureLadder.ts, liveCompositeV2's
  ladder application, session.stepDisplayDown/applyEncoderBudget/markCompositeUnusable/setChannelActive,
  acquire.ts's four constraint sites, rawVideo.worker.ts, compositor.worker's pressure sampler,
  reportCard's `elastic` dimension.

## NEXT
Write `src/core/door.ts` (ticket + passDoor + bounded ring + mirror into elasticLog).

## MEASURED
(nothing yet)

## BLOCKED
- M1(a) audio input latency: NOT MINE. B13 owns it; read its result.

## DO NOT
- Do not re-fix (a). Do not move any default: this ships flag-gated OFF, the flip is Robert's.
- Do not judge rate/fps/elastic through the agent browser pane (1 Hz timer clamp) — headed scripts only.
