# M1 — the combine: ONE elastic system, ONE DOOR (resume snapshot, rewritten in place)

## DONE — THE STRUCTURAL HALF (the door), on branch task/m1
- `src/core/door.ts`: `passDoor(request, apply)` writes the line and hands `apply` the only `DoorTicket`
  that reaches a dial-moving primitive. The ticket is branded with a MODULE-PRIVATE unique symbol, so a
  bypassing path cannot compile. Outcomes: applied / refused / failed (a refusal used to be written as
  a step). Bounded ring (400) + dropped count, never throws, one object per DECISION.
- Contract types in `core/types.ts`: `DoorDecision` / `CaptureDial` / `DoorDecider` / `DoorOutcome`,
  persisted as `TakeStopStats.decisions` (+ `decisionsDropped`).
- ONE WRITE, TWO VIEWS: a decision carrying a `layer` mirrors into E2's elastic ledger ON ITS OUTCOME,
  so `stopStats.elastic` still means exactly what E2's grader reads.
- ROUTED THROUGH IT (11 sites): ladder rate step · the ladder step max REFUSES · encoder-budget
  composite drop · encoder-budget screen narrowing · composite watchdog whole-mix drop (audit d) ·
  acquire export-ceiling cap · acquire F15 rate budget · acquire CAPTURE_MAX cap (audit h) ·
  odd-side evening · raw tab-audio re-ask · P0-tail throttle + a timed-out drain (audit b) ·
  background broker (layer unseen) · burst absorber (layer burst) · capture codec rung
  (measuredVideo) · export codec rung skips (audit f) · Chrome's own adaptation (detected, 1 Hz).
- ENFORCEMENT `src/core/doorOnly.test.ts`: `applyConstraints` exists in ONE shipped file (door.ts);
  `noteElastic` in two (its definition + the door's mirror); every `encoder.configure` is a named
  allowlist entry with a reason. PROVEN TO BITE: adding a bypassing fallback to deviceGuard.ts turns
  the suite red and names the file.
- `decisions` dimension in the report card; `__inoutDoor()` reads the ledger live (mid-take).
- Tests: door.test.ts (12) + doorOnly.test.ts (4) + codecDoor.test.ts (2).

## NEXT
The emergency floor at max, behind a flag that is OFF: lift the pressure sampler into
rawVideo.worker.ts (shared module with compositor.worker — never a second implementation), sacrifice
order composite→camera fps→screen 60→30→resolution LAST, audio never, every event through the door.

## MEASURED
- typecheck green · unit tests 1079 pass / 96 files (was 1061 / 93 at baseline), 3.9 s.
- The enforcement scan fails the build on a bypass (proven both ways, deviceGuard.ts probe).
- Audit (b) ANSWERED: the 2 s DRAIN_BUDGET_MS runs ONLY on the MediaRecorder lanes (the drain's own
  filter requires `ch.recorder`) and on composite v1; the default raw/measured path flushes its own
  encoder with no budget. So (b) is a fallback-lane defect.
- Codec ladder in node: every rung skipped, each one recorded through the door (unit-proven).

## BLOCKED
- M1(a) audio input latency: NOT MINE. B13 owns it.
- Oracle/oracle:long/rig runs are HEAVY → `scripts/gate.sh`, one at a time across sessions.

## DO NOT
- Do not re-fix (a). Do not move a default: the floor ships flag-gated OFF, the flip is Robert's.
- Do not judge rate/fps/elastic through the agent browser pane (1 Hz timer clamp) — headed scripts.
- Do not write the elastic ledger from anywhere but the door (the scan will refuse the commit).
