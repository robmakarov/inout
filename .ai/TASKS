# Task pack v2 — 2026-08-22. Roadmap approved by Robert: OPTIMIZATION first (O), then FEATURES (F).
# Executor: an Opus agent session. This file is the assignment; nothing outside it is.
# Supersedes pack v1 (git history archives it): v1 tasks 1,2 merged · 7 obsolete (synchronized start
# superseded instant-arm) · 9 shipped (awaits Robert real-Safari recheck, tracked in STATE) · 3 → O4 ·
# 4 → absorbed by O4 · 5 → O7 · 8 residual → BACKLOG P2 (fidelity-runner retry; oracle null-metric fix merged, 86ed200).

Operating rules (every task):
- One branch per task: `task/<id>`. Never commit to main; it lands only with the evidence below.
  (Branches already open predate this and keep their `ee/` names — they are real refs, not a convention.)
- Evidence required for merge = the task's GATES pasted as numbers/output in a handoff appended under
  the task, plus `npm run typecheck && npm test` green, plus the named oracles green.
- FROZEN RULE (Robert, 2026-07-21): never break a working path. Every new engine ships capability-gated
  with the current path as runtime fallback; defaults unchanged unless the task says otherwise.
- Frozen constraints: instant record start · instant default export · no idle device access ·
  no streaming-as-capture · src/core never imports src/app · contracts live in src/core/types.ts.
- A gate you cannot meet = stop and report WHY with measurements. A failed spike with data is a
  success; code without evidence is not.
- No refactors outside task scope. Don't touch .ai/* except appending your handoff under your task.
- HEAVY RUNS (oracle matrices, load rigs) are announced first and never run while Robert is using the machine.
- Order is dependency order WITHIN each phase, not between them: the O-phase-before-F-phase rule was
  about not building features on an engine that was about to change. In practice the whole F phase
  except F6/F8 landed on top of the shipped engine while O4 stayed dormant, because features that
  touch compose/* and the timeline do not touch the capture engine O4 replaces. What is left in F
  (F5b, F2b) is the same shape; F6 and F8 genuinely need O4.

── ROADMAP PROTOCOL (Robert 2026-08-22) ──
Robert says "roadmap" → the session prints the READY map below (unblocked tasks, parallel-safe combos,
cost bands) and WAITS. Robert answers "go <id>". One task per session (one branch per task); if Robert names
several ids, take the first and tell Robert to open parallel sessions for the rest. Whoever MERGES a task
updates the READY map (move newly-unblocked dependents in, remove the done task).

── READY MAP — current truth, updated on every merge ──
Last update 2026-08-26 (THIRTEENTH session, Robert: "go roadmap all in one" → "go next" → "go" →
"fucking finish roadmap"). THE MAP WAS EMPTY OF BUILDABLE WORK. Everything that could be decided by
measurement was; everything left needed Robert — a ruling, a listen, or hardware.
LATER SAME DAY (review session): Robert reports Xcode 27 AND an iOS 27 device ARE available → P5a moves
to READY. And Robert asked for the text measurements to run in a parallel session → authored X15
(measurement-only, changes no default) → READY. Two buildable tasks exist again.
FOURTEENTH SESSION (Robert: "go X15"): X15 DONE, all three lanes, no default moved. It ANSWERED the
question X6's ruling was waiting on and REFUTED O9's headline, which the map had been treating as
settled fact — see the amended X6/O9/X5 lines below. X15 leaves READY; X13 loses its "after X15"
dependency.
FIFTEENTH SESSION (Robert: "go R1"): R1 DONE — all 15 findings of the chroma-rig review, one step. The
instrument that measured the colour loss can no longer report a score for a measurement it did not
take: missing data reads MISSING and FAILS both chroma gates, a drifted palette reads MASK EMPTY, an
out-of-bounds rect throws instead of counting the overrun as mask hits, and three drills prove it in
one command each. THE COMMITTED NUMBERS ALL HELD — the reference is now built through the wire's own
canvas context (the fix most likely to move them) and reproduced the tables bit-identically, so
nothing was re-anchored. ONE NEW RESULT: a matched flat-slab/thin-glyph control pair separates 4:2:0
subsampling from a YUV matrix/range drift, which this fixture could not do before and which the whole
4:4:4 case rested on — slabs keep 99-101 %, the same page's glyphs 80-82 %. The mechanism is what
O9/X15(b) said it is, and 4:4:4 will deliver. Two chroma gates changed verdict ON PURPOSE (one was
unreachable-red at >= 90 % against a 70.3 % chain, one cherry-picked the colour the render loses);
both are explained in the handoff. R1 leaves READY and unblocks nothing new.
SAME SESSION (Robert: "go O3b"): O3b DONE — and it is the task R1 was the instrument for. On a screen-only
1080p take the unedited export now packet-copies the RAW CHANNEL instead of the composite: same
picture, one 4:2:0 generation earlier. Measured on one take with nothing but the copy source changed —
colour 80.0/89.1 % against 70.3/75.2, luma 37.3 dB against 35.5, fringe 8.66 against 10.24, and the
export is FASTER because the file it copies is one we did not have to make. THE COST THE MAP'S "free"
FRAMING MISSED: the download grows 14-23 %, because the raw channel spends more of the same ceiling
on the same seconds. The colour half of the win is structural and not bought with those bytes (X15(b):
3x the bitrate buys 4 % of the fringe on 4:2:0); part of the luma dB is. `?singlegen=off` reverts.
The CAPTURE half — never recording the composite at all, 45-49 % less written per second of take and
a whole hardware encoder that never runs — is BUILT AND OFF, behind `?singlegen=capture`, because it
gives up source-liveness detection and the composited preview. That flip is Robert's, on the evidence in
the handoff. O3b leaves READY.
SAME SESSION (Robert: "go P9"): P9 BUILT AND RUN — `scripts/camera-check.mjs`, real Chrome, real device,
the deployed build, answers read out of the FILES and not the interface. The answer is PARTIAL and the
missing half belongs to the machine: this Mac's built-in camera enumerates, negotiates 1920x1080@30,
keeps its track live — and delivers ZERO frames, which is what a closed lid looks like from software.
The identical code path with Chrome's synthetic camera counts 50 frames and passes every gate, so the
runner is sound. VERIFIED on the real device: the camera OFFERS 1080p (width/height max 1920) and
ACCEPTS the product's own camera-only constraint at exactly 1920x1080@30 — the constraint half of
O3a's claim. VERIFIED end to end on a controlled 1080p source: raw channel, manifest and export all
1920x1080, no upscale. UNPLANNED BONUS: that export's certification reads copiedFrom
"single-generation", so O3b's path is now cross-validated on a CAMERA-only take too. One command
finishes the row on any machine with a live camera. P9 stays on the map as a one-command re-run, not
as work.

SIXTEENTH SESSION 2026-08-29 (Robert: "?singlegen=capture, ?loudness=r128 the fuck is this" → "it must
be device resolution, how the fuck mobile will make 1920x1080? its vertical" → "yes and find out what
we can improve without that shit" → "find out what else is suspicios like this fucked up rule that
limiting us" → "put it all in roadmap"). NO CODE SHIPPED — this was an audit, and it found five
things, all now tasks. Robert READ docs/FLAGS.md and rejected the premise the whole geometry chain rests
on: the output is a landscape constant and a phone records vertical. VERIFIED ON PROD rather than
argued from the source, which is the standing rule this session's first answer broke and its second
kept: a camera-only take draws with `fit: 'cover'`, so a 4:3 source keeps 75 % of its height — the
same path on a 9:16 phone camera discards 68 % of the frame, and camera-only is the ONLY take a
phone can make. → F13 (heavy).
THE AUDIT FOUND FOUR MORE CEILINGS OF THE SAME SHAPE, two of them live on the default path TODAY:
  · P0-ladder-static — a static screen delivers 0 fps and the degradation ladder reads that as
    backpressure, stepping resolution down one-way. Seen live twice on the deployed build while the
    same take's raw channel logged "source was static". Live since nativeres went default 08-29.
  · O3c — nativeres and singlegen both went default 2026-08-29 and the first CANCELS the second:
    single generation needs the raw channel to equal 1920×1080 exactly, and nativeres makes it the
    monitor's size. O3b's measured colour win is reaching nobody whose screen is not 1080p.
  · F14 — the export step is sticky forever, so one accidental 1440p makes every later export
    re-render. Hit live: the panel opened on 1440p with the INSTANT badge on the row it was not using.
  · F15 — 30 fps is hardcoded in every tier and every constraint, with no flag. Robert-GATED: the
    question is whether INOUT should be able to make 60 fps at all, and it needs a ruling, not code.
CORRECTED THE SAME DAY IT WAS WRITTEN: docs/FLAGS.md claimed `?singlegen=capture` costs preview
quality. Measured live, the fallback preview carries ~1.8× the pixels of the compositor's 960×540
canvas — it is SHARPER. The flag's only real cost is source-liveness detection.

SEVENTEENTH SESSION 2026-08-29 (Robert: "there was rule that video always must be 1920x1080, i reverted
it ... now quality of video sucks and file size is fucking huge ... i want every device to record
best quality it can, 60 fps, and i want minimal size for exported files ... couple minutes video can
be around 10 mb with good quality, go"). TWO RULINGS LANDED IN THAT MESSAGE and both are recorded in
DECISIONS: F15's gate is answered — 60 FPS IS A YES ("every device records the best it can") — and
minimal export size is a standing objective with a stated reference point (~10 MB for a couple of
minutes at good quality), which prices the codec decision below. SHIPPED AND VERIFIED ON PROD:
  · P0-ladder-static DONE — the ladder judges deliveredFps against min(arrivedFps, requestedFps);
    framesIn was already in the stats. BEFORE on prod: a static synthetic screen consumed BOTH rungs
    in 10 s ("delivered 0.0 of 30 fps ... → 1440p", then "→ 1080p"). AFTER on prod: same conditions,
    26+ s static at 0.0-0.1 fps, zero steps. Starved-pipeline case still steps (unit-pinned).
    Priced tradeoff in the commit: crushed-arrival-without-drops no longer steps — that case is
    Chrome's compositor, keep-alive holds the file, the watchdog still guards collapse.
  · O3c DONE — single generation follows the SELECTED tier; the composite stays fenced by
    allowPacketCopy. VERIFIED ON PROD: a 720p-geometry take at the 720p step exported
    "[bits] instant copy avc from single-generation" in ~1 s — before the push that click was a
    full re-render. Quality wiring: copySourceForTier gives the panel the ladder's own answer, so
    the INSTANT badge is per step and a copyable step's size is exact (ChannelRecording.bytes, new,
    additive). On a 1440p/4K monitor the step matching the screen is now instant, native and small —
    which is the "quality sucks + files huge" pair in one fix for unedited takes.
  Gates: 510 tests · typecheck · exp o3b 7/7 twice-shipped lanes · oracle v2 PASS · oracle v1 PASS ·
  fidelity PASS · verify-deploy OK (f85aed0) · both prod repros above.

EIGHTEENTH SESSION 2026-08-29 (Robert: "go F13"). F13 BUILT AND VERIFIED ON PROD, FLAG-GATED, DEFAULT
NOT MOVED — the whole handoff is under the task. THE PRODUCT CAN NOW MAKE A VERTICAL VIDEO:
`?sourceframe=1` and a 9:16 camera take exports 1080x1920, read out of the FILE, with the instant
packet copy still firing; a 4:3 camera take keeps its full height (1920x1440, against the 1920x1080
that discarded 25 % of it); a 16:9 take is unchanged to the pixel on both copy paths. One seam,
`src/core/frame.ts`. A step keeps its NAME and its PIXEL BUDGET and changes only shape, so the
minimal-size objective is untouched. NEW ON THE MAP: F13-FLIP, which is Robert's eye on a real phone —
that was the task's own last gate and it is the only thing between here and vertical video by
default. F15 is unblocked and is the natural next one.
FOUND BY DRIVING IT RATHER THAN READING IT (note 10's shape, the other way round): the copy fence
`allowComposite` silently meant "the composite is 1920x1080", and once the composite could be any
shape a take recorded with the flag and exported without it copied a 1920x1440 file out under a
"1080p" badge. Fixed and pinned in the same push.

NINETEENTH SESSION 2026-08-29 (Robert: "go f15"). F15 BUILT AND VERIFIED ON PROD, FLAG-GATED,
DEFAULT NOT MOVED — the whole handoff is under the task. THE PRODUCT CAN NOW MAKE A 60 fps VIDEO:
`?sourcefps=1` and a 60 fps source records and exports 411 frames of an 8 s take where the same take
at 30 holds 204, read out of the FILES; the composite's own media grid says 60 against the control's
30, and the export is still the INSTANT PACKET COPY at both rates. One seam, `src/core/rate.ts`, and
it is not a 60 constant: the rate follows the source and ONLY GOES UP, so a 24 fps webcam is
untouched and a source at or below 30 cannot reach a changed line. PRICED: twice the frames for
1.5× the bytes (2.0 MB against 1.3 MB), which is the number the minimal-size objective should be
read against. NEW ON THE MAP: F15-FLIP, Robert's eye on one real 60 fps take — the task's own last
gate, and the only thing between here and 60 fps by default. `oracle:load` was announced, approved
and RUN — and it is RED ON BOTH SIDES OF F15 (identical verdict on 79d6def in a throwaway worktree),
so it is filed P1 in BACKLOG rather than counted against this task; the lead is that its own 4K
source is a rAF-painted canvas driven headless, which paints nothing.
FOUND BY DRIVING IT RATHER THAN READING IT, third session running: EditorScreen resolved its step
against the take's SHAPE and not its RATE, so the export asked for 30 fps of a 60 fps take, the new
copy fence correctly refused the mismatch, and the take silently RE-RENDERED ITSELF DOWN. Nothing
about that is visible from the source; the rig read it off the file in one run. Fixed and pinned in
the same push.
NEW TOOL: `scripts/fps-check.mjs` — real Chrome, the deployed build, both rate lanes, answers read
out of the OPFS files. It exists because the Browser pane CANNOT answer this question: a hidden tab
does not run requestAnimationFrame, so the synthetic screen paints nothing and a take driven that
way declares 60 and delivers 0.1.

SHIPPED THIS SESSION
  · X9   the For-AI build runs in a worker. Byte-identical PDF on four lanes; the UI thread loses
         13 ms of a build instead of 84. The worker is not faster — it is elsewhere.
  · X6   raw channels can record through WebCodecs → hardware AVC → fragmented MP4 (?rawcodec=
         webcodecs). O3a's objection answered with O3a's own test: a kill at 50 % of the bytes leaves
         56-88 % of the take decodable, against 753 bytes. CPU 149→95 % peak, 80→47 % mean.
  · F6   PAUSE AND RESUME, and the pause is left out of the take. Verified on prod: 8 segments,
         seams 99-218 ms, 17.3 s of recorded material from a longer wall clock. Retake NOT built.
  · O10a BS.1770 loudness — measured, implemented (14 tests), wired as an additive mode.
  · O6   the degradation ladder (9 tests) + ?nativeres=1, default off.
  · O4-polish CLOSED — the e2e wedge case, and a 4K row whose premise was wrong twice.
  · O8b  the delivered-fps band ships in oracle:load.
  · F8   CLOSED — the timeline holds 120 fps and its cost does not depend on take length.

REFUTED ON MEASUREMENT, and each one is why the map is now honest
  · X4   the decode farm: byte-identical files at 0.99-1.08× against a ≥2× gate. Decode is not
         thread-bound. DELETED; the verdict is re-runnable in one command.
  · X5   the GPU export compositor: refused on its own parity gate before the shader was written —
         the two painters disagree by 156 of 255 on 3.8 % of screen-TEXT pixels. AMENDED by X15(c):
         in PRODUCTION the divergence is a quarter of that — the render costs 2.8 dB more than a
         plain re-encode of the same frame (37.1 vs 39.9 dB), max 60, 7.8 % of pixels off by >8.
         Real, bounded, and much cheaper than the lab number implied. Still Robert's to rule on.
  · X12  capture writes 5.21/7.63 Mbps, not 18.5, and the rung it existed to price is BACKWARDS on
         screen content (−32.5 % disk for a +31.5 % bigger delivered file).
  · O9   AV1 4:4:4 halves glyph chroma fringe (9.15 -> 3.03) at fewer bytes, and 3x the bitrate on
         4:2:0 buys 4 %, so the lever is chroma resolution and not bits. THE REST OF O9's HEADLINE IS
         REFUTED BY X15(b): "capture destroys 51 % of glyph edge contrast" was its reference at the
         wrong scroll position — measured against the picture that was actually encoded, capture
         costs fringe 8.87 / contrast 1.00, and shifting the reference one scroll step reproduces
         O9's published 23.2 / 68.6 / 0.49 on demand. There is no "big half" at capture.

WHAT IS LEFT, AND EVERY ITEM IS ROBERT'S
  1. AUDIO LISTEN TEST — DELIVERED 2026-08-26: Robert listened, sound is STILL WRONG ("mic is kinda too
     loud now"), a separate live session owns the fix. Robert also asked for a per-channel gain slider
     → F9 (READY, after that session lands). Do not touch mix constants outside that session.
  2. THE FOR-AI FILE re-test — DEFERRED by Robert 2026-08-26 ("later"). AI2-AI7 stay frozen.
  3. X6 — FLIPPED. DEFAULT IS webcodecs SINCE 2026-08-26, on Robert's own ruling ("yes now it okay —
     with ?rawcodec=webcodecs i mean"), given after days of field takes on the flag. The evidence
     stack: X15(a) refuted the picture objection (identical pictures: AVC 0.52x bytes at 47.8 dB,
     glyph contrast 1.00; the old 27.9 dB was the rig comparing two separate takes of a moving
     source), and the black-boxed take pair measured the CPU relief Robert heard — audio clocks
     starved 1.6 % of wall time on MediaRecorder vs 0.16 % on WebCodecs, same regime, 10×. Robert's
     production takes on real GPU-resident sources are the field confirmation the sweep could not
     give (the formal `npm run exp -- x6` GPU-frames cell remains available). MediaRecorder stays
     the full ladder (capability + start-failure fallback; `?rawcodec=mediarecorder` / localStorage
     revert). Both oracles PASS on the flipped default (v2 36.4 ms, v1 40.6 ms) — the oracle
     records through the production session, so the gate exercised the WebCodecs raw path.
     → O3b COLLECTS THE REWARD: moved to READY below.
  4. O10a's ATTENUATION RULING — exports scatter 7.84 dB by content and sit 1-9 dB ABOVE the −14 LUFS
     convention. R128 can only fix that by turning takes DOWN, and the shipped bounding never
     attenuates. The measurement, the implementation and the switch are all in place.
  5. O6's 4K RE-VERIFY — `?nativeres=1`, record the game tab that froze on 08-22, report the console.
     No rig can stand in: a synthetic 4K source is a rAF-painted canvas, so its fps is dominated by
     a cost a real display does not have (measured — capping it moved oracle:load 14 → 14).
  6. X5's TEXT QUESTION (BACKLOG P1) — MEASURED 2026-08-26 by X15(c) and now a ruling, not a
     question: a trimmed take is BIT-IDENTICAL to the untrimmed one (smart cut copies the packets),
     and the full render costs 2.8 dB more than a plain re-encode. Artifacts to look at:
     ~/Downloads/x15-text-truth/c-*.png. Worth anything? Robert's call.
  7. THE OLD ONES, re-triaged by Robert 2026-08-26: Safari (P8 + X7) — "later", stays blocked ·
     camera-only 1080p → P9, agent-run, off Robert's plate · Yandex + RU — Robert: "fuck this yandex
     bullshit, we are building for them without testing" → PARKED; the code half stays (zero
     ongoing cost), no further RU work unless Robert either approves the Yandex download for an agent
     QA run or asks to drop the lane outright · Playwright/Firefox — "later".

NEEDS A RULING BEFORE ANYONE BUILDS: X7 only — and Robert's order is CHROME SOUND FIRST (2026-08-26
"lets fucking fix chrome sound first"), so X7 waits for the audio fix to land and be heard.
X10=O5e REJECTED by Robert 2026-08-26 ("fuck this") — moved to the REJECTED list.
O12 DEFERRED to the UI phase by Robert 2026-08-26 ("that is ux, we will add it later on ui works").
X13 APPROVED (Robert "lets try") → READY above, Tier 1 first, opt-in, default flip Robert-gated.
PARKED: X11 · X14. BLOCKED ON HARDWARE/DOWNLOADS: P1's two gates · O11d · P8. (P5a unblocked
2026-08-26 — Robert has Xcode 27 + an iOS 27 device.)
NOT STARTED, and deliberately: O10(b) multitrack · O10(c) spectral gating · O10(d) AEC (P4) ·
O9(c) near-lossless static spans (its premise is refuted by O9's own table — 3× the bits bought 4 %) ·
F6's RETAKE (a resume of a FINISHED recording: recovery, editsRepo and editor state, its own task).

FULL SWEEP, 2026-08-29 (fifteenth session, R1 + O3b): 480 tests · typecheck clean · x15c twice with
the chroma chain identical run to run and equal to the committed baselines · all three R1 self-drills
as specified · `npm run exp -- o3b` twice, seven gates green both times · `node scripts/oracle.mjs
--engine=v1` PASS · `npm run exp -- armcancel` green · f3 3/3.
THE v2 ORACLE IS FLAKY ON MAIN AND IT IS NOT THIS SESSION'S: HEAD with all of this stashed failed 2 of
4 cold runs on `instant/trimmed export sync maxAbs > 90 ms`; the same tree with O3b in it failed 1 of
2 and 1 of 3, same failure, same distribution. Filed in BACKLOG. Do not read a red v2 oracle as a
regression without running HEAD cold beside it.

EIGHTEENTH SESSION 2026-08-29 (Robert: "put bugs on roadmap"): the open DEFECTS were living in
BACKLOG.md, which is a dump and not an assignment — so nothing in it was ever picked up by the roadmap
protocol, and several have been re-found by a later session instead of fixed. Twelve are promoted below
as tasks with gates: B1-B8 are defects a USER meets, G1-G4 are INSTRUMENT defects — gates that lie, which
outrank the product bugs because a coin-flip gate is how a real regression gets merged (note 17). The
BACKLOG entries keep their evidence and now carry a pointer, the way the Safari mic bug did on 08-25.
NOT promoted, and why: anything already tracked as a task (F14/F15/F16, the Safari mic P8), anything
awaiting only Robert's ear or eye (the loudness listen tests, F13-FLIP, F15-FLIP), and the SCREEN WEDGE,
whose cause is Chrome's and whose strongest lever is O12 (Robert-gated). ORACLE-LOAD is absorbed by G2.

START HERE IF YOU ARE FRESH:
  1. read notes 5, 6, 9, 10, 11, 17 below. Note 10 (the rig is wrong before the product is) and
     note 17 (a gate that cannot fail is not a gate) between them account for most of what went
     wrong in the last two sessions — including several cases where a GREEN result was the fault,
     which is the kind this codebase keeps paying for twice.
  2. `npm run typecheck && npm test` (449 tests) and `npm run oracle` should be green before you
     change anything, so a red gate later is yours and not inherited.
  3. THERE IS BUILDABLE WORK AGAIN — see READY NOW. Two of its entries wait on the live audio-fix
     session (F9, F10); P5a needs Robert at the machine. Do not invent a task that is not on the map.
  4. anything touching capture/* keeps BOTH engines green: `npm run oracle` (v2 default) AND
     `node scripts/oracle.mjs --engine=v1`. Anything touching the STOP path also runs
     `npm run oracle:load` (HEAVY, announce it) and `npm run exp -- armcancel`.

READY NOW:
  F13-FLIP  Robert's own eye           [ROBERT OWED]  F13 IS BUILT, VERIFIED ON PROD AND OFF BY DEFAULT
                                             (handoff under the task). Its last gate is yours:
                                             open https://inout-kappa.vercel.app/?sourceframe=1 on
                                             a PHONE, record yourself, and look at the export. If
                                             it is right, the default flips and INOUT can make a
                                             vertical video; if it is not, say what is wrong and
                                             nothing has moved in the meantime.
  W1   the wedges go               [medium]  Robert 2026-08-29: "i want them gone", after hitting it
                                             three times in one evening. Chrome's bug is still
                                             Chrome's — but our safe-mode ladder had no way back
                                             except a 24 h timer, counted an OS PERMISSION failure
                                             as a wedge, and (fixed today) dropped the size bounds
                                             while backing off. The only exit anyone found was a
                                             console line.
  F18  the user's own resolution   [medium]  Robert 2026-08-29: "i want 3024x1964 or whatever users
                                             resolution is on roadmap". The export ladder stops at
                                             1440p for inherited reasons, not decided ones, and the
                                             capture ceiling follows it — one constant raises both.
                                             GATED ON O15, or ships behind a flag.
  O15  how many encoders may a     [heavy]   THE FREEZE'S ROOT, read off his own console: three
       take open                             hardware AVC encoders with nothing budgeting them,
                                             plus a game on the same GPU. The capture ceiling is
                                             doing the work a budget should do.
  O16  a resolution step a raw     [medium-heavy]  His question, answered: it CAN go safely, and
       channel can follow                    back up — F6's segment machinery is the mechanism.
                                             Costs a stepped take its instant export, so O15 first.
  F15-FLIP  Robert's own eye       [ROBERT OWED]  F15 IS BUILT, VERIFIED ON PROD AND OFF BY DEFAULT
                                             (handoff under the task). Its last gate is yours:
                                             open https://inout-kappa.vercel.app/?sourcefps=1,
                                             record something that MOVES at 60 (a game, a scroll,
                                             yourself), and say whether it is worth the bytes.
                                             Measured on prod, same 8 s: 411 frames against 204,
                                             2.0 MB against 1.3. If it is right the default flips;
                                             if it is not, nothing has moved in the meantime.
  ORACLE-LOAD — ABSORBED BY G2 2026-08-29 (same mechanism kills the whole makeRig family; do not
               run this entry, run G2). NOT F15 — same verdict on the commit before it
                                             (79d6def, throwaway worktree): composite tail 9848 ms
                                             / 7366 ms against a 400 ms band, delivered 0.2 / 2.5
                                             fps against a band of 10, both raw runs erroring "the
                                             take produced no screen channel". LEAD: the rig's 4K
                                             source is a rAF-painted canvas and `oracle:load` drives
                                             through exp.mjs, which is HEADLESS by default — a
                                             headless page runs rAF at 0, so the SOURCE starves and
                                             the band reads starvation as tail loss. FIRST MOVE:
                                             re-run `--headed`. P1 in BACKLOG. A rig that cannot
                                             make its own source move has not been a gate for an
                                             unknown number of sessions.
  SIZE-CODEC — RESOLVED 2026-08-29: APPLE'S WAY (Robert: "i like apples way"). Codec is never a user
                                             question; the DESTINATION decides it — unknown egress
                                             (saved file, OS share sheet) gets most-compatible AVC;
                                             controlled playback (our cloud player) and the explicit
                                             "smaller file" row get high efficiency (HEVC where the
                                             platform encodes it, AV1 software fallback — capability
                                             probe joins F16's T1 spike). No codec picker anywhere.
                                             Executed by F16's lanes + F17's sheet; qp-targeted
                                             encoding stays on (~11 %); the ~2x arrives with the
                                             efficiency lane. DECISIONS carries the ruling.
  F17  the Share sheet             [medium]  drafted 2026-08-29 from Robert: "its time to think
                                             through how share button will work". Destinations, not
                                             formats: Copy link / Save file (instant, always) /
                                             Save smaller file (background lane, readiness ON the
                                             row) / For AI, with the ONE quality option shown
                                             quietly. Spec below — the sheet is design, so Robert's eye
                                             gates it. UI half AFTER F16's lanes exist; the Save
                                             file + For AI rows are re-arrangement of what ships
                                             today and can land first.
  F16  semantic rungs, everything  [medium-heavy]  Robert 2026-08-29, design v2 on Robert's own counter
       already made when asked              ("cant it be same high instant quality rendered
                                             already and it just converts in smaller file ...
                                             squeeze rendering in parallel of edits"): RECORD HIGH
                                             ALWAYS (= today), then Min/Medium are BACKGROUND
                                             TRANSCODES of the High file started at stop, Max is
                                             the raw-channel copy (instant, shipped) or a
                                             background native render on camera takes, and an
                                             EDITED take renders in the background while the user
                                             edits — editing is when the machine is idle. INSTANT
                                             = the rung capture recorded: High today; the LATER
                                             pre-record picker makes any selected rung the
                                             instant one (Robert, sealed). Absorbs F14.
                                             UNBLOCKED 2026-08-29 — F15 landed. Two spikes gate it
                                             (T1 transcode speed, T2 background-render cost) —
                                             spec in the task. It is the biggest thing on the map
                                             and it is where "minimal size" actually gets paid.

  ── D1  vision + project data for an agent working on ANOTHER repo — Robert 2026-08-29, "i need
     real tool ... with minimal tokens, browser or not, find way how to do it with our recorder"
  D1   inout-dev                   [medium · T0 spike first]  PULL, DO NOT PUSH — AI1's own numbers
                                             kill the file-shaped answer (167 pages = ~144k tokens for
                                             97 s). D1 ships MCP TOOLS over a take folder instead: a
                                             cheap text index, pixels one frame at a time, and a CROP
                                             of the changed rect (~30 tokens) as the usual answer.
                                             Typical diagnosis ~1.5k tokens — cheaper than pasting a
                                             screenshot, and it carries TIME. Web projects get a
                                             dev-only hook (console/network/input/a11y/game state);
                                             native gets frames + the engine's own log, tailed and
                                             wall-clock aligned. Spec below. T0 must beat
                                             screenshot+console on tokens AND turns, or it is deleted.
  P5a  iOS SCK spike               [light]   UNBLOCKED 2026-08-26: Robert confirms Xcode 27 + a real
                                             iOS 27 device are available. Needs Robert present (device
                                             plugged in, signing). TIME-SENSITIVE (iOS 27 ~Sept).
  X13  local-adaptive-encoding     [heavy]   Robert 2026-08-26: "lets try". TIER 1 FIRST — NO model:
                                             deterministic per-5s-chunk features (luma-grid delta +
                                             edge density) steering bitrate. Changes the output →
                                             ships OPT-IN behind a flag; the default flip is a
                                             separate Robert gate with A/B files. A learned model only
                                             if Tier 1's misclassification is MEASURED, never first.
                                             UNBLOCKED 2026-08-26: X15 is done. Read its handoff
                                             first — bitrateMode is inert, quantizer mode is the one
                                             rate-control knob Chrome honours.
  F9   per-channel gain slider     [light-medium]  Robert 2026-08-26: "mic is kinda too loud now, can
                                             we add slider?" Spec in NEW TASKS below. Run AFTER the
                                             live audio-fix session lands (shared mix area).
  P9   camera-1080 verify        [light]   RUN 2026-08-29 — answer PARTIAL and the missing half is
                                             the MACHINE's, not the product's: this Mac's camera
                                             negotiates 1920x1080@30 and delivers ZERO frames to an
                                             automated Chrome (a closed lid does exactly this).
                                             VERIFIED anyway: the camera OFFERS 1080p and accepts the
                                             product's own camera-only constraint, and the pipeline
                                             preserves 1080p end to end on a controlled source.
                                             TO CLOSE IT: `node scripts/camera-check.mjs` on any
                                             machine with a live camera. Exit 0 = the claim holds.
  F10  mic noise removal           [medium]  Robert 2026-08-26: "i want noise removal". Local model,
                                             zero tokens, export-time, raw channels untouched.
                                             Spec below. AFTER the live audio session lands.
  F11  voice-aware tighten         [light-medium]  Robert 2026-08-26. A local VAD gates F5a's proposals
                                             so cuts never land on speech. Spec below.
  F12  super-resolution on zoom    [heavy]   Robert 2026-08-26: "only if zoom used". Render path only,
                                             zoomed spans only, opt-in. Spec below.

  ── BUGS — promoted from BACKLOG 2026-08-29 ("put bugs on roadmap"). Specs in BUG PACK below.
     The G lane goes FIRST when two sessions are free: a gate that lies makes every B verdict cheap talk.
  G1   the merge gate is a coin flip [medium]  `npm run oracle` fails ~half its COLD runs on main,
                                             always `instant/trimmed export sync maxAbs 97-105 > 90ms`,
                                             never the render. Measured 2 of 4 on a stashed HEAD. It is
                                             the per-task merge gate every task here is required to
                                             pass, so it teaches sessions to re-run until green. Measure
                                             whether it is the instrument or the copy paths. NOT by
                                             widening the band.
  G2   a rig that cannot move its    [light]  ABSORBS the old ORACLE-LOAD entry and widens it: the same
       own source                              mechanism kills the whole makeRig family. `oracle:load`
                                             drives through exp.mjs, HEADLESS by default; a headless page
                                             runs rAF at 0, so a rAF-painted 4K canvas source starves and
                                             the tail band reads starvation as tail loss. Red on both
                                             sides of F15 (79d6def identical), so it is not F15's.
  G3   gates that cannot fail        [light]  two evidence gates are phrased in `longtask` counts, and
                                             nothing here that awaits per frame ever forms one — the
                                             counter reads 0 on the blocking lane AND the non-blocking
                                             one. O1's memory lane samples the MAIN thread while the
                                             export renders in a worker. Both are decoration today.
  G4   nothing measured past 30 s    [light-medium]  Robert records 938-1800 s; the widest cell in every
                                             matrix is 30 s. At 120 s the instant path read 117.2/153.8 ms
                                             before 89e250e against 97-102 at 6 s — the short gate passed
                                             a defect that grew with length. Make the long cell a command.
  B1   the size panel lies           [light]  4.7 GB promised, 10.09 GB delivered at 1440p on a 140-min
                                             take (2.15x) — and it is the ONLY warning before a ~2 h
                                             render. Cheaper sibling: the PROVISIONAL numbers rank 1440p
                                             BELOW 1080p, which is an upscale of that very file.
  B2   the noises Robert can hear    [medium]  "i need them gone". TWO defects, not one: (A) the preview's
                                             own correction (cold decode -> hard seek / +-20 % playbackRate
                                             time-stretch) which HEALS on repeat, and (B) ~15 pad notches
                                             a minute already in the FILE. Discriminator: export and listen.
  B3   a stale tab kills the export  [light-medium]  a tab that spans a deploy 404s its hashed chunks and
       worker                                  the export worker dies silently — the fallback renders
                                             in-thread (7.5 s of an 8.2 s render in encode-wait). May also
                                             explain past "the fix wasn't there to hear" rounds.
  B4   a channel that recorded       [light]  a camera delivering 0 fps gives a 28-byte file, a 5 s stop
       nothing must say so                     timeout, `durationMs: 0` and ZERO channels — with nothing on
                                             screen to say so. Same family as the old silent mic-timeout
                                             loss. The machinery to notice it already exists and was not
                                             checked.
  B5   no disk guard on capture      [light]  the 30-min cap is gone and nothing reads
                                             `navigator.storage.estimate()` before or during a take.
                                             ~1.1 GB/hour is a number the UI can show.
  B6   "the device never connected"  [light]  blames the DEVICE for an OS permission: with macOS screen
       blames the wrong thing                  recording ungranted, Chrome's picker says exactly what to
                                             do, getDisplayMedia never rejects, and our 30 s deadline
                                             throws that message away. Behaviour change -> Robert's yes.
  B7   the constant start offset     [light-medium]  the two alignment errors a synthetic rig CANNOT see,
                                             both pushing audio late. This task ships INSTRUMENTATION into
                                             the take's certification, not compensation.
  B8   the two export paths disagree [light]  the render places the camera PiP -15 frames (-0.5 s) from
       about WHEN                              where the instant path places it, on the same output
                                             instants. The oracle bands audio-video sync and cannot see
                                             video-video placement between paths.

BUG PACK (promoted from BACKLOG.md 2026-08-29 on Robert's "put bugs on roadmap"; all operating rules
apply — own branch, gates pasted as numbers, `npm run oracle` green, no behaviour change without Robert).
Every entry below keeps its FULL evidence in BACKLOG.md; what is here is the assignment.
THE G LANE OUTRANKS THE B LANE. Note 10 (the rig is wrong before the product is) and note 17 (a gate
that cannot fail is not a gate) are why: three of the four G tasks describe a gate that has been
reporting a verdict it did not measure, and a B task's handoff is worth exactly what its gates are.

## D1 inout-dev — vision AND project data for the agent working on ANOTHER repo, at ~1.5k tokens
##     [medium, 3 lanes · Robert 2026-08-29: "i need real tool that will have vision and data from
##     project i work on, with minimal tokens, browser or not, find way how to do it with our recorder"]
## (NOT the frozen AI2 slot — that id belongs to AI1's candidate ladder. New D lane: developer
##  collaboration, i.e. INOUT's output used inside a project that is NOT INOUT.)

THE ONE DESIGN DECISION, AND EVERYTHING FOLLOWS FROM IT: **PULL, DO NOT PUSH.** AI1 pushes — it builds
one file carrying every keyframe, and its own measurement is why that cannot be the answer here: a
full-view page is ~800 tokens, a 97-second take built 167 pages = ~144k tokens (docs/AI_EXPORT.md).
Handing that to an agent that also has to hold a codebase is bankrupt before it reads a line.
So D1 ships TOOLS, not a file. The agent gets a cheap TEXT index and pays for pixels one frame at a
time, only when text cannot answer. Pixels are the last resort, not the medium.

WHY TEXT CARRIES MOST OF IT: everything that makes a bug diagnosable is already text or a number —
console lines, errors, network calls, input events, the app's own state, and WHICH RECTANGLE CHANGED
WHEN. That last one INOUT already computes: `core/ai/select.ts` picks keyframes by pixel delta, so
"at 4.2 s a 180x40 region at (340,220) changed" costs a few tokens and often IS the answer. A frame
costs 800; a CROP of that rect costs ~30, and AI1 already has the rule for when a crop is worth it.

LANES:
 (a) THE TAKE FOLDER [light]. Export writes `<dir>/<take>/` — the mp4, `index.json` (geometry, fps,
     per-channel startOffsetMs, ChannelDiagnostics, edit decisions, and the keyframe/delta list
     select.ts already computes), and `events.jsonl`. Takes live in OPFS/IndexedDB inside the browser
     origin and NO local process can read them, so this folder is the entire enabling step for (b).
     Offline, no cloud, no auth.
 (b) THE MCP SERVER [light-medium] — THE PRODUCT. Reads that folder; any MCP client (Claude Code,
     Cursor) in ANY repo. Tools, with their measured cost as the acceptance criterion:
        takes()                  ~50 tokens    what was recorded, when
        summary(id)              ~200-400      duration, channels, errors, the change timeline
        events(id, from, to)     ~100s         console/network/input/state text in a window
        find(id, query)          ~100s         search the text index
        frame(id, t)             ~800          ONE full view — the agent chooses to pay
        crop(id, t, rect)        ~30-100       the changed region only; the usual answer
     A TYPICAL DIAGNOSIS IS summary + events + one crop = **~1.5k tokens**, which is cheaper than
     pasting a screenshot, and it carries TIME, which a screenshot cannot.
 (c) THE HOST HOOK, TWO SHAPES, because "browser or not" is Robert's requirement [light each]:
     WEB PROJECT — a dev-only script the project adds. Records to events.jsonl, time-aligned to the
       frames: console + errors + unhandled rejections, network (method/url/status/ms), input events,
       an accessibility-tree snapshot on change (text, precise, far cheaper than a screenshot), and
       whatever the app volunteers through one optional hook (`window.__inout.state = () => ({...})`)
       — for a game that is the position/collider/entity data no pixel can carry. Never in a
       production build; the task's gate says so.
     NATIVE PROJECT (Unity/Godot/desktop) — no script tag and no DOM. INOUT already records the SCREEN,
       and every engine writes a log file. The server TAILS that log and aligns it to the take by wall
       clock. Frames + logs + timing survive; the a11y/state lane does not. Degraded, not absent.
 (d) POINTING [light, optional after (b)]. A click or dragged region on a frame becomes {t, rect} in
     index.json. It is not a feature in its own right — it is a cheap way to seed `crop(id, t, rect)`
     with one number instead of making the agent search.

WHAT INOUT CONTRIBUTES, STATED HONESTLY so the build is not mistaken for a screen recorder:
capture that works in a browser with no install · THE TIME ALIGNMENT (channel offsets, the wall-clock
hold, a sample-counted timeline — a frame, a log line and a click that agree about WHEN is the whole
payload, and it is the thing this codebase spent months on) · pixel-delta keyframe selection, which is
what makes summary() cheap · the black box. Everything else on this list is new.

THE STANDING OBJECTION, answered by measurement and not by argument: a screenshot plus a pasted console
costs ~1-2k tokens, needs no code, and solves most bugs. D1 must beat that, not tie it. It wins only
where the bug is about TIME or about STATE the pixels do not carry — a stutter, a sequence, a collider
at coordinates nothing draws. If T0 does not find that, the task is DELETED, not parked.

T0 SPIKE, AND NOTHING SHIPS BEFORE IT [light]: one REAL bug in Robert's game. Give an agent (1) the
sentence alone, (2) the sentence + a screenshot + a pasted console, (3) the sentence + the (a)+(b)
payload. Record TOKENS SPENT and turns-to-correct-diagnosis for each. (3) must beat (2) on both.

GATES: T0 run and reported, with token counts, before any lane merges · every tool's measured token
cost within the band above, asserted by a test that counts them · the server works offline against a
folder, no cloud, no auth · the web hook is absent from a production build, pinned by a test · the
native path proven once against a real engine log · zero runtime model calls anywhere (the ledger's
boundary: local only, no token cost the USER pays) · INOUT's own exports byte-identical to today.

## G1 gate-coinflip — the per-task merge gate fails half its cold runs [medium · measurement first]
`npm run oracle` (v2) failed 2 of 4 COLD runs on a stashed HEAD, and the tree under test failed 1 of 2
and 1 of 3 — same failure every time: `instant export sync maxAbs 97.1 > 90ms` or `trimmed export sync
maxAbs 105.5 > 90ms`. Never the render, never the mean. O4b already recorded the instrument's noise
floor at ~8-9 ms 1sigma against a 90 ms band, and a maxAbs over four samples is the statistic most
exposed to that — a CANDIDATE, not a measurement.
WHY IT IS FIRST: every task in this file must pass this gate to merge. A coin-flip gate teaches
sessions to re-run until green, which is how a real regression eventually lands on main.
THE FIX IS NOT TO WIDEN THE BAND. Measure: N>=10 cold runs recording the PER-SAMPLE offset
distribution (not just maxAbs), mean against max, the same take through the RENDER beside it, and the
number of samples the copy paths actually contribute. Then either the instrument is named and fixed
(more samples, a better statistic, the detection bias re-measured) or the copy paths own 97-105 ms and
that is a P1 defect of its own, promoted with the numbers.
GATES: N>=10 cold runs on one commit, distribution reported · a verdict naming instrument OR copy path,
with numbers · if the instrument: the fix runs green 10 of 10 cold · if the copy paths: a new task
authored here with the measurement attached · the band is NOT widened in this task · the fidelity
oracle's own bimodal instant lane (0.03 dB or 18.83 dB, never between) is measured the same way and
gets the same verdict, because it smells like the same wrong-window family.

## G2 rig-source-dead — a rig that cannot move its own source is not a gate [light · rigs only]
ABSORBS the ORACLE-LOAD entry. `npm run oracle:load` is RED and it is NOT F15: HEAD and the commit
before F15's first line (79d6def, throwaway worktree) give the IDENTICAL verdict — composite tail
9848 / 7366 ms against a 400 ms band, delivered 0.2 / 2.5 fps against a band of 10, both raw runs
erroring "the take produced no screen channel".
THE LEAD, already written down twice in this repo: the rig's 4K source is a canvas painted from
requestAnimationFrame, `oracle:load` drives through `exp.mjs` which is HEADLESS by default, and a
headless/hidden page runs rAF at 0. The source starves and the band reads starvation as tail loss.
The same mechanism killed the whole makeRig family on Chrome 151 (sources 15.9 fps then 0.0 forever,
composite degraded, blob deleted, every cell dying as an opaque `blobStore: no blob stored`), and the
mitigation there was exactly this: paint on setInterval, not rAF.
FIRST MOVE IS ONE COMMAND: re-run `--headed` (exp.mjs takes the flag) and see whether the source
delivers. If it does, the rig's DEFAULT is the bug and this band has been measuring the harness for an
unknown number of sessions — say how many, out loud, in the handoff.
GATES: `oracle:load` headed vs headless run side by side, delivered fps of the SOURCE reported for both
· every rAF-painted rig source either moved to setInterval or declared headed-only in ONE place a
runner enforces · the tail/fps band re-read once the source moves, and the old verdict retracted or
confirmed in writing · no product line changes in this task.

## G3 gates-that-cannot-fail — two bands are decoration [light · rigs only]
(a) Two evidence gates are phrased in `longtask` counts. Nothing in this codebase that awaits per frame
or per sample — the render, the For-AI build, the AI selection loop — ever forms a single >=50 ms task
while owning the thread end to end, so the counter reads 0 on the BLOCKING lane and 0 on the
non-blocking one. The O5 rig learned this in August and X9 relearned it in August. The shared
instrument that replaces it already exists: `perf/mainThreadWatch.ts`, SchedulingDelayWatch.
(b) O1's MEMORY lane polls the heap on the MAIN thread while the export renders in a WORKER (since
O5a), and `measureUserAgentSpecificMemory` is unavailable in the rig's Chrome — `totalSamples: 0`,
`totalPeakDeltaMB: null` on every row, and the 0.1-0.2 MB deltas it does print are the wrong thread's.
The scratch and buffer lanes therefore extrapolate to the SAME 145 MB at 30 minutes, which is the one
thing O1 exists to distinguish. O1's actual claim rests on `targetHeldMB` (ScratchStats, measured
inside the worker) and is sound — the lane around it is not.
GATES: an audit listing EVERY gate phrased as a long-task count · each one moved onto scheduling
lateness or deleted, never left green-by-construction · O1's heap sampled inside the export worker and
posted back with the done message (or the rig run cross-origin-isolated so the API exists) · any band
whose green rested on either is re-read and the verdict stated, including "unchanged".

## G4 long-take-gate — every number this project quotes describes a take 156x too short
[light-medium · rigs + one operating rule]
`runOracle` defaults to 6000 ms and the widest cell in the matrix is 30 s. Robert records 938-1800 s.
Measured once at 120 s: the instant path read 67.3 / 96.4 ms after 89e250e and 117.2 / 153.8 ms at the
SAME length before it, against 97-102 at 6 s — the defect GREW with length and the 6 s gate
under-reported it by ~20 %. The render path was flat (34.5 / 46.8 ms, all 119 pairs identical) and
drift is dead as an explanation (beta-1 = -0.003 ms/s = 2.8 ms across a 938 s take): what is left is a
CONSTANT offset, which is B7's subject.
THE TASK IS TO MAKE THE LONG CELL CHEAP ENOUGH TO BE MANDATORY: one command, announced as HEAVY, that
runs a >=120 s cell on the packet-copy paths, plus a line in the operating rules at the top of this
file requiring it before any flip that touches instant/smart cut.
GATES: the command exists and is named in the operating rules · one run on HEAD with numbers in the
handoff · it fails loudly rather than emitting nulls under contention (the ALL-NULL family) · runtime
stated so a session can budget it.

## B1 size-panel-lies — the only warning before a two-hour render is wrong by 2.15x
[light · quality.ts + the panel; no capture change]
Robert 2026-08-29: the panel said 4.7 GB at 1440p and the file came out 10.09 GB on a 140-minute take.
Every OTHER step landed within ~100 MB, so this is the top rung specifically, not the model as a whole —
`quality.ts` already admits the sqrt-pixel model came in 47 % low at 1440p on text content, which at
Robert's length is 5.4 GB of surprise. O3c NARROWED it: on a take whose single raw channel matches the
step's geometry the number is now EXACT (the file's own byte rate) and that export does not render at
all. The defect remains for every step that genuinely re-renders — camera takes, mismatched geometry.
THE CHEAPER SIBLING, SEEN THE SAME DAY on a 6 s synthetic take on the deployed build: the PROVISIONAL
numbers — what the panel shows for the seconds before sizeProbe's calibration lands — ranked 1440p
BELOW the 1080p step, `1080p 400 KB (exact)` against `1440p ~308 KB`. 1440p is an UPSCALE of that very
file; it cannot be smaller, and the model already has the evidence to know it (`isDefaultTier` returns
the composite's exact bytes). Fix: floor every re-encoding step at the exact size of any step whose
pixel count it exceeds. One comparison, no new measurement.
GATES: a unit test asserts the panel is MONOTONIC in pixel count in both the provisional and the
calibrated state · the 1440p re-render estimate measured against the file produced, on text content, at
>=120 s, with the residual stated as a ratio · steps that are already exact are byte-identical to today
· no change to which steps are instant.

## B2 tab-audio-noises — Robert's bar is "i need them gone" [medium · playback + capture; two defects]
"when video recorded and edit starts a lot of minor noises in tab audio, but after some time editing
noises almost completly stops in same places they were in begining, but not completly."
THAT SHAPE IS THE WHOLE CLUE AND IT SAYS THERE ARE TWO DEFECTS. A noise that HEALS with repetition is
not in the file; one that survives every repetition is. Chasing them as one bug is how this stays open.
(A) THE HEALING HALF IS PLAYBACK, and what is heard is the preview's own CORRECTION. Channel blobs
    reach the player as OPFS-backed Files, so the first pass over any region pays a disk read and a
    cold decode however `preload` is set — "the same places, quieter each time". The stall then meets
    `usePlayback.sync`: past RESYNC_HARD_MS (250 ms) it hard-seeks a PLAYING element (a click), and
    between SYNC_DEADBAND_MS (15 ms) and that it slews playbackRate by -20 %/+25 %. Media elements
    preserve pitch by default, so that slew engages the browser's TIME-STRETCHER — worst on tab audio,
    which is usually music or continuous speech where stretch artefacts have nowhere to hide.
    Candidates, cheapest first: warm the decode before playback (audio channels are small — decoding to
    an AudioBuffer once removes the class outright); ramp the correction instead of applying it; or stop
    slewing AUDIO at all and slew only video, letting audio be the clock.
(B) THE RESIDUAL IS CAPTURE and is immune to warming. Every starved audio quantum is deliberately
    turned into 64 frames of fade-out / silence / fade-in (PAD_FADE) so the sample-counted timeline
    stays honest — a starve is audible BY DESIGN, a ~1.3 ms notch. Measured 2026-08-26: tab audio
    padded 1281 ms in 84 s under load, ~15 notches a minute. Fix is starve less (that is capture CPU)
    or splice better: crossfade ACROSS the gap instead of through zero.
DO THE DISCRIMINATOR FIRST AND REPORT IT, so nobody argues about which half is which: export the take
and listen to the FILE. Anything audible there is (B). `ChannelDiagnostics.paddedMs` says how much (B)
exists before anyone listens — 0 there means (A) is the whole story.
GATES: the discriminator run and reported (paddedMs + a listen of the exported file) BEFORE any fix ·
(A) no hard seek of a playing element, slew capped, measured against the same take twice — first pass
and second pass — showing the first pass no longer differs · (B) the crossfade keeps the timeline
sample-exact (wallClockHold tests green) and the notch is measured before and after in dB · fidelity
oracle green · `npm run oracle` green both engines · Robert's listen closes it, not the numbers.

## B3 stale-tab-chunks — a tab that spans a deploy silently loses the export worker
[light-medium · service worker + boot notice]
From Robert's own console dump on real takes against the deployed build: `/assets/sizeProbe-*.js` 404
and `index-*.css` 404 (hashed names from a build Vercel no longer serves), and twice `[compose] export
worker unusable, rendering in-thread — Error: export worker error`. The fallback WORKED, so files were
delivered — on the slow lane, on the UI thread: one 32 s take spent 7.5 s of its 8.2 s render in
encode-wait at 1440p.
MECHANISM: the PWA service worker is assets cache-first / documents network-first, so a tab (or a
cached document) from build N requests build N's hashed chunks after build N+1 deploys and Vercel
serves 404. REPRO: open the app, deploy, open the export panel.
THIS MAY ALSO EXPLAIN PAST "the fix wasn't there to hear" ROUNDS — a stale tab IS an old build even
when prod serves HEAD, which makes it a testing bug as much as a product one.
FIX SHAPE, not chosen: precache ALL hashed assets at activate · a version-mismatch "refresh for the new
version" prompt (the owed-flag boot-notice machinery already exists) · or keep build N-1's assets alive
in the SW cache.
GATES: the repro no longer 404s, or surfaces a refresh prompt the user cannot miss · the export worker
is still used after a deploy (assert the in-thread fallback did NOT fire) · a fresh tab is unchanged ·
the boot notice, if used, is an owed flag consumed once (a 4 s toast was already learned to be wrong).

## B4 dead-channel-silence — a channel that recorded nothing must say so [light · UI surface + the
##    liveness path that already exists]
Observed four times on a real device by P9's runner: a camera whose track is live, unmuted and
negotiated at 1920x1080@30 while the sensor is off (a closed lid does this) records for the take's full
length, logs `camera delivering 0.0 fps`, writes a **28-byte** raw channel file, times the recorder stop
out after 5 s, and saves a recording with `durationMs: 0` and **zero channels** — with nothing on screen
to say any of that happened. Same family as the older report where a mic acquisition timed out and the
take completed with no mic and no unmissable warning, discovered only on playback.
THE PIECES TO NOTICE IT ALREADY EXIST: `capture/sourceLiveness.ts` watches exactly this and emits
`channel-stalled`, and the UI already renders a stalled chip. WHETHER IT FIRED HERE WAS NEVER
ESTABLISHED — the runner was not looking at the chips. That is the first thing to check, and if it did
fire, this is a surfacing bug, not a detection one.
IT IS P2 AND NOT P1 because the trigger is a dead camera the user can usually see in the preview. It
becomes P1 the moment the preview looks fine while the file is empty.
GATES: a synthetic channel that delivers ZERO frames is reproducible from a URL (a knob in the same
family as `?slow=`) · the stalled chip is asserted to render for it · a take that saved zero channels,
or lost a channel it armed, never lands silently — one loud post-record surface, wording is Robert's ·
the 5 s stop timeout is either avoided on a known-dead channel or explained in the handoff · evidence:
docs/qa/camera-1080-2026-08-29.json and the P9 handoff console lines.

## B5 disk-headroom — an uncapped take can fill the disk with no warning [light · additive]
Exposed by removing the 30-minute cap (2026-08-23). Capture streams every channel to OPFS as it
records, so length costs DISK and not RAM — but nothing checks `navigator.storage.estimate()` before or
during a take. A write failure is at least loud (session.ts ondataavailable -> channel-error +
onTrackEnded, partial take kept), so this is degradation and not silent loss. Still: an uncapped take
should warn on low headroom before it starts and while it runs, and ~1.1 GB/hour at the production
8 Mbps + 128 kbps is a number the UI can show.
GATES: the estimate is read before start and on a cadence during a take · a low-headroom machine warns
before and during, a healthy one shows nothing new · the rate shown matches the measured byte rate of a
real take within 10 % · additive only — no take is ever refused or stopped by this task.

## B6 display-permission-message — we blame the device for an OS permission
[light · arming errors + the wedge ladder; BEHAVIOUR CHANGE -> Robert's yes before it ships]
Hit on the dev machine while reproducing something else: when macOS has not granted Chrome screen
recording, Chrome's picker opens and says exactly what to do ("allow screen recording for Chrome in
System Settings. You'll then need to restart Chrome") with Share greyed out, `getDisplayMedia` NEVER
REJECTS, and INOUT's own 30 s deadline fires and reports the generic "the device never connected" —
throwing away the one message that would have solved it. Measured: `display timeout +30023ms` /
`+30021ms`, with and without the flag.
SECOND HALF, same mechanism: the wedge ladder ESCALATES on each such timeout (`rememberDisplayWedge`),
spending a constraint escalation on something no constraint can fix. It self-heals on the first
success, so this is waste rather than damage — but it also means a permission problem walks the user
down the safe-mode ladder for no reason.
GATES: a display request that is still outstanding says what it probably is BEFORE the deadline, not
only in the post-take banner · the wedge ladder does not escalate on this class · a genuine wedge still
escalates exactly as today (pinned by test) · Robert approves the wording before it ships.

## B7 start-offset-cert — put the alignment inputs in the file, then argue [light-medium ·
##    instrumentation ONLY; no compensation ships in this task]
TWO ALIGNMENT ERRORS A SYNTHETIC RIG CANNOT SEE, both pushing audio LATE:
  (1) the mic anchor subtracts only the platform-REPORTED track latency (measuredAudio.ts) — a
      Bluetooth headset's real 100-300 ms is invisible to it, and Robert's 15-minute Zoom take is
      exactly that case;
  (2) the video channel is anchored to the `recorder.start()` CALL, not to when its first frame landed.
      A canvas delivers instantly on the rig; a real getDisplayMedia surface does not — the composite's
      own first frame took 233 ms in the same run.
FIELD EVIDENCE THAT IT IS THIS FAMILY: "mic/camera unsynch in beggining of video seems to be smaller on
other try" — a CONSTANT start-of-take offset that varies take to take and SHRINKS on a warm second try,
which is what (2) looks like when a cold first take pays device spin-up inside the anchor. Distinct
from the progressive drift, which is fixed; G4's 120 s cell already ruled drift out arithmetically
(2.8 ms across a 938 s take).
THE TASK: each channel's `startOffsetMs`, the raw anchor, the platform-reported input latency and the
first-frame delay persist in ChannelDiagnostics / the certification, so the next field report arrives
with NUMBERS instead of an adjective. Compensating from theory is what has failed here before.
GATES: the four values persist with the take and survive a reload · read out of one REAL take and
quoted in the handoff · zero behaviour change, pinned by a byte-identical export test · X14 (<=20 ms)
stays blocked; this task does not claim it.

## B8 pip-placement — the two export paths disagree about WHEN [light · render path]
Found while measuring X15(c), never chased: in the same take, on the same output instants, the moving
camera region best-matches at -15 frames (-0.5 s) and even there reads only 23.6 dB (max 115), while
the STILL screen region of the same two files matches at 37.1 dB. So the two paths agree about pixels
and disagree about time: an EDITED export's PiP shows a different moment than an unedited export's at
the same timestamp.
NOT DIAGNOSED. Two candidates: the camera channel's `startOffsetMs` against the composite's, or the
render resampling the camera onto its own grid. `npm run exp -- x15c` prints it as `alignFrames` on the
camera row; `{"thumbs":true}` dumps the frames, which is what made it visible.
WHY NO GATE CAUGHT IT: the oracle bands audio-against-video sync. This is video-against-video placement
BETWEEN two export paths, which nothing here measures.
GATES: `npm run exp -- x15c` reads alignFrames 0 +-1 on the camera row · the screen row's dB is
unchanged · instant and smart-cut paths are untouched, byte-identical · the diagnosis is stated (which
of the two candidates it was), because the other one stays a hazard if it is not.

NEW TASKS (authored 2026-08-26 from Robert's answers; all operating rules apply):

## F9 per-channel-gain [light-medium · editor + mix; all three export paths share the mixer]
Robert: "mic is kinda too loud now, can we add slider?" A gain slider per AUDIO channel in the editor,
default 0 dB, persisted with the edit (editsRepo — must survive refresh), applied in ONE place so
preview and every export path hear the same thing (the certified mixer; note 14: every export mixes
audio from the RAW channels, so this composes with instant and smart cut). MUST NOT cost the
instant path: a gain-only edit changes no pixel, so the video side stays packet-copyable — assert
the ladder still picks instant. An untouched/returned-to-0 slider is dropped by clampEditState and
behaviour is bit-identical to today (pinned by test). X1 FENCE REQUIRED: a per-channel gain changes
the mix the stored envelope summed, so envelope-shortcut consumers must return null and re-probe on
any take with a non-zero gain — new fence case, unit-tested, stated in the handoff.
GATES: fidelity oracle green · 0 dB = today, byte-identical behaviour · gain-only edit still takes
instant/smart cut on video · edit survives refresh · X1 fence added + tested · `npm run exp -- o2`
green.
COORDINATION: Robert's underlying report ("mic kinda too loud NOW") may be the normalization itself —
ANOTHER LIVE SESSION owns that fix (Robert 2026-08-26). F9 is the CONTROL, not the fix: touch gain
plumbing only, no mix constants, and start only after that session's merge.

## P9 camera-1080-verify — RUN 2026-08-29, answer PARTIAL (handoff below): the constraint and the
##     pipeline are verified; the real sensor delivers no frames on this machine. Re-run on a live camera.
[light · QA only, zero product change, zero Robert time]
The O3a claim "camera-only records 1080p, not upscaled 720p" needs a REAL camera (synthetic mode
bypasses getUserMedia) and resolution is not judged by eye. Drive real Chrome on the deployed build
with the real built-in camera (browser-check.mjs shape; --use-fake-ui-for-media-stream auto-grants
the permission while keeping the REAL device), camera-only take → export → read width×height from
the FILE ITSELF (mp4 header + the O8 certification tag) and the delivered track settings from the
console.
GATES: the exported file's own header reads the camera's true max (≥1920×1080 if the camera offers
it; if it offers less, report the capability list — that is the answer, not a failure) · evidence
JSON in docs/qa/ · no changes outside scripts/.

## P9 HANDOFF — 2026-08-29. BUILT AND RUN. THE ANSWER IS PARTIAL, AND THE MISSING HALF IS THIS MACHINE'S.
Runner: `node scripts/camera-check.mjs` (and `--fake-device`, the control). Evidence:
docs/qa/camera-1080-2026-08-29.json and …-harness.json. Nothing outside scripts/ and docs/ changed.

WHAT THE TASK ASSUMED AND GOT WRONG, and it is worth stating because it changes what the runner had
to do: "read width×height from the FILE ITSELF" cannot answer this on the EXPORT. The export is
1920×1080 either way — the live compositor COVER-FITS a camera-only take into its own fixed
1920×1080 canvas, so a 720p camera produces a 1080p file that is 720p pixels stretched, i.e. the
defect wearing the header of the fix. The number that cannot be fabricated by a downstream scaler is
the RAW CAMERA CHANNEL's coded size, so that is the gate, and it is read from OPFS along with the
composite and the export through one MP4 box parser.

WHAT IS ANSWERED, on the real device:
  · the camera's own capability list — width max 1920, height max 1920, frameRate max 30, so it
    OFFERS 1080p;
  · under the product's own camera-only constraint (acquire.ts asks ideal 1920×1080 when there is no
    screen channel) the real device NEGOTIATES exactly 1920×1080@30. That is the constraint half of
    O3a's claim, verified against the actual hardware.

WHAT IS ANSWERED, end to end, with a controlled 1080p source (`--fake-device`, all gates green):
    raw camera channel   1920×1080 avc1        ← the number no scaler can fake
    product's manifest   1920×1080 video/mp4   ← ChannelRecording.width/height out of IndexedDB
    exported file        1920×1080 avc1
    capture's own line   measured video camera first-frame 1920×1080@20
  …so the pipeline preserves whatever size it is handed, and does not upscale. AND, unplanned but
  useful: that export's certification reads copiedFrom "single-generation" — O3b's new path, on a
  CAMERA-only take, cross-validating that the compositor's cover-fit at equal dimensions is the
  identity. First evidence of O3b outside a screen take.

WHAT IS NOT ANSWERED, AND WHY IT IS NOT A PRODUCT FINDING: this machine's built-in camera DELIVERS NO
FRAMES to an automated Chrome. It enumerates, gUM resolves, it negotiates 1920×1080@30, the track
stays `live` and unmuted — and 0 frames arrive in 2.5 s through MediaStreamTrackProcessor, the very
API capture reads. The product says the same thing from the other end: `camera delivering 0.0 fps`, a
28-byte channel file, and a recorder stop that times out. The identical code path with Chrome's
synthetic camera counts 50 frames and passes every gate, so the runner is sound. The likeliest cause
is a CLOSED LID — a MacBook in clamshell enumerates its built-in camera and negotiates a resolution
while the sensor is off — with an app holding the device, or the OS camera permission for the Chrome
binary, giving the same signature. `--use-fake-ui-for-media-stream` answers the SITE prompt, not the
OS one, and no flag can.
TO FINISH THE ROW: run `node scripts/camera-check.mjs` on a machine whose camera is live. One
command, exit 0 = the claim holds. It is not Robert's time in any meaningful sense.

THREE INSTRUMENT FAULTS IN THIS RUNNER, all mine, all caught before anything was reported (note 10):
 1. IT REPORTED WHAT IT INTENDED, NOT WHAT HAPPENED. The first version set the channel chips and
    returned the config it wanted; the take it actually recorded was screen+camera+mic and spent 18 s
    waiting for a microphone. Chips are now set, re-read from aria-pressed, retried, and a gate fails
    if the app is not camera-only.
 2. A PROBE THAT COUNTED ZERO FROM A SOURCE THAT PROVABLY RECORDS. Opening a
    MediaStreamTrackProcessor on a just-acquired track counts 0 frames from Chrome's SYNTHETIC camera
    — the source that then records a full take. An earlier version worked by accident, spending three
    seconds on a video element's play() before counting. The source is warmed on purpose now. Without
    the fake-device control this would have been published as "the real camera is dead".
 3. A HANG, NOT A FAILURE. `video.play()` on a stream that never produces a frame does not reject; it
    never settles, and an awaited CDP evaluate then wedges the run past 600 s. Every page-side call is
    bounded at 30 s now.

## F10 mic-noise-removal [medium · local model, zero tokens · export mix + preview; raw untouched]
Robert 2026-08-26: "i want noise removal". A per-take toggle that denoises the MIC channel. PLACEMENT:
at EXPORT (mirrored in preview), never at capture — the raw channel on disk stays untouched, so the
feature is reversible and "never break working features" holds. Runs in the export worker on the
mic PCM before the mix; the in-thread fallback is the same code. Default OFF until Robert flips it.
MODEL: verify the current open-model landscape IN-SESSION before choosing (never from memory).
Candidates to price: RNNoise-class (~0.1-2 MB, faster than realtime on WASM) vs DeepFilterNet-class
(better, heavier). Permissive license only; model lazy-loaded on first use — the O7 first-paint
gate must not move.
GATES: toggle OFF = byte-identical to today (pinned) · ON: measured SNR gain on a known-noise rig
with the fidelity bands still green (no musical-noise artifacts on tones) · A/B files to
~/Downloads for Robert's ears · toggle persists with the edit (editsRepo) · X1 fence: a denoised mix is
not what capture summed → envelope shortcut returns null, re-probe (unit-tested) · export time cost
printed. COORDINATION: starts after the live audio session merges (shared mix area); composes with
F9's slider.

## F11 voice-aware-tighten [light-medium · extends F5a's analyzer; proposals only, zero tokens]
Robert 2026-08-26. F5a proposes cuts from window-RMS alone, so quiet speech near the floor can be
proposed as a cut. A local VAD (Silero-class ~1-2 MB — verify landscape in-session, permissive
license, lazy-loaded at Tighten press) gates the proposals: a window the VAD calls speech is never
inside a proposed cut, whatever its RMS. Still a PROPOSAL — Apply stays the user's.
GATES: on the known-silence rig, recall unchanged and cuts-in-speech ZERO, including a new
breathy-speech fixture · interactive runtime (the envelope path stays ~ms; VAD runs only on
candidate spans) · VAD unavailable → exactly today's behaviour (fallback pinned).

## F12 zoom-super-resolution [heavy · render path only, zoomed spans only, opt-in]
Robert 2026-08-26: "super resolution on zoom but only if zoom used". F2 caps zoom at 2.5× because
upscaling 1080p past that is mush; SR raises what a zoom can show. Applied ONLY to frames inside a
zoom span past a threshold (start ~1.5×), ONLY in the render (a zoomed take is never pixel-default,
so instant/smart-cut are structurally untouched), as an opt-in quality step in the export panel
that PRINTS its time cost. WebGPU inference; compact SR model verified in-session (landscape check,
permissive license, lazy-loaded). Raising the 2.5× zoom cap where SR is on is Robert's knob AFTER
seeing files.
GATES: un-zoomed takes and SR-off exports byte-identical to today · A/B crops at 2.5× to
~/Downloads — Robert judges by eye · per-frame cost measured, export estimate includes it · no WebGPU →
today's scaler, silently · oracle green.

## P0-ladder-static — DONE 2026-08-29 (seventeenth session, handoff at the end of this task)
[light · authored 2026-08-29 · resolutionLadder.ts + its caller in liveCompositeV2.ts · LIVE ON
THE DEFAULT PATH since nativeres went default the same day]
Chrome's `getDisplayMedia` emits frames ON CHANGE. A screen nobody is touching delivers 0 fps, not
30. `ladderVerdict` compares `deliveredFps` to `requestedFps` against `DELIVERY_FLOOR_RATIO 0.6`
for `SUSTAINED_MS 2000` and cannot tell the two apart, and Rule 1 makes the step one-way.
SEEN LIVE, TWICE, deployed build, static synthetic screen:
  `[capture] native-res backpressure — stepping down: delivered 0.0 of 30 fps (0 % of requested)
   for 3001 ms → 1440p`
and in the SAME take:
  `[capture] raw video channel: 7 frames encoded of 2 in, 5 keep-alive (source was static)`
The pipeline already distinguishes "static" from "starved" — the ladder does not ask it. The step
was a no-op in this repro only because the synthetic source (1280×720) is below both rungs; on a
1440p or 4K screen it is a real, permanent loss of resolution. USER-VISIBLE COST: read a document,
a slide, a paused video or your own editor for ~2 s after warmup and capture walks 4K → 1440p →
1080p and never returns.
FIX SHAPE (pick on evidence, do not guess): a source that sent nothing because nothing changed is
not a source that failed. Either feed the raw channel's static/keep-alive signal into `LadderInput`,
or judge on frames the compositor DROPPED rather than frames it received — dropping is what
backpressure actually looks like.
GATES: a new boundary test — static source at 0 fps for 10 s does NOT step, starved source at 0 fps
DOES · every existing resolutionLadder test still green (the ladder's other three rules are
untouched) · reproduce the false step on the deployed build BEFORE the fix and show it gone after,
with the console lines pasted · nativeres off = byte-identical behaviour.
NOT YET REPRODUCED on a real 4K screen; the console lines above are what to look for.

## P0-LADDER-STATIC HANDOFF — DONE 2026-08-29, every gate met, verified on prod both sides.
CHOSEN FIX SHAPE (the task's second option): judge on what the compositor failed to keep up with.
`framesIn` was already in CompositorStats and counts only genuine source arrivals — keep-alives are
a worker-internal setInterval and never pass through the 'frame' message. Rule 4 in
resolutionLadder.ts: demand = min(arrivedFps, requestedFps) (the cadence gate drops a 60 fps
source's excess ON PURPOSE — not failure); zero demand = the source sent nothing = no verdict;
otherwise step when deliveredFps/demand < 0.6 sustained, exactly the old thresholds.
GATES MET: 13 ladder tests (4 new: static-never-steps, starved-still-steps, sparse-kept-up,
60fps-not-punished) · BEFORE on deployed build: static synthetic screen, "delivered 0.0 of 30 fps
(0 % of requested) for 3999 ms → 1440p" then "→ 1080p" — BOTH rungs consumed in 10 s · AFTER on
deployed build (f85aed0): same conditions, screen at 0.0-0.1 fps for 26+ s, ZERO steps · nativeres
off untouched (the ladder only runs under onDegradeStep, which nativeres wires) · oracle v2 PASS
46.8 ms + v1 PASS 32.5 ms.
STATED COST (was in the task as a fix-shape choice, now a shipped fact): a source whose ARRIVALS are
crushed below the floor without our pipeline dropping anything no longer steps. The 08-22 4K story
says that case is Chrome's own compositor starving the source; a step cannot force frames out of a
hidden crushed tab (autopsied), keep-alive holds the file, and the watchdog still ends a collapsed
composite. If a real 4K screen ever shows sustained arrivals-crushed-no-drops where a step WOULD
have helped, the discriminator to add is framesDropped/maxEncodeGapMs, both already in the stats.

## O3c — DONE 2026-08-29 (seventeenth session, handoff at the end of this task)
[light-medium · authored 2026-08-29 · copySource.ts, session.ts, quality wiring · restores a
measured win that is currently reaching nobody]
Two defaults flipped 2026-08-29. `?singlegen=` (O3b, ON) copies the raw channel instead of the
composite ONLY when that channel equals the export geometry exactly — `copySource.ts:143` and
`session.ts:905`, both against the 1920×1080 constant. `?nativeres=` (O6, ON) drops the 1920×1080
capture bound so the screen records at its own size. On any monitor that is not exactly 1920×1080
the equality now fails, single generation declines, and the export is the composite again.
SO O3b's MEASURED WIN IS NOT BEING DELIVERED: colour 80.0 % vs 70.3 % green retention, luma 37.3 vs
35.5 dB, fringe 8.66 vs 10.24, and a faster export — all of it, to everyone whose screen is not
1080p, on the same day it shipped. nativeRes.ts states the mechanism ("the composite stays
1920×1080 … so the default export is unchanged") but reads it as a non-loss; it is the loss of a
shipped improvement.
FIX SHAPE: compare the raw channel against the SELECTED export tier instead of the 1080p constant —
a 1440p screen exporting at the 1440p step already matches, and the composite can follow the tier
so the copy has something to be equal to. F13 makes this true by construction, but this does NOT
need F13 and should not wait for it.
GATES: a 1080p screen behaves exactly as today (pinned) · a 1440p screen + 1440p step reports
`copiedFrom: "single-generation"`, read from the export's own certification and not from a console
line · `npm run exp -- o3b` green · the declined-reason string still names the real reason on a take
that genuinely cannot copy · fidelity oracle green.

## O3c HANDOFF — DONE 2026-08-29, every gate met.
WHAT SHIPPED: chooseCopySource takes the REQUESTED settings for the single-generation equality and
an `allowComposite` fence (default true — rigs unchanged); exportByBestPath consults it on EVERY
tier with allowComposite = allowPacketCopy, so the flag keeps its documented meaning (the request IS
the composite's geometry) and a matching raw channel is copyable at any step. instant/smartCut
already derive geometry from the source — no copy path changed. QUALITY WIRING: copySourceForTier
(quality.ts) answers the panel and the estimator with the ladder's own function — the INSTANT badge
is per step, and a copyable step's size is EXACT off the channel's own byte rate. That also corrects
the DEFAULT step on single-generation takes, which quoted the composite's size while copying the raw
channel (+12-23 % under-quote, live since O3b). ChannelRecording.bytes is new and additive; old
takes fall back to the model exactly as before (pinned by test).
GATES MET: 1080p-screen-at-default pinned byte-identical (copySource tests) · tier-match cert
verified: exp o3b's own cert-reading lanes green 7/7 (A/B lanes read copiedFrom out of the FILE),
and live on the DEPLOYED build a 1280x720 take at the 720p step exported "[bits] instant copy avc
from single-generation" in ~1 s with the panel badging 720p INSTANT — before this push that click
was a full re-render (the geometry generalisation is one equality; the 720p repro exercises the
same code path a 1440p screen takes) · declined reasons name real geometry on both origins
(unit-pinned: "3840x2160" / "1920x1080 ... 2560x1440") · fidelity oracle PASS · oracle v2+v1 PASS ·
510 tests · typecheck.
NOT TOUCHED, deliberately: session.ts's CAPTURE half (?singlegen=capture) still compares against
the composite constant — that rung records NO composite, so tier-chasing there would strand the
other steps; F13's derive-from-the-take rewrite is where it becomes general. The F7c probe still
prices copyable steps it does not need to (harmless; the exact number wins the display).

## F14 — RULED 2026-08-29 AND ABSORBED BY F16 (Robert: "later we will add quality choosing before
##     record too, so make it one option for now everytime"). No per-export sticky resolution
##     ladder: quality is ONE product-wide option; F16 below is the task. The original hazard
##     (a sticky pick silently re-rendering every export) dissolves once capture records the
##     chosen rung — until F16 lands the panel keeps O3c's honest per-step INSTANT badges.
## (original spec kept below for the record)
[light · editor/export panel only, zero capture, zero compose]
The export quality step persists in `inout.export.tier` forever. 1080p is the ONLY step that keeps
the packet copy; every other step re-renders the whole take. SEEN LIVE on the deployed build: the
panel opened on 1440p left by an earlier session, the INSTANT badge sat on the 1080p row it was not
using, and the button read "Export 1440p". One accidental pick, once, makes every future export
slow and large with nothing on screen explaining why the instant path stopped being taken.
ROBERT DECIDES WHICH: (a) do not persist the tier — every take opens on 1080p; (b) persist it but say
plainly in the panel that this take will re-render and roughly what that costs in time and bytes.
(a) is the smaller change and the one that matches "instant default export" in the frozen
constraints; (b) keeps a deliberate choice. Do not pick without Robert.
GATES: whichever is chosen, the instant path is still taken on a default take (assert the ladder
picks instant) · the panel's INSTANT badge and the selected step can never disagree — pinned by
test, because that disagreement is the bug · no change to any export path's output.

## F16 one-quality-option — capture follows the chosen rung; instant export at any set quality
[medium-heavy · authored 2026-08-29 from Robert's ruling ("make it one option for now everytime ...
min-medium-high(default)-max ... we need to try to achieve instant export for any quality") ·
AFTER F13 (geometry seam — LANDED 2026-08-29, `resolveTier` is the seam) and F15 (fps) — it rides both]
THE PHYSICS THIS TASK IS BUILT ON, so nobody re-derives it: instant = packet copy = the bytes
already exist. The render floor is decode-bound at 5-6x realtime (O5, X4 refuted threading), so
"fast render" can never be instant. Four live encoders for four rungs is dead on X6's own numbers
(capture CPU starvation is THE failure family on Robert's machine). What remains is: capture writes the
file the user is going to ask for — affordable precisely because quality is ONE option.
WHAT IT IS — DESIGN v2, 2026-08-29, on Robert's OWN COUNTER ("cant it be same high instant quality
rendered already and it just converts in smaller file if choosing? ... can we squezze rendering in
parallel of edits so user wont notice?"), SEALED by Robert the same day: "High instant — or any, only
if user will select it before record, which we will add later." THE INSTANT CONTRACT, exactly:
INSTANT IS THE RUNG CAPTURE RECORDED — High by default today; the pre-record picker (LATER, its
own UI task) makes the selected rung the recorded one, and therefore the instant one (v1's
capture-follows-the-choice mechanism is that later half, not retired). Every other rung is a FAST
LANE, pre-made in idle time, and is never labelled instant. The v2 principle:
EVERYTHING IS ALREADY MADE BY THE TIME IT IS ASKED FOR, using the time the machine is idle.
  · RECORD HIGH ALWAYS = exactly today's capture: raw native channels + the composite,
    take-shaped after F13, quality-targeted. Nothing changes at capture.
  · HIGH  = instant copy of the composite (today's default, unchanged).
  · MAX   = instant copy of the raw native channel on single-video takes (O3c, SHIPPED); on
    camera/multi-channel takes a BACKGROUND render at native geometry starting at stop — Robert:
    "some quick extra render for max quality is okay, but it must be fast too".
  · MIN / MEDIUM = BACKGROUND TRANSCODE of the High composite (or of the raw channel on
    single-video takes — pick by measurement: one extra 4:2:0 generation against a heavier
    decode), started at stop. An OFFLINE encode beats any live encode per bit — so v2's Min is
    SMALLER than v1's live-encoded Min would have been, and the realtime-encoding tax (the one
    honest way "editable" was costing bytes) is paid only by High. Panel shows the exact size
    once the job lands, progress until.
  · EDITED TAKES = BACKGROUND RENDER IN PARALLEL WITH EDITING: render the current edit state
    while the user reviews; an edit invalidates what it touched (segment-level if T2 justifies
    it, debounce-restart otherwise). Editing is when the machine is idle — capture is over, the
    contention family does not apply, and the render already lives in a worker (O5a). Export
    foregrounds the same job with an honest time-left when it is not done.
  · UI: semantic rungs MIN / MEDIUM / HIGH (default) / MAX (pro-gated later — product gate, not
    engineering), one option, no per-export sticky resolution ladder (F14 dissolved).
TWO SPIKES GATE THE BUILD — both are claims currently made from reasoning, and this project does
not ship reasoning:
  · T1 transcode-speed: measure High→Min/Medium (one hardware AVC decode → scale → encode small)
    against the full render on the same takes. Expectation is a several-times-faster lane;
    the number decides whether "usually done before the panel opens" is true at Robert's lengths.
  · T2 background-render cost: the render beside a playing editor — UI-thread lateness through
    perf/mainThreadWatch.ts (the instrument X9 standardized), plus a stated thermal/battery
    policy (interruptible, deprioritized, and what happens on battery is Robert's call).
GATES: unedited take — Min/Medium/High/Max each export a file that EXISTED at press time (cert
copiedFrom, or the background job's output served with its hash), measured on a take longer than
the job needs · the panel never promises a size it cannot yet know (sizeConfidence extends to the
job states) · exported files carry no edit machinery: size delta vs a bare remux ≤ container
overhead · changing rungs after the jobs ran serves the pre-made file, not a re-render · oracle
v2+v1 + fidelity green · T2's lateness inside the O5/X9 bands · a 20-minute take's Min ready
before a user plausibly reaches the panel (number from T1, stated in the handoff).

## MIN-floor — what "still readable" measures  [light-medium · measurement only, feeds F16]
Robert's definition of Min: "minimal size where text and everything will be still readable". Make that
a number, not a vibe, with instruments that already exist: the glyph contrast/fringe metrics
(perf/textSource.ts chromaRows, R1-hardened) and the F7c probe (composes 10 s of THIS take and
encodes it with the export's own encoder). DELIVERABLE: a readability floor — the smallest
(long-edge, qp) whose probed frames keep glyph contrast/fringe above threshold against the take's
own native frames — plus the threshold itself justified on the three fixture contents (text /
motion / talking head). UNDER F16 v2 MIN IS AN OFFLINE TRANSCODE, which makes this easier and
better: the job can probe the take's ACTUAL frames (no pre-record blindness) and even sweep qp per
span — X13's live per-chunk steering shrinks to an option rather than a dependency; re-price X13
after this lands. NOTE the codec dependency honestly: Min's absolute size is ~2x better on
hevc/av1 — SIZE-CODEC (resolved: Apple's way) delivers that through the efficiency lane.

## F17 share-sheet — the share button, Apple's way  [medium · UI + share plumbing · the SHEET
##     LAYOUT IS ROBERT-GATED (it is design); the codec rule underneath is settled (SIZE-CODEC)]
[drafted 2026-08-29 from Robert's "i like apples way, its time to think through how share button
will work". Three rules, then the sheet.]
RULES:
  1. CODEC IS NEVER A USER QUESTION. The destination decides: unknown egress (Save file, OS share
     sheet) = most-compatible AVC; controlled playback (cloud player) and the explicit smaller-file
     row = high efficiency. Plain words only ("plays everywhere" / "about half the size, needs an
     up-to-date player") — never a codec name in the UI.
  2. SHARE LEADS WITH WHERE IT GOES. Quality (Min/Medium/High/Max) is F16's one option — how it
     looks — shown as one quiet row, tap to change; destinations are the sheet.
  3. SOMETHING ALWAYS HAPPENS INSTANTLY (frozen law). Save file = the recorded rung, packet-copied,
     ~1 s at any length. Slower things show readiness ON THE ROW before the tap (exact size when a
     lane finished, honest progress when not) — never a surprise after.
THE SHEET (one primary Share button after a take):
     Copy link            "Anyone can watch · lasts 7 days"   [headline once cloud lands]
     Save file            "Plays everywhere"                   [instant, always]
     Save smaller file    "~34 MB · ready | ~12s"              [F16 efficiency lane]
     For AI               [existing PDF artifact]
     ───────────────
     Quality: High ▾      [the one option]
DESTINATION WIRING:
  · Save file → the recorded rung's compatible artifact. Can never be slow, big-surprise or absent.
  · Save smaller file → F16 background lane: HEVC where the platform encodes it, AV1 software
    fallback (measured ~2.6x realtime — fine for idle), AVC-higher-qp as the floor; the capability
    probe is part of T1. Row states exact size or progress; if the lane cannot run on this machine
    the row says so and points at Save file — it never dangles.
  · Copy link → upload with progress, link copied on completion. CLOUD V1 HONESTY: Supabase has no
    transcode and quota is 512 MB, so v1 uploads ONE file — the COMPATIBLE one (a link must play
    for anyone); the efficient rendition + per-viewer pick is phase 2 (player detects capability);
    link-before-upload-finishes is the approved-but-OFF instant-link mode, later still.
  · OS share sheet → a "Share…" row only where navigator.share accepts files (feature-detect);
    hands over the COMPATIBLE file — unknown egress is Apple's own rule.
  · Recipient page: stream + title + duration + one Download (compatible). Expiry is the SHARER's
    information, shown at copy time.
FAILURE HONESTY: upload fails → "the file is still on your machine" + Save file right there ·
quota full → name which links to let expire · every row that cannot serve says why in one line.
SEQUENCING: the Save file + For AI rows and the sheet shell are a re-arrangement of what ships
today and can land before F16; the smaller-file and link rows light up as their lanes/cloud land.
GATES when built: Save file measured instant on a long take · no codec word anywhere in the sheet
(copy reviewed by Robert) · the smaller-file row never promises a size before its lane reports one ·
the sheet drives compose/choose.ts unchanged (the oracle still gates the paths users get) · Robert
judges the sheet by eye before it becomes the default surface.

## W1 the wedges go — AUTHORED 2026-08-29 from Robert: "i want them gone"
[medium · mitigation is shipped and he still hit it three times in one evening]
docs/SCREEN_WEDGE.md says MITIGATED since 2026-08-25 and it was true when written — his own stress
test came back "seems to be allright now". It is not true today: in one evening he hit "got chrome
wedge again" and reached rung 2 of the safe-mode ladder, which is the rung for a machine that has
wedged repeatedly. The root cause remains Chrome's (`getDisplayMedia` never settles after the native
picker confirms; the stuck state lives in the browser process and survives a refresh) and no web page
can cure it. What this task is about is that WE MADE IT WORSE and then could not get out of it.
THREE THINGS FOUND THE HARD WAY 2026-08-29, one already fixed:
 · FIXED — the safe-mode rungs were dropping the SIZE and RATE bounds along with the exotic options,
   so a machine that had already choked once went on to capture its WHOLE monitor uncapped. Backing
   off was asking for MORE. Rung 1 keeps the bounds now; rung 2 is bare on purpose and is covered on
   the track by capDisplayTrack.
 · THE ESCALATION HAS NO WAY BACK BUT TIME. `rememberDisplayWedge` raises the rung and pins it for a
   24 h TTL; `rememberDisplaySuccess` clears it only from rung 0. So a machine that wedged once for a
   reason that is now GONE — his was the macOS screen-recording grant, which needs a Chrome relaunch
   — stays degraded for a day, and the only exit anyone found was a localStorage line in a console.
   Robert's answer to being handed that line was "what the fuck is this?", and he is right.
 · A TIMEOUT IS COUNTED AS A WEDGE WHATEVER CAUSED IT. When macOS has not granted Chrome screen
   recording, Chrome's picker opens, says so, and `getDisplayMedia` never settles — identical from
   the page, so the ladder escalates against a permission no constraint can fix, and reports "the
   device never connected" while Chrome is displaying the actual answer on screen.
WHAT THE TASK IS: (1) a successful share clears the mark from ANY rung, not only rung 0 — one good
take is stronger evidence than an old bad one; (2) climb back one rung at a time on success rather
than only falling; (3) tell the difference between "Chrome wedged" and "the OS has not granted
permission" — the second is not a wedge, must not escalate, and must say what it is while the
request is still outstanding rather than only in the post-take banner; (4) a visible way out that is
not a console (the capture screen already surfaces notices; a "reset screen sharing" affordance
belongs there).
GATES: a machine at rung 2 returns to rung 0 after N good takes without waiting out the TTL · a
permission failure does not raise the rung at all and names the permission · the reduced request
still carries the bounds (pinned) · his repro recipe from docs/SCREEN_WEDGE.md, run again.

## F18 the export goes to the user's own resolution — AUTHORED 2026-08-29 from Robert:
##     "i want 3024x1964 or whatever users resolution is on roadmap"
[medium · the ladder's top step, not a constant]
THE PRODUCT CANNOT EXPORT A MACBOOK PRO'S OWN SCREEN. QUALITY_TIERS stops at 1440p, and the reason is
inherited rather than decided: F7/F7b chose 540p/720p/1080p/1440p by MEASURED FILE-SIZE SEPARATION on
a product whose capture was hard-capped at 1080p, O6 later made the 1440p step real detail, and
nobody asked what sits above it. So a 3024x1964 display — an ordinary laptop — is downscaled on the
way out, and the capture ceiling added 2026-08-29 (CAPTURE_MAX_LONG_EDGE) FOLLOWS that ladder rather
than causing it. Raise the ladder and the ceiling follows for free: they are one constant,
MAX_OUTPUT_LONG_EDGE in core/frame.ts.
WHAT THE TASK IS: a top step that IS the take's own long edge. Most of the machinery exists — F13
made a step a PIXEL BUDGET resolved against the take's aspect, so `tiersForTake` can inject one per
take; F15 made the rate follow the take the same way. What does NOT exist is the step's IDENTITY:
`tierById`/`loadQualityTier` persist an id, and "source" is a different number on every machine, so
the remembered step has to survive a 3024-wide take being followed by a 1920-wide one.
THE COST, STATED, because Robert has already been bitten by it: capturing 3024x1964 instead of
2560x1662 is 40 % more pixels through three hardware encoders, which is exactly where his freeze
lived. GATED ON O15, or it ships behind a flag so the cost is his to find rather than everyone's.
GATES: a take already at or below the ladder's top is byte-identical (the step does not appear) ·
the source step's file measures the take's own geometry, read out of the FILE · the instant copy
still fires on it (it is the composite's own size by construction) · the remembered step survives a
smaller take on the next load · size estimates cover it · oracle v2+v1.

## O15 how many encoders may a take open — AUTHORED 2026-08-29 from Robert's own console
[heavy · the freeze's root, and what F18 is gated on]
A SCREEN+CAMERA TAKE OPENS THREE HARDWARE AVC ENCODERS AND NOTHING BUDGETS THEM AGAINST EACH OTHER:
the raw screen channel, the raw camera channel, and the composite. On his game take that was
3024x1964 (level 5.1) + 1280x720 + 1920x1080 while a game rendered on the same GPU, and the WHOLE
MACHINE froze — "some movement on record but after a while freezes and whole screen, not just this
tab". The 2026-08-22 entry blamed a 4K surface consumed four times over; this is the same shape one
layer up, and it is why the capture ceiling is currently doing work a budget should do.
WHAT IS ALREADY KNOWN: `?singlegen=capture` removes the composite encoder entirely, measured at
45-49 % less written per second — but it only fires when the ONE video channel already equals the
export geometry, so it cannot help a take with a camera. The capture ceiling (2026-08-29) now makes a
SCREEN-ONLY take satisfy that equality by construction, which is the first real use it has had.
WHAT THE TASK IS: decide at ARM TIME how many encoders this take can afford and at what sizes, before
any of them opens — because the collapse is instant and unrecoverable (measured: a composite that
produces nothing in its first second never produces anything). Candidates, cheapest first: composite
the camera PiP and do not record it raw when the take will not be edited channel-by-channel · feed
the composite the raw channel's frames instead of a second decode · make single generation reachable
for screen+camera by compositing at the raw channel's own geometry.
GATES: his own take, his machine, with a game running · no take opens more encoders than it does
today · the instant export still fires wherever it fires today · oracle:load both phases.

## O16 a resolution step a raw channel can follow — AUTHORED 2026-08-29 from Robert:
##     "why resolution cannot go safely, make it go up safely too"
[medium-heavy · only worth doing after O15]
THE ANSWER IS THAT IT CAN, and the machinery exists. A raw channel's VideoEncoder is configured ONCE
and an MP4 track carries ONE frame size in its sample description, so a mid-file size change is not
representable — which is why captureLadder.ts now moves only the RATE, and why the resolution steps
it used to make left Chrome UPSCALING every frame back to the configured size (`screen channel
recorded 3024x1964 (the track said 2217x1440)`, his console).
BUT A CHANNEL IS ALREADY ALLOWED SEVERAL RECORDINGS. F6 built exactly this for pause/resume: `pause`
closes the current segment, `resume` opens segment N+1 on the same track with its own startOffsetMs,
and the timeline and the render already compose channels of several segments. A resolution step is
that move without the gap: close the segment, reopen at the new size — and back up the same way,
which is the half he asked for.
THE COST, AND IT IS WHY THIS IS NOT FREE: a raw channel with two segments of different geometry
cannot be packet-copied as one file, so a take that stepped loses its INSTANT export and renders
instead — a real regression for the user who was already struggling. That argues for O15 first, so
that stepping is rare.
GATES: a take that never steps is byte-identical · a stepped take's segments each measure their own
geometry out of the FILE · the timeline shows no gap and no jump · export renders across the seam
and the sync oracle holds over it · the instant path is DECLINED rather than wrong.

## F15 fps-ceiling — BUILT AND VERIFIED ON PROD 2026-08-29, flag-gated (handoff at the end of this
##     task). RULED YES 2026-08-29 (Robert: "i want every device to record best quality it can,
##     60 fps"). F13 LANDED 2026-08-29 — this is next. See the ruling note at the bottom.
[medium · authored 2026-08-29, raised by Robert's own objection to the 1920×1080 rule]
`DEFAULT_EXPORT_SETTINGS.fps = 30`, all four quality steps are `fps: 30` (quality.ts), and capture
asks the OS for `frameRate: { ideal: 30, max: 30 }` (acquire.ts:360). It is a `max`, so a 60 fps
game tab or a 60 fps camera is throttled AT THE SOURCE. Native-res drops the SIZE bound and keeps
this one deliberately — "every frame above 30 is encoded twice and then dropped at export" — which
is only true because the export is 30. THAT IS THE SAME SHAPE AS THE 1920×1080 RULE Robert OBJECTED TO:
an output constant reaching back to cap the input. Nothing here is broken today; it is a ceiling
with no flag, and no recent decision behind it.
THE QUESTION FOR ROBERT, and it is the whole gate: should INOUT be able to make a 60 fps video at all?
A yes costs bytes and encode time on every take that uses it and wants its own quality step; a no is
fine and should simply be written down as a decision instead of surviving as a constant. If yes,
this rides F13's chain — the same derive-from-the-take rewrite that carries geometry carries fps —
so it is scheduled AFTER F13, which landed 2026-08-29: `session.compositeFrame()` is where the
take is asked what it is, once, before any encoder is configured, and fps belongs in that answer.
RULED YES 2026-08-29 (DECISIONS). SHAPE, so the next session does not re-derive it: fps FOLLOWS THE
SOURCE up to 60 — not a new 60 constant, which would rebuild the shape Robert rejected. The chain:
displayVideoConstraints/cameraVideoConstraints frameRate (acquire.ts, currently ideal:30 max:30
unconditional) → composite FPS + cadence gate (liveCompositeV2/compositor.worker) → tier fps
(quality.ts) → render loop (render.ts drives everything off settings.fps) → encoderWarm shape →
sizeProbe's 30-fps-derived WINDOW_FRAMES. THE GUARDS ALREADY IN PLACE: the ladder judges arrivals
(P0-ladder-static handoff — a 60 fps source is explicitly not punished, pinned), keep-alive cadence
is rate-relative, and the throughput risk (4K-freeze family) is why this ships flagged first with
the ladder watching. GATES when built: a 30 fps source's take is byte-identical to today · a 60 fps
synthetic source records and exports 60 (read fps from the FILE) · oracle v2+v1 green · oracle:load
green (announce it) · one real 60 fps take judged by Robert.

## F15 HANDOFF — BUILT AND VERIFIED ON PROD 2026-08-29. DEFAULT NOT MOVED.
SHIPPED BEHIND `?sourcefps=1` (sticky `inout.frame.rate`), OFF by default — the task's own last gate
is "one real 60 fps take judged by Robert", so the flip is his. Off, every path answers exactly what
the constant answered.

THE SEAM IS `src/core/rate.ts`, and it is deliberately NOT a new 60 constant — that would rebuild
the shape Robert rejected in F13. The rate FOLLOWS THE SOURCE up to 60 and ONLY EVER GOES UP: a
24 fps webcam still records and exports 30, because the ceiling moved and the floor did not. That is
what makes the byte-identical gate structural rather than tested-for — a source at or below 30
cannot reach a single changed line.
 · `normalizeRate` rounds (capturers say 29.97 / 59.94), floors at 30, caps at 60. Total: every
   unknown answers 30, never NaN.
 · `takeRate(recording)` — THE COMPOSITE ANSWERS FIRST, the same argument `takeAspect` gives: that
   file is what the default step packet-copies, so returning the camera's rate for a take whose
   composite is 30 would have the panel promise 60 and the copy hand back 30.
 · Absent rate reads as 30 and that is a FACT, not a guess: until this shipped capture asked every
   source for `max: 30` and nothing downstream could exceed it.

WHAT IS GATED BY THE FLAG AND WHAT IS NOT, because it is the one asymmetry here: the flag governs
what gets RECORDED. The EXPORT side is not gated — it reads the rate out of the take. So turning the
flag off cannot strand a 60 fps file behind a 30 fps step that would have to re-render it down; a
take exports at the rate it was RECORDED at, the same sentence F13 writes about shape.

THE CHAIN, one answer each: `displayVideoConstraints` drops the ask entirely above 30 and keeps a
bare `max` (an `ideal: 60` would trade a source's resolution for a rate it may not have — nothing is
REQUESTED, the throttle is lifted) · `session.compositeRate()` decides once and hands the same
number to whichever engine starts, so a v2→v1 fallback cannot change the take's rate · both engines
paint, encode and declare it (`CompositeRecording.fps`, `ChannelRecording.fps`, both additive and
absent-means-30) · `resolveTier` carries it into every step, bitrate ceiling re-scaled by the rate
for the same reason F13 re-scaled it by pixels · `copySource` fences BOTH origins on it ·
`sizeProbe`'s window becomes a GOP-DURATION instead of 150 frames (150 is half a GOP at 60, and half
a GOP priced as a whole one reads high on every 60 fps step).

FOUND BY DRIVING IT RATHER THAN READING IT, and it is the whole reason the rig exists: EditorScreen
resolved its step against the take's SHAPE and not its RATE, so `settingsForTier` asked the export
for 30 fps of a 60 fps take. The fence did exactly its job — refused to hand a 60 fps file over
under a 30 fps label — and the cost of being right was a SILENT RE-RENDER DOWN TO 30, measured on
prod as composite 49.3 fps / export 30 fps / no copy where the control lane copied in one second.
Fixed and pinned by a test asserting the DEFAULT step of a 60 fps take is still the instant copy.

THE INSTRUMENT IS `scripts/fps-check.mjs`, and the Browser pane could not have been it: the
synthetic screen is a canvas painted from requestAnimationFrame and a HIDDEN tab does not run rAF —
a take driven that way recorded "37 frames encoded of 2 in, 35 keep-alive (source was static)",
declared 60 and delivered 0.1. Real, visible Chrome against the deployed build is the instrument.
It reads TWO numbers out of every file and gates on one: GRID (timescale over the most common sample
duration — the frame grid the writer worked on, which is "what rate is this file") and CADENCE
(samples over duration — how many frames the MACHINE managed). Conflating them is how its own first
run called a file whose every frame is stamped 1/60 s "22 fps".

GATES MET, everything geometric and temporal read out of the FILES in OPFS, never from the UI
(prod build f0b85d1, both lanes green, machine quiet):
 · 60 fps LANE — source handed over `1280×720@60` (before F15 the `max: 30` made this 30), 462
   frames ARRIVED, composite 411 encoded / 0 dropped, composite FILE grid 60 fps · 411 frames,
   export 411 frames 1920x1080 by INSTANT COPY, 2.0 MB. Product's own record: channel fps 60,
   composite fps 60.
 · 30 fps CONTROL — source `@30`, 234 arrived, composite 204 / 0 dropped, FILE grid 30 fps, export
   204 frames by instant copy, 1.3 MB, and no take announced a moved rate.
 · THE PRICE, measured rather than asserted: twice the frames for 1.5× the bytes (2.0 MB against
   1.3 MB on the same 8 s). Sub-linear, which is the temporal-redundancy argument — and it is the
   number Robert's standing minimal-size objective should be read against.
 · unit — 28 new tests in `src/core/rate.test.ts`. The first block is the safety net: `tiersForTake`
   deep-equals QUALITY_TIERS on a 30 fps take, `resolveTier` returns the same OBJECT, and the
   capture ask is `{ideal:30,max:30}` field for field.
 · 585 tests · typecheck · oracle v2 PASS · oracle v1 PASS · fidelity PASS · verify-deploy OK.
   HONESTLY: v1 and fidelity each failed ONCE on a loaded machine (v1 export throughput 0.76× < 1×;
   fidelity render residual 3.39 dB) and passed on every re-run once it was quiet (v1 1.17× and
   1.25×; fidelity residual 0.04 dB). Both are the timing-gate flakiness `.ai/DECISIONS` already
   records, and neither commit here touches the audio path or the render.

THE ONE GATE THIS TASK CANNOT MEET, with the measurement rather than an excuse: `oracle:load` IS
RED, AND IT IS RED WITHOUT F15. Announced, approved by Robert, run — and then run again on 79d6def
(the commit before F15's first line) in a throwaway worktree. IDENTICAL verdict both sides:
`composite tail FAIL/fps FAIL · raw tail INCONCLUSIVE/fps INCONCLUSIVE`, tail 9848 ms against HEAD
and 7366 ms against the baseline over a 400 ms band, delivered 0.2 and 2.5 fps over a band of 10,
and both raw runs erroring "the take produced no screen channel". THE LEAD is the same mechanism
this session hit in the Browser pane: the rig's 4K source is a rAF-painted canvas and `oracle:load`
drives through `exp.mjs`, which is HEADLESS by default, and a headless page runs rAF at 0 — so the
SOURCE starves and the tail band reads the starvation as tail loss. DECISIONS records this rig at
14 fps under the same 4K load, so 0.2-2.5 is a different regime and points at Chrome, not at us.
Filed P1 in BACKLOG with the first move (`--headed`). F15 is merged with this gate explicitly unmet
and explicitly not caused by it.

STILL OPEN, and it is Robert's:
 · ONE REAL 60 fps TAKE JUDGED BY EYE, which is the task's own last gate and the only thing between
   here and turning `?sourcefps=1` on by default.

TWO THINGS DELIBERATELY NOT DONE, so the next session does not re-derive them:
 · THE CAMERA IS NOT ASKED FOR 60. `cameraVideoConstraints` gains no `frameRate` at all: a camera
   that natively runs 60 now flows through unthrottled, but an `ideal: 60` would let fitness
   distance trade 1080p30 for 720p60 on a real sensor, and no synthetic rig can measure that trade
   (a canvas track ignores constraints). It needs a real-device run in the shape of P9's
   `camera-check.mjs`. Until then a 30 fps sensor stays 30, which is what "follows the source"
   means.
 · `encoderWarm` still warms at 30. Its own header measures the cost as a per-PROCESS GPU
   initialization, not a per-config one, so matching the rate buys nothing and risks a different
   throwaway config for no evidence.

THROUGHPUT, STATED PLAINLY because it is what the flag is guarding: a 60 fps take asks the
compositor and the encoder for twice the frames. On a QUIET machine both kept up (0 dropped, 411
frames). On a LOADED one — two browsers and a build running — the same rig measured 341 of 401
composite frames dropped and 393 of 455 raw frames dropped at 60, against 0 at 30. The degradation
ladder is the designed guard and it judges arrivals (P0-ladder-static), but an 8 s take is shorter
than its grace period, so it was never seen to fire. That is the case a longer real take should be
watched for, and it is why the rig reports throughput beside the contract and never folds it into
the exit code — a green-by-default number there would only mean the machine was quiet.

## F13 output-follows-source — DONE 2026-08-29, flag-gated (handoff at the end of this task)
[heavy · authored 2026-08-29, Robert: "it must be device resolution, how the fuck mobile will make
1920x1080? its vertical" → "yes"]
THE PRODUCT CANNOT MAKE A VERTICAL VIDEO. `DEFAULT_EXPORT_SETTINGS` is 1920x1080 (types.ts:546),
all four quality steps are landscape 16:9 (quality.ts:66), the live composite canvas is
`COMPOSITE_WIDTH/HEIGHT = 1920/1080` (session.ts:177), and the layout is authored against a fixed
1920 width (layout.ts:17). Nothing in the chain asks the take what shape it is.

MEASURED ON PROD 2026-08-29, camera-only synthetic take, live DOM read mid-take: the stage is
1.778 and the source is 1.333, and a camera-only take draws with `fit: 'cover'` (layout.ts:120), so
the compositor KEPT 75 % OF THE PICTURE HEIGHT AND CROPPED 25 %. It does not letterbox — it cuts.
Apply the same cover to a real 9:16 phone camera: 0.5625/1.778 = 31.6 % of the frame survives,
68 % is discarded, head and chin gone. And camera-only is the ONLY take a phone can make, because
iOS gives browsers no screen capture at all (CaptureScreen.tsx isKindSupported drops screen+tab
audio there) — so the crop path is not an edge case on mobile, it is the whole of mobile.

SECOND, SMALLER HALF, same root: `cameraVideoConstraints` asks a screenless take for
`CAPTURE_MAX_WIDTH x CAPTURE_MAX_HEIGHT` = a LANDSCAPE 1920x1080 (acquire.ts:390) — the app
requests the wrong orientation from the sensor before anything is composited.

WHAT THE TASK IS: the output geometry is derived from the take instead of being a constant.
  · capture   — the composite canvas sizes itself from the take's video channel (or from a chosen
                aspect), not from COMPOSITE_WIDTH/HEIGHT; camera constraints stop naming a
                landscape box on a screenless take.
  · quality   — a step becomes "how many pixels" (a long-edge target), not "which landscape box".
                540p/720p/1080p/1440p keep their names and their meaning on a 16:9 take.
  · layout    — drawComposition stops being authored at 1920w; `cameraFull` letterboxes or fills by
                the take's own aspect rather than always cropping to 16:9.
  · editor    — the stage, the preview and the export panel follow the same aspect.
  · export    — render + both copy paths carry the take's geometry.
THE PAYOFF BEYOND MOBILE: single generation (O3b) currently requires the raw channel to equal
1920x1080 exactly (copySource.ts:143, session.ts:905). If the output follows the source, that
equality is TRUE BY CONSTRUCTION on every screen-only take — the export half stops silently
declining (see the nativeres interaction in BACKLOG) and the capture half becomes flippable for
everyone instead of for 1080p monitors only.

GATES: a 16:9 take is BYTE-IDENTICAL to today, pinned by test — this task may add shapes, never
move the existing one · a portrait source exports portrait with NO crop, verified by reading
width/height out of the exported FILE (not the UI) · a camera-only 4:3 take keeps its full height
· the instant and smart-cut copy paths still fire on a screen-only take (assert copiedFrom) ·
fidelity + sync oracles green · `npm run exp -- o3b` still green · Robert judges one real phone take by
eye before any default moves.
RISK, STATED: this moves what an existing take exports to for any take that is not 16:9. Nothing
about a 16:9 take may change — that is the byte-identical gate, and it is the whole safety net.

## F13 HANDOFF — BUILT AND VERIFIED ON PROD 2026-08-29. Every gate met. DEFAULT NOT MOVED.
SHIPPED BEHIND `?sourceframe=1` (sticky `inout.frame.source`), OFF by default — the task's own last
gate is "Robert judges one real phone take by eye before any default moves", so the flip is Robert's and
the flag is how it gets judged. Off, every path answers exactly what the constant answered.

THE ONE SEAM IS `src/core/frame.ts`, and it is two functions:
 · `takeAspect(recording)` — THE COMPOSITE ANSWERS FIRST, then the screen, then the camera, then
   16/9. Composite-first is not a detail: that file is what the default step packet-copies, so a
   take must stay the shape it was RECORDED at or the panel promises one picture and the copy path
   hands over another. It also means turning the flag on does not retroactively un-crop old takes.
 · `frameForAspect(aspect, longEdge)` — 960x540 / 1280x720 / 1920x1080 / 2560x1440 to the pixel on
   16:9, both sides always even. A step is a PIXEL BUDGET (its long edge), not a landscape box:
   "1080p" is 1920 on the long edge whatever the shape, which is what a phone calls 1080p and what
   keeps Robert's minimal-size objective from being re-priced by the shape of somebody's monitor.
   THE PIXEL COUNT DELIBERATELY DOES NOT FOLLOW THE SOURCE — a 4K monitor does not turn every
   default export into a 4K file; that win is O3c's (the step matching the screen copies the raw
   channel) and it stays there.
THE CHAIN: both composite engines size their own canvas from `session.compositeFrame()` (one answer,
handed to whichever engine starts, so a v2→v1 fallback cannot change the take's shape) ·
`resolveTier`/`tiersForTake` in quality.ts (idempotent, identity on 16:9, bitrate re-scaled by the
pixel ratio so a taller frame is not a quieter encode wearing the same label) · `frameScale` keyed
to the LONG edge in layout/render/sizeProbe/ai-build, so a portrait PiP keeps the 1.5 px border and
16 px radius the layout was authored with instead of losing 44 % of them · the editor stage, the
capture preview canvas and the CSS `--stage-ar` · `cameraVideoConstraints` on a SCREENLESS take
follows the device's orientation instead of naming a landscape box · `stepDisplayDown` turns the
ladder's landscape rungs for a rotated source.
`cameraFull` KEPT `fit: 'cover'` on purpose: with the frame following the take it is the identity,
and where the two disagree (a source that changed shape mid-take, a take made before any of this) a
filled frame is what this product has always done — adding black bars there is not this task's to do.

GATES MET, all four geometry ones read out of the exported FILE by the browser's own demuxer (OPFS
`blobs/xport-*` → `<video>.videoWidth/Height`), never from the UI:
 · 16:9 UNCHANGED — with the flag ON a 1280x720 screen take logged NO "composite follows the source"
   line (the derived frame IS 1920x1080) and exported 1920x1080, `copiedFrom: "composite"`. Unit
   side: `tiersForTake` deep-equals QUALITY_TIERS and `settingsForTier(t, rec)` equals
   `settingsForTier(t)` on a 16:9 take, field for field.
 · PORTRAIT — `?synthetic=1&sourceframe=1&camsize=1080x1920`, camera-only: console "composite follows
   the source: 1080x1920", channel 1080x1920@30, FILE 1080x1920, cert `copiedFrom:
   "single-generation"` (O3c's equality true by construction — the raw channel IS the export).
 · 4:3 CAMERA KEEPS ITS FULL HEIGHT — FILE 1920x1440, cert `copiedFrom: "composite"`. The same take
   before this was 1920x1080 with 25 % of the picture cut off.
 · BOTH COPY PATHS ON A SCREEN-ONLY TAKE — instant: `copiedFrom: "composite"` 1920x1080; smart cut
   after a trim: "[smart cut] 1 spans · 15 packets copied · 0 frames re-encoded · 100.0 % of the
   video never touched a codec", FILE 1920x1080.
 · ORACLES — v1 3/3 PASS interleaved against a 263d2a7 baseline at identical throughput
   (1.29-1.38x both trees); v2 4 PASS / 1 FAIL against the baseline's 2 PASS / 3 FAIL in the same
   interleaved runs, same signature the map already files as flaky. THE EARLIER RED WAS THE MACHINE:
   with the Browser pane open BOTH trees failed on export throughput; with it closed both pass.
   Fidelity 3 PASS / 1 FAIL, the passes matching the baseline's residual to 0.01 dB (0.03/0.04) and
   the one failure (residual 10.21 dB) not reproducing in three later runs.
 · `npm run exp -- o3b` 7/7 green, chroma table reproduced unchanged (80.0/89.1 vs 70.3/75.2,
   37.3 vs 35.5 dB, fringe 8.66 vs 10.24).
 · 542 tests (32 new), typecheck, build, verify-deploy OK.

A BUG THIS TASK CREATED AND FIXED, FOUND BY DRIVING IT AND NOT BY READING IT: `chooseCopySource`
fenced the composite with `allowComposite` ALONE, which meant "this is the default step" — sound
only because the composite was always 1920x1080. Measured live: a 4:3 take recorded with
`?sourceframe=1` and exported with `?sourceframe=0` packet-copied a 1920x1440 file under a "1080p"
badge, beside a stage showing the 16:9 crop. The composite is now checked against the request as
well; every pre-F13 take has a 1920x1080 composite and a 1920x1080 default step, so it refuses
nothing that ever worked. Re-verified on prod: that same take now renders 1920x1080 and the panel
says "Re-renders the whole video at 1920x1080" instead of badging it INSTANT.

HONEST RESIDUE, none of it blocking:
 · the timeline's filmstrip thumbnails are still 16:9 boxes (`app/lib/filmstripPlan.ts`
   THUMB_ASPECT) — cosmetic in a 30 px lane, and outside the chain this task names.
 · the fallback capture preview's `.stage__pip` percentages are still derived from a 1920x1080
   frame. Only visible when the compositor is NOT painting the preview; the composite preview is
   exact.
 · smartCut certifies `path: "render"` (certify.ts only has 'instant' | 'render') — pre-existing,
   noticed while reading certifications out of files. `copiedFrom` is the field that tells the truth.
 · the CAPTURE half of `?singlegen=capture` on a native-res 1440p/4K screen still declines, because
   the composite takes the take's ASPECT and not its pixel count. That is the same trade O3c named
   and F16 is the task that closes it.
 · `.tl` / `.xp` / `.quality` deliberately do NOT follow the stage aspect: tying them to it squeezed
   the whole editor into a column on a portrait take (seen, then fixed). Only the stage and the box
   that must fit it vertically follow.

## F13 SECOND PASS — Robert JUDGED IT ON A PHONE AND IT WAS STILL WRONG. Fixed 2026-08-29.
Robert: "preview on phone still wrong proportions and cutted and in editing even more cutted preview,
fix it." THE FIRST PASS BELIEVED `track.getSettings()`, AND THAT CALL DESCRIBES THE SENSOR. A phone
held portrait reports 1920x1080 while every frame the platform delivers is 1080x1920, rotated. So
`compositeFrame()` derived a LANDSCAPE canvas from a portrait take, the compositor cover-cropped
into it, and the editor then cropped the raw portrait channel a SECOND time into the same landscape
stage — which is exactly "even more cutted", and it is the only reading of Robert's sentence that
explains why editing was worse than capture. Nothing in the capture chain had ever looked at what a
delivered frame actually is; `displayWidth/displayHeight` were used only to fit INTO the canvas.
THE FIX: the settings are the GUESS, the first frame is the ANSWER — one shared decision
(`adoptedFrame`, core/frame.ts, unit-tested) in all three places that encode.
 · compositor worker (v2, Android/desktop) — adopts the first frame's aspect at the 1920 long-edge
   budget and reconfigures its encoder BEFORE anything is in the file. The muxer is never told a
   size (`addVideoTrack` takes only a frame rate; the geometry reaches the file through the
   encoder's SPS), so the whole correction is one canvas and one `configure()`. The keep-alive
   holds off for a bounded 700 ms so it cannot freeze the guess in first, and a source that never
   delivers (a still screen) simply keeps the guess — which is the shape it always had.
 · live composite v1 — THE ENGINE AN IPHONE ACTUALLY USES (Safari has no MediaStreamTrackProcessor,
   so v2 is never available there, which is why this half is not optional). Sizes its canvas from
   the `<video>` element's videoWidth/videoHeight, the rotated truth, behind the same 700 ms bound.
 · raw channel worker — was configuring its encoder from the settings and then RESCALING portrait
   frames into a landscape config. Encodes the frame's own size now, and the session writes what
   was written into ChannelRecording, so the single-generation copy and the editor's PiP stop
   inheriting the lie. NOT flag-gated: a channel whose declared geometry disagrees with its frames
   is broken for everyone, and on a truthful source it is a no-op.
Both engines report the shape they settled on; the session emits `composite-geometry` and the
recording preview follows THAT instead of the track settings. The preview canvas is no longer
re-sized from React (writing width on a transferred canvas throws) — the worker owns that bitmap
and turns it, with `object-fit: contain` as the safety net for the moments they disagree.

A SECOND BUG, CREATED AND CAUGHT INSIDE THE SAME HOUR: the reconfigure is ASYNC (it probes the
config) and both workers kept encoding through it, so the first frames went in at the old geometry
and the file carried two SPS. Seen on prod: `EncodingError: Decoding error`, the filmstrip refused
the channel and the editor stage was black. Both workers now hold every encode AND the keep-alive
until the new config is applied; the raw worker already clones each frame, so the keep-alive puts
the picture in the moment the encoder is ready. Cost: a few ms at the very start of a take.

`?camlies=1` IS WHY BOTH WERE FOUND. It makes the synthetic camera report its dimensions transposed
— the one lie a phone tells and the one no rig could express, since a canvas track always tells the
truth about itself. The phone failure and its fix are now reproducible on a desktop from a URL:
`?synthetic=1&sourceframe=1&camsize=1080x1920&camlies=1`. It paid for itself on its first run.
`?sourceframe=` ALSO STICKS NOW when it comes from the URL, against the convention every other flag
follows, and the reason is the device: it is turned on from a phone, where there is no console, and
a reload between recording and judging would silently put the take back to landscape.
GATES RE-RUN AFTER THE CAPTURE CHANGE: with the sensor lying, console says "raw channel: the frames
are 1080x1920, not the 1920x1080 the track reported" and "compositor: the frames are 1080x1920 —
composing 1080x1920, not 1920x1080"; capture preview portrait and uncropped; editor stage portrait
and uncropped; filmstrip decodes 8/8; exported FILE 1080x1920, `copiedFrom: "single-generation"`.
oracle v2 4 PASS / 1 FAIL over five runs · oracle v1 2/2 PASS · `exp o3b` 7/7 with the chroma table
unchanged · `exp armcancel` green · 547 tests · typecheck · build · verify-deploy OK.
FIDELITY IS FLAKY ON THIS MACHINE AND IT IS NOT THIS CHANGE: interleaved against e75e34d, HEAD read
3/3 PASS at residual 0.03 dB while the BASELINE failed one of three at residual 18.83 — worse than
anything this tree produced. Filed in BACKLOG beside the v2 oracle's own flake. Every PASS on both
trees reads 0.03.

## F13 THIRD PASS — THE PHONE'S REAL BUG WAS NOT GEOMETRY AT ALL. Fixed 2026-08-29.
Robert, on an iPhone: "it is still fucking horizontal on phone, make it like it supposed on phone,
everything camera sees" · "i m talking about only camera recording, and mic dont work in iphone
safari too" · "recorded mic beatrate is shown on editing but no sound hearable".
THAT LAST SENTENCE IS THE DIAGNOSIS, and it is worth reading twice: a waveform drawn correctly
beside an element that plays nothing means the BYTES are fine and the LABEL is not.
MediaRecorder on Safari has no WebM encoder — it produces MP4 — and `armChannel` chose the storage
key's extension as "mp4 for the WebCodecs path, webm for everything else". On an iPhone
"everything else" is EVERY channel: Safari has no MediaStreamTrackProcessor, so both measured paths
are unavailable and the composite is v1. So every file was an MP4 stored under a `.webm` name,
`File.type` is derived from that name, and `URL.createObjectURL` handed each element a blob
labelled `video/webm`. Safari trusts the type instead of sniffing. Silent mic, blank camera.
AND THAT IS ALSO WHY IT WAS STILL HORIZONTAL. An element that will not load reports videoWidth 0,
so nothing downstream could ever learn the take's shape from the FILE — it fell back to
`track.getSettings()`, which describes the sensor and is landscape on a phone held portrait. Two
reports, one root cause, and the geometry work of passes one and two could not reach it.
WHAT SHIPPED:
 · `core/store/mediaUrl.ts` re-types the blob from the recorder's own `mimeType` before it reaches
   an element. `slice` re-types without copying, and this REPAIRS TAKES ALREADY RECORDED, which
   renaming cannot. Every media element in the app goes through it.
 · new channels are named by the container they actually hold (`containerExt`), and the composite
   is `.mp4` — which both engines have always written. `recovery.ts` sweeps both names.
 · THE EDITOR TAKES THE TAKE'S SHAPE FROM WHAT ITS DECODER OPENED, not from anything capture
   claimed. `tiersForTake(recording, aspect?)` is now the single place a step is resolved, and both
   the stage and the export follow that one number; `copySourceForTier`/`estimateExportBytes` stop
   re-resolving so the badge cannot disagree with the path.
 · A CAMERA-ONLY TAKE LETTERBOXES INSTEAD OF CROPPING under the flag — Robert's "everything camera
   sees". Identical to the old crop whenever frame and picture agree; where they do NOT it is the
   difference between a bounded cosmetic cost and losing someone's head, which is the right way
   round for a chain that has now been wrong about a phone twice.
 · THE DEFAULT IS ON WHERE `getDisplayMedia` DOES NOT EXIST. Narrow on purpose: on a desktop the
   shipped 16:9 behaviour is what every take was made under and Robert has not judged the new one.
   On a device with no screen capture the only possible take is camera-only, held portrait, and the
   landscape box threw 68 % of it away — there is no working path to protect. `?sourceframe=0` off.
VERIFIED ON PROD, on the closest reproduction of iPhone Safari this rig can build —
`?synthetic=1&sourceframe=1&camsize=1080x1920&camlies=1&rawcodec=mediarecorder&engine=v1`
(MediaRecorder channels, v1 composite, a sensor that lies about orientation):
console "composite v1: the picture is 1080x1920 — composing 1080x1920, not 1920x1080" · capture
preview portrait, uncropped · editor stage portrait with `stage__full` reporting w1080 h1920 from
its own decoder · the audio element readyState 4, error null, and the blob it was handed typed
`audio/webm` (the RECORDER's mime) where the filename would have said `video/webm` — on Safari that
difference is the whole bug · exported FILE 1080x1920, `path: instant`, `copiedFrom: composite`, so
the fast path still fires · composite stored as `_composite.mp4`.
584 tests · typecheck · build · `exp o3b` 7/7 · verify-deploy OK.
THE ORACLES COULD NOT BE READ CLEANLY THIS ROUND and the reason is on the record rather than
hidden: another session was running F15 in the same worktree at load average 16.4, and under it the
v1 oracle fails on export throughput for BOTH trees — measured interleaved, the pre-fix baseline
read 0.72x against this tree's 0.96x, i.e. this tree was the FASTER of the two. v2 read 2 PASS /
1 FAIL in three runs. The one gate that is load-immune, `exp o3b`, is 7/7 green. Re-run both
oracles on a quiet machine before reading anything into them.
WHAT IS STILL UNPROVEN, and only Robert's phone can prove it: that Safari plays the re-typed blob.
The mechanism is certain and the fix is the only thing that can address it, but every check above
ran on Chromium, which sniffs past the wrong label and therefore cannot fail the way Safari does.

WHAT F15 AND F16 INHERIT: `resolveTier` is the seam F16's rungs resolve through (F16 says so
itself), and fps follows the source through the same `session.compositeFrame()`-shaped question F15
needs — one place that asks the take what it is, at capture, before any encoder is configured.

## R1 x15-chroma-rig-hardening — DONE 2026-08-29, all 15 fixed (handoff at the end of this task)
[medium · authored 2026-08-29, Robert: "make fix all of them as one step"]
ALL 15 findings of the 094b13d code review, one task. The chroma rig is now the evidence for O3b
and the 4:4:4 decision, so its failure modes ARE the task. Everything lives in
src/experimental/perf/{trimTextParity,textSource}.ts unless a path is named. Line numbers are
094b13d's.
INSTRUMENT LIES (the rig can report wrong without saying so — fix these first):
 1. the "fast path is not the worst-coloured file" gate passes on null (trimTextParity 550) and
    the export loop skips undecodable/non-1:1 frames with a bare `continue`, no SKIPPED row (466)
    — missing data must read MISSING, loudly, in BOTH chroma gates. Same trigger via sat()'s
    startsWith on display labels: give rows a real key.
 2. chromaRows counts out-of-bounds pixels as mask hits (undefined → NaN>tol is false; textSource
    712) and rect is derived from the INSTANT READER's dims, not the reference's (316) — assert
    source/decoded/rect geometry at the top of chromaRows, derive rect from W×H.
 3. the reference is rasterized on {alpha:false} but the wire canvas is get2d()'s default
    alpha:true (synthetic.ts 48) — opaque canvases are eligible for different text AA, and the
    0-source self-row is structurally blind to it. Build the reference through the same context
    the wire uses. IF THE COMMITTED NUMBERS MOVE, the tables in TASKS/BACKLOG/CONTEXT update in
    the same change — the numbers are quoted in three docs.
 4. the "chain keeps colour" gate is born red ((instSat ?? 0) >= 90 vs measured 70.3, still red
    at O3b's 80; 543) and null reads as total loss — regression-baseline form (>= today − eps) or
    chromaPrice's own DIAGNOSTIC ONLY convention (pass: true + self-describing detail), and the
    two gates must agree on what null means.
 5. an rd.at() rejection aborts the ENTIRE report (452 — the big try has finally, no catch): one
    corrupt channel tail throws away pair rows that never touched chroma. Per-stage catch;
    blob-read/openNative failures get SKIPPED rows like the size-mismatch branch already does.
 6. the fast-path gate margin (1 pt, 550) is tighter than the rig's own stated ~2 pt run
    variance, and it gates green only while render wins blue and loses green — margin ≥ 2 pts,
    min over the coloured rows.
 7. the composite 1:1 guard is vacuous: the compositor contain-fits into a hardcoded 1920×1080
    canvas (compositor.worker 517-520), so rd.width===W checks the container, never the content —
    guard the screen track's own dims.
 8. nothing separates 4:2:0 loss from a YUV matrix/range round-trip drift (same fingerprint:
    saturated glyphs fade, grey holds) — add a flat-saturated-patch control stage: nothing for
    subsampling to damage, so if IT loses colour the attribution is wrong and 4:4:4 under-delivers.
 9. grey's keptPct divides by a 7.4 % source saturation, so ±1 LSB of decode noise reads as
    ±6 pts — report grey as ABSOLUTE saturation delta; the "not brightness or gamma" argument
    survives without the amplifier.
SHARED SEAMS:
 10. the palette exists as FIVE unlinked copies and the rig's is the only decimal one
     (textSource 662 vs synthetic 119, textSource 51, bitsAudit 71, aiExport 66) — export ONE
     constant from synthetic.ts beside drawTextScreen, derive PAGE_COLOURS and the painters from
     it (additive export, zero behaviour change). A drift today zeroes the mask and gate 2 passes
     vacuously while gate 1 screams a fake P1.
 11. the encoder warm is pasted per-rig and ELEVEN rigs calling createCaptureSession are still
     cold (note 6's mirage, ninth instance and counting) — one shared rig-harness warm. Warming
     inside createCaptureSession itself is production capture = Robert's explicit yes, NOT this task.
WASTE:
 12. the chroma mask is recomputed 18× (6 stages × 3 colours, full-rect scans) for a mask that
     depends only on constants — build per-colour index lists from the reference once.
 13. sampledAtSec[0] is decoded up to 3× per reader (chroma/crops/thumbs) and .at() re-seeks from
     the keyframe every time — decode once per reader, share; also keeps the crop Robert sees and the
     number beside it the same frame by construction.
 14. the second getContext('2d', {willReadFrequently:true}) is dead — repeat-call attributes are
     ignored per spec (432) — one call with both options, the openNative pattern.
DOCS:
 15. CONTEXT.md ~120: the old parenthetical retracts "the damage happens when the recording is
     first captured" VERBATIM while the new block 40 lines later asserts exactly that — scope the
     retraction to sharpness so the doc stops contradicting its own headline finding.
GATES: typecheck + tests green · `npm run exp -- x15c` twice, numbers inside the stated ~2 pt band
of the committed baselines (or baselines updated in the same change if fix 3 legitimately moves
them — say which) · missing-data drill: point one stage at a dead blobKey → BOTH chroma gates read
missing/skipped and the report SURVIVES · palette-drift drill: perturb one palette entry by +8 →
the run says MASK EMPTY, not "0 % kept" · flat-patch control present in the report · no capture/*
behaviour change, oracle untouched.

## R1 HANDOFF — DONE 2026-08-29, all 15 fixed, every gate met. ALL NUMBERS HELD.
THE HEADLINE: fix 3 did NOT move the committed baselines. The reference is now rasterized through
capture/synthetic.ts's OWN context factory (get2d, alpha:true) instead of a private {alpha:false}
canvas, and the chain reproduced BIT-IDENTICALLY across two runs: raw screen 79.7/80.0/89.1 ·
composite and instant and smartcut 71.6/70.3/75.2 · render 75.7-78.4/67.0-67.3/78.3-78.7 (grey/
green/blue). So the tables in TASKS, BACKLOG and CONTEXT stand as written; nothing was re-anchored.
The alpha attribute was a real hazard and it was not firing.

THE ONE NEW FINDING, and it is fix 8's whole reason: THE ATTRIBUTION IS NOW CONTROLLED. 4:2:0 and a
YUV matrix/range round-trip drift are indistinguishable on this fixture — both fade saturated glyphs
and leave grey — so every stage in the table above was consistent with either, and O9/X15(b)'s 4:4:4
case rested on it being the first. New matched control pair, same palette, ONE identical AVC 4:2:0
encode:
    C1 flat slabs      grey 98.6 %  green 101.1 %  blue  99.8 %   nothing to subsample away
    C2 thin glyphs     grey 71.6 %  green  79.8 %  blue  82.1 %   the same page, same encoder
Subsampling on thin glyphs is the mechanism, as claimed. 4:4:4 will deliver.
(C2 also prices ONE standalone encode of the source page at green 79.8 %, against the raw capture
channel's 80.0 % — the capture encode costs what a generic 4:2:0 encode costs, no more.)

WHAT EACH FIX CHANGED, and the two that changed a VERDICT are marked:
 1. ChromaRow carries `key: 'grey'|'green'|'blue'`; the gates look up by key, never by the padded
    display label they used to `startsWith`. Undecodable / non-1:1 / missing-blob stages push a
    SKIPPED stage with a reason instead of `continue`. BOTH chroma gates now FAIL loudly on missing
    data through one shared `missingOf()`; they used to disagree (one passed on null, one read null
    as total loss).
 2. `chromaMask()` asserts the rect is inside the source and `chromaRows()` asserts the decoded
    frame is 1:1 with it — both throw with the numbers, and the caller turns that into a SKIPPED
    row. The chroma rect is derived from W x H (the reference), not from the instant reader. Unit
    tests pin the old OOB-counts-as-hit and sheared-stride behaviours (chromaMask.test.ts, 6 tests).
 3. reference through `textScreenReference()` in synthetic.ts — the wire's own get2d. NUMBERS HELD.
 4. VERDICT CHANGED: the "chain keeps colour" gate was `>= 90` against a chain that measures 70.3
    and whose best proposed future is O3b's 80 — unreachable by construction, permanently red. It
    is now a REGRESSION gate against a committed baseline (green 70.3 / blue 75.2, ±2 pts), and it
    PASSES today. The ~30 % loss stays stated in the detail as the standing BACKLOG P1 finding.
    Move CHROMA_BASELINE in the same commit as the doc tables when the chain legitimately improves.
 5. per-pair and per-stage catch. Proven by the dead-composite-blob drill: stage 2 reads MISSING and
    every other stage, pair row and gate in the report survives. It used to throw the whole report.
 6. VERDICT CHANGED: the fast-path gate was green-only at a 1 pt margin — tighter than the rig's own
    ~2 pt variance, and green is the colour the render LOSES. Min over the coloured rows at 2 pts:
    green +3.0, blue -3.3/-3.5 → the gate now FAILS, correctly. That is X15(d)'s settled "it is a
    wash", said out loud instead of cherry-picked. It goes green when O3b lands. The detail says so
    in full, so nobody reads it as a new regression.
 7. the composite's `rd.width === W` guard only ever checked the compositor's HARDCODED 1920x1080
    container and could not fail. It now also requires the SCREEN TRACK to be W x H, so the
    contain-fit is provably the identity; otherwise SKIPPED, because the stage would be measuring
    the scaler.
 8. the flat/thin control pair above (C1/C2), plus its own gate.
 9. every row carries `saturationDeltaPts` (decoded − source, absolute). Grey's "barely moves" is
    now argued at -2.1 pts against green -13.5 / blue -13.0, instead of through a ratio whose
    denominator is a 7.4 % source saturation and turns ±1 LSB into ±6 points. Its own gate asserts
    |grey| < 0.5 × min(|green|,|blue|).
10. ONE palette, exported from capture/synthetic.ts beside drawTextScreen (TEXT_SCREEN_PALETTE +
    glyphColour). All five copies now derive from it: synthetic's own painter, textSource's LINES
    and its decimal PAGE_COLOURS, bitsAudit, aiExport. Additive export, zero behaviour change —
    drawTextScreen paints the identical hex strings, and the oracle agrees (below).
11. `experimental/rigWarm.ts`. Eleven cold rigs now warm before they open a session: armCancel,
    backgroundFrame, cameraMove, captureLoudness, compositeWedge, pauseTake, qualityTiers, rawTail,
    smartCutRun, zoomPan, recovery/run. The three that already warmed (trimTextParity, rawCodecTake,
    chromaPrice, loadedSync) route through the same helper. NOT inside createCaptureSession — that
    is production capture and needs Robert. Spot-checked: armcancel green (reject 132 ms, arm 331 ms,
    4 channels), f3 3/3 PASS.
12. per-colour index lists built once from the reference; the mask was being recomputed 18x
    (6 stages x 3 colours) for something that depends only on constants.
13. sampledAtSec[0] decoded ONCE per reader and shared by the chroma row, the crop and the thumb —
    it was up to 3 decodes each, every one re-seeking from the keyframe. The crop Robert looks at and
    the number printed beside it are now the same frame by construction. (Also: the crop labels
    used `id === 'instant' ? 1 : 2`, which labelled smartcut and render both `c-02`.)
14. the dead second `getContext('2d', {willReadFrequently:true})` is gone with fix 3 — one call,
    and repeat-call attributes are ignored per spec so it never applied anyway.
15. CONTEXT.md ~120: the retraction is scoped to SHARPNESS ("capture throws away half of every
    letter's edge contrast" — retracted) and now points at the colour finding as real and separate,
    instead of verbatim retracting the headline the section 40 lines below asserts.

DRILLS — all three are options on the rig, so they are re-runnable in one command:
  `npm run exp -- x15c '{"drill":"dead-composite-blob"}'`  → 2-composite SKIPPED "MISSING — blobStore
     has no …-R1-DRILL-DEAD", every other stage and gate intact, report produced.
  `npm run exp -- x15c '{"drill":"dead-instant-export"}'`  → 3-export-instant SKIPPED (mediabunny
     RangeError, caught), and BOTH chroma gates read "NOT MEASURED — … A missing measurement is a
     FAILED gate here, never a quiet pass" and FAIL. Report produced.
  `npm run exp -- x15c '{"drill":"palette-drift"}'`        → green reads MASK EMPTY with keptPct null
     on EVERY stage, never "0 % kept"; all four chroma gates FAIL naming it.

GATES MET: typecheck clean · 464 tests (was 458; +6 for the instrument) · x15c twice, chroma
identical run to run and equal to the committed baselines · all three drills as above · flat-patch
control in the report · `npm run oracle` PASS (v2, sync 34.6 ms) AND `--engine=v1` PASS (sync
51.0/64.3 ms) · `npm run exp -- armcancel` green · f3 3/3 PASS.

TWO THINGS FOUND, NOT CHASED (both outside R1's 15, both to triage):
  (a) THE PiP PLACEMENT NUMBER IN X15(c) IS STALE. That entry records the render placing the camera
      PiP 14-15 frames from the instant path at 23.6 dB. Two runs today read -1 and -2 frames at
      29.5 and 28.0 dB. Nothing in R1 touches placement (the anchor frame is the identical decode),
      so something in the product improved it between 08-26 and today. Worth one look before that
      BACKLOG P2 line is quoted again.
  (b) AND THAT MAKES THE ALIGNMENT GATE FLIP. Its bar is |align| <= 1 frame; today's takes sit at
      1-2, so the gate passed on run 1 and failed on run 2 of the same build. A gate whose verdict
      depends on take jitter is note 17's problem in the other direction. I did NOT move the bar —
      moving a gate to make it green is the thing this codebase pays for — so it is reported here
      instead. Fixing it properly means deciding whether 1-2 frames of PiP placement is acceptable,
      which is (a)'s question.

BLOCKED ON HARDWARE OR A DOWNLOAD, unchanged:
  P1   firefox + 3-engine oracle   [medium]  code half shipped 2026-08-23. What is left needs
                                             downloads Robert must approve (Playwright's gecko driver)
                                             and a Windows machine for the one row that cannot be
                                             spoofed. See the handoff.
  O11d codec ladder, probed        [medium]  needs P1's 3-engine rig. (a)(b)(c) are shipped.
  P8   safari-mic                  [P0]      blocked on ONE Robert artifact (the exported file, or a
                                             Safari-console take). Do not code it from theory.

BLOCKED ON A RULING Robert HAS ALREADY BEEN ASKED FOR: (none — X6's ruling landed 2026-08-26 and
  moved O3b to READY NOW.)

NEEDS ROBERT DISCUSSION BEFORE ANYONE BUILDS (Robert 2026-08-25: "questionable mark as need to be discussed
again" — these change the file a user gets, or need Robert's hardware, so they start on a ruling, not a go):
  X10=O5e VFR export — REJECTED by Robert 2026-08-26 ("fuck this"). The size win does not buy a file
                                             some players and editors mishandle. In the REJECTED
                                             list; do not resurrect without Robert.
  X13  local-adaptive-encoding     [heavy]   on-device zero-token model (per Robert's 2026-08-25 "no AI"
                                             clarification = no runtime token spend) classifying 5 s
                                             chunks text/motion/still to steer bitrate; realistic
                                             20-40 % smaller files. Changes output → principle
                                             ruling first, then per-instance gate.
  X7   safari-premise-retest       [light]   Safari 26 shipped AudioEncoder/AudioData and Safari 18+
                                             has MediaStreamTrackProcessor (worker-only, video-only)
                                             — the premises behind "v1 exists because Apple lacks
                                             MSTP" and the ~1 s audio truncation are STALE. Needs
                                             Robert's real Mac/Safari and pairs with the P8 artifact Robert
                                             still owes. If green, a whole Safari special-case pile
                                             deletes and P8 may close itself.
PARKED (nothing to build today): X11 capture-audio-mix-to-worker — heavy, touches the tuned mix
graph (limiter/gains), do after X6; kills the last main-thread capture cost and the ~430 ms PCM port
lag; capture change → Robert gate. · X14 sync ≤20 ms — blocked on Q4 2026 Web Audio deliverables
(audio-thread performance.now(), configurable quantum); nothing buildable until implementers ship.
· 4K/WebGPU importExternalTexture stays inside O6 (second-order until X5).
FOLLOW-UP FOUND WHILE FIXING P0-tail-raw, not fixed and not urgent: `setChannelActive(kind, false)`
stops a channel mid-take through onTrackEnded, which does NOT drain — so a channel the user switches
off loses whatever the encoder had not caught up on, exactly the defect P0-tail-raw fixed for the END
of a take. It needs onTrackEnded to become async (throttle, drain, then stop), which is the same
stop-path surgery and wants its own session. Under load it is the same 200-1300 ms.

CONFLICT RULES (capture/* is shared by TWO capture engines and compose/* by THREE export paths):
· THE EXPORT HAS THREE PATHS AND EditorScreen PICKS BETWEEN THEM IN ORDER: instant (unedited +
  composite + default tier → packet copy), smart cut (pixel-default edit + composite + default tier +
  the flag → copy most packets, re-encode the cut boundaries), full render (everything else, and the
  fallback for both). Anything touching compose/* has to say which of the three it changed. The render
  itself lives in render.ts and runs in a WORKER by default — nothing in it may touch a Window-only
  global (no AudioBuffer, no DOM), or the worker dies and every export silently takes the slow lane.
· The composite geometry lives in THREE places that must agree — compose/layout.ts, capture/
  compositor.worker.ts and capture/liveComposite.ts. cameraTrack.ts owns the PiP rect; F3's frame is
  an EDIT (render path only), so the two live composites deliberately do not know about it.
· F7c touches compose/quality.ts and the sizeProbe spike, which nothing else does.
· Any capture/* change must run BOTH `npm run oracle` and `npm run oracle --engine=v2`, and any
  change to the STOP path must run `npm run oracle:load` (both phases) and `npm run exp -- armcancel`.
BLOCKED (dep → unblocks): P4←O5 (now unblocked — O5 is done) · P5b←P5a · P6←P5b · P7←P4 ·
O10 (AEC part only)←P4. (O4's flip unblocked O5/O6/F6/F8/O3b; O5's completion on 2026-08-24 unblocks
O9, the O8 remainder and P4. O10 (a)(b)(c) never needed anything and has always been READY.)
REJECTED (do not resurrect): pre-roll rolling buffer — no capture before the record press, ever
(Robert 2026-08-22; extends "no idle device access"). MP4 MediaRecorder capture — measured 2026-08-23,
Chrome does not stream it and a tab kill loses the whole take (O3a). Ending a raw channel's TRACK to
drain its encoder — measured 2026-08-23, Chrome stops the recorder itself and takes the backlog with
it (P0-tail-raw). The related HARD requirement lives in O4/O8: zero tail loss at stop, now gated on
BOTH the composite and the raw channels. VFR export (X10=O5e) — Robert refused 2026-08-26; the
delivered file stays constant-frame-rate.

── WHAT A FRESH SESSION MUST KNOW ──
1. SYNC IS WORSE THAN OLD NOTES SAY, and the instrument was why. Every sync number quoted before
   2026-08-23 was ~31 ms optimistic: the oracle carried an exact +18.00 ms detection bias (half a
   30 fps frame + half a 128-sample envelope window) and never measured the VIDEO reference at all
   (13.5 ms) while correcting the audio one. CI now gates the SYMMETRIC metric at ≤90 ms and reads
   40-66 ms. The instrument's own noise floor is ~8-9 ms 1σ — no band below that is measurable.
   O4's job is to bring this to ≤20 ms; re-tighten the band then.
2. THE AUDIO ANCHOR CANNOT SEE INPUT LATENCY. It dates sample 0 from when a worklet batch ARRIVES,
   so device/stream buffering is invisible and audio lands late. Loopback-measured at +128.7 ms
   (sd 0.70); dating from the audio clock instead changes it by 1.1 ms, so this is signal-path, not
   messaging. Production now subtracts the platform-reported track latency (bounded, logged, never a
   fitted constant — a 90 ms fallback was rejected on those grounds in 2026-07).
3. NOTHING IN ARMING MAY AWAIT WITHOUT A DEADLINE. Each step was bounded but the joins were not, and
   that froze recording with a stale "Waiting for microphone…" label. Budgets now bound the joins,
   every durable-worker round trip has a deadline, the record button cancels an arm, and a pagehide
   guard releases devices. Keep it that way.
4. EVIDENCE IS BROWSER-SIDE. Unit tests cover pure logic only; anything about capture, export,
   memory or bundles must be measured in a real browser on the PRODUCTION build. QA on the prod-build
   port only, never the dev server.
4b. EVERY NUMBER BEFORE 2026-08-25 WAS TAKEN ON AN IDLE MACHINE, AND ONE OF THEM WAS WRONG BECAUSE
   OF IT. "Drift is dead" (beta−1 = −0.003 ms/s) is true idle and false under load: a saturated
   machine's AudioContext returned 56.1 s of quanta in 59.2 s of wall, which drifted the composite's
   audio 2.6-3.8 s per 60 s until it was fixed (see STATE). The take a user records beside a game is
   a different regime from the take a gate records on a quiet machine, and `npm run exp -- syncload`
   is the only cell that runs in it. Anything touching audio placement runs it BOTH ways.
5. THE TAIL IS A DRAIN PROBLEM, AND MediaRecorder LIES ABOUT BEING IDLE. The shipped composite used to
   lose 2734 ms off a 4K take; it now loses 43-320 ms, because stop() kills the painting and then
   PROBES the encoder with requestData() until it answers empty. Waiting for the byte flow to go quiet
   instead looks equivalent and is not — under load this recorder emits four chunks in ten seconds, so
   silence is the normal state and means nothing. Anything that stops a MediaRecorder in this codebase
   should assume the same. The band now runs where it can fail: `npm run oracle:load` (HEAVY, two
   phases). THE RAW HALF IS ALSO FIXED (P0-tail-raw) AND ITS LESSON IS THE OPPOSITE ONE: you cannot
   drain a raw channel by ending its track, because Chrome stops a MediaRecorder whose stream has
   gone inactive and takes the backlog with it. Starve the source instead — 1 fps, same frame size —
   and drain. Do not write a new tail claim without running `npm run exp -- o4step2` and
   `npm run exp -- p0tailraw`.
6. THERE ARE TWO LIVE-COMPOSITE ENGINES, AND v2 IS THE DEFAULT (flipped 2026-08-24). v2 = worker +
   WebCodecs + WebGL2 + fMP4 to its own SyncAccessHandle. v1 (MediaRecorder) is the LADDER under
   it, all three rungs real: capability (no TrackProcessor/AudioEncoder → v1 from the start, which
   is all of Apple WebKit and Firefox), start failure (v2 throws → v1 takes the take), mid-take
   watchdog (sustained <12 fps after first output → composite refused, take renders from raw
   channels). `?engine=v1` / localStorage force the old engine. HISTORY THAT WILL SAVE YOU A WEEK:
   three sessions of "WebCodecs delivers 2-10 fps" were the INSTRUMENT — a fresh throwaway
   profile's FIRST VideoEncoder pays a multi-second init (PER LAUNCH, so production prewarms at
   mount: prearm.ts → encoderWarm.ts), every rig take fit inside it, and the old watchdog killed
   the take mid-init. Warm, v2 beats v1 on every measured axis (28.4 vs 29.3 fps; main thread 0 vs
   198 ms; CPU peak 127 vs 196 %; oracle sync 33-48 vs ~60 ms; drains its own encoder). o4step2
   warms before measuring ({"cold":true} measures the transient on purpose); the watchdog clock
   starts at first output (capture/compositorWatchdog.ts). Anything touching capture/* keeps BOTH
   engines green: `npm run oracle` (v2 by default) AND `node scripts/oracle.mjs --engine=v1`.
   AND THE WARM IS ONE FUNCTION NOW, `experimental/rigWarm.ts` (R1, 2026-08-29). It had been pasted
   into two rigs while ELEVEN others that call createCaptureSession were still cold, each able to
   reinvent the mirage independently — X15(c) did, and read 200 of 283 raw frames DROPPED on the
   newly-default WebCodecs path, which looks exactly like a throughput regression and is not one.
   Every rig that opens a session calls it first. It does NOT go inside createCaptureSession: that
   is production capture, prearm already covers every real user, and moving it needs Robert.
7. THE EDIT IS PERSISTED NOW (F4, editsRepo, IndexedDB v2). Before that a refresh silently threw away
   every trim and cut while faithfully restoring the recording. If you add a field to EditState, it is
   automatically persisted — make sure it survives a round trip through structured clone.
8. THE COMPOSITE'S GEOMETRY LIVES IN THREE PLACES and they must agree, because an unedited export
   packet-copies the composite: compose/layout.ts (render), capture/compositor.worker.ts (v2 live) and
   liveComposite.ts (v1 live). timeline/cameraTrack.ts owns the PiP rect for the first two; a unit test
   pins the default pose against the editor's historical CSS numbers.
9. ANYTHING THAT CHANGES PIXELS IS AN EDIT, AND MUST SAY SO. isDefaultEdit is what decides whether the
   export may packet-copy the composite, so every new visual feature has to make it false: F4's camera
   track, F3's background frame and F2's viewport all do. The other half of the same rule is that an
   INACTIVE style must be dropped rather than stored (clampEditState deletes a background that paints
   and insets nothing), or an untouched take silently loses the instant path.
10. THE RIG IS WRONG BEFORE THE PRODUCT IS — three times in one day, and twice more since. A 450 px
   zoom "error" was an expectation that had not been clipped to the canvas. A 9851 ms "tail gap" was a
   measurement lane started at stop instead of at the beginning. A 16.2 fps "config catastrophe" was
   the first row of a matrix paying for cold start. A cold-oracle sync failure was a stall longer than
   the rig's own ruler (GATE-alias). And the nastiest one, 2026-08-26: THE O1 RIG WAS COMPARING A PATH
   WITH ITSELF. It flips `setExportScratchEnabled` on the main thread, and the render moved into a
   worker at O5a — a worker has its own module instance, so the flag reached nothing and the "buffer"
   lane was measuring the scratch path. It read GREEN the whole time. Before reporting an alarming
   number, check the instrument; and before reporting a REASSURING one, check that the lever you
   pulled is attached to the thing you pulled it for.
   THREE MORE, 2026-08-26 (X15), AND THEY ARE ALL ONE FAULT: A REFERENCE THAT MOVES. O9's headline
   compared decoded frames at fixed instants against a canvas readback taken at an unrelated moment,
   on a fixture that SCROLLS — "capture destroys 51 % of glyph edge contrast" was a scroll position,
   and shifting the reference one step reproduces the published numbers on demand. X15's own lane (c)
   then made the identical mistake and read 13.1 dB with the identical fingerprint. And X15's lane
   (b) told the exporter a channel was 6.0 s when its file was 6.6, then measured the render placing
   the picture 0.967 s from where the channel's timestamps predicted — a rig's own arithmetic,
   debugged as if it were the product's.
   THE THREE RULES THAT FALL OUT, each of which would have caught it alone:
     · A FIXTURE THAT CHANGES OVER TIME NEEDS A REFERENCE THAT IS A FUNCTION OF THE SAME TIME.
       Deterministic-by-frame-index (perf/textSource.ts) makes the reference free: frame i is frame
       i forever. Where the question is about PIXELS and not timing, hold the fixture STILL instead.
     · DUMP THE FRAMES. `{"thumbs":true}` settled in one look what three rounds of reasoning about
       offsets did not — the two files were the same page one line apart. A number says two things
       differ; only a picture says how.
     · AN ALIGNMENT SEARCH ON STILL CONTENT IS ILL-CONDITIONED and will saturate at its own span
       edge while looking like a measurement. Localise on a region that MOVES, or do not localise.
   A TENTH INSTANCE, FOUND BY REVIEW RATHER THAN BY A WRONG NUMBER (R1, 2026-08-29), and that is the
   new part: the chroma rig was measuring correctly and could have stopped at any moment WITHOUT
   SAYING SO. An out-of-bounds mask counted the overrun as HITS (undefined fails every tolerance
   test, then sums to NaN); an empty mask reported "0 % kept", which is a fabricated total colour
   loss; a stage that failed to decode vanished on a bare `continue` and both chroma gates read the
   absence as their own answer; one rejected `at()` threw the entire report away. None of it had
   fired yet. THE RULE: A MEASUREMENT THAT DID NOT HAPPEN MUST BE REPRESENTABLE, and it must not be
   representable as a score — MISSING, SKIPPED and MASK EMPTY are values, `null` is not a number,
   and every gate has to decide what they mean before it decides what a number means. The way to
   prove that is to POINT THE RIG AT ITS OWN BLIND SPOTS AND SHIP THE DRILLS: x15c now takes
   `{"drill":"dead-composite-blob"|"dead-instant-export"|"palette-drift"}`, one command each.
   And its palette lives in ONE place (capture/synthetic.ts) because it lived in five, and a mask
   that has drifted from the painter measures nothing while looking like a P1.
11. THE ONE-BRANCH-PER-TASK RULE COLLIDES WITH THE AUTO-COMMIT HOOK. A Stop hook runs
   `git add -A && git commit && git push origin HEAD:main` after every turn, so work is on main
   before a session can open its own branch, and a branch left checked out at the end would be pushed
   TO main. Sessions that cannot merge inside one turn should work on main and carry the reasoning in
   the commit message (see the F3 record commit for the shape).

15. CAPTURE MEASURES THE MIX ONCE AND EVERY CONSUMER SHOULD ASK IT (X1, 2026-08-26). The take's
   100 ms window envelope — RMS and per-window peak, in TIME ORDER, on the recording timeline — is on
   `Recording.loudness.envelope`. A percentile is a statistic of a multiset, so an EDIT is a
   SELECTION of those windows and needs no second decode: `loudnessFromCaptureEnvelope` serves the
   render and smart cut, and analyzeSilence builds an output-grid array from it for F5a. Before
   adding a fourth consumer, read the fences — they are the whole safety argument, and each one
   marks a case where the stored windows describe a signal the file will not carry (a per-channel
   trim, a sped span, a channel set that is not what capture summed, the p20 codec floor). And the
   two ways this family of statistics has already gone wrong are both DROPPED FIELDS, not wrong
   maths: `peakRobust` was computed in two places and copied to neither consumer for a day and a
   half, which silently kept the audio regression Robert reported. If you add a statistic here, follow
   it all the way to the thing that reads it.

13. THE EXPORT'S FLOOR IS DECODE, NOT SCHEDULING — measured, and it retires a whole family of ideas.
   O5 assumed the render loop was badly overlapped. It is not: mediabunny's encoder wrapper already
   keeps 4 frames in the VideoEncoder queue, and the expensive part of "decode" is JS on the same
   thread, so it cannot hide behind the draw either. 12 s 1080p edited take: decode-wait 1295 ms,
   draw 450 ms, encode-wait 28 ms — against 962 ms to decode the same frames with no draw and no
   encode at all. An encode lookahead window AND a decoder prefetch pump were both built, measured,
   and deleted for buying nothing. Do not propose "pipeline the export" again without first running
   `npm run exp -- o5` and beating the decode floor it prints. The way past the floor is to decode
   FEWER frames, which is smart cut.
   Two traps in the same area, both paid for already: (a) the FIRST lane of any export matrix pays the
   codec cold start — warm with a discarded export and run both orders, or you will report the second
   engine as faster whichever one it is; (b) compose/scratch.ts deletes the previous finished export
   when a new one finishes, so an earlier ExportResult.blob goes dead and reads as a bare
   `TypeError: network error`. A rig comparing several files must copy each one into memory first.

14. EVERY SYNC NUMBER THIS PROJECT QUOTED BEFORE 2026-08-24 WAS THE RENDER'S, and the render is the
   path a user gets LAST. The oracle exported by calling exportRecording directly, so the instant
   packet copy (what an unedited take takes) and smart cut were invisible to it. They are not any
   more: the rig records a composite, the trim goes through compose/choose.ts — the product's own
   ladder — and every oracle line now prints `inst=` (instant-path sync), `trim=<path>/<sync>` and
   `compT0=` (where the composite's clock starts). The first honest run found the instant path
   outside the band. If you change anything in compose/*, read those three fields, not just `sync=`.
   THE AUDIO-QUALITY HALF OF THE SAME HOLE IS CLOSED NOW TOO (BACKLOG P0, 2026-08-26), and closing
   it REFUTED the premise it was written on. The backlog expected the instant lane to read ~3.1 dB
   down because the live mix applies a shared 0.7 bus and a 12:1 compressor. IT DOES NOT, because
   NO EXPORT PATH CARRIES THE COMPOSITE'S AUDIO: exportInstant copies the composite's VIDEO packets
   and mixes the audio from the RAW channels through the same certified mixer the render uses —
   deliberately, and instant.ts has said so in its opening comment since it was written ("without
   the composite's uncertified MediaRecorder audio, which was the pervasive-noise cause"). Measured
   on one take: raw channel 0.01 dB · the composite's own audio −2.00 dB · the instant export
   −6.07 dB · the render of the same take −6.07 dB, IDENTICAL. So the instant path's audio quality
   IS the render's, the 1/N headroom rule is the whole of its level cost, and the 0.7 bus lives in
   a track every export discards. If you are hunting audio quality, the composite's audio is the
   WRONG file to look at — `npm run oracle:fidelity` prints it as a diagnostic lane precisely so
   nobody re-derives that the hard way.

16. A LEVEL GATE MUST NOT MEASURE A WINDOW ITS SOURCES ARE NOT IN, AND METADATA CANNOT TELL IT
   WHERE THEY ARE (2026-08-26; note 10's sixth instance, and it took THREE tries to actually fix).
   A tone absent for fraction f of the measured window reads (1−f) low across EVERY tone at once —
   the exact shape of a mix-bus regression, which is what makes it dangerous. The fidelity oracle
   read phantom attenuation four different ways before the window was right: 0.5 dB twice, a
   30.2 dB "tone error" with 26.7 dB "separation" that FAILED A GREEN BUILD, and 11.9/16.1 dB once
   the first two fixes were in. Each fix was a smaller wrong idea than the last:
     (a) a FIXED 0.25 s skip — clears encoder delay on a take whose audio runs from t≈0, clears
         nothing on a multi-source take whose channels start at their own offsets (258-624 ms);
     (b) skip past the last channel's `startOffsetMs` — better, and still blind, because a channel
         can be RECORDING SILENCE: the generator's AudioContext stalls before it renders (rig.ts
         says 115-500 ms; observed 2.15 s here), and no metadata anywhere records that;
     (c) FIND THE SIGNAL — `findProgrammeOnsetSec` opens the window once every channel the fixture
         expects is above the floor, plus a ramp. Per CHANNEL, because on a multi-source take the
         two sources stall independently and an onset taken on the mix fires on whichever arrives
         first (that was the 11.9/16.1 dB).
   And then the cheaper half nobody reached for first: `waitForAudioClock` holds until the
   generator is genuinely rendering BEFORE the take starts, so the silence is not recorded at all —
   the same move production makes by warming its encoder at mount. Onsets went from 2.15 s to
   0.12-0.34 s and four consecutive runs read residual 0.01-0.03 dB on both lanes. The seeker stays
   as the net. Every fidelity report now prints `win=<start>s/onset=<found>s`: a level number is
   only as good as its window, so the window is evidence, not an implementation detail.

17. A GATE THAT CANNOT FAIL IS NOT A GATE, AND BOTH KINDS OF IT READ GREEN (2026-08-26, paid twice
   in one session). Note 10 is about the rig being wrong; this is its quieter sibling — the rig is
   fine and the QUESTION is unanswerable as posed.
     (a) THE WRONG QUANTITY. X9's gate was "long-task count during a For-AI export ~0". It read 0
         in the worker AND 0 in-thread, so it would have passed before the task was done. The build
         awaits a decode every sample, and an await ends the task, so its work arrives as hundreds
         of 5-15 ms tasks that never trip the 50 ms rule while owning the thread end to end. THE O5
         RIG HAD ALREADY HIT THIS IN AUGUST AND WRITTEN THE LESSON INSIDE ITSELF, where the next rig
         did not read it. The instrument is a shared file now — `perf/mainThreadWatch.ts`,
         SchedulingDelayWatch (how late a 16 ms ticker runs = what a user feels) with LongTaskWatch
         kept beside it as a diagnostic that is never a gate.
     (b) THE WRONG THRESHOLD. The replacement gate then demanded the in-thread lane lose >100 ms of
         ABSOLUTE wall clock before the comparison counted, and read FAIL on a run showing a clean
         fivefold effect — because this fixture's build is one second long. A threshold that
         encodes the fixture is a gate on the fixture. It is a SHARE now (≥3 % of the build's own
         wall), which is the quantity that survives a change of take length.
   BEFORE WRITING A BAND, ASK WHAT NUMBER WOULD FAIL IT — and if the failing case is one this
   codebase cannot produce, the band is decoration. X12 paid the same tax in its proposal RULE: the
   first version asked only "is it still the same picture?" and duly recommended a bitrate that made
   the delivered file 31.5 % bigger. It is a pure tested function now (perf/screenRung.ts).
     (c) AND THE OPPOSITE FAULT, same day: O8b's first delivered-fps band was 20, which failed BOTH
         phases of oracle:load immediately — a band nothing can pass is as useless as one nothing can
         fail, and it was gating the FIXTURE (an uncapped synthetic 4K source) rather than the
         product. It is 10 now, chosen as a principle (below a third of the target the footage is not
         usable) rather than fitted to a run.
     (d) F8's own gate said "60 fps with a 30-min take". A still timeline repaints nothing and would
         have scored 60 fps with any amount of work in it, so the trace DRAGS the playhead. The
         measurable form of "with a 30-min take" turned out to be FLATNESS across lengths, because
         the design's claim is that cost does not depend on length at all — and that is testable in
         two minutes where a 30-minute fixture is not.
     (e) THE BORN-RED GATE, and it is (c)'s exact twin (R1, 2026-08-29). X15(c) shipped
         "the shipped chain keeps the colour of coloured text: instant >= 90 %" against a chain that
         measures 70.3 % and whose best PROPOSED future — O3b, already on the map — is 80 %. It could
         not pass, in this world or in the intended one, and it had been quoted as a live FAIL. It is
         a REGRESSION gate now (>= the committed baseline − the rig's own stated run variance), with
         the absolute loss stated in the detail where it belongs: the loss is a FINDING for Robert to
         spend CPU on, not an alarm to re-fire every run. THE TEST BEFORE WRITING A BAND IS NOW TWO
         QUESTIONS: what number would fail it, AND what number would pass it — and if the passing
         case is one the roadmap does not contain, the gate is measuring a wish.
         ITS SIBLING IN THE SAME REVIEW went the other way: "the fast path is not the worst-coloured
         file" compared GREEN alone at a 1 pt margin, tighter than the rig's own ~2 pt run variance,
         and green is the one colour the render loses. Min over the coloured rows at 2 pts, and it
         correctly FAILS — a gate whose scope was chosen, however unconsciously, to make it pass.

12. THE UI RIG SERVES dist/, AND DOES NOT BUILD IT. `scripts/ui-shot.mjs` runs `vite preview`, so a
   flow that "does not work" may simply be yesterday's bundle. Run `npm run build` first. The same
   file is also where a synthetic drag goes wrong: `startDrag` listens on the ELEMENT and leans on
   pointer capture, which a synthetic pointer id never gets, so pointermove has to be dispatched on
   the element rather than the window.

── TOOLING (built 2026-08-23; all spawn their own ephemeral server + throwaway Chrome profile) ──
  npm run exp -- <id> [json] [--rss] [--query=k=v] [--ua=...]   generic experiment driver
     o5        THE EXPORT ENGINE: worker vs main-thread render on one fixture, warmed and run in BOTH
               orders, with the per-stage split (decode / draw / encode-wait / finalize) and a
               decode-ONLY floor for the same frames. The floor is the number that matters — it is
               what says whether any scheduling change can pay at all. ALSO PRINTS X4'S DECODE SITE
               per channel since 2026-08-26 — in-thread · in a worker ALONE · in a worker WHILE the
               render runs — which is the three-number refutation of "shard the decode across
               threads". `{"camera":true}` gives it a two-video-channel fixture.
     o5cut     SMART CUT vs the full render on a real take with a trim-only edit: wall clock, how much
               of the video was copied rather than re-encoded, and PSNR of decoded frames at matching
               output instants — plus a CONTROL comparing the shipped INSTANT path with the same
               render, which is what proves the one-frame offset belongs to the composite and not to
               smart cut.
     o1        export memory / parity / orphan sweep, PLUS X2's writer lanes: the export A/B (which
               proves only that the encoder is not bit-reproducible) and `inlineWriter.equivalence`,
               which is the real gate — one scripted positioned-write sequence with a back-patch
               through BOTH writers from inside a dedicated worker, hashes compared. Its scratch and
               inline levers are FORWARDED INTO THE EXPORT WORKER; flipping them on the main thread
               does nothing, which is what made its buffer lane meaningless from O5a to 2026-08-26.
     o2        capture-loudness vs probe, PLUS X1's `edited` lane: a trimmed-and-cut take's makeup
               from the kept windows against the probe's, and the render's own `probeDecodes` count
               with the envelope and with it stripped (must be 0 and >0)
     o4step1   sync residual decomposition (KEPT under X3: sync ≤20 ms is still a live objective)
     audiolat  measured-audio anchor latency (loopback)      armcancel arming cancel + device release
     f1        mid-take cuts: sync + joint discontinuity     f7       quality tiers + certification
     f4        camera pose LOCATED in decoded export frames vs the pose function
     o4step2   v1 vs v2 live composite: delivered fps, main-thread long tasks, tail gap, bits audit
               (warms v2 first since 2026-08-24 — every earlier v2 number was a cold-start artifact;
               {"cold":true} measures the transient on purpose)
     o11       bits audit: GOP ladder + quality steps + camera-PiP bitrate, all demuxed from the file
     o4step2   also takes {"engines":["v1"]} and reports the tail band + a raw-channel lane
     f2        timed zoom LOCATED in decoded export frames, via the PiP as a fiducial
     f3        background frame LOCATED in decoded export frames vs the pure geometry
     encprobe  is WebCodecs the wall, or is our config? main thread vs worker vs GL, per config
     x6        X6: a raw channel on WebCodecs against the same channel on MediaRecorder, both
               through the REAL createCaptureSession. Carries O3a's own gate (truncate a copy at
               50 % of the bytes and decode it), the tail band, the drop counter, and a
               phase-aligned quality row. `--cpu` for the whole-browser comparison; the encoder is
               WARMED first (note 6) or the WebCodecs lane silently drops half its frames.
     x5        X5: the render's 2D composite against the capture GL painter on the SAME decoded
               frames — interleaved, scored on each painter's best, GL timed with gl.finish() so the
               GPU work is inside the number, and the two outputs PSNR'd per region. The parity row
               is the one that decided X5.
     x12       X12: what CAPTURE writes and what each part of it buys. Weighs every artifact of a
               production-shaped take ON DISK (raw screen + raw camera + mic + the live composite
               through the shipped ladder) against the take's own length, then prices the raw-screen
               rung by recording 8/6/4/2.5 Mbps off ONE stream and RENDERING each. Measurement only.
     aiexport  AI1: the export for AI on real takes — a wandering cursor, a blinking caret, a
               tooltip, a still minute with a motion burst — then READS THE FILE BACK (JPEGs scanned
               out of the PDF, the rig's fiducial timecode decoded off the picture) to prove each page
               shows the frame its caption claims and that no page comes from a cut span
     aiexport  ALSO CARRIES X9's THREAD LANES: the same take built worker/in-thread/in-thread/worker,
               compared by SHA-256 and by how much wall clock a 16 ms UI ticker lost during each
     f5a       silence tightening against a known map: recall, cuts-in-speech, join clicks — PLUS
               X1's `x1` lane, which attaches the envelope capture would have stored and runs the
               PRODUCTION analyzeSilence a second time: the two proposals must be the SAME cuts
     f5b       per-segment speed: pitch of a known tone IN THE EXPORT, sync across the boundaries,
               output length vs arithmetic, cost per second of output
     f7c       delta cost by DISTANCE FROM THE KEYFRAME, both content types (F7c attempt 4)
     p0tailraw does a RAW channel keep its ending? four stop procedures under 4K load, plus a
               `production` run through the real createCaptureSession and a `wedged` forced case
     o4clock   WHICH CLOCK DOES A CAPTURED FRAME CARRY? arrival − VideoFrame.timestamp per source:
               its SPREAD is the delivery jitter a media-clock stamp would remove, its SLOPE is the
               rate error that stamp would introduce over a 900 s take. Both answers are in the
               O4-polish handoff and both say no; run it before proposing that idea again
     f8        <video> seek vs the export's random-access reader at off-grid instants — the rig that
               refuted F8's premise (same frame, 0 ms apart, and the element is faster)
     syncload  DOES THE TAKE STAY IN SYNC WHEN THE MACHINE IS BUSY? The regime no gate covered:
               records the composite under a saturating CPU + 4K-paint load and measures each
               TRACK'S SPAN against the take's own wall clock, plus an independent AudioContext
               clock probe that names which clock lost the time. `{"load":false}` is the control
               and the number to read is the DIFFERENCE — a loaded cell alone proves nothing.
               Found the audio-timeline drift Robert reported (audio 2.6-3.8 s short per 60 s) and
               proved the fix (padding → +395 ms, the idle control's own figure).
               CARRIES A `measured` LANE since 2026-08-26 — the RAW measured-audio path beside the
               composite, the lane whose absence let a DEAD pad ship as verified (see BACKLOG).
               HEADLESS IS BROKEN for this rig family on Chrome 151 (canvas capture delivers 0 fps
               to uncomposited windows, composite degrades, degraded stop deletes its blob): run it
               in a VISIBLE unoccluded window and read `measured` + `clock` + audioPaddedSec.
     tabaudio  does CAPTURED TAB AUDIO survive the tab going silent and sounding again? (Robert:
               "maybe audio dies when one youtube video ends and other starts"). Real
               getDisplayMedia via Chrome's auto-accept testing flags — no picker; tones play in
               THIS tab, or in a captured child tab with {"crossTab":true} plus `--refocus
               --capture-title=TONECHILD` (the child steals focus, and getDisplayMedia throws from
               an unfocused document — the driver re-activates the page). {"occlude":true} runs
               the OCCLUSION cell (continuous tone, captured window fully covered mid-run) — pair
               with `--headed --real-throttling`, which strips the anti-throttling flags so the
               rig runs what a USER's Chrome runs. `--keep-audio` or the run is vacuous.
               2026-08-26: NOT reproduced in any of the three shapes; negatives + the field
               autopsy that reframed the bug (the display PAIR dies together — likely a PLAYER
               stall, see BACKLOG) are recorded so no shape is re-run blind. cdp-run's debug port
               is EPHEMERAL now — fixed 9333 cost three runs in one day to orphaned Chromes — and
               it prints the page console tail on failure.
  THE REMOTE TAKE AUTOPSY (2026-08-26, proven on Robert's dying take): a browser session on the app
     origin shares its storage, so the take IS readable in place — IndexedDB `inout`/`recordings`
     for the metadata, OPFS `blobs/<blobKey>` for the channels, `decodeAudioData` for envelopes,
     mediabunny (esm.sh) `VideoSampleSink.getSample` for frames of the UNSEEKABLE live-recorded
     webm (a <video> element cannot seek it). Per-channel silence maps, frame diffs and frame
     screenshots of a real field take, with nothing needed from Robert except NOT DISCARDING it.
     oracle    accepts {"injectTailLossMs":N} to prove the tail band red
  node scripts/ai-pdf-check.mjs    AI1's "any AI can read this" gate: runs the aiexport rig, saves the
     PDF to docs/qa/ai-export.pdf, and puts it through TWO readers that know nothing about the writer —
     our independent xref-walking parser (src/experimental/tools/pdf-verify.mjs) and Apple PDFKit via
     JXA. `--reuse=<report.json>` re-runs only the reader gates. No downloads: pdftotext/mutool/qpdf
     are absent on this machine and PDFKit ships with macOS.
  node scripts/o7-measure.mjs      first-paint bytes, record-click latency, editor prefetch (prod build)
  sh scripts/mirror-measure.sh <npm script> [args]   RUN A LONG RIG FROM A /tmp MIRROR OF THE WORKING
     TREE, including uncommitted changes. Every rig here spawns a vite server on THIS worktree and
     vite watches it, so editing a file mid-run reloads the harness page and the run dies quietly —
     the node process then sits at 0 % CPU until its timeout with the log stuck on "ephemeral server
     on …". That cost this session 25 minutes; it is now one command.
  node scripts/ui-shot.mjs --flow=quality|cuts|camera|frame|tighten|zoom|speed|zoomdrag|scrub|film|roughsize
     film      F8: does the timeline show the take's own frames? reports per lane whether the bar
               carries a strip, the lane heights, and the per-channel decode budget off the console
     roughsize F7d: switches the video inputs OFF before the take so the size probe fails for a REAL
               reason, then reads what the panel claims about numbers it could not measure
     screenshots a flow on the PROD BUILD. It serves dist/ and does NOT rebuild — run `npm run build`
     first or you will screenshot yesterday's bundle
     (camera also drags the PiP, reloads the page, and reports preview↔export fractions + reload drift)
  node scripts/browser-check.mjs --list | --browser=<name> [--ua-of=yandex]   QA matrix, real browser,
     prod build, record→edit→export, 8 gates → docs/qa/*.json (see docs/QA_MATRIX.md)
  node scripts/ru-reachability.mjs --label="<ISP>, no VPN"   DNS/TLS/HTTP from the network under test
  node scripts/pwa-check.mjs       manifest/SW/offline/capture-under-worker gates
  node scripts/make-icons.mjs      regenerates PWA icons (deterministic, no image dep)
  npm run oracle:load   the O8 tail band UNDER 4K LOAD, in TWO phases (P0-tail and P0-tail-raw): the
     composite through the o4step2 rig, and the RAW channels through the real createCaptureSession.
     HEAVY: announce it, keep it out of the pre-push hook, never run it while Robert is using the
     machine. The composite phase may read INCONCLUSIVE when its watchdog gives up under load — that
     is the fallback working; the raw phase does not depend on the composite surviving.
  npm run oracle | oracle:cold | oracle:fidelity  MERGE gates — run them per task, by hand. The
     pre-push hook does NOT run the oracle and must not: it is a timing gate, and the cold first run
     a hook always pays read 76.87 ms of its 90 ms band where a warm run read 37.3 (2026-08-26).
     The hook runs build (tsc+vite) + tests, ~7 s.
     `node scripts/oracle.mjs --engine=v2` gates the WebCodecs composite instead of the shipped one
  oracle:fidelity GATES TWO FILES SINCE 2026-08-26 (BACKLOG P0): the single-source RENDER it always
     gated, AND the INSTANT packet copy — the default export, whose audio quality nothing had ever
     measured. The second take is production-shaped (screen + TWO audio sources) and goes through
     compose/choose.ts, so the lane reports WHICH path ran and fails if the packet copy quietly
     declined. Its level gate is the RESIDUAL past the mix bus the code is designed to apply
     (1/N — −6.02 dB at N=2), band ±1 dB, because gating the raw tone error would gate a documented
     design choice that is Robert's to change. Three diagnostics print unGATED and are the decomposition
     that answers "which stage costs what" on ONE take: the raw channel (capture alone), the
     composite's OWN audio (the 0.7 bus + 12:1 chain — which no export carries, see note 14), and
     the render of the same take. `--no-composite` is the red proof: the instant lane must FAIL
     LOUD when it cannot run rather than let the render pass for both.

── AWAITING ROBERT (carried forward) ──
  · THE REWORKED AI FILE (AI1, second pass 2026-08-24): Robert's first real take exposed both defects
    (the file did not say what it was; it dropped the moments) and both are fixed. Re-test: export a
    take For AI and give it to an agent for the job Robert actually has — recreating a UI and its
    animations. The rebuilt version of Robert's own take is at
    ~/Downloads/inout-20260824-183853-for-ai-v2.pdf for a side-by-side with the original file and
    the MP4. That report is AI1's acceptance and the only thing that unfreezes AI2-AI7.
  · FIRST REAL TAKE ON THE NEW DEFAULT ENGINE (v2, flipped 2026-08-24): record something normal and
    listen/watch. Sync should be audibly better (33-48 ms vs ~60). If anything is off, `?engine=v1`
    reverts this load and localStorage['inout.capture.engine']='v1' reverts sticky.
  · camera-only take on a REAL camera should now record 1080p, not upscaled 720p (O3a; synthetic mode
    bypasses getUserMedia so it cannot be verified headlessly)
  · real Safari + iOS Safari recheck of the MediaRecorder-audio path (long-standing)
  · 4K game-tab recheck of the capture ceiling — PARTIAL 2026-08-25: video records (no frozen
    frame reported; 300 MB/5 min = the 8 Mbps ceiling, expected), but the take exposed progressive
    audio desync + noises under that load (NEW BACKLOG P1, loaded-regime sync never measured) and
    a game-first wedge ordering where the refresh ritual also failed to surface (case file +
    BACKLOG P1). Still unseen: the take's `[capture] display capped …` / `delivering N fps` lines.

═══ X — OPTIMIZATION FRONTIER (audit 2026-08-25; standing bar: theoretical practical limit,
no accepted-engine-limitation answers; Robert ordered the map 2026-08-25) ═══
Source: a data-flow trace of render.ts / layout.ts / audio.ts / loudnessAccumulator.ts /
compositor.worker.ts / blobStore.ts / scratch.ts — not the docs — plus a platform re-baseline:
WebGPU default in ALL FOUR engines since 2025-11 (Firefox stable still lacks importExternalTexture;
Linux still gated) · MediaStreamTrackProcessor in Safari 18+ (SPEC version: worker-only, VIDEO-only;
Chrome's 2021 main-thread+audio version is proprietary — two code paths, feature-detect in a worker)
· Safari 26 ships AudioEncoder/AudioData (before 26 Safari had NO audio encoding — the ~1 s
truncation that routed Apple to MediaRecorder was measured against that Safari) · Firefox has no AAC
encoder anywhere, nor does any browser on desktop Linux.
AT-LIMIT, measured, do not re-open without new evidence: instant packet copy (a copy cannot be
beaten) · scratch muxing (flat 4 MB at any length) · GL capture composite (0.86 ms/frame) ·
frame-driven capture drawing · per-chunk write+flush (batching measured no better) · encoder queue
depth (lookahead built, measured, deleted — note 13) · first paint 233.9 KB · the 4K capture cap.

## X1 keep-loudness-envelope [light · ~200 lines: loudnessAccumulator.ts, types.ts, audio.ts,
   analyzeSilence.ts, tests · touches the RENDER path only of the three]
loudnessAccumulator.finish() SORTS windowRms (destroying time order) and returns 3 scalars; the
envelope itself — 10 floats/s, 72 KB per 30 min — is discarded. Three consumers then re-decode all
audio to rebuild what was thrown away: render.ts's probe pass (openAudioMixers a second time —
every EDITED export, because O2's shortcut gates on isDefaultEdit), compose/analyzeSilence.ts
(F5a), and measureRecordingMakeup (every editor open). Keep the envelope in time order on
Recording.loudness; edited exports re-percentile the windows inside kept segments instead of
decoding. The p20-floor null-return guard (loudnessFromCaptureStats) generalizes unchanged — the
codec-floor argument is about the statistic, not how many are kept.
GATES: an edited export whose envelope covers its channels runs ZERO probe decodes (count them);
makeup agrees with the probe within the O2 tolerances on the rig (p90 ≤0.4 dB, peak ≤0.03 dB, floor
term → probe, same as today); F5a proposal identical on the known-silence rig; oracle + fidelity
green; takes predating the field keep today's exact behaviour.

HANDOFF X1 — DONE 2026-08-26 (TWELFTH SESSION, Robert: "go x1 2 3"). THE ENVELOPE IS KEPT, AND KEEPING IT
FOUND TWO LIVE DEFECTS THAT MATTERED MORE THAN THE OPTIMIZATION.
SHIPPED: `CaptureLoudness.envelope` — windowRms + windowPeak IN TIME ORDER, the window length, and the
recording-timeline ms of window 0. finish() already sorted those series and threw the order away; it
now copies before it sorts (the same shape compose/audio's envelope pass has always used). 121 KB per
30 minutes, measured on the rig, both arrays together.
THREE CONSUMERS, ALL THREE OFF THE DECODE:
  · THE RENDER'S EDITED EXPORT (`loudnessFromCaptureEnvelope`, compose/audio.ts). A percentile is a
    statistic of a MULTISET, so the windows lying inside the kept spans ARE the answer and their order
    never mattered. RenderStats now counts `probeDecodes` — the render's own count of channels opened
    purely to measure. On a trimmed-and-cut take: 0 with the envelope, 2 without. p90 agrees with the
    probe to 0.267 dB, and THE APPLIED MAKEUP GAIN IS IDENTICAL (makeupDiffDb 0.000).
  · SMART CUT, which is the one that most wanted it and was not even on the task's list: it copies its
    video packets and finishes in under a second, and then paid a full audio decode to measure. Its
    comment said "a cut changes the mix, so the capture-time shortcut does not describe it" — true of
    the SCALARS, false of the envelope. Same function, same fences.
  · F5a TIGHTEN (compose/analyzeSilence.ts), and this one is gated on IDENTITY, not tolerance, because
    its output is cuts a user sees. `npm run exp -- f5a`, production analyzeSilence run twice on one
    take, once with the envelope attached and once without: THE SAME TWO CUTS, maxBoundaryDelta 0 ms,
    0 ms inside speech, threshold identical (0.042), loud/floor within 0.03/0.04 dB — in 1 ms instead
    of 70. The probe costs ~147 ms per minute of audio, so Tighten on a 30-minute take goes from ~4.4 s
    to ~1 ms. NARROWER FENCES than the makeup shortcut, on purpose: the analyser indexes its windows by
    OUTPUT position, so ONE kept span only (a take already cut re-decodes, and is shorter by then), the
    array is zero-padded wherever the output has no audio because that is exactly the digital silence
    the probe measures there, and the sub-window remainder between the two grids is carried as an exact
    offset — nothing rounds a cut boundary.
FENCES (all return null → probe, all unit-tested): no envelope (a take predating X1 behaves exactly as
today) · a channel set that is not what capture summed · a PER-CHANNEL trim, which removes one
contributor part-way through a span the stored windows still hold it in · a SPED span (F5b retimes the
material, so the multiset reweights) · fewer than 30 whole windows · and the p20 codec-floor guard,
which generalizes unchanged and is now one shared function both shortcuts call.
TWO LIVE DEFECTS FOUND ON THE WAY, both the same shape — `peakRobust` computed and then DROPPED:
  (1) session.ts built `recording.loudness` field by field and never copied it, so EVERY take on disk
      read `peakRobust: undefined` and took NORMALIZE_PEAK_OVERDRIVE_RAW = 4;
  (2) `measureMixLoudness` destructured three of measureMixEnvelope's four fields, so the PROBE path
      did the same. Found by the rig lane printing `probe.peakRobust: 0`.
So the 2026-08-25 fix for Robert's "audio quality regressed" — the one whose whole point was to return the
licence to 2 — was in force NOWHERE. Both fixed, one line each, and the probe passthrough is pinned by
a test (one full-scale spike over sustained 0.2 programme: peak > 0.9, peakRobust < 0.25). Robert's listen
test is still owed and is now owed on a build where the fix actually runs.
GATE NOT MET AS WRITTEN, and the number says why: the task asked for peak within 0.03 dB. Measured
−0.083 dB on the edited lane — while THE SHIPPED O2 DEFAULT-EDIT SHORTCUT measured −0.463 dB on the
same run, so 0.03 dB is not a tolerance any path in this codebase meets. `peak` also no longer decides
anything: the makeup bounds on peakRobust. What the gate is really asking — do the two agree about the
gain — reads 0.000 dB.

## X2 scratch-writes-inline [light · ~100 lines net-negative: scratch.ts, export.worker.ts wiring;
   blobStore's worker stays for MAIN-thread callers (the in-thread render fallback, v1 capture)]
createDurablePositionedWriter pays, per chunk: a full buffer.slice COPY, a transfer to a SECOND
worker, and an awaited round trip. The export render already lives in a dedicated worker and
FileSystemSyncAccessHandle is [Exposed=DedicatedWorker] — own the handle inline, exactly as
compositor.worker.ts:572 already does. Same fix applies to the AI-PDF writer once X9 lands.
GATES: A/B export byte-identical; O8 peakOut band unchanged (`npm run exp -- o1`); worker-death
fallback still lands on BufferTarget; oracle green.

HANDOFF X2 — DONE 2026-08-26 (TWELFTH SESSION). SHIPPED: `createPositionedWriter` grew a THIRD rung
above the other two — own the FileSystemSyncAccessHandle inline when the caller is already in a
dedicated worker (`canOwnSyncHandle()`), fall back to the durable writer worker, then to
createWritable. The export render is in a worker by default (O5a), so its scratch writes stop paying,
per 4 MB chunk, a full `buffer.slice()` copy, a structured-clone transfer and an awaited round trip
through a SECOND worker's event loop — and an export stops spawning that worker at all.
"BYTE-IDENTICAL A/B EXPORT" IS NOT A TEST THIS CODEBASE CAN RUN, and finding that out is part of the
result: two exports of the SAME take through the SAME writer come out the same LENGTH and a different
SHA-256 (measured: 926,230 bytes both ways, hashes 361cc54f… vs 246bc021…). The video encoder is not
bit-reproducible run to run — which X4's own gate already half-admits ("byte-identical where encoder
determinism allows"). So equivalence is proven on WHAT X2 CHANGED instead, from inside a dedicated
worker where the inline rung actually exists (src/experimental/perf/writerEquivalence.worker.ts): one
scripted sequence of positioned writes — 4 appends, a BACK-PATCH over byte 0 (what an mp4 finalize
does), a straddling rewrite mid-file, an append past the end — driven through both writers.
  13,635,584 bytes out of both · SHA-256 3f4bb8438c5ee9b0 BOTH · inline 100 ms vs worker 105 ms
The rig refuses to pass vacuously: it reports `inlineAvailable` from the same predicate the ladder
branches on, so a worker-vs-worker comparison would say so instead of reading green.
WALL CLOCK, HONESTLY: on the 6 s export the rig exports (0.9 MB written) the two are indistinguishable
— 1047/1044 ms inline against 1045/1039 ms worker, both orders warmed. The win is the copy and the hop,
which only show where the bytes are: 5 % of writer time on 13.6 MB. A 30-minute export writes 1744 MB.
O8 peakOut band unchanged: targetHeld 0.9 MB inline, 0.9 MB worker.
AN INSTRUMENT DEFECT FIXED TO GET HERE, and it had been silently wrong since O5a: the O1 rig flips
`setExportScratchEnabled` ON THE MAIN THREAD, and the render has not been on the main thread since the
export moved into a worker — which has its OWN module instance. So O1's "buffer" lane was measuring the
scratch path, and its parity check was comparing scratch against scratch. pipeline.ts now FORWARDS both
levers into the worker's start message. Same class as note 10, one layer down: the rig was wrong before
the product was, and it had been reporting a green comparison of a thing with itself.
STILL OPEN, logged P2 in BACKLOG rather than fixed here: O1's MEMORY lane samples heap on the main
thread while the export renders in the worker, and `measureUserAgentSpecificMemory` is unavailable in
the rig's Chrome (totalSamples 0, totalPeakDeltaMB null) — so its headline gate metric is absent and its
heap numbers are the wrong thread's. `targetHeldMB`, which comes from ScratchStats measured inside the
worker, is the number O1's claim actually rests on and it is sound.

## X3 experiments-merge-or-delete [light-medium · deletes ~2,300 lines now, triages ~10,000 ·
   touches no product path]
ROBERT RULING 2026-08-25: no standing experimental tree — "all the shit must get in worked or be
deleted". A module is (a) tooling a live gate/task runs → it stays and is owned, or (b) spent — its
question answered, verdict in DECISIONS → deleted on sight (git is the archive; the doc rule
already says so). Immediate (b): session-log/, timemap/, wcap/ (became liveCompositeV2),
scene/, datachan/, semantic/, streamx/, shared/. Then triage the ~30 perf rigs: a rig a READY task
names (o5, o11, f8, o4step2, armcancel, aiexport…) is (a); mp4Capture (O3a verdict in), workerBisect
(cold-start verdict in), sizeProbe (F7c shipped attempt 5) and peers are (b). Oracle is (a) by
definition — it is the merge gate. Prune the harness registry and exp.mjs ids to match.
GATES: typecheck + full test suite + oracle + oracle --engine=v1 + fidelity green AFTER the
deletions; browser-check 8/8; no scripts/*.mjs references a deleted module; READY-map tooling index
updated to only name survivors.

HANDOFF X3 — DONE 2026-08-26 (TWELFTH SESSION). DELETED, 2,745 lines: session-log/ · timemap/ · wcap/ ·
scene/ · datachan/ · semantic/ · streamx/ · perf/mp4Capture.ts (O3a: MediaRecorder MP4, REJECTED on
evidence and in the REJECTED list) · perf/workerBisect.ts (the cold-start verdict is in note 6) · and
RESEARCH.md, verdict.md, sync-diagnosis.md, which existed only to describe that tree. Seven runners
left the harness registry with them. The verdicts stay in .ai/DECISIONS; the code stays in git; what
they proved is in production — wcap BECAME the v2 live composite and streamx BECAME O1's
stream-to-disk export, which is the ruling working rather than an exception to it.
TWO CORRECTIONS TO THE SPEC, both from looking rather than from the list:
  · `shared/` IS NOT DORMANT. It was on the delete list; oracle/rig.ts and recovery/salvage.ts both
    import `shared/opfs`. Restored. A live gate's dependency is gate tooling.
  · `sizeProbe` HAS NOTHING TO DELETE. The spec calls it spent because F7c shipped — and shipping is
    exactly what happened to it: it is `src/core/compose/sizeProbe.ts` now, production, and the
    experimental spike is gone. "Merged or deleted" already resolved that one.
KEPT, AND THE RULE THAT DECIDED IT: a rig stays if a READY task's GATES name it, or a CONFLICT RULE or
a "what a fresh session must know" note requires running it, or a scripts/*.mjs gate imports it. That
keeps the whole oracle (it IS the merge gate), recovery/ (the only end-to-end check of crash salvage,
plus a unit test), tools/pdf-verify.mjs (scripts/ai-pdf-check.mjs), and the perf rigs those notes name.
o4step1 and audiolat were the closest call — their questions are answered — and they STAY, because
sync ≤20 ms is a live objective (note 1, X14 parked on browser deliverables) and they are the only
instruments that decompose the residual. Deleting them would mean rebuilding them the day it moves.
COST OF THE SWEEP: 28 unit tests went with the deleted pure modules (438 → 410). No product line
changed; no scripts/*.mjs referenced anything deleted; first paint unmoved (nothing here was in it).

## X4 parallel-decode-workers [medium · ~500 new lines: decode worker + range split in
   compose/video.ts + merge · RENDER path only]
The render's floor is DECODE ON ONE THREAD: 1295 ms of a ~1900 ms 12 s render, against 962 ms for
decode alone (note 13). Note 13 retired single-thread scheduling — prefetch pump and lookahead were
built, measured, deleted. It says NOTHING about N threads: the expensive part is JS, so shard the
JS. Step 1: one decoder worker per CHANNEL (separate files, zero GOP logic, trivially correct).
Step 2: GOP-range sharding within a channel — GOPs are independent at OUR OWN 5 s cadence
(KEYFRAME_INTERVAL_SEC, O11b) and the F8 verdict proved the random-access reader lands frame-exact
off-grid. Ceiling is core count, not 6×.
GATES: output byte-identical to the single-thread render where encoder determinism allows, else
per-frame PSNR ≥ the smart-cut bound (37.5 dB) with cut boundaries exact; export ≥2× today's 5.2×
realtime on a 60 s edited take, measured both orders warmed (note 10); peak memory band unchanged;
oracle + fidelity green; worker unavailable → today's loop untouched (frozen rule).

HANDOFF X4 — BUILT, MEASURED, DELETED 2026-08-26 (THIRTEENTH SESSION). THE GATE IS NOT MET AND THE
REASON IS MEASURED, WHICH ALSO RETIRES STEP 2.
WHAT WAS BUILT (step 1, in full): one decoder worker per video channel, each handed the WHOLE
schedule of source instants up front — `channelSourceTimeAt` is a pure function of the recording and
the edit, so every instant the render will ask for is known before the loop starts — each pushing
decoded frames ahead of the render inside a credit window, with `frame` / `repeat` / `none` messages
reproducing VideoChannelReader's exact semantics. It WORKED: byte-identical output in all nine lanes
of every matrix run (14,225,372 B on the 60 s two-channel fixture; 10,665,772 on the single-channel).
IT BOUGHT 0.99-1.08×, AGAINST A GATE ASKING FOR ≥2×. Four runs, two fixtures, A/B/B/A warmed:
  60 s screen+camera edited   farm 2.77×  ·  in-thread 2.80×   → 0.99×
  60 s screen only            farm 5.67×  ·  in-thread 5.25×   → 1.08× (run-to-run spread on the
                              SAME config was 9.0-12.2 s, so this is inside the noise)
  12 s screen+camera edited   farm 3.80-4.13× · in-thread 3.87-3.92× → 1.04-1.05×
WHY, decomposed, and each piece rules out a fix:
  · THE SITE IS FREE. The same schedule decoded in a worker with nothing else running costs
    0.93-1.04× what it costs in-thread (screen 4668 ms vs 4909; camera 3266 vs 3206). A worker is
    not a slower place to decode.
  · THE HANDOFF IS FREE. Transferring the pictures cost 57-117 ms of an 18-26 s render — 0.4 %.
  · THE READ-AHEAD WINDOW IS NOT THE CONSTRAINT. Swept 6/16/32: at 32 the workers stall on credit
    for 0 ms — they run their whole schedule flat out — and the render's decode WAIT does not
    improve (1075 ms vs 1135 at depth 6). Deepening it further only holds more decoded 1080p frames.
  · WHAT IS LEFT IS CONTENTION, AND IT IS LARGE. The screen worker's own decode goes from 4668 ms
    alone to 17,272-23,142 ms while the render is encoding beside it — 3.7-5×. The render's
    encode-wait rises at the same time (3641-5942 ms with the farm against 2585-3653 in-thread).
    Both sides get slower: they are competing for something a second JS thread does not create.
    On an 8-core M3 with three busy threads, that is not core starvation — the likeliest remaining
    mechanism is the browser's own media pipeline (software VP9 decode and the export's encoder),
    and either way the remedy is not more threads.
SO STEP 2 IS RETIRED WITH STEP 1, and harder: GOP-range sharding within a channel adds MORE
concurrent decoders against exactly the resource that is already the constraint.
DELETED, per the X3 ruling and note 13's own precedent (the encode lookahead and the decoder
prefetch pump were built, measured and deleted too): `compose/decodeFarm.ts` and
`compose/decode.worker.ts` are gone and render.ts / pipeline.ts / export.worker.ts are byte-for-byte
what they were. The shipped render is untouched — this cost the product nothing and left nothing
behind.
WHAT SURVIVES, so the verdict is re-runnable rather than remembered: `npm run exp -- o5` now prints
an X4 DECODE SITE line per channel — in-thread · in a worker ALONE · in a worker WHILE the render
runs — which is the refutation in three numbers (src/experimental/perf/decodeSite.ts +
decodeSite.worker.ts). `{"camera":true}` gives it the two-video-channel fixture X4 needed.
DO NOT RE-PROPOSE "shard the decode across threads" without running it and beating what it prints.
IF ANYTHING REOPENS THIS: X6. It replaces software VP9 on the raw channels with hardware AVC, which
changes what the decode is competing FOR. Re-run the decode-site line then, not before.

## X5 gpu-export-compositor [medium-heavy · ~600 lines: compositorGL grows background/pose/viewport,
   render.ts swaps painters · RENDER path only; UNIFIES two of the three geometry places]
The render composites through a 2D OffscreenCanvas — the EXACT mistake capture measured (~150 ms
per 1080p frame readback, 6.7 fps) and fixed with compositorGL (0.86 ms), while the render never
got the fix: every frame pays GPU→CPU readback + CPU composite + CPU→GPU upload for the encoder.
And the two painters are REQUIRED pixel-identical anyway (instant packet-copies one, the render
produces the other — conflict rule 8). Port the render draw onto the GL painter: extend the SDF
shader with F3 backgrounds (gradient as data), F4 poses, F2 viewport transform; 2D stays as the
no-WebGL2 fallback. Pairs with O6: once the render is GL, importExternalTexture is the 4K probe.
GATES: pixel parity against layout.ts on a golden-frame set covering default/F3/F4/F2 (per-channel
max delta ≤1 LSB or PSNR ≥60 dB); drawMs measured before/after; all three export paths named
(render changed, instant and smart cut untouched); oracle + fidelity green; waveform mode untouched.

HANDOFF X5 — REFUSED ON ITS OWN PARITY GATE 2026-08-26 (THIRTEENTH SESSION), before the shader was
written. The premise check is `npm run exp -- x5` (src/experimental/perf/glComposite.ts) and it is
three numbers.
(1) THE PREMISE'S REASON IS WRONG, AND ITS CONCLUSION SURVIVES ANYWAY. The task says the render
    pays "the EXACT readback capture measured (~150 ms/frame)". It does not: capture's frames come
    from MediaStreamTrackProcessor and live in GPU memory, so drawImage drags them back — the
    RENDER's frames come from a SOFTWARE VP9 decoder and are already in CPU memory. There is no
    readback to remove, which is why the render's draw reads 2-4 ms/frame and not 150. GL is
    nonetheless FASTER: 1.67 ms/frame against 2.86 (1.71×), INCLUDING the per-frame CPU→GPU upload
    the 2D path never pays. So the port would work — it just is not the fix the task described.
(2) AND IT IS WORTH LESS THAN IT LOOKS. Draw is 25-30 % of the render's wall clock (o5's stage
    split), so removing 41 % of it caps X5's whole benefit at ~10-13 % of export time.
(3) THE PARITY GATE FAILS ON THE EASIEST CASE, AND THAT IS THE VERDICT. Same decoded frames, same
    default composition — the one the GL painter ALREADY implements for capture, before a single
    line of F3/F4/F2 shader work exists:
        screen picture interior   PSNR 37.3 dB · max 156-159 of 255 · 3.8 % of pixels off by >8
        camera PiP interior       PSNR 47.0 dB · max 5-8       · 0 % of pixels off by >8
    against a gate of ≤1 LSB or ≥60 dB. Stable across three runs. The mean signed delta is ≈0 and
    the smooth camera region is clean, so this is NOT a colour shift and NOT geometry (the screen
    draws 1:1 at the origin — dw=1920, x=0 — so there is no resampling and no subpixel offset). It
    is sharp-edge colour handling: `texImage2D(VideoFrame)` and `drawImage(VideoFrame)` upsample
    4:2:0 chroma differently, and the fixture's content is coloured text on dark ground, which is
    exactly where that shows. PORTING THE RENDER ONTO THE GL PAINTER WOULD CHANGE HOW EXPORTED TEXT
    LOOKS — which needs Robert's yes, is not obviously an improvement, and collides head-on with O9
    ("text is important").
A REFEREE THAT IS NOT ONE, kept so nobody re-reaches for it: `VideoFrame.copyTo({format:'RGBA'})`
was tried as a neutral third party to say WHICH painter is wrong. Both painters sit ~20.7 dB from it
(max 244) while sitting 37 dB from each other — a referee further from both contestants than they
are from each other is applying its own range/matrix convention, not adjudicating.
TWO RIG ERRORS FIXED ON THE WAY, both note 10, and the first would have shipped the wrong answer:
  (a) the run held 240 frames of BOTH channels alive — over a gigabyte of decoded VideoFrames, which
      the render never does — and under that pressure the 2D lane degraded 3.8 → 7.7 → 14.3 ms/frame
      across three rounds while GL barely moved, which read as a 1.87× painter win. Small working
      set, repeated, now.
  (b) lanes ran A,B,B,A and were scored on the MEAN. The host drifts monotonically within a run
      (worst/best is 3.0× on one painter), so a mean is a measurement of when a lane happened to
      run. Interleaved and scored on each painter's BEST now.
AMENDED BY X15(c), 2026-08-26 — THE DIVERGENCE IS REAL IN PRODUCTION AND A QUARTER OF THE SIZE.
Measured on one real take through the product's own export ladder rather than on frames handed to
two painters in a lab: instant (packet copy of the GL composite) against render (2D painter) reads
37.1 dB on the screen text region, max 60, 7.8 % of pixels off by >8 — against a CONTROL of 39.9 dB,
max 50, 3.8 %, which is what ONE re-encode of that same frame costs on its own. So the painter
difference is 2.8 dB and a doubling of visibly-wrong pixels, not the "max 156 on 3.8 % of pixels"
this handoff measured. The lab number was inflated by exactly the gap this handoff named: software-
decoded frames standing in for GPU-resident capture frames.
WHAT WOULD REOPEN IT: a ruling from Robert that the text difference is acceptable or an improvement (it is a
picture change, so it is Robert's call, not an engineering one), or a way to make texImage2D and drawImage agree on
chroma — which is a browser behaviour, not ours. The ~12 % ceiling should be weighed against that
before anyone writes the F3-shadow shader, which is the genuinely hard part and was never reached.

## X6 raw-channels-webcodecs — BUILT AND SHIPPED OFF BY DEFAULT 2026-08-26 (handoff below)
   [what is left is not code: the default flip is Robert's, because the AVC and VP9 channels agree
   to only 27.9 dB on screen content and that is a picture change · CAPTURE side; additive per the
   frozen rule, MediaRecorder stays the ladder. The branch was REFUSED, not rebased — see the
   handoff — and was deleted 2026-08-29 once nothing needed it.]
Screen+camera raw channels still encode via SOFTWARE VP8/VP9 (the largest capture CPU cost, O3a)
while the composite uses hardware AVC. O3a's rejection reason — Chrome's MediaRecorder does not
stream MP4, a mid-take kill leaves 753 bytes — is SOLVED by the pattern v2 already ships: WebCodecs
→ fMP4 → own SyncAccessHandle, bytes on disk as produced. Keyframe control on raw channels also
opens smart-cut-over-raw later.
GATES: the O3a mid-take snapshot test — kill at 50 %, the prefix DECODES to within the tail band;
oracle BOTH engines + oracle:load both phases + armcancel + salvage smoke green; capture CPU
measured before/after (the two software encodes are the claim); real-device take AWAITS ROBERT.

HANDOFF X6 — BUILT AND SHIPPED OFF BY DEFAULT 2026-08-26 (THIRTEENTH SESSION). Every gate the task
wrote PASSES, including O3a's. One gate the task did NOT write does not, and it is why the default
does not move: the picture changes, and a picture change is Robert's.
THE BRANCH IS NOT THE STARTING POINT AND WAS NOT REBASED. `ee/webcodecs-capture-2` samples a paced
<video> element on the MAIN THREAD into an OffscreenCanvas, and its own header says why: "waiting on
VideoEncoder stalls the [track] processor". That stall is note 6's instrument fault — a fresh
process's first VideoEncoder pays a multi-second init, per launch — which O4 later solved by warming
at mount. Rebasing it would have carried the workaround for a problem that no longer exists. Built on
v2's shipped pattern instead.
SHIPPED (~700 lines): `capture/rawVideo.worker.ts` (one channel per worker: VideoEncoder AVC →
EncodedVideoPacketSource → fragmented MP4 → its OWN SyncAccessHandle, written AND FLUSHED per chunk),
`capture/measuredVideo.ts` (the main-thread handle, deliberately shaped exactly like
MeasuredAudioHandle so session.ts's existing `useMeasured` seam carries it and NO new stop path was
written), `capture/rawCodec.ts` (the preference, default `mediarecorder`). session.ts changes are
~40 lines and every one of them is at the seam.
IT NEEDS ITS OWN TRACK, and that is not incidental: a MediaStreamTrackProcessor CONSUMES a track's
frames and the v2 composite already has one on screen and camera. The channel clones the track — an
independent sink on the same source, which is exactly what MediaRecorder was as a second consumer.
GATES, all measured through the REAL createCaptureSession (`npm run exp -- x6`):
  · O3a's, THE ONE THAT REFUSED THIS IN THE FIRST PLACE: truncate a copy at 50 % of the bytes (what
    a tab kill leaves on disk) and decode it → 56-88 % of the take survives, against MediaRecorder
    MP4's 753 bytes. The objection is answered.
  · every raw channel decodes, 447 frames of 447 in, 0 dropped;
  · tail band: −344/−343 ms (the FILE runs past the declared length; nothing is lost);
  · both paths record the same take length (14880 vs 15044 ms);
  · oracle PASS on v2 (sync 58.2, inst 60.3, trim=smartcut/61.2, tail 79) AND v1 (45.2, 52.6,
    render/54.2, tail 67) · armcancel green (cancel 212 ms, normal take arms 443 ms, 4 channels,
    zero live tracks after stop) · 419 tests.
  · CPU, THE TASK'S CENTRAL CLAIM, whole-browser through the process sampler
    (`npm run exp -- x6 --cpu`), both lanes WARM and both at 448 frames of 448 in:
        MediaRecorder (software VP8/VP9)   peak 149 %   mean 80 %   over 51 samples
        WebCodecs      (hardware AVC)      peak  95 %   mean 47 %   over 48 samples
    −41 % mean, −36 % peak. The two software encodes were the claim and they were the cost.
    AN EARLIER READING OF THIS SAID 187→119 % peak and 100→66 % mean, and it was NOT a better
    result — it was the cold run, where the WebCodecs lane was 45 % lighter partly because it was
    encoding 45 % fewer frames. A CPU number taken while a path is dropping work is not a
    comparison. Both figures above are from lanes that dropped nothing.
NOTE 6 BIT FOR THE THIRD TIME, AND THIS TIME IT WAS A PRODUCTION GAP TOO. Measured cold, the raw
channels dropped 196/440 then 285/440 frames and read as a hard throughput ceiling. Two things were
wrong. The rig never warmed (fixed: it calls warmVideoEncoder() before any lane — warmed, drops go
to 0/447). And prearm.ts only warmed the encoder when the COMPOSITE was v2, so in production X6's
raw encoders would have been cold on every launch: it now warms when EITHER the composite is v2 or
a raw channel will use WebCodecs. Before the warm the drop counter did not exist either — a channel
silently dropping 45 % of its frames is indistinguishable from one recorded at a lower rate, so
measuredVideo.ts now logs `N frames encoded of M in` and warns when they differ.
THE GATE X6 DID NOT WRITE, AND THE REASON THE DEFAULT STAYS PUT. At the same requested ceiling and
with zero drops, the AVC channel writes 0.21× the VP9 channel's bytes on screen content (1.31 MB vs
6.25 MB over 15 s) and the two pictures agree to only 27.9 dB. The CAMERA channel is fine — 0.75×
bytes, 37.2 dB, inside the "same picture" band. So this is rate control, not loss: `latencyMode:
'realtime'` with no `bitrateMode` undershoots hard on this content. WORTH KNOWING BEFORE ANYONE
CALLS IT A REGRESSION: the shipped v2 COMPOSITE uses the same config shape and X12 measured it
spending 11.8 % of its own ceiling, and that composite is what the INSTANT export copies to users
today. So the undershoot is the product's existing accepted behaviour on the default path — but the
raw channels feed the EDITED path, so adopting it there changes a picture users already have. That
is Robert's call, which is exactly why the frozen rule's "defaults unchanged" is the right answer here.
NEXT MEASUREMENT, named so it is not guessed at: sweep `bitrateMode` ('constant' vs the default
'variable') and re-run the quality row. If constant closes the 27.9 dB gap at a bitrate the disk
still likes, X6 becomes a flip decision with numbers instead of an open question.
  → DONE 2026-08-26 (X15(a)), AND BOTH HALVES CAME BACK NEGATIVE — IN X6's FAVOUR.
    bitrateMode is INERT: constant / variable / unset produce BYTE-IDENTICAL files, on the hardware
    encoder AND on the software one. `isConfigSupported` accepts a value the encoder ignores, so
    support was never evidence of effect. Quantizer mode IS honoured (qp14/20/26 → 959/692/511 KB).
    AND THE 27.9 dB IS THIS RIG'S, NOT THE ENCODER'S. It compares two SEPARATE takes of a MOVING
    source and searches ±0.6 s for the best-matching phase; the header above already calls the
    result a LOWER BOUND. Today's re-run reads 28.0 dB at 0.22× with its own alignment lines showing
    the best match 100-200 ms off nominal, so the rig is stable — the METHOD is what differs.
    Measured on IDENTICAL pictures compared by ordinal, the shipped AVC config reads 47.8 dB against
    the VP9 lane on screen text at 0.52× its bytes (motion: 51.6 dB at 0.23×), glyph contrast 1.00
    either way. The byte saving reproduces; the quality gap does not exist.
    SO THE REASON THE DEFAULT STAYED PUT IS GONE. What remains is a picture-changing flip, which is
    Robert's, and one confirmation nobody has done: X15(a) feeds CANVAS frames while this path feeds
    GPU-resident frames from a MediaStreamTrackProcessor. Confirm through this rig before flipping.
TWO DEFECTS FOUND ON THE DEPLOYED BUILD AFTER THE RIG WAS GREEN, AND THE RIG COULD NOT HAVE FOUND
EITHER. Both are the same root cause: MediaStreamTrackProcessor is FRAME-DRIVEN, so a screen that is
not changing delivers nothing, and the rig's synthetic source is ANIMATED and never goes quiet —
447 frames of 447, perfect. A real static screen (a slide, a document, a paused UI, a backgrounded
tab) is the COMMON case.
  (1) A 9 s take recorded "2 frames encoded of 2 in" on both raw channels. MediaRecorder never had
      this problem because it encodes at its own cadence whatever the source does, and the live
      composite has carried a keep-alive since O4 for exactly this reason — the raw path shipped
      without one. In the same take the composite logged 7 frames of which 6 were keep-alive.
      FIX: keep a clone of the most recent picture and re-encode it when nothing has reached the
      encoder for a second. Two things came with it — the channel's timeline is now built from
      ARRIVAL stamps rather than source timestamps (startOffsetMs is already an arrival quantity, so
      the file's t=0 and its placement on the session timeline come from one clock, and a keep-alive
      frame has no source timestamp to use), and the worker min-filters its own clock offset out of
      the arrival stamps the frame messages already carry, because the timer fires in the worker
      while the main thread owns the clock. Both are the constructions compositor.worker.ts uses.
  (2) EVEN THEN the channel declared durationMs 0, and the editor opened the take as AUDIO-ONLY with
      a waveform. A channel's LENGTH is how long it was LIVE, not how long frames happened to
      arrive: with a static source exactly one frame arrives, so first === last. The stop message
      now carries the main thread's stop instant, the worker emits ONE last keep-alive at it, and
      the duration is that span — so the file also COVERS the length it declares. Tail gap went
      from −344 ms to 9 ms on the rig.
  VERIFIED ON THE DEPLOYED BUILD: "13 frames encoded of 1 in, 12 keep-alive (source was static)",
  against the composite's 14 frames / 11 keep-alive in the same take. The counter is on the console
  precisely because a channel silently recording 2 frames looks exactly like a short take.
ORACLE FLAKE, NOT OURS, AND CHECKED RATHER THAN ASSUMED: three post-change runs read FAIL/FAIL/PASS
with starvation signatures (spur −2.7 to −26 dB, compT0=0, skew 559). The same rig on 27f376b — the
commit BEFORE any X6 code — read FAIL/FAIL/PASS too, with healthy spur, on the same machine in the
same hour. The machine has been running heavy rigs for hours; the flake is environmental and
predates this change. A settled-machine oracle is owed before any flip, and is listed below.
SETTLED-MACHINE RE-RUN, after the fixes: oracle PASS (sync 49.7, inst=instant/64.4,
trim=smartcut/58.9, tail 69, spur −51.8, export 4.66x) · browser-check real Chrome PASS, zero
failures, export 511 ms. And the FIXES THEMSELVES verified on the deployed build: screen 9108 ms /
camera 9059 ms with offsets 849/898 against a 9957 ms take — the two add up, and the take opens as
video instead of as a waveform.
STILL OWED BEFORE ANY FLIP: `npm run oracle:load` both phases (HEAVY — announce it), a salvage smoke
over a killed take, and Robert's real-device take. None of them gate the code as shipped, because as
shipped the default path is untouched.

## X9 ai-pdf-off-main-thread [light · ~100 lines: core/ai/build.ts into a worker, same O5 shape]
The For-AI build runs on the UI thread at 0.74× a full render — seconds of jank. core/ai is already
DOM-free by construction; move the build into a worker, stream progress like export.worker does.
GATES: byte-identical PDF on the rig take; `node scripts/ai-pdf-check.mjs` green; long-task count
during a For-AI export ~0 on the prod build.

HANDOFF X9 — DONE 2026-08-26 (THIRTEENTH SESSION, Robert: "go roadmap all in one").
SHIPPED: `core/ai/run.ts` picks the thread and `core/ai/ai.worker.ts` runs the build, in the exact
shape compose/pipeline.ts has used since O5a — worker is the fast path, the SAME build.ts is the
in-thread fallback (no module workers, a worker that fails to construct, or one that dies before its
first progress report), and the fallback is not a second implementation that could drift. The
one-shot-fallback rule is O5a's and for O5a's reason: past the first real progress the worker has
already written PDF bytes into its own OPFS sink, so a retry would leave two files racing for one
result. `getLastAiExportStats()` is published across the boundary the way `setLastRenderStats` is,
or every AI1 gate would read null on the thread that asked.
BYTE-IDENTICAL, FOUR LANES, worker/in-thread/in-thread/worker: 706,432 B and SHA-256 ac13f1e9f0…
on all four. Nothing in the PDF is clock-derived, so this is a real identity and not a tolerance.
THE GATE AS WRITTEN — "long-task count ~0" — CANNOT FAIL, AND THEREFORE COULD NOT PASS. It read 0
in-thread and 0 in the worker. The build awaits a decode on every sample (8/s), and an await ends
the task, so its work arrives as hundreds of 5-15 ms tasks that never trip the 50 ms rule while
occupying the thread end to end. THE O5 RIG HAD ALREADY LEARNED THIS AND WRITTEN IT DOWN, and this
session reached for the long-task counter anyway — so the instrument is now a shared file
(`src/experimental/perf/mainThreadWatch.ts`) that both rigs import, with the lesson in its header
instead of inside one rig. Long tasks are still reported, unGATED, because they are free and because
they are the number that lies.
MEASURED ON THE REAL QUANTITY (scheduling lateness of a 16 ms ticker — what a user experiences as
jank): the UI thread lost 78 ms in-thread against 15 ms in the worker, worst single gap 10 ms → 2 ms,
7.6-8.3 % of the build's wall clock against 1.3-1.8 %. THE WORKER IS NOT FASTER (wall 980 vs 998 ms)
— it is elsewhere, which is the whole point. The share scales with take length: the build is 0.74× a
full render, so Robert's 97 s take pays this for ~10 s instead of ~1.
ONE GATE EXPRESSION WAS FIXED MID-RUN AND IT IS WORTH SAYING WHY: the vacuity guard demanded the
in-thread lane lose >100 ms of ABSOLUTE wall clock before the comparison counted, and read FAIL on a
run showing a clean fivefold effect purely because this fixture's build is one second long. It is a
SHARE now (≥3 % of the build's own wall), which is the quantity that does not depend on the fixture.

## X12 capture-bitrate-pricing [light · measurement only, zero product lines]
Capture writes ~18.5 Mbps across three artifacts (raw screen 8 + raw camera 2.5 + composite 8),
~2.3 GB/hour, to ship an 8 Mbps file. Both artifacts are needed GIVEN THE FEATURES (edit
flexibility; instant copy) — but the SPLIT has never been priced. O11a's BitsAudit is the
instrument; O11c already ran this exact play once (camera 4→2.5 Mbps, −29.3 % disk, +0.2 % file).
OUTPUT: numbers + a proposal appended here. Any actual bitrate change is its own Robert-gated task.

HANDOFF X12 — DONE 2026-08-26 (THIRTEENTH SESSION). THE TASK'S OWN PREMISE IS REFUTED, AND SO IS
THE LEVER IT WAS WRITTEN TO PRICE.
RIG: `npm run exp -- x12` (src/experimental/perf/captureBitrate.ts). A production-shaped take —
screen 1080p + camera 720p + a mic channel + the live composite through the SHIPPED engine ladder —
with every artifact WEIGHED ON DISK afterwards and divided by the take's own length.
(1) CAPTURE DOES NOT WRITE 18.5 Mbps. It writes 5.21 Mbps (2.35 GB/hour) on screen content and 7.63
    (3.44 GB/hour) on motion. 18.5 was the sum of the requested CEILINGS, and none of them is
    reached: raw screen uses 33.2 % of its 8 Mbps on screen content, THE COMPOSITE USES 11.8 %, the
    camera 60.5 %, audio 84.4 %. The "2.3 GB/hour" figure in the task happens to be right for screen
    content and is right for the wrong reason. Write amplification against the delivered file is
    2.1× (screen) and 1.48× (motion) — not the order-of-magnitude waste the framing implied.
    Per artifact, screen content: raw screen 2.653 Mbps · raw camera 1.512 · composite 0.941 ·
    audio 0.108. Motion: 4.537 · 1.530 · 1.438 · 0.128.
(2) THE RAW-SCREEN RUNG IS NOT A LEVER — IT IS BACKWARDS ON THE CONTENT Robert RECORDS. Recorded at
    8/6/4/2.5 Mbps off ONE stream and each RENDERED through the production exporter:
      screen content   6 Mbps: channel −6.8 %, DELIVERED FILE +15.7 %, PSNR 45.5 dB
                       4 Mbps: channel −15.1 %, FILE +25.8 %, PSNR 44.6
                     2.5 Mbps: channel −32.5 %, FILE +31.5 %, PSNR 43.7
      motion content   6 Mbps: channel −20.3 %, file −9.3 %, PSNR 49.1
                     2.5 Mbps: channel −63.4 %, file −23.3 %, PSNR 48.5
    A lower-bitrate VP9 source does not hand the render a smaller job, it hands it a NOISIER one,
    and quantization noise is expensive to re-encode. On screen content every rung costs disk AND
    costs the file a user downloads. PROPOSAL: keep 8 Mbps on the raw screen channel. Not "defer" —
    the measurement says the change is negative on the content this product is for.
(3) THE RULE THAT SAYS SO IS PURE AND TESTED (src/experimental/perf/screenRung.ts, 4 tests), because
    THE FIRST VERSION OF IT RECOMMENDED THE 2.5 Mbps RUNG. It asked only "is the render still the
    same picture (PSNR ≥40 dB)?" — 43.7 dB, so yes — and never looked at the file getting 31.5 %
    bigger. A rung now has to clear three bars: same picture, the delivered file does not grow, and
    the saving is worth touching capture for (≥10 %). The tests are the MEASURED rows, both contents.
(4) NOT PRICED HERE, DELIBERATELY: the composite rung. It is the file the instant path copies
    verbatim, so its bitrate IS the delivered quality of an unedited take — lowering it is an export
    quality change, not a capture saving, and belongs to F7 and to Robert. Worth noting anyway that it
    only spends 11.8 % of its ceiling on screen content, so the ceiling is not what costs the disk.
WHAT IS LEFT UNPRICED: the camera rung was weighed but not swept (O11c swept it already, 4 → 2.5),
and no rung was measured at 4K or under load. Neither changes the verdict on the screen rung.

## X15 text-truth — where text quality is lost and what buys it back [medium · MEASUREMENT ONLY —
   changes no default, ships numbers; authored 2026-08-26 on Robert "prepare other session to
   measure shit with text"]
Context the session must load first: O9 (capture destroys 51 % of glyph edge contrast, the export
preserves it) · X6's handoff (AVC raw channel writes 0.21× the VP9 bytes at the same ceiling and
agrees to only 27.9 dB on screen text — rate control undershoot, `latencyMode:'realtime'` with no
`bitrateMode`; the named next measurement is the bitrateMode sweep) · X5 (texImage2D vs drawImage
4:2:0 chroma upsampling diverge on text; the two composite painters disagree 37.3 dB / 3.8 % of
pixels on screen-TEXT interior) · BACKLOG P1 "the two composite painters" (the in-app experiment is
already written down there). Rigs that exist: `npm run exp -- x6` (extend with a config matrix),
`npm run exp -- x5`, `npm run exp -- o9` if present — check the tooling index.
THREE LANES, all numbers, no behaviour change:
(a) X6 bitrateMode SWEEP: the WebCodecs raw lane across encoder configs — bitrateMode
    constant/variable (and quantizer mode if the probe says it exists), latencyMode realtime vs
    quality, on SCREEN-TEXT content, against the VP9 lane on the same stream. Deliverable: either a
    config where the AVC channel matches the VP9 picture (PSNR + glyph-edge contrast) at ≤~1.2× its
    bytes, or the measured frontier showing no such config exists. This is the number Robert's X6
    ruling waits on.
(b) THE 4:4:4 PRICE TAG: same take through a SOFTWARE encoder at 4:4:4 chroma (probe what this
    machine offers — hardware 4:4:4 is unlikely and the probe result is itself a finding; AV1
    availability too). Measure glyph-edge contrast retained vs the O9 baseline AND whole-browser
    CPU (`--cpu`) vs both current lanes. This prices the "crisp text" capture mode Robert asked about.
(c) X5'S IN-APP QUESTION (BACKLOG P1): ONE real take → export unedited (instant path, copies the GL
    composite) AND with a one-frame trim (render path, 2D painter) → PSNR over a TEXT region at
    matching instants. Settles whether adding a trim changes how text looks in production, not in
    the lab. If it diverges, it is a live defect on the default export — report, do not fix in this
    session.
GATES: every lane re-runnable in one command · both content types where applicable (text + motion
control) · no default moves, no production behaviour change · handoff appended here with the
numbers · BACKLOG P1 (painters) updated: resolved or confirmed live · if (a) finds a matching
config, say so in the READY map so Robert's X6 ruling is a yes/no on closed evidence · Robert-VISIBLE
ARTIFACTS (Robert: "i need to see it"): per lane, side-by-side exported files or magnified text crops
saved to ~/Downloads with a one-line README naming which is which — the ruling is made by eye.
Operating notes: rigs only — production code untouched except where a rig needs a read-only probe
seam · heavy sweeps announced, not while Robert uses the machine · a lane that cannot run (no 4:4:4
encoder at all) reports that as its result, per the operating rules.

HANDOFF X15 — MEASURED 2026-08-26 (FOURTEENTH SESSION). ALL THREE LANES RAN. NO DEFAULT MOVED, no
production behaviour changed. THE HEADLINE IS THAT TWO OF THE THREE NUMBERS THIS TASK WAS BUILT ON
TURNED OUT TO BE THE INSTRUMENT, and the third is real but four times smaller than it looked.
RIGS: `npm run exp -- x15a` (bitrateMode sweep) · `x15b` (4:4:4 price + O9 baseline) · `x15c` (the
trim question). `{"crops":true}` on any of them writes Robert artifacts. New files:
`perf/textSource.ts` (a text source DETERMINISTIC IN FRAME INDEX + encode/decode/compare plumbing),
`perf/bitrateModeSweep.ts`, `perf/chromaPrice.ts`, `perf/trimTextParity.ts`. One harness-only seam in
production code: `setSyntheticScreenContent()` in capture/synthetic.ts, the same shape and the same
"production never calls it" as the existing `setSyntheticScreenSize()`; default unchanged.
Robert ARTIFACTS, per the task's own condition: ~/Downloads/x15-text-truth/ — 15 magnified glyph crops
(4x, nearest-neighbour) + a README naming which is which and what to look for. The rulings below are
checkable by eye.

(a) THE bitrateMode SWEEP: THE KNOB DOES NOTHING, AND THE GAP IT WAS MEANT TO CLOSE IS NOT THERE.
    bitrateMode IS INERT on this machine, and the software arm proves it is the API and not the GPU:
        hardware AVC   default / constant / variable → 1027900 B, 1027900 B, 1027900 B
        SOFTWARE AVC   default / constant / variable → 1141867 B, 1141867 B, 1141867 B
    Byte-identical, three ways, both arms. `isConfigSupported` returns true for a value the encoder
    then ignores — so support was never evidence of effect. The ONE rate-control knob that IS
    honoured is quantizer mode: qp14 / qp20 / qp26 → 958673 / 692392 / 510847 B.
    AND X6's QUALITY GAP DOES NOT REPRODUCE. Screen TEXT, 180 identical pictures through both
    encoders at the same 8 Mbps ceiling, compared by frame ORDINAL (no search, no phase):
        shipped AVC   1.37 Mbps · 0.52x the VP9 bytes · 47.8 dB against VP9 · glyph contrast 1.00
        realtime-qp14 1.28 Mbps · 0.48x            · 49.4 dB              · glyph contrast 1.00
        VP9 (the shipped MediaRecorder raw lane)  2.42 Mbps · 38.0 dB against the canvas
    MOTION control: shipped AVC 0.23x the bytes at 51.6 dB. So X15(a)'s deliverable is answered YES,
    by the config that already ships: the AVC channel matches the VP9 picture at HALF its bytes, not
    at the 1.2x ceiling the task allowed.
    THE RECONCILIATION WITH X6, because two numbers that far apart need one. `npm run exp -- x6`
    re-run the same day still reads screen 28.0 dB at 0.22x — its numbers are stable. The difference
    is method: that rig records TWO SEPARATE TAKES of a MOVING source and searches +-0.6 s for the
    best matching phase, and its own notes call the result a LOWER BOUND ("at least this close").
    Its alignment lines from today's run say how much phase is left: the best match sat 100-200 ms
    off the nominal instant, and the search steps by a whole frame, so up to half a frame of motion
    is still inside every reading. The BYTE ratio reproduces (0.22 there, 0.23 here on motion
    content — and X6's fixture is the gradient-and-moving-bar synthetic screen, which is motion, not
    text). The dB does not reproduce anywhere.
    WHAT THIS MEANS FOR X6's RULING: the objection that stopped the flip — "the smaller file is a
    worse picture" — is not supported. The saving is real and the picture is not worse. It is still
    Robert's call, and one gap is named rather than papered over: this sweep feeds CANVAS frames while
    the production raw channel feeds GPU-resident frames from a MediaStreamTrackProcessor. The
    config transfers; the frame source does not. Confirm through `npm run exp -- x6` before a flip.

(b) THE 4:4:4 PRICE TAG — REAL, VISIBLE, SOFTWARE-ONLY. Probed across ALL THREE acceleration modes
    (O9 stopped at the first mode that said yes, so "software" and "hardware" were one row):
        AVC 4:4:4 (avc1.f40028)    NO, in every mode
        VP9 profile 1 (4:4:4)      NO, in every mode
        AV1 main / AV1 profile 1   yes — SOFTWARE only, never prefer-hardware
    On identical text frames: AV1 4:4:4 takes glyph chroma fringe 9.15 -> 3.03 at 5705 B/frame
    against the shipped AVC's 5142, and the control says why nothing else can: THREE TIMES THE
    BITRATE on 4:2:0 moves the fringe 9.15 -> 9.06. It is chroma resolution, not bits. Visible in
    b-01-*.png on the blue and green lines.
    WHAT IT COSTS TO RUN, which O9 never priced — whole-browser, one encoder per run
    (`npm run exp -- x15b '{"only":"<id>"}' --cpu`), 3x180 frames at 1080p:
        AVC 4:2:0  hardware   207 fps   CPU peak 111 %  mean 52 %
        VP9 4:2:0  software   152 fps   CPU peak 278 %  mean 87 %
        AV1 4:4:4  software    80 fps   CPU peak 245 %  mean 75 %
    80 fps is 2.7x a 30 fps capture on an IDLE machine with one encoder running. A real take runs
    two raw channels and a composite, so this is a headroom question and not a free win.
    AND THE BASELINE IT WAS TO BE PRICED AGAINST WAS WRONG — this is the note-10 finding of the
    session. O9's headline ("capture destroys 51 % of glyph edge contrast, the export preserves it")
    compares decoded frames at t=1.0/3.5/6.0 s against ONE canvas readback taken after the export
    finished, and bitsAudit's screenLikeSource scrolls a line every 2.5 s and never repeats. The
    rig's own comment claims the frame at t and at t+2.5k are the same picture; `scroll =
    floor(t/2.5)` is monotonic, not periodic. Measured both ways in one run:
        aligned by ordinal (the picture that was encoded)   fringe  8.87 · smear  0.80 · contrast 1.00
        the same, reference shifted ONE SCROLL STEP         fringe 25.62 · smear 66.28 · contrast 0.51
        O9 PUBLISHED                                        fringe 23.2  · smear 68.6  · contrast 0.49
    The deliberate misalignment lands on O9's published numbers. Chromium's VP9 does NOT destroy half
    the glyph contrast at capture — it keeps essentially all of it. The tell was in O9's own table:
    its EXPORT stage (file against file, genuinely aligned) reads smear 2.4 while its CAPTURE stage
    reads 68.6, and a mean luma error of 68 of 255 on every glyph edge is not an encoder, it is a
    different page. Re-measured correctly, the whole chain is: capture 8.87 fringe, export 3.02,
    total 10.81, contrast 1.00 throughout.
    SO O9's RECOMMENDATION SURVIVES AND ITS REASONING DOES NOT. "Flip the export to AV1 4:4:4" is
    still the cheap real win (fringe 9.15 -> 3.03). But "the export is the small half, capture is the
    big half, so the lever is X6" is refuted: capture is not the big half, there is no big half, and
    X6-as-built (AVC 4:2:0) was never going to help text either way.

(c) THE TRIM QUESTION — CONFIRMED LIVE, AND FOUR TIMES SMALLER THAN THE LAB SAID. One real take
    (still code-editor screen + camera PiP, v2 composite), three exports through the product's own
    compose/choose.ts, screen region, worst of four instants:
        instant <-> smart cut   99 dB · max 0                          BIT-IDENTICAL
        instant <-> render      37.1 dB (mean 37.7) · max 60 · 7.8 % of pixels off by >8 · fringe 4.03
        CONTROL: one re-encode of the instant frame alone  39.9 dB · max 50 · 3.8 % · fringe 3.00
    THE CONTROL IS WHY THIS IS NOT A 37 dB SCARE. compose/smartCutFlag.ts already records ~37.5 dB as
    "the ceiling for two independent encodes of one frame", so a bare 37.1 dB proves nothing at all.
    Against a re-encode of the very same picture, the render costs 2.8 dB more and doubles the share
    of visibly-wrong pixels. That is X5's divergence, surviving into production, at a quarter of the
    lab's severity (X5: max 156 on 3.8 % of pixels, fed software-decoded frames).
    AND THE BACKLOG ENTRY ASKED FOR THE WRONG FILE. It says a trim takes the render path. It does
    not: smart cut has been the default since 2026-08-25 and copies the composite's packets, so a
    trimmed take is BIT-IDENTICAL to the untrimmed one. The render's number is what a user sees only
    when smart cut declines. BACKLOG P1 rewritten: confirmed live, dropped to P2, Robert's to rule on.
    FOUND ON THE WAY, NOT CHASED (new BACKLOG P2): the render places the CAMERA PiP about half a
    second from where the instant path places it — the moving PiP best-matches at -14/-15 frames and
    even there reads 23.6 dB, while the STILL screen region of the same two files matches at 37.1.
    The two paths agree about pixels and disagree about WHEN. The oracle bands audio-vs-video and
    would not see a video-vs-video placement difference between export paths.

(d) FOLLOW-ON, ON ROBERT'S "c shit is worse colors" (2026-08-26, Robert looked at the artifacts): HE IS
    RIGHT, IT IS THE BIGGEST EFFECT IN THIS TASK, AND NO PAIR ROW IN X15 COULD HAVE SEEN IT.
    Lanes (a) and (c) compare files with each OTHER, so a loss every path shares — which is exactly
    what 4:2:0 chroma subsampling is — cancels to zero. Measured instead against the canvas the
    synthetic screen actually painted (the painter is deterministic and now exported for the rig),
    masked BY THE SOURCE's own palette, saturation kept per glyph colour, warmed take, two runs
    agreeing within ~2 points:
        stage                         grey    green    blue
        0-source                      100 %   100 %    100 %
        1-raw screen channel         79.7 %  80.0 %   89.3 %     one 4:2:0 generation
        2-composite                  71.6 %  70.3 %   75.2 %     a SECOND chroma generation
        3-export instant             71.6 %  70.3 %   75.2 %     the composite, copied verbatim
        3-export smart cut           71.6 %  70.3 %   75.2 %     identical again
        3-export render              78.4 %  67.3 %   78.3 %     better on blue/grey, worse on green
    SO THE COLOUR DIES AT CAPTURE AND THE EXPORT ADDS NOTHING. A raw channel costs one chroma
    generation; the composite costs a second (capture frames arrive already 4:2:0, the GL painter
    upsamples them to RGB, and the AVC encode re-subsamples), and instant is that file byte for byte.
    THE LEAD THAT DID NOT SURVIVE, recorded because it was reported before it was measured: from a
    single crop it looked as though the RENDER kept more colour than instant, i.e. a free win. It
    does not — better on blue (78.3 vs 75.2) and grey, WORSE on green (67.3 vs 70.3). It is a wash
    and it is content-dependent. There is no free colour in choosing a slower export path.
    WHERE THE FREE COLOUR ACTUALLY IS: O3b, which is already READY. A single-screen take that skips
    the composite and packet-copies the raw channel delivers 80 % of the source's green instead of
    70.3 % — about a third of the total loss recovered, for LESS work, at no CPU cost. It does not
    help a take with a camera PiP, which must composite; that case still needs 4:4:4 at capture, and
    on this machine that is software (lane (b): 80 fps at 1080p, ~2x CPU).
    AND THE CEILING IS NOT OURS — THE SOURCE IS ALREADY 4:2:0 (2026-08-27, on Robert's "how to fix
    it"). measuredVideo.ts now logs the pixel format of the first frame of every raw channel, and a
    take reports `format NV12 · coded 1920x1080`. NV12 IS 4:2:0: the chroma is at half resolution in
    both axes BEFORE our encoder is handed the frame. That re-reads the whole table above:
        source canvas RGB → NV12, done by Chrome's capture pipeline   ≈20 % of the colour, THEIRS
        NV12 → the raw channel's AVC 4:2:0 encode                     ≈free (80 % is the NV12 loss)
        NV12 → GL upsample to RGB → composite → AVC re-subsample      ≈10 % more, OURS
    So a 4:4:4 capture mode would be encoding chroma that has ALREADY been thrown away — it cannot
    recover the 20 %, and lane (b)'s 100 %-retaining AV1 4:4:4 row is only reachable when the
    encoder is fed RGB directly, which no capture path does. What 4:4:4 at capture WOULD buy is the
    second generation alone: ~10 %, for software encoding at ~2x CPU. That is a bad trade and the
    recommendation is now CAN'T rather than SHOULDN'T for the big half.
    MEASURED ON THE SYNTHETIC SCREEN, WHICH IS A CANVAS — and a canvas already arrives NV12, so a
    real getDisplayMedia frame from the OS capturer is not going to be better. Robert's next take
    settles it from the console either way; the line is there now.
    WHAT IS LEFT THAT IS ACTUALLY OURS: the second generation, and O3b removes it for a
    single-screen take at no cost. A take WITH a camera must composite, and for that case the only
    lever is a 4:4:4 composite encode — 10 % of colour for double the capture CPU. Not recommended.

    ALSO CHECKED, BECAUSE X6 IS THE DEFAULT NOW AND IT LOOKED LIKE A REGRESSION: the first warmed-less
    run of this lane reported the raw channels dropping 200 of 283 frames ("encoder behind"). It is
    note 6 and it is the RIG — the lane called createCaptureSession without prearm's warm. Warmed,
    the same take encodes 205 of 203 with ZERO drops. The flipped default is not dropping frames.

(e) "I WANT 100% COLORS" (Robert, 2026-08-29) — AND IT IS REACHABLE, ON THE HARDWARE ENCODER.
    THE PREVIOUS ANSWER WAS TRUE AT 1:1 AND ONLY AT 1:1, and stopping there was the mistake. 4:2:0
    stores chroma at half resolution IN EACH AXIS OF WHATEVER FRAME IT IS GIVEN, so what a file
    carries is fixed in ABSOLUTE chroma samples, not in percent:
        1080p 4:2:0  ->  960x1080/2 chroma  ->  seen at 1080p, a quarter of the samples
        2160p 4:2:0  -> 1920x1080  chroma   ->  seen at 1080p, ONE SAMPLE PER PIXEL
    So a supersampled 4:2:0 file is chroma-complete at the size it is watched. `npm run exp -- x15e`,
    same still code page, every rung encoded at the same bits per pixel, decoded, downscaled to
    1920x1080 the way a player would, and scored against the 1920x1080 reference:
        rung                capture      bytes      green kept   blue kept
        1x 4:2:0 (SHIPS)    1920x1080    583752       79.6 %       81.7 %
        1.5x 4:2:0          2880x1620    981874       96.9 %       99.0 %
        2x 4:2:0            3840x2160   1343479       96.0 %       98.3 %
        1x 4:4:4 AV1        1920x1080    668472       99.6 %       97.9 %
    1.5x ON HARDWARE MATCHES THE SOFTWARE 4:4:4 CEILING, at 1.68x the bytes. 2x is not better than
    1.5x (96.0 vs 96.9) — past one chroma sample per delivered pixel there is nothing left to win,
    and the extra pixels only thin the bits. THE TRADE IS PIXELS, NOT CPU CLASS, which is an
    entirely different proposition from software AV1 4:4:4 at ~2x capture CPU.
    WHAT THIS MAKES ACTIONABLE: the lever already exists. O6's `?nativeres=1` stops capping a
    native-resolution screen down, and on a 2x display that IS the supersample — the capture frame
    already carries 1x-complete chroma, and the only thing left is to DELIVER at that resolution
    instead of stepping down to 1080p. Nothing has to be encoded in software.
    WHAT IS STILL UNMEASURED, and it is one console line away: whether Chrome hands us the screen at
    the display's native resolution or already downscaled. capture/measuredVideo.ts logs
    `format ... · coded WxH` on the first frame of every raw channel, so Robert's next real take says
    both the format and the resolution. THE RIG CANNOT GET THIS ITSELF: a headless Chrome has no
    desktop for a screen-capture picker, and `{"probeSource":true}` hung a run for ten minutes before
    it was given a deadline. Run it headed, or read the take's console.
    A LEVEL-4.0 CONSTANT NEARLY KILLED THE FINDING: the first run reported the 1.5x and 2x rungs as
    "config unsupported" and would have concluded that supersampling is unavailable. `avc1.640028` is
    High@4.0 and caps at 1920x1080; the rig walks AVC levels per size now.
    GREY IS NOT QUOTED ABOVE and R1 says why — its keptPct divides by a 7.4 % source saturation, so
    it amplifies decode noise (this ladder prints grey at 197 % and 224 %, which is that artefact and
    not a measurement).

THREE INSTRUMENT FAULTS THIS SESSION, and note 10 now has a seventh, eighth and ninth instance —
ALL THREE WERE MINE except the first, and the pattern is one thing: A REFERENCE THAT MOVES.
  (1) O9's, above: a scrolling fixture compared against a readback taken at another time.
  (2) THIS RIG'S OWN FIRST TWO RUNS of lane (c) read 13.1 dB with fringe 22.4 / smear 67.6 /
      contrast 0.49 — O9's fingerprint exactly, and for the same reason. `{"thumbs":true}` dumps the
      frames; they showed the same page ONE LINE APART, which settled in one look what three rounds
      of reasoning about offsets had not. The fixture is now STILL (setSyntheticScreenContent paints
      no scroll and no caret), so the screen row is pixels and only pixels, and placement is measured
      on the camera PiP, which moves.
  (3) lane (b) declared a channel's duration as frames/30 while the file ran 6.6 s for 180 frames
      (setTimeout pacing), and the render then placed the picture 0.967 s from where the channel's
      own timestamps predicted. A rig that invents a recording's length gets to debug its own
      arithmetic as if it were the product's. It reads the file's duration now.
AND ONE GATE WAS DELETED FOR BEING UNANSWERABLE rather than left reading FAIL: the export-alignment
search in lane (b) runs on a page that holds still for 2.5 s, so every candidate inside one scroll
interval is the same picture and the search saturates at its span edge. It reports itself as a
DIAGNOSTIC and says when it saturated. The stage metrics are unaffected — within one interval a
same-page frame IS the right reference for a glyph statistic — but a gate that claimed to have
localised the export would have been quoted as a placement bug it never measured.
WHAT WAS NOT MEASURED, named rather than left silent: lane (b) runs on TEXT only. 4:4:4 is a
question about coloured glyph edges and the motion control has none, so a motion row would have
priced nothing — but it is an omission and not a null result. And no lane ran at 4K or under load.
FULL SWEEP: 458 tests · typecheck clean · every lane re-runnable in one command.

(X7, X10=O5e, X13: NEEDS ROBERT DISCUSSION — see the READY map. X11, X14: PARKED — see the READY map.)

═══ P0 ═══

## GATE-alias. the cold oracle was measuring past the end of its own ruler — FIXED 2026-08-25
THE FIRST DIAGNOSIS IN THIS FILE WAS WRONG, and the way it was wrong is the lesson. It said the
click↔flash PAIRING was ambiguous on a uniform 1 s grid and that the estimator was picking the wrong
alignment. Both halves sounded right and neither was the cause. What settled it was making the rig
dump the failing run instead of arguing about the summary line — the numbers, verbatim, from a cold
run that failed at −438.7 ms:
    beep arrivals   1537, 2537, 3537, 4537, 5537   residual vs k·1000 = 537 on EVERY one
    flash arrivals  1019, 2020, 3020, 4020, 5020   residual = 19-20
    export onsets   1.395 …   export flashes 0.833 …   true offset = +562 ms
THE AUDIOCONTEXT STARTUP STALL WAS 537 ms — perfectly regular, trivially measurable, and past TWO
hard limits at once:
  (1) estimateScheduleSkewFromArrivals refused it (MAX_SCHEDULE_STALL_MS = 450), so there was no
      correction at all and the gate fell to its RAW rung — which is why the failure line said
      `(raw)` and why three sessions of theorising about aliasing missed it;
  (2) the pairing window is half the grid wide and centred on ZERO, so +562 is not representable and
      came back as its complement against the next flash, −438.
So the run was not out of sync. The ruler was too short and nothing said so.
FOUR FIXES, each measured:
  · THE STALL IS ARITHMETIC, NOT A SEARCH. A beep is SCHEDULED at k·interval and can only render
    LATE — an AudioContext that has not started cannot make a sound early. So arrival = k·interval +
    stall with stall ∈ [0, interval), k = floor(arrival / interval), and the whole candidate-index
    sweep and its 450 ms clamp go away. Missed first beep still works (k moves, the relation does
    not). The magnitude clamps in isAliasedScheduleCorrection went with it: what distinguishes an
    alias from a stall is not size, it is whether the raw reading AGREES (a real stall is in the raw
    offset too) — the clause that caught every historical flake, and it stays.
  · THE PAIRING IS TOLD WHERE TO LOOK. The rig measures both reference schedules before it decodes
    anything, and their difference is where a correctly-paired export must land. flashSyncStats takes
    that expectation and centres its window there; omitted, it is the old behaviour exactly.
  · THE PAIRING PICKS BY CONSENSUS, NOT BY COUNT. The old sweep kept whichever alignment produced the
    MOST pairs. Injection-proven red: with a doubled detector it reports −400 ms where the truth is
    +50. Now every (onset, flash) pair proposes an offset and the one most events agree on wins, ties
    to the smaller |offset| — the band is ±90 ms against a 1000 ms grid, so "a whole second out" needs
    more evidence than "in band", not less.
  · THE RIG WARMS ITS ENCODER, BECAUSE PRODUCTION DOES (note 6's fourth instance). A Chrome PROCESS's
    first VideoEncoder pays a multi-second init and every oracle run is a fresh process; production
    pays it at MOUNT (prearm.ts → encoderWarm.ts) while this rig paid it DURING the take. On a 6 s
    take that is the whole take: two dumped failures show the render finding all five flashes while
    the packet-copied exports found one and none, so inst/trim sync could not be computed — and the
    run PASSED, because the band was written `typeof mean === 'number'`.
TWO VACUITY HOLES CLOSED WITH IT, and the second one caught a real run within minutes: a fast path
that RAN but produced no number now fails instead of passing silently, on both the instant and the
trimmed path.
ONE SAFETY NET, for the case the arithmetic still cannot see: a stall of a FULL interval wraps
silently (1100 ms reads as 100). Nothing in the signal can distinguish that, so the gate refuses a run
whose stall exceeds 0.75 × the grid. Measured headroom: ~130 ms before the rig warmed its encoder,
326-531 ms after, so the threshold is real headroom and not a hair trigger.
MEASURED AFTER, 22 cold runs across three batches: ZERO sync failures, zero aliasing, zero `n/a`
fast-path numbers, and every run on the `metric=symmetric` rung (before: raw, corrected and n/a
scattered across runs). sync 36.6-65.4 ms, instant 41.8-78.5, smart cut 41.3-78.8.
TRIED AND DELETED: a 750 ms settle between the warm and the take, on the theory that the warm and the
AudioContext were competing for the machine. Eight cold runs with it (stall 326-498) against eight
without (343-462) say they are not. The warm does raise the stall ~3x and that is simply its price.
STILL OPEN, and it is NOT this: the SPUR band fails about one cold run in seven at −33 to −38 dB
against a −40 band, on a settled machine as well as a busy one (1 of 6 idle, 2 of 8 under load). That
is the known capture-starvation artifact STATE already describes for the fidelity oracle, it predates
this work, and it is the only remaining cold-gate flake. It wants its own session.
WHAT THE INSTRUMENT STILL CANNOT DO, named so the next attempt starts here: the beep grid is uniform,
so a stall beyond one full interval is undetectable rather than merely unrepresentable. The structural
answer is a NON-UNIFORM or coded beep schedule, which makes the index unique at any stall. Not built —
the arithmetic above covers everything measured so far, and a coded schedule changes the content every
historical number was taken against.

## P0-instant-sync. the packet-copy paths were on the wrong clock — DONE 2026-08-25, merged
WHAT IT WAS, and it is a CONTRACT hole rather than a bug in either path: the composite is a second
file with its own clock, and that clock does not start when the take does. v1's file begins when its
MediaRecorder begins — after the <video> elements, the canvas, the audio graph and the durable write
stream have all been built (measured 134-215 ms into the take); v2's begins at whichever of audio or
video first reached the worker (66-90 ms). CompositeRecording had no field that could express this, so
instant.ts and smartCut.ts both assumed composite time IS recording time and placed copied video
against audio mixed from the raw channels on the recording timeline.
SHIPPED:
  · CompositeRecording.startOffsetMs — "composite t=0 is recording time startOffsetMs", the same
    sentence ChannelRecording.startOffsetMs already makes for a channel. ABSENT means 0, i.e. the old
    assumption preserved for takes recorded before the field existed (nothing can recover their origin
    now, and a guess would be worse than the behaviour they were exported with).
  · both engines produce it: the v2 worker reports its origin as a MAIN-THREAD stamp
    (CompositorStats.originAtMs — every arrival is stamped there, so the subtraction is between two
    readings of one clock); v1 stamps the instant after recorder.start(). session.ts passes the epoch
    and REBASES the composite by the same minOffset the channels get. The oracle rig does both too —
    it drives the engines directly, so wiring production alone would have left the gate blind.
  · v1's durationMs is now measured from that same instant instead of from before the recorder
    existed. Total output length is unchanged (offset + duration = stop − epoch either way); what
    moves is where the video sits inside it.
  · compose/compositeTime.ts — the four-line conversion, pure, unit-tested, so the decision has one
    home and a test rather than being re-derived at each call site.
  · instant.ts places every copied packet at packet.timestamp + offset and mixes audio over the whole
    take; smartCut.ts maps each kept span through the offset (negative composite time = the span
    begins before the composite's first frame, which is a real hole in it and NOT a reason to slide
    its first frame backwards).
MEASURED (symmetric flash+click, the same metric `sync=` uses):
    v2   inst 185.1 ms BEFORE → 48.6 / 59.6 / 75.8 ms after · same takes' render 50.0 / 64.2 / 54.8
    v1   inst 244.8 ms before → 55.7 / 57.8 ms          · same takes' render 61.7 / 59.5
    the declared offsets on those runs: v2 66-90 ms, v1 134-215 ms
GATED NOW (the previous session deliberately left it measured-but-ungated): scripts/oracle-gate.mjs
bands the instant path at ≤90 ms symmetric, mean and maxAbs, PLUS the anti-vacuity check that the
unedited export really took 'instant' — a silently declining copy path fails the run instead of
passing it on the render's numbers. `compOff=` joins `inst=`/`compT0=` on every oracle line.
TWO THINGS THE FIX ALSO SETTLED:
  (1) THE "ONE FRAME AHEAD" OFFSET WAS THIS, not compositing. Three sessions attributed it to the
      composite being built from source frames as they arrive; `npm run exp -- o5cut` now reports
      compositeOffsetFrames 0 where it read +1, with the same rig and the same take shape.
  (2) THE FAST PATHS AND `sync=` WERE READ ON DIFFERENT METRICS — inst/trim on the audio-corrected
      number, sync= on the symmetric one, a ~13.5 ms systematic between two figures printed side by
      side on one line. All three read symmetric now (run.ts pathSync).
REFINED THE SAME DAY, by the band itself: a v1 run failed at 93.5 ms maxAbs, because the file's zero
was stamped at recorder.start() while a canvas capture stream only produces a frame when the canvas is
PAINTED — up to a frame of nothing in between, and every copied packet a frame early. Dated from the
first paint at or after start() now (start() is the fallback). v1 instant, three cold runs: 44.3 /
48.2 / 40.7 ms against the same takes' render at 57.4 / 49.1 / 34.0 — better than the render on two of
three, where it read 73.9 / 86.3 before the refinement and 244.8 before the task.
LEFT NAMED, MEASURED, NOT CHASED — the next session's question: on v2 the instant path sits ~25-33 ms
ABOVE the same take's render (65.8 / 77.2 / 75.6 against 41.8 / 51.6 / 42.6), inside the 90 ms band but
systematically. That engine stamps video frames with their ARRIVAL time (performance.now() when the
frame is read) while its origin is the first audio batch's CAPTURE time (wallForContextTime of its
first sample). Those are two different clocks and the gap between them is the shape of this residual.
Start there, not in the export.

═══ P0 — FOUND 2026-08-23, FIXED ═══

## P0-tail. v1 LOSES THE TAIL UNDER LOAD — FIXED 2026-08-23, merged (evidence in DECISIONS)
WAS: on a 4K source the shipped composite delivered 13.3 fps and its last decodable frame sat 3769 ms
before the end of the take (re-measured at the start of the fix session: 2734 ms on a 10 s take).
"Loom cuts last seconds, we don't do that shit" — happening in our own engine.
FIX, in the order the evidence forced:
  1. TIMESLICE 1000 -> 250 ms. 2734 -> 675/922 ms on its own.
  2. STOP PAINTING BEFORE STOPPING. The compositor kills its tick, then lets the encoder work.
  3. PROBE THE ENCODER with requestData() instead of waiting for the byte flow to go quiet. This is
     the fix: under load the recorder emits FOUR chunks in ten seconds, so "no bytes for 200 ms" is
     true immediately and proves nothing (that version measured 150/198/579 ms — better, and wrong).
     A requestData() flushes what has been encoded, so an EMPTY answer is real evidence.
MEASURED at 4K, 10 s takes: tail 43, 56, 167, 320 ms — all inside O8's 400 ms band, with ~1.0-1.1 MB
per take recovered by the drain that used to be discarded. Healthy 1080p: tail 71/80 ms (was 65-76 —
unchanged), delivered 29.7/29.9 fps (unchanged), drain costs 242 ms at stop and recovers 0 bytes,
because there is nothing to recover.
(a) THE GATE CAN NOW SEE IT: `npm run oracle:load` (scripts/oracle-load.mjs) runs the band under the
4K load and exits non-zero when it is red. HEAVY — announce it, never run it under Robert's feet, and
it is deliberately NOT in the pre-push hook. Proven red before the fix by the same harness (675/922 ms
with the drain disabled, 2734 ms as shipped); reproduce by setting DRAIN_BUDGET_MS = 0.
(b) INSTRUMENTED: LiveCompositeStats (frames painted, chunks, drain ms, bytes recovered, timed out) in
one console line at every stop, warn-level when the drain timed out.
(c) DECIDED, per the precedent F1 and the liveness work set: a composite whose drain TIMED OUT is
marked CompositeRecording.tailIncomplete, and exportInstant refuses to packet-copy it — the export
falls back to the full render from the raw channels. Correctness over instant. Note the signal is
conservative: it means "the encoder never caught up in 2 s", not "the tail is short by >400 ms",
because nothing cheap can measure the latter at stop.
FOUND ALONGSIDE, and it is now task P0-tail-raw: the RAW channels lose tail too. Same rig, same take:
203-753 ms depending on load. Two caveats keep it off P0: the rig records the UNCAPPED 4K source
straight into a raw channel, which production never does (capDisplayTrack pins the display track to
1080p before any recorder exists), and the composite carries the instant path while the raw channels
only matter to an edited take. The fix is not the same one: a raw channel's source is live, so there
is no "stop painting" — it needs track.stop() before recorder.stop(), which is a reorder of the stop
path that produced a P0 hang two sessions ago and wants its own session with a deadline around it.

## P0-tail-raw. the raw channels lose their tail too — DONE 2026-08-23, merged
MEASURED FIRST (`npm run exp -- p0tailraw`, 4K source with the live composite running alongside,
tail = the lane's own length minus its last decodable frame):
    shipped   requestData() + stop(), 1000 ms slice    1344 ms missing
    slice250  the same at a 250 ms timeslice           1222 ms
    cut       end the TRACK, drain, then stop           622 ms
    throttle  drop the source to 1 fps, drain, stop      59 ms   <- shipped
The interesting loser is `cut`, because it is P0-tail's own fix ported literally: the rig reports
selfStoppedOnTrackEnd, i.e. CHROME STOPS A MEDIARECORDER WHOSE STREAM HAS GONE INACTIVE and takes the
backlog with it. So the source is STARVED instead — applyConstraints to 1 fps, keeping the frame SIZE
exactly as it is — and the recorder stays alive with almost nothing new to encode while the same
probe drain the composite uses recovers the rest.
SHIPPED: capture/recorderDrain.ts (one drain, both MediaRecorder paths, unit-tested; a self-stop is a
finished drain and NOT a timeout, or healthy takes get marked tailIncomplete) · session.doStop
reordered — composite stop kicked FIRST so its painting stops before anything throttles the tracks it
draws from, then the AUDIO channels stop at the press, then the video channels throttle and drain ·
every wait on the stop path now has a deadline, including the composite's own stop, which was
unbounded · synthetic.ts gains setSyntheticScreenSize so a rig can drive the REAL createCaptureSession
at 4K · scripts/oracle-load.mjs gained a RAW phase.
AUDIO HAD TO BE SPLIT OUT and the number says why: with the audio stopping after the drain, one take
measured mic 11947 ms against a screen channel of 10059 ms — nearly two seconds of soundtrack over a
motionless picture.
GATES, all green: production path at 4K twice — tail 7 and 216 ms (also 96 ms, and 7/27 ms at 1080p)
against the 400 ms band, `npm run oracle:load` PASS on both phases · healthy 1080p unchanged
(27.8-30.3 fps delivered, composite drain 241-243 ms recovering 0 B, whole stop 505-554 ms) · the
forced case, every recorder made deaf to stop(): the take still comes back, in 5569 ms, with all
three channels on disk, where it used to hang forever · `npm run exp -- armcancel` green · oracle
PASS on v1 and v2 · 236 tests.
KNOWN AND DELIBERATE: the throttled source keeps delivering ~1 fps during the drain, so the file runs
6-855 ms PAST the length the channel declares. Those frames are bytes, not tail — the timeline never
reads past durationMs.

═══ PHASE O — OPTIMIZATION ═══

## O1. export-stream-to-disk — DONE 2026-08-23, merged (evidence in DECISIONS)
Shipped: compose/scratch.ts (StreamTarget → OPFS scratch via blobStore.createPositionedWriter),
wired into pipeline.ts + instant.ts, BufferTarget kept as fallback. Gates green — 30-min export
peak live memory 39.2 MB vs 294.2 MB (target held 4.0 MB vs 253.4 MB), parity equal, zero orphans,
oracle 39.5 ms + fidelity 0.019 dB. Evidence harness: `npm run exp -- o1` (src/experimental/perf).

## O2. capture-time-loudness — DONE 2026-08-23, merged (evidence in DECISIONS)
Shipped: capture/loudnessAccumulator.ts + onPcm tap in measuredAudio.ts → Recording.loudness
(types.ts CaptureLoudness); consumed by instant.ts, pipeline.ts (unedited only) and the editor-open
preview probe. Probe cost measured at 145-156 ms/min ⇒ 1.5 s saved on a 10-min take, ~4.4 s on 30-min.
Floor-bound takes still probe (capture measures PCM, export measures the decoded file — they disagree
on p20 by up to 15 dB below the codec floor); healthy mixes skip it entirely. Fallback proven on
stripped stats AND spoofed Apple WebKit UA. Harness: `npm run exp -- o2`.
OPEN for O10: re-measure p20 agreement on a realistic room-tone rig; if it holds, drop the floor guard.

## O3. chromium-mp4-capture — (a) DONE 2026-08-23 with the MP4 flip REJECTED on evidence
##     (b) = O3b: DONE 2026-08-29 — export half shipped ON, capture half shipped OPT-IN (handoff below)
(a) shipped: contentHint text/motion at acquire + camera 1920×1080 ideal when the config has no
screen channel (camera-full takes were upscaling 720p). MP4 preference REJECTED: Chrome's
MediaRecorder does not stream mp4 — 753 B on disk halfway through an 8 s take vs 1.09 MB of
decodable webm — so it would trade crash salvage for CPU. Hardware encode moves to O4, which owns
its own fragmented-MP4 muxer. THE RE-TEST RIG IS GONE (X3, 2026-08-26): the verdict is settled and
the approach is in the REJECTED list, so `perf/mp4Capture.ts` was deleted. X6 will write its own
mid-take-kill snapshot check against the WebCodecs path it builds, which is the shape that matters now.
NEEDS ROBERT RECHECK: camera-only take on a real camera should now record 1080p, not 720p.
(b) STATUS 2026-08-26: X6 UNBLOCKS THE HALF THAT WAS IMPOSSIBLE, and gates it on X6's own flip.
The re-scope below concluded "Chromium raw channels stay VP9, so a single-generation packet copy into
MP4 is impossible there". That is no longer true: X6 records raw channels as AVC in fragmented MP4,
which is exactly the copyable shape O3b needed. So both halves are now buildable —
  · the CPU half (skip the live composite when one video channel already matches the export) saves a
    whole encoder during capture, and X12 priced what that is worth: the composite writes 0.94 Mbps
    on screen content and 1.44 on motion, ~18 % of the take's total write bandwidth;
  · the COPY half (unedited export packet-copies the RAW channel) is what keeps the instant path
    alive once the composite is gone — and without it, skipping the composite would trade "instant
    is law" for CPU, which is not a trade this product may make.
The copy half only works when the raw channel IS mp4/AVC, i.e. when X6 is on, and X6 is off by
default pending Robert's ruling on the picture change it makes (see the X6 handoff). SO O3b IS NOT
BLOCKED ON CODE ANY MORE — it is blocked on the same decision X6 is, and it should be taken up in the
same breath: if X6 flips, O3b is the task that collects the reward.
(b) ee/o3-single-gen — original note (re-scoped 2026-08-23), still to do: a take with exactly ONE video channel whose dims ==
export dims and codec is mp4-compatible → skip the live composite entirely (audio mix graph still
runs); unedited export = packet-copy the RAW channel. RE-SCOPE RESULT: with (a)'s mp4 flip rejected on
evidence, Chromium raw channels stay VP9, so a single-generation packet copy into MP4 is impossible
there — the only raw channels that could be copied are Apple WebKit's (already mp4/h264). The CPU
half of the idea (skip the live composite when one video channel already matches the export) survives
but costs the instant path unless the raw channel is copyable. Both halves become straightforward
once O4 owns the encoder and the muxer, so this waits for O4 rather than being built twice.
GATES (b): screen-only 1080p take: console proves no composite recorder · instant export ≤ current ·
PSNR/visual evidence that single-generation ≥ composite-generation quality · capture write bandwidth
~halved (bytes/s logged) · all oracles green.

## O3b HANDOFF — DONE 2026-08-29. BOTH HALVES BUILT; the EXPORT half is on, the CAPTURE half is Robert's.
Rig: `npm run exp -- o3b` (add `{"crops":true}` for magnified before/after glyphs). Seven gates, two
runs, all green both times.

THE MEASUREMENT, one screen-only 1080p take exported twice with NOTHING but the copy source changed:
    against the SOURCE canvas       grey    green    blue     luma PSNR   fringe   bytes
    copy the COMPOSITE (today)     71.6 %  70.3 %   75.2 %    35.5 dB     10.24   1.66-1.69 MB
    copy the RAW CHANNEL (O3b)     79.7 %  80.0 %   89.1 %    37.3 dB      8.66   1.89-2.08 MB
So it is better on colour by 9.7 / 13.9 points, better on luma by 1.8 dB, lower max error (52 vs 59),
lower fringe — and the instant export is FASTER (73-84 ms against 108-149 ms), because a byte copy of
a file we did not have to make is strictly less work.

THE ONE COST, AND IT IS NOT IN THIS TASK'S OWN "free — strictly less work" FRAMING: the download grows
13.9-22.6 %. The raw channel spends more of the same 8 Mbps ceiling on the same seconds than the
composite does. THE COLOUR WIN IS NOT BOUGHT WITH THOSE BYTES and that distinction matters — X15(b)
measured 3x the bitrate buying 4 % of the fringe on 4:2:0, so chroma is structural, one generation
against two. Part of the 1.8 dB luma IS the bits. Robert should see the size number; `?singlegen=off`
reverts in one load. The rig gates the size at ≤25 % so a future rate-control change that doubles a
download cannot land quietly.

THE CAPTURE HALF, and why it is OFF: `?singlegen=capture` makes startComposite() return before it
starts anything. Measured: capture writes 0.36-0.38 MB/s with the composite and 0.20 without —
45-49 % less per second of take, and one hardware encoder that never runs. WHAT IT GIVES UP IS REAL
and is why it is Robert's, not mine:
  · SOURCE LIVENESS lives inside the compositor (capture/sourceLiveness.ts), so "your screen froze"
    detection goes with it;
  · the recording preview can no longer render the compositor's own output and falls back to the raw
    <video> preview (attachCompositePreview returns false, which is already a normal answer on every
    Apple WebKit and Firefox take);
  · a take whose measured-video start falls back to MediaRecorder mid-start would have neither a
    composite nor a copyable channel, so its unedited export renders.
Same shape X6 shipped in, and X6's flip is the precedent.

WHAT WAS BUILT
  · core/singleGen.ts — the three rungs (off / export / capture) with the losses named beside them.
    A runtime setter that works without localStorage, because a flag a test cannot set is a flag no
    gate can prove.
  · compose/copySource.ts — PURE, 16 tests. Which file the copy paths copy, and every reason it said
    no. THE PROPERTY THAT MATTERS: it may only ever ADD a copyable case, never remove one — there is
    deliberately NO geometry check on the composite, because `allowPacketCopy` already owns that
    question and a second opinion could only refuse a case that works today. Four regression tests
    pin exactly that.
  · instant.ts and smartCut.ts take a CopySource instead of reading recording.composite. smartCut's
    v1 refusal became a refusal on source.writer === 'mediarecorder', which is the property it always
    meant: a boundary splice needs an encoder whose avcC we can reproduce.
  · choose.ts resolves the source ONCE and hands it to whichever path runs, so the origin it reports
    and the file that was copied cannot drift apart. ChosenExport gained copiedFrom + copyDeclined.
  · certify.ts gained `copiedFrom`, so the FILE says which generation it is — two exports of one take
    differ visibly on coloured text depending on it, and a field report would otherwise be
    unattributable. The rig reads it back off the blob and gates on it matching.
  · session.ts skips the composite on a qualifying take, conservatively: the guard reads facts that
    are settled before start() (useMeasured, decided in armChannel; the track's own dimensions) and
    every "no" records the take exactly as it always was.

THE SMART-CUT LANES ARE NOT DECORATION. Without smart cut over the raw channel the capture rung would
quietly send every TRIMMED take to the full render — a speed regression bought with colour, and
nothing would have said so. Gated: A-smartcut-single-gen and B-smartcut-no-composite both take
smartcut/single-generation, and the file's own certification tag agrees.

WHY THE ORACLE IS NOT AFFECTED, checked rather than assumed: the oracle rig's synthetic screen is
1280x720 (RIG_WIDTH/RIG_HEIGHT), so single generation is REFUSED on every oracle take and the
composite is copied exactly as before. `npm run oracle --engine=v1` PASS. The v2 oracle is FLAKY ON
MAIN TODAY AND IT IS NOT O3b — see the BACKLOG entry: stashing this work and running HEAD cold four
times failed 2 of 4 on `instant/trimmed export sync maxAbs > 90 ms`, against 1 of 2 and 1 of 3 with
O3b in the tree. Same failure, same distribution, and the band was already known to sit ~8-9 ms 1σ
above its own instrument noise floor (O4b).

NOT DONE, DELIBERATELY: a window share (any size but 1920x1080) never qualifies, and neither does any
take with a camera — both fall back to the composite with a stated reason. Whole-browser CPU is the
sampler's to measure, not a rig's: `npm run exp -- o3b --cpu --query=singlegen=capture` against
`--query=singlegen=off`.

## O4. webcodecs-engine — the keystone  [P1; steps 1, 1b and 2 all DONE (handoffs below), none of
   them from a branch. The WebGPU backend is all that is left, and it is the 4K lever, not the 1080p one.]
Why: the pixel path (MediaRecorder + <video> + main-thread 2D canvas) forces 4–6 redundant decodes
during capture, fixed-tick redraws of static frames, no keyframe control, ±45 ms epoch jitter, and the
1080p capture ceiling.
Step 1 DONE 2026-08-23 (×10 runs, evidence in DECISIONS): the residual is the INSTRUMENT.
Measured: an exact +18.00 ms detection bias (half a 30 fps frame + half a 128-sample envelope window;
predicted 18.00, measured 18.00 sd 0.00, 10/10) plus a video reference delay of 13.22 ms that the
oracle had always assumed to be zero while measuring the audio side (new probeFlashArrivals). CI's
metric read 31.55 ms (sd 7.86) over 10 runs; the same exports fully referenced read 62.77 ms (sd 8.73).
So the true offset is ~45–63 ms, every past sync number is ~31 ms optimistic, and the ≤10 ms gate is
NOT measurable here: 1σ is ~8–9 ms, so 10/10 under 10 ms would fail on noise even with a perfect engine.
Step 1b DONE 2026-08-23 (O4b): CI now gates the SYMMETRIC metric (both rig references measured, both
detection biases removed) at ≤90 ms — 5/5 cold PASS at 40.5-66.2 ms. Production anchor now subtracts
the platform-reported input latency (loopback: the anchor placed impulses +128.7 ms late, sd 0.70; the
audio clock agreed within 1.1 ms, so the delay is signal-path, not messaging). Instrument noise floor is
~8-9 ms 1σ — the honest limit for any future band.
STEP 2 (the engine, still to do): MediaStreamTrackProcessor → worker compositor (OffscreenCanvas,
WebGPU→WebGL→2D) → VideoEncoder (prefer-hardware, realtime, keyframe ≤2 s) → fMP4 → positioned OPFS
writer. Frame-driven draw, preview renders the compositor's own output, MediaRecorder stays as the
watchdog fallback, crash-salvage must recover the fMP4 mid-write. THIS is where O3a's rejected hardware
encode returns — we own the muxer, so bytes land on disk as they are produced.
GOAL FOR THE GATE: bring the symmetric sync metric to ≤20 ms and re-tighten the band.
Harness: `npm run exp -- o4step1`, `npm run exp -- audiolat`.
Step 2: MediaStreamTrackProcessor → VideoFrames transferred to a WORKER compositor (OffscreenCanvas;
WebGPU importExternalTexture preferred, WebGL then 2D fallback) → VideoEncoder (prefer-hardware,
realtime, keyframe cadence ≤2 s — the smart-cut prerequisite) → fMP4 mux → positioned durable OPFS
writer. Frame-driven draw (encode on frame arrival, ~1 fps keep-alive when static), not a fixed 30 Hz
tick. The on-screen recording preview renders the compositor's own output — zero extra <video>
decodes. SourceLiveness feeds from frame arrivals (worklet tick remains the clock). Raw channel
capture may also move to VideoEncoder on Chromium in the same worker — optional, evidence decides.
Constraints: MediaRecorder path stays as capability/watchdog fallback (weak machine degrades, proven);
crash-salvage must recover the fMP4 composite mid-write (ABSORBS v1 task 4 — composite salvage becomes
free); measured-audio pipeline untouched; instant export contract unchanged.
Size notes (O11): encode geometry follows RENDER geometry — the camera channel encodes at its PiP
max-render size (O11c), not its capture size. Frame-driven draw is the single biggest size lever for
screen takes (static spans cost ~1 fps). Keyframe cadence: ≤2 s is the smart-cut default, but GOP
length is also bytes — O11a's audit prices the trade (cut granularity vs size), decide on evidence.
The COMPOSITE stays avc: it is what instant export packet-copies into the blind-shared file (O11d).
GATES: oracle sync ≤10 ms 10/10 · 08-22 4K rig: delivered ≥28 fps at 1080p output (baseline 21.5/11.9)
· mid-record tab-kill salvages composite + all channels · forced-slow harness proves watchdog fallback
· capture CPU ≤ O3 baseline (report) · unedited instant export ≤ 200 ms · ZERO TAIL LOSS at stop
(distinctive final-second event present in export, drain-verified — Robert hard requirement) · all oracles green.
── HANDOFF 2026-08-24 (SIXTH session, "go O4") — SOLVED. THE "2-10 FPS WALL" WAS THE INSTRUMENT'S
   COLD START, AND THE THROUGHPUT GATE IS MET. Engine remains merged + DORMANT behind ?engine=v2.
THE FINDING, with the cells that forced it (the o4worker bisect, deleted by X3 once it had answered): the
   UNTOUCHED compositor.worker.ts driven by the probe's own feeder runs 27-28 fps at 13 ms encoder
   latency — with the engine's audio graph attached, with an idle AudioContext, with a longtask
   observer, with the o4step2 rig's own sources, and even through the REAL startLiveCompositeV2
   (26.7-29.1 fps warm). The same cells run COLD (fresh throwaway Chrome profile, which every
   exp.mjs run is) collapse to 0.6-5 fps with 400-8600 ms "encoder latency": the FIRST VideoEncoder
   of a fresh profile pays a multi-second initialization. A 30 s cold take proves it is a
   transient: 747 frames = 24.9 fps overall, all 102 drops inside the first ~5 s, 42 ms mean
   latency once flowing. Every v2 number this rig ever produced was measured INSIDE that window —
   and the old watchdog (cumulative fps from START, 5 s deadline) then degraded the take mid-init,
   which stops the pumps, so nothing could ever recover: a one-time transient read as a permanent
   2-10 fps. Note 10's fourth instance. The prior sessions' EIGHT refutations (texture re-spec, the
   thread crossing, GL-in-worker, the second source, wall-clock timestamps, take duration, the
   muxer + OPFS handle, audio) all still stand — they were all true, which is exactly why only the
   environment was left to differ.
SHIPPED THIS SESSION: o4step2 warms v2 with one discarded 2.5 s take before measuring
   ({"cold":true} restores the old behaviour on purpose; the report's notes say which way it ran) ·
   the watchdog clock now starts at the encoder's FIRST real output (capture/compositorWatchdog.ts,
   pure, 6 unit tests): init excluded, sustained slowness still degrades at 12 fps over 5 s, a
   never-outputs wedge degrades at 15 s — a degrade costs only the composite (raw channels keep
   recording), so patience beats a false kill · the o4worker bisect harness was DELETED by X3
   (2026-08-26) — its question is answered and the answer is note 6; git history has the rig if a
   future throughput mystery ever needs the same cell-by-cell method.
MEASURED ON THE FIXED INSTRUMENT (warm, 10 s takes, same day, dev machine):
   1080p: v2 delivers 28.4 fps vs v1 29.3 — THE ≥28 GATE IS MET (this line read "2-10" yesterday) ·
   v2 main-thread long tasks 0 ms vs v1 198 ms · encoder latency 14 ms, 0 drops, peak queue 1 ·
   tail −233 ms (the drain runs past the declared end — bytes, not tail) · oracle PASS on both
   engines, v2 sync 33.7 ms vs v1 63.4 ms same day.
   4K SOURCE row: v2 still collapses (2 encoded, 1468 ms latency — the 4K GL texture upload,
   measured earlier at ~35.6 ms/frame) and v1 reads 13.6 fps with tail 3812 ms on the SAME row.
   BUT the row feeds an UNCAPPED 4K canvas straight into the composite, a shape production forbids
   (capDisplayTrack pins display tracks to 1080p before any consumer exists), and the rig cannot
   yet reproduce that cap on a canvas track. Read neither engine's 4K row as production truth.
   v1's 3812 was also measured with several Chromes alive; oracle:load on a quiet machine before
   calling it a regression.
FINISH LIST FOR THE DEFAULT FLIP — updated same day (second "go O4" session):
   1. FIRST-TAKE COLD START — CLOSED. Measured PER LAUNCH, not per profile: a NEW Chrome process on
      a REUSED profile dir still read 15.9 fps / 177 ms latency cold (fresh dir: 13 / 260), so disk
      caches do not save real users — the first take after every browser launch would drop its
      opening ~4 s. SHIPPED: capture/encoderWarm.ts (throwaway 1080p VideoEncoder, 5 frames +
      flush) called from prearm.ts at mount when the engine preference is v2 — no device touched,
      off every critical path, v1 users spend nothing. PROVEN COLD END-TO-END: the warm itself
      absorbs the init (3864 ms) and the WORKER's encoder then runs 28.2 fps / 13 ms / 0 drops in a
      fully cold Chrome (measured with the since-deleted o4worker bisect, cell `mainwarm` — the warm
      is main-thread, the engine encodes in a worker, and that it carries across threads is
      measured, not assumed).
   2. mid-record tab-kill salvage of the fMP4 — still unverified (gate). Note the standing decision
      (third session): a crash-truncated composite must never be packet-copied, so salvage keeps
      restoring the CHANNELS; what needs verifying is that a v2 take's channels salvage exactly as
      v1's do and the orphaned fMP4 is cleaned up.
   3. the recording preview still decodes sources in <video>; the compositor's own output should
      feed it (gate: zero extra decodes). The biggest remaining item.
   4. CAPTURE CPU — CLOSED, AND v2 WINS: whole-browser CPU sampled from the OS during identical
      warm 10 s 1080p takes (`--cpu`, new exp.mjs flag): v1 peak 196 % / mean 72 % delivering 25
      fps with 179 ms of long tasks · v2 peak 127 % / mean 63 % delivering 28.6 fps with 0 ms —
      and v2's window includes its own warm-up take.
   5. watchdog fallback proof — MOSTLY CLOSED: the new first-output-clocked watchdog fired LIVE on
      genuine sustained slowness (the uncapped-4K row: "only 0.3 fps reached the file" → degrade →
      composite refused, take renders from channels), and the no-output wedge deadline (15 s) is
      unit-tested (compositorWatchdog.test.ts). RESIDUAL, honestly: no e2e case forces an encoder
      that ACCEPTS frames and never returns them — faking that needs test surface inside the
      production worker, which was judged not worth it today. Revisit if a real wedge is ever seen.
   6. sync: v2 reads 33.7-47.8 ms on the ≤90 band (v1: 63) across today's runs; the ≤20 ms goal is
      anchor work (input-latency subtraction), not throughput.
   7. raw channels on VideoEncoder — DONE as task X6 (2026-08-26), built on v2's shipped pattern
      rather than the old branch, and shipped off by default. Evidence decided: −41 % mean capture
      CPU, and a picture change that is Robert's to accept.
NOT DONE, unchanged: WebGPU backend — now clearly the 4K lever, not the 1080p one.
HARNESS GREW (second session): exp.mjs `--cpu` (OS-sampled whole-browser CPU, throwaway-profile
   runs only) and `--profile=<dir>` (REUSE a Chrome profile across runs and keep it — how the
   per-launch cold-start verdict was measured).
── THE DEFAULT FLIPPED (third 08-24 session, Robert: "finish this shit already") ──
engine.ts default v1 → v2; v1 stays as the three-rung ladder (capability / start failure / mid-take
watchdog) and behind `?engine=v1` + localStorage. Two scope calls, both recorded as decisions:
preview-from-compositor is an OPTIMIZATION (both engines pay the same preview <video> decodes
today; v2 with the current preview is still strictly cheaper than v1 with it) and moved to
O4-polish rather than blocking the flip; the sync ≤20 ms goal is ANCHOR work shared by both
engines — flipping IMPROVES sync now (33-48 ms vs v1's ~60) and the goal stays open in O4-polish.
ALSO SHIPPED WITH THE FLIP: recovery.ts now removes the interrupted take's orphaned composite blob
at salvage (it was left in OPFS forever, both engines, pre-existing); channel salvage itself is
engine-independent by construction (raw channels are the same MediaRecorder code either way).
FLIP GATES, all green same day: typecheck · 287 tests · oracle DEFAULT (v2) PASS twice (sync 60.8
under load / 35.4 settled) · oracle --engine=v1 PASS (the fallback stays gated) · browser-check
prod QA in real Chrome: 8/8 gates, 0 console errors, export 504 ms against v1's historical 505-508
· o7-measure arming UNCHANGED and A/B-proven independent of the mount warm (+349/+257 ms with the
warm suppressed vs +359/+259/+249 with it — the spread is the loaded machine; o7-measure gained an
INOUT_O7_QUERY env for exactly this A/B) · `npm run exp -- armcancel` green (cancel 374 ms, arm
215 ms, zero live tracks after stop).
AWAITING ROBERT: the first real take on the new default. If anything is off: `?engine=v1` reverts this
load, localStorage['inout.capture.engine']='v1' reverts sticky, and the session console prints
which engine ran.
── HANDOFF 2026-08-25 (ELEVENTH session) — O4-POLISH'S SYNC-ANCHOR ITEM IS RETIRED, WITH NUMBERS.
   IT WAS BUILT, MEASURED AND DELETED, which is the same verdict shape O5 reached about pipelining
   the export. Do not propose it again without beating the numbers below.
THE IDEA, as P0-instant-sync left it: the composite's two ends are read off different clocks — the
   file's origin is the first audio batch's CAPTURE time, while every video frame is stamped
   `performance.now()` when the reader hands it over, an ARRIVAL time late by the pipeline delay. A
   VideoFrame carries its own `timestamp`, the capture side of the same event, so stamp frames with
   that instead.
WHAT THE RIG FOUND (`npm run exp -- o4clock`, new; 20 s, both sources, canvas capture tracks —
   getUserMedia is unreachable headless without a fake device, and the run says so):
     lane            frames   mean d    sd    mean excess   p95 excess   drift
     canvas-screen    1201    486.1   1.04       2.74         4.05      −26 ppm  (−23.3 ms / 900 s)
     canvas-camera     601    487.2   1.00       2.97         4.10      −20 ppm  (−18.1 ms / 900 s)
   d = arrival − frame.timestamp. THREE THINGS FOLLOW AND THEY KILL IT:
   (1) `firstTimestampMs` is 0 — the frame clock's EPOCH IS TAKE-RELATIVE, not performance.now()'s.
       So d's absolute value is meaningless and only its shape can be used. The best a min-filter can
       do is `timestamp + min d`, whose zero is the FIRST frame's arrival — which is itself late by
       exactly the delivery latency we wanted to remove. The absolute latency is not recoverable from
       this signal at all; the browser exposes no capture-time reference for a local video track (the
       audio path's `track.getSettings().latency` has no video equivalent).
   (2) What IS recoverable is the JITTER, and there is 2.7-3.0 ms of it (4.1 ms at p95). The
       instrument's own sync noise floor is ~8-9 ms 1σ (note 1), so this is not measurable on the
       gate even if it were free.
   (3) It is not free. The media clock runs −20 to −26 ppm against the wall clock, so a whole-take
       minimum would trade a 3 ms fixed error for 18-23 ms of DRIFT on a 900 s take — and Robert records
       938-1800 s. A sliding-window minimum bounds that, and was written and unit-tested, but it is
       eight fenced files-worth of capture-path risk for ≤3 ms nobody can measure.
   AND THE RESIDUAL ITSELF IS NOT JITTER, provably: the oracle rig uses these same canvas sources,
   where delivery jitter is 1 ms sd, and it still reads the instant path 25-33 ms above the render.
WHAT WOULD ACTUALLY CLOSE IT, named for the next session: the composite and the raw channels need to
   share ONE media clock. Today the composite is placed on wall-clock arrivals and the raw channels
   on MediaRecorder's own media timeline, and the gap between those two is the residual. That
   converges when we own the raw channel's encoder too — i.e. it is O3b's, not O4-polish's.
The rig stays (`npm run exp -- o4clock`), because the next attempt should start from these numbers.

## O5. export-engine-v2 — worker, pipelined, smart-cut, codec ladder  [P1, after O4]
Why: the render is main-thread and strictly serial (decode awaits draw awaits encode), sleeps every
8 frames, tops out near realtime. Hardware decode+encode overlapped → multi-× realtime.
Goal: (a) exportRecording moves to a dedicated worker (already OffscreenCanvas-based; progress via
postMessage); yield hacks deleted. (b) stage pipelining with small ring buffers + encodeQueueSize
backpressure (decode-ahead 4–8 frames per source). (c) SMART-CUT: packet-copy full GOPs between cut
points, re-encode only boundary GOPs (needs O4 keyframe cadence; full render remains for old takes).
Generalize to DIRTY-SEGMENT rendering: only spans whose PIXELS an edit changed re-encode (zoom/camera
move during minute 2 → render minute 2, packet-copy the rest) — this is what keeps even visual edits
near-instant; F2/F4 tracks must expose per-span dirty ranges for it.
(d) codec ladder above avc: probe hevc/av1 hardware encode (isConfigSupported, mp4 container); avc
floor, vp9/webm last (codecs.ts FALLBACK_CHAINS is the seam). (e) VFR option (Robert-approved): preserve
source presentation timestamps from frame-driven takes instead of CFR resampling — high-Hz scrolls
stay smooth; CFR 30 stays the default.
Constraints: certified audio path behavior identical (fidelity oracle is the proof); progress UI keeps
working; abort works mid-pipeline.
GATES: 60 s 1080p30 edited synthetic take ≥5× realtime on the dev machine (report ×; stretch 8×) ·
trim-only edit of a 30 s take ≤1 s · zero main-thread long tasks >50 ms during export (trace) · every
ladder rung's file plays (Chrome + Safari where the codec applies) · fidelity+sync green.
── HANDOFF 2026-08-24 (SEVENTH session, "roadmap all O and F") — (a)(c)(d) SHIPPED, (b) MEASURED AND
   REJECTED, (e) NOT DONE. THE TASK'S CENTRAL PREMISE WAS FALSE AND THE STAGE SPLIT IS WHY.
O5 was written on "the render is main-thread and strictly serial (decode awaits draw awaits encode)".
   render.ts now counts every stage (RenderStats, one console line per export, `npm run exp -- o5`).
   12 s 1080p edited take, main-thread lane: decode-wait 1295 ms · draw 450 ms · ENCODE-WAIT 28 ms ·
   finalize 65 ms. Decode ALONE for the same frames, no draw and no encode: 962 ms.
   So (i) the encode was never the stall — mediabunny's VideoEncoderWrapper already keeps 4 frames in
   the VideoEncoder queue and returns before they are encoded (read the source: `if
   (encoder.encodeQueueSize >= 4) await dequeue`), and (ii) the decode cannot hide behind the draw,
   because the expensive part of "decode" is JS on the same thread. A lookahead window on the encode
   AND a prefetch pump on the decoder were both built, measured, and REMOVED for changing nothing.
   The render is already overlapped as far as one JS thread allows; its floor is the cost of decoding
   every frame. That is what makes (c) the whole task rather than a nicety.
(a) WORKER — SHIPPED AND DEFAULT. compose/render.ts (the render, portable) + export.worker.ts +
   pipeline.ts as a facade that falls back to the SAME code in-thread (one-shot, and only while the
   worker has produced no output). Getting there meant porting the certified audio path off Web Audio,
   which is [Exposed=Window]: AudioBufferSink → AudioSampleSink and AudioBufferSource →
   AudioSampleSource, same PCM, one shared makeStereoSample builder for both export paths. THE
   FIDELITY ORACLE IS THE PROOF and it is green (THD −77.4 dB, IMD −57.6, separation 53.4 dB,
   limiterHits 0).
   MEASURED, warmed, both orders (A,B,B,A — the first version of this rig ran worker-first once and
   reported the worker 35 % slower, which was note 10's fifth instance):
     60 s take: worker 6.00× realtime vs main 5.93× — THE ≥5× GATE IS MET, engines at parity.
     12 s take: worker 5.29× vs main 5.56×; fixed worker costs (draw ~17 % slower in a worker,
       finalize ~2× slower through the nested OPFS writer) amortize away by 60 s.
     ALL FOUR LANES PRODUCE BYTE-IDENTICAL FILES — the strongest evidence the worker render IS the
       in-thread render.
   THE LONG-TASK GATE CANNOT FAIL AND THEREFORE CANNOT PASS: both engines report ZERO long tasks,
   because the in-thread render awaits a decoded sample every frame and an await ends the task. It
   never blocks 50 ms at a stretch; it just never gives the thread back for long. The rig measures
   SCHEDULING LATENESS instead (a 16 ms ticker, how much of its 60 Hz it does not get): worker 31-48 ms
   vs main 77-104 ms at 12 s (2.4× freer), but only 184 vs 217 ms at 60 s. Honest reading: the benefit
   is real and smaller than assumed, and this rig's main thread is otherwise IDLE, so it understates
   the case that matters (an editor UI competing with the render).
(b) PIPELINING — REJECTED ON EVIDENCE, see above. Both stages were already pipelined by the library.
(c) SMART CUT — SHIPPED, MERGED, DEFAULT OFF behind `?smartcut=1` / localStorage (compose/smartCut.ts,
   smartCutFlag.ts, wired into EditorScreen between the instant path and the render). A trim-only edit
   changes no pixel, so the composite already holds what the output wants: copy its packets, re-encode
   only [cut → next keyframe]. Both halves share ONE avcC, so the boundary encoder's emitted decoder
   description is COMPARED BYTE FOR BYTE with the composite's before a single packet is written, and a
   mismatch throws → the caller renders. We can be strict because the capture encoder is ours (v2).
   MEASURED (`npm run exp -- o5cut`, 30 s take, trim-only, both boundaries off the 2 s keyframe grid,
   five runs): 669-944 ms against 3718-3780 ms for the render — 4.3-5.6×, and THE ≤1 s GATE IS MET.
   90-94 % of frames copied (44-97 re-encoded of 720). Picture matches the render at 37.5-42.8 dB,
   which is the ceiling for two independent encodes of one frame.
   THE ONE-FRAME OFFSET IS NOT OURS, AND THE CONTROL IS HOW WE KNOW. Smart cut sits one frame ahead of
   the render; the same rig's control shows the SHIPPED INSTANT PATH sitting one frame ahead of the
   same render too (41.8 and 38.4 dB at +1, with no smart cut anywhere in that comparison). The offset
   belongs to the composite — composed from source frames as they arrive, while the render samples the
   raw channel. Smart cut inherits it and adds nothing.
   WHY IT IS STILL OFF BY DEFAULT: no gate can see it. The oracle's non-frame-aligned trim goes
   through exportRecording directly, so it would pass while never touching smart cut. Same call as O4's
   dormant flip, for the same reason. TO FLIP: route the oracle's trim case through the editor's export
   decision, confirm 3/3 green under the sync band, change the fallback in smartCutFlag.ts to true.
(d) CODEC LADDER — SEAM SHIPPED, DEFAULT UNCHANGED. codecs.ts FALLBACK_CHAINS now runs av1-hw →
   hevc-hw → avc → avc-opus → vp9-webm, with the rungs above the avc floor UNREACHABLE unless a caller
   passes `allowAboveFloor` (nothing does yet). Above the floor a rung must also probe as HARDWARE
   (VideoEncoder.isConfigSupported, a real probe not a table) — a software hevc encode is slower than
   the avc it replaces. The certification tag carries the rung. This is O11d's standing decision made
   executable: the file is blind-shared and no probe can ask the recipient what it can play, so the
   rung only flips where the recipient is known (raw channels, the cloud player).
   Also shipped: `hardwareAcceleration: 'prefer-hardware'` on the render encoder (a hint; a platform
   without one ignores it), latencyMode left at 'quality' deliberately — an export has no clock to keep
   and must not drop frames.
(e) VFR — NOT DONE, untouched.
── HANDOFF 2026-08-24 (EIGHTH session, "go O5-flip") — THE GATE IS BUILT; THE FLIP IS REFUSED, BY IT.
WHAT WAS BUILT, and all of it stays:
   · compose/choose.ts — the instant → smartcut → render ladder as ONE function. It used to live
     inside EditorScreen, where no gate could reach it, which is the whole reason the oracle had only
     ever measured the render. EditorScreen and the oracle now call the same thing, and it returns
     WHICH path ran plus why each faster one declined.
   · the oracle rig records a LIVE COMPOSITE (RecordOptions.composite, on by default) exactly as
     production does, v2-or-v1 by the same capability choice. Without one, `recording.composite` was
     always absent, so neither packet-copying path could ever run here and routing the trim through
     the ladder would have fallen straight through to the render — green, and proving nothing.
   · the trimmed export goes through the ladder, and the gate bands ITS sync (same ≤90 ms band) and
     asserts the path it took. A fast path that silently declines now FAILS the run.
   · `npm run oracle -- --no-composite` restores the old rig, which is how the change was checked:
     3 runs each, composite OFF 50.2/67.8/62.4 ms vs ON 55.1/37.9/55.7 ms — no systematic shift. (An
     early single run read 87.1 and was a loaded-machine outlier, not a signal. Note 10, again.)
   · the instant path's sync and the composite's own clock origin are measured and printed on every
     oracle line (`inst=`, `compT0=`) — neither had ever been measured before.
WHAT IT FOUND, which is why the boolean is still false: see task P0-instant-sync at the top of the
   map. Both packet-copying paths are outside the sync band, the instant one included, and smart cut
   inherits it by copying the same composite. Flipping would have shipped a worse file than the render
   for every trimmed take.
HONEST CALL RECORDED: the instant path's sync is measured but NOT banded yet. Banding it today turns
   CI red on a pre-existing defect and blocks every other task; it is printed on every run instead so
   it cannot regress unnoticed, and it gets its band in the session that fixes it.
ONE MORE THING THE COMPOSITE RIG SURFACED: with a composite running alongside, the audio boundary
   jump reads 0.002-0.064 where it used to read a flat 0 (band 0.1, so it passes). Production takes
   have always had that load; the oracle simply never carried it. More representative, not worse.
FOUND WHILE DOING IT, both fixed: (1) moving the export into a worker made getLastScratchStats()
   main-thread-blind, so O8's new peak-memory band read `n/a` on its first run — the gate caught its
   own prerequisite; the worker now forwards both RenderStats and ScratchStats and pipeline.ts
   republishes them. (2) compose/scratch.ts deletes the previous finished export when a new one
   finishes (correct: one result is live at a time), which invalidates any earlier ExportResult.blob
   and surfaces as a bare `TypeError: network error`. It cost this session two rig debugging rounds;
   smartCutRun.ts now detaches every file into memory as it is produced.
── HANDOFF 2026-08-25 (TENTH session) — THE FLIP HAPPENED. smartCutFlag default false → true, after
   P0-instant-sync removed the reason the gate refused it. Gates, all green the same day:
   `npm run oracle -- --cold=3` 3/3 PASS with trim=smartcut (56.3 / 69.9 / 76.9 ms, band 90) and
   inst=instant inside the band on the same runs · `node scripts/oracle.mjs --engine=v1 --cold=2` 2/2
   PASS · `npm run exp -- o5cut` 684 ms against 3766 ms of render (5.5×, the ≤1 s gate met), 94.3 % of
   frames copied (41 re-encoded of 720), picture 37.3 dB aligned / 42.9 dB at the first sample,
   compositeOffsetFrames 0 · typecheck + 353 tests.
   ONE BEHAVIOUR CHANGE RECORDED WITH IT: smart cut refuses a v1 composite UP FRONT (the new
   CompositeRecording.engine field) rather than after decoding and encoding a GOP to discover that
   MediaRecorder's avcC is not ours to reproduce. Same outcome as before (the render takes it), one
   honest reason instead of a late throw, and the v1 oracle expects 'render' accordingly.
   (e) VFR is still NOT DONE — it is task O5e and untouched.

## O6. native-res-capture — lift the ceiling the right way  [P2, after O4]
Why: CAPTURE_MAX_* (1080p30) was correct for the old engine; the product bar is "no visual
compromises" — a 4K screen should be recordable at native res once the engine affords it.
Goal: raise capture caps to source-native (+ optional 60 fps, TRUE VFR timestamps end-to-end —
pairs with O5(e)) BEHIND measured backpressure: the O4
compositor watches encoder queue/frame delivery and degrades stepwise (native → 1440p → 1080p) BEFORE
delivery collapses, console-evidenced. Export resolution options tie into F7.
Constraints: NEVER regress the 08-22 4K-freeze fix — degradation engages before starvation; default
export stays 1080p30 unless the user chooses higher (F7).
GATES: 4K rig on a capable machine sustains ≥25 fps at 4K encode OR degrades stepwise with console
evidence and zero freeze · Robert's real 4K-game-tab scenario re-verified · oracles green.

HANDOFF O6 — THE LADDER IS BUILT AND TESTED; THE CAP RAISE IS BEHIND A FLAG AND AWAITS ROBERT'S HARDWARE
(2026-08-26, THIRTEENTH SESSION).
WHY IT IS NOT SIMPLY ON. O6's own gate says "Robert's real 4K-game-tab scenario re-verified", and this
session MEASURED why no rig can stand in for that: a synthetic 4K source is a rAF-painted canvas, so
its delivered fps is dominated by the painting, a cost a real 4K display (composited by the OS) does
not have. Capping the synthetic track to 1080p moved oracle:load's fps from 14 to 14. So the thing
the ladder protects against cannot be reproduced here, and turning the cap off by default would be
shipping the 08-22 freeze back with an untested safety net.
BUILT:
  · `capture/resolutionLadder.ts` — pure, 9 tests, the safety-critical decision. Three rules and each
    is a way this goes wrong: NEVER STEP UP (changing a channel's frame size mid-file is what
    P0-tail-raw is careful never to do, so down is a one-way door); GIVE THE ENCODER TIME (note 6 —
    every "2-10 fps" panic in this project was a first-VideoEncoder init being measured, so nothing
    is judged until output has started AND a warm-up window has passed); ONE STEP AT A TIME WITH A
    SETTLE (re-measuring straight after a step scores the step's own transient and cascades to the
    floor in a fraction of a second). Rungs are 1440p then 1080p; below that the answer is the
    WATCHDOG's, not the ladder's — the composite is refused and the take renders from raw channels.
    The floor is 60 % of requested rate sustained 2 s, which is deliberately far below the 28-of-30 a
    healthy 1080p lane reads, because a ladder that fires on a good take is worse than no ladder.
  · `capture/nativeRes.ts` — `?nativeres=1`, default OFF. capDisplayTrack skips the cap when it is on.
  · liveCompositeV2 gained `onDegradeStep`, driven by the same 1 Hz stats the watchdog reads, using
    fps over the INTERVAL rather than since the start so a take that recovers stops being judged on
    how it began. session.ts applies the constraint, because the compositor sees delivery and the
    session owns the tracks. The RATE is deliberately left alone: this ladder trades pixels for
    keeping up, and moving two things at once would make the next measurement unreadable.
GATES MET: 440 tests · typecheck clean · oracle PASS on v2 (sync 53.0, inst 63.7, smartcut 69.0, tail
71) AND v1 (45.2, 47.9, render/62.5, tail 66) · armcancel green (arms 401 ms, zero live tracks after
stop) · build clean. The DEFAULT PATH IS BYTE-FOR-BYTE UNCHANGED — with the flag off, capDisplayTrack
behaves exactly as before and onDegradeStep never fires.
GATE NOT MET, and it is the one that matters: "4K rig sustains ≥25 fps OR degrades stepwise with
console evidence". The degradation logic is tested at every boundary; the 4K rig cannot exercise it
for the reason above. TO FINISH O6: Robert turns on `?nativeres=1`, records the 4K game tab that froze on
08-22, and reports what the console says — `[capture] native-res capture: leaving display at …`
followed by either nothing (it held) or `[capture] display stepped 3840×2160 → 2560×1440 (…)`. That
one report is the whole remaining gate.

## O7. bundle-split — DONE 2026-08-23, merged (evidence in DECISIONS)
First paint 802,975 B → 218,793 B (gate ≤300 KB). Editor lazy behind app/editorChunk.ts + prefetch at
record start (chunk ready ~3.3 s into a 6 s take); capture engine + recovery behind core/capture/lazy.ts
and warmed at mount (record latency unchanged: 282 ms → 296 ms, armed +212 → +215 ms); supabase-js in
its own 216 KB chunk behind a lazy provider that still warms eagerly on an OAuth callback URL.
Evidence: `node scripts/o7-measure.mjs` (prod build + CDP).

## O8. perf-oracle — PARTIALLY DONE 2026-08-23 (evidence in DECISIONS)
SHIPPED: tail-integrity band (export ≥ take − 400 ms; last flash AND last click within 2000 ms of the
end) proven red by injection (900 ms chop → −822 ms → FAIL); export throughput band ≥1× realtime
(currently 5.0-5.2×), proven red at 0.7×; certified-export metadata in every file's comment tag (path,
output settings, loudness makeup/p90/peak/source, stalled+missing channels, cut count, CI sync bound),
probed back out of real exports on both paths and all three tiers.
EXPORT PEAK-MEMORY BAND — DONE 2026-08-24 (seventh session). scripts/oracle-gate.mjs now fails when
the muxer holds more than 12 MB of OUTPUT at once (compose/scratch.ts coalesces at 4 MB, so that is
three chunks of headroom); oracle.mjs prints it as `peakOut=`. Reads 0.1 MB on the 6 s oracle take.
This is the band O1's claim never had: putting the whole file back in one ArrayBuffer costs nothing at
6 s and OOMs the tab at 30 min, so only a band that fails on a SHORT take is worth anything. Its first
run read `n/a` and that was real — the export had just moved into a worker and getLastScratchStats()
was main-thread-blind; the worker now forwards the stats (see O5's handoff).
O8b DONE 2026-08-26 (THIRTEENTH SESSION) — the delivered-fps band ships in `npm run oracle:load`.
oracle-load already MEASURED deliveredFps in both phases and gated NEITHER, which is note 17's shape:
the tail band answers "did the ending survive" and says nothing about the middle. A composite
delivering 8 fps under load has a perfect tail and is still a ruined take, and that is not
hypothetical — the 08-22 4K freeze delivered 21.5/11.9 fps against a 30 target and no gate could see
it. Both phases are banded now (`--fpsBand`, exit code carries it), and a lane with no fps number is
INCONCLUSIVE rather than a silent pass.
THE BAND IS 10, AND WHAT IT CAN AND CANNOT MEAN IS NOW MEASURED RATHER THAN GUESSED.
FIRST GUESS, WRONG: "a canvas captureStream does not honour a resolution constraint, so the rig
measures the uncapped regime". Checked directly (`npm run exp -- capcheck`, new) and it is FALSE — a
canvas track accepts the cap exactly as a display track does: 3840×2160@30 → 1920×1080@30, no error.
SO THE PRODUCTION SHAPE WAS BUILDABLE ALL ALONG, and O4-polish's remaining "4K row" is now closed:
synthetic mode applies `capDisplayTrack` to its screen track, AWAITED BEFORE DELIVERY exactly as the
real acquirer does. (The first version of that fired the constraint and delivered in the same tick,
which would have re-constrained the track MID-TAKE — changing a channel's frame size after its
recorder had started, the one thing P0-tail-raw is careful never to do. Caught before it shipped.)
AND THE FPS DID NOT MOVE: 14 → 14. That is the real finding. With the track capped to 1080p the rig
still delivers ~13-15 fps, because in THIS rig the 4K cost is the SOURCE — a rAF-driven 4K canvas
painted in JS — and capping the track downscales after that painting has already been paid. A real
4K display is composited by the OS and costs the page nothing to produce, so no synthetic fixture can
model its delivered fps. The band therefore stays at 10, not because the rig is uncapped but because
its fps number is dominated by a cost production does not have.
WHAT WOULD TIGHTEN IT is Robert's hardware: a real 4K display, which is also O6's own gate ("Robert's real
4K-game-tab scenario re-verified"). Until then the band catches collapse and nothing finer, and it
says so.

## O9. text-perfect — screen text without smear  [P2, after O4+O5; Robert: "text is important"]
Why: 4:2:0 chroma subsampling + generic encoder tuning fringes and smears colored text — the visible
difference between a mushy Loom and a crisp file. Frame-driven capture makes static text cheap.
Goal: (a) 4:4:4 encode where hardware supports it (probe); (b) AV1 screen-content tools (palette/IBC)
on hardware AV1 encoders; (c) near-lossless mode for static screen spans (bitrate spent only when
frames change); (d) synthetic text rig — code-editor frame, colored-on-colored text — with a
measurable text-edge/chroma-fringe metric added to the oracle.
GATES: text metric improves vs baseline (numbers) · static-screen file size does not balloon (report)
· every mode plays cross-browser per ladder · perf bands hold.

HANDOFF O9 — MEASURED 2026-08-26 (THIRTEENTH SESSION). THE METRIC EXISTS, 4:4:4 IS REAL AND FREE,
AND IT IS AIMED AT THE SMALLER HALF OF THE PROBLEM. No default changed.
(1) THE INSTRUMENT CAME FIRST, because none of (a)(b)(c) can be judged without it. PSNR cannot see
    this: a frame of coloured text on a dark ground is ~90 % flat background, so a codec can destroy
    every glyph edge and still score well — exactly what X5 hit when two painters differed by 156
    levels on 3.8 % of pixels. `oracle/textEdge.ts` (pure, 7 tests) looks ONLY at glyph edges, chosen
    from the SOURCE so a codec that erases an edge cannot also erase the evidence, and separates the
    two ways text dies: `chromaFringeMean` (4:2:0 bleeding colour across a boundary), `lumaSmearMean`
    (the shape blurred) and `edgeContrastKept` (decoded gradient / source gradient, clamped at 1 so
    ringing is not scored as sharpness).
(2) WHAT THIS MACHINE ACTUALLY ACCEPTS — O9(a) and O9(b) in full, probed per acceleration mode:
        AVC High 4:4:4 (avc1.f40028)   NOT SUPPORTED, any mode
        VP9 profile 1 (4:4:4)          NOT SUPPORTED, any mode
        AV1 main                       supported (software)
        AV1 profile 1 = 4:4:4          SUPPORTED (software)
    So "4:4:4 where hardware supports it" resolves to: nowhere in hardware, and only through AV1 in
    software.
(3) THE GATE, on identical source frames straight off the canvas (`npm run exp -- o9`):
        codec              bytes/frame   chromaFringe   lumaSmear   edgeContrastKept
        avc 4:2:0 shipped        9,856           9.45        2.85               1.00
        avc 4:2:0 @24 Mbps      12,441           9.08        1.85               1.00
        vp9 4:2:0               13,257           9.09        2.33               1.00
        AV1 4:4:4                9,155           4.63        3.44               0.99
        AV1 main 4:2:0           7,532           9.95        3.48               1.00
    AV1 4:4:4 HALVES the chroma fringe and costs FEWER bytes than what ships. And the control is the
    part that makes it a finding rather than a coincidence: THREE TIMES THE BITRATE on 4:2:0 moves
    the fringe by 4 % (9.45 → 9.08). The lever is chroma resolution, not bits — which is why nobody
    was ever going to fix this by raising the ceiling.
(4) THIS SECTION WAS WRONG, AND X15(b) SHOWS EXACTLY HOW — 2026-08-26. It said "the export is the
    small half": capture chromaFringe 23.2 · lumaSmear 68.6 · edgeContrastKept 0.49 against the
    export's 5.7 · 2.4 · 1.00, i.e. Chromium's VP9 destroying half of every glyph's edge contrast
    before the export sees a pixel.
    THE CAPTURE ROW IS A MEASUREMENT OF A SCROLL POSITION. `runTextPerfect` samples the decoded
    files at t = 1.0 / 3.5 / 6.0 s and compares them against ONE `canvasFrame(source.canvas)` taken
    AFTER the export finished — and screenLikeSource scrolls a line every 2.5 s and never repeats.
    The code comment asserting "the frame at 1.0 s and at 1.0 s + 2.5·k are the same picture apart
    from the caret" is the bug: `scroll = Math.floor(t / 2.5)` is monotonic, not periodic. The
    EXPORT row was file-against-file at matching instants and was therefore fine, which is why the
    two rows disagree by a factor of 28 — a mean luma error of 68 of 255 on every glyph edge is not
    an encoder, it is a different page.
    RE-MEASURED against the picture that was actually encoded (x15b, deterministic-by-frame-index
    source, compared by ORDINAL):
        capture (canvas → raw channel)   chromaFringe  8.87 · lumaSmear 0.80 · edgeContrastKept 1.00
        export  (raw channel → file)     chromaFringe  3.02 · lumaSmear 1.79 · edgeContrastKept 1.00
        total   (canvas → file)          chromaFringe 10.81 · lumaSmear 1.95 · edgeContrastKept 1.00
    and shifting the reference by ONE SCROLL STEP reproduces 25.62 / 66.28 / 0.51 on demand, which
    is O9's published row. Neither stage destroys glyph contrast. There is no big half.
    WHAT SURVIVES: (3)'s codec table, which was always measured on identical frames and is
    unaffected — AV1 4:4:4 really does halve the fringe at fewer bytes, and 3x the bitrate on 4:2:0
    really does buy 4 %. WHAT DIES: the recommendation's reasoning ("the lever that reaches the big
    half is CAPTURE, which is X6"). X6 encodes AVC 4:2:0 and was never going to help text, and now
    there is no capture-side damage for it to have helped with either.
RECOMMENDATION, and it has a tension in it that Robert should see rather than have resolved quietly: the
lever that reaches the big half is CAPTURE, which is X6. But X6 as built encodes AVC 4:2:0, so it
does not fix text either; fixing text at capture means AV1 4:4:4 at capture, which on this machine is
SOFTWARE — the exact CPU cost X6 exists to remove. Flipping the EXPORT to AV1 4:4:4 is the cheap,
real, small win (half the export's fringe, fewer bytes) and it is Robert's call because AV1 playback is
not universal — O9's own gate says "every mode plays cross-browser per ladder", and that is the
question to answer before the ladder gains an AV1 rung.
NOT DONE: (c) near-lossless static spans. It is a bitrate-allocation idea, and the measurement above
says allocation is not what text is losing — 3× the bits bought 4 %. It should be re-proposed only
against the codec table, not against intuition.

## O10. audio-depth — beyond certified  [P2, after O2; (d) lands with P4; Robert: "quality all doing"]
Goal: (a) EBU R128 loudness targeting (−14 LUFS integrated) as an ADDITIVE mode first — fidelity
oracle updated in the same branch, current behavior stays until parity proven; (b) multitrack export
option: mic + system audio as separate mp4 tracks, default stays flattened; (c) deterministic
spectral-gating noise reduction (DSP, not ML), opt-in; (d) cross-channel AEC (system-audio bleed
removed from mic) — in the P4 shell where a proper reference signal exists.
GATES: R128 integrated −14 ±0.5 LUFS on the synthetic set · multitrack file: tracks separate, default
unchanged · gating: zero triggers on the clean-speech fidelity set · fidelity oracle green throughout.

HANDOFF O10(a) — MEASURED AND BUILT 2026-08-26 (THIRTEENTH SESSION), AND THE MODE IS A NO-OP UNTIL ROBERT
ALLOWS ATTENUATION. (b)(c)(d) not started.
(1) THE MEASUREMENT THAT JUSTIFIES THE TASK. The shipped makeup normalises the p90 of 100 ms window
    RMS — unweighted, ungated. Measured on the DECODED EXPORT of three content shapes
    (`npm run exp -- o10`):
        speech-like (bursts with pauses)   −13.08 LUFS
        bass-heavy                         −10.20 LUFS
        bright                              −5.24 LUFS
    A SPREAD OF 7.84 dB. Three takes of the same nominal level land almost 8 dB apart on nothing but
    their frequency content, and a viewer moving between two INOUT exports hears that. An offset can
    be dialled in; a spread cannot. This is the argument for a weighted, gated measure.
(2) BUILT: `compose/lufs.ts`, BS.1770-4 K-weighting + 400 ms blocks at 75 % overlap + the absolute
    (−70 LUFS) and relative (−10 LU) gates. 14 unit tests. Calibrated against the standard's own
    1 kHz reference to 0.15 dB across three amplitudes — and that test is worth reading, because the
    first version of it asserted the wrong thing: the naive prediction is 20·log10(A) − 0.691, and it
    is wrong because K-weighting is +0.69 dB at 1 kHz and the two CANCEL by construction. A second
    wrong expectation went the same way (50 Hz "should" read much quieter — the RLB's −3 dB point is
    near 38 Hz, so 50 Hz is barely touched; 20 Hz is the right probe, and the HIGH shelf is the half
    an RMS measure misses entirely).
    STREAMING, because the render needs it that way: `LufsAccumulator` carries biquad state across
    chunks and keeps one number per 100 ms, not 690 MB of PCM for a 30-minute take (O1's rule).
    Pinned by test to give the same answer chunked as whole, to 0.05 dB, across four chunk sizes.
(3) EVERY PROBING EXPORT NOW REPORTS ITS INTEGRATED LOUDNESS, in both modes — the probe already has
    the mixed PCM in hand, so this costs one biquad pair per sample and no memory. RenderStats gains
    `integratedLufs`. A number nobody can see is a number nobody can check.
(4) THE ADDITIVE MODE IS WIRED END TO END (`?loudness=r128`, default `p90`, forwarded into the export
    worker because a worker has no `location` — note 10's lesson applied up front this time). It
    reuses the SHIPPED bounds deliberately: the peak licence and the p20 noise-floor ceiling are the
    same, so a take that is unsafe under one mode is unsafe under the other, and only `wanted`
    changes.
(5) AND IT DOES NOTHING, WHICH IS THE FINDING. Same takes through both modes: identical files,
    spread unchanged at 7.84 dB. The reason is direction, not statistic — every shape asks for a gain
    BELOW 1 (0.899 / 0.646 / 0.365), because INOUT's exports are ALREADY 1-9 dB LOUDER than the
    −14 LUFS convention, and the bounding never attenuates (`Math.max(1, …)`, inherited from the p90
    path where turning a take down was never on the table).
WHAT ROBERT HAS TO DECIDE, stated plainly because it is not an engineering call: R128 targeting can only close
this spread by turning takes DOWN. That makes every export quieter — most of all the bright ones,
which lose ~9 dB — and it interacts with the limiter's knee. It is the correct thing by the standard
and it is a change to how everything sounds. The measurement, the implementation and the switch are
all in place; the only missing piece is permission to attenuate.
NOT STARTED: (b) multitrack export, (c) spectral-gating noise reduction, (d) cross-channel AEC (P4).

## O11. bits-audit + codec-autoswitch — (a)(b)(c) DONE 2026-08-23, (d) BLOCKED ON P1's 3-engine rig
Robert 2026-08-23: "top shit; auto-switch per browser, invisible; never make it worse for everyone
because one system can't; camera-with-screen is a small rectangle anyway."
Why: file size is set by flat 8 Mbps ceilings chosen blind — nobody has measured where the bits go.
The autoswitch Robert asked for is already the house pattern (recorder mime probe, WebKit audio path,
encoder fallback chains, capability gating); this extends it to the codec rung, evidence first.
(a) BITS AUDIT (light, FIRST — prices every other lever): onEncodedPacket telemetry on the render
path + composite stats — keyframe share of bytes, achieved-vs-ceiling bitrate, per-channel bytes/s —
console-logged; certification tag gains the codec rung. No lever below ships without its number.
(b) GOP STRETCH (light): render-path keyFrameInterval 2 s (mediabunny default, never overridden) →
~5 s. Identical pixels; only player seek granularity coarsens. Expected 10-30 % off static-screen
exports — (a) decides. The instant path gets GOP control only when O4 owns the capture encoder.
(c) CAMERA-WHEN-PIP (light): capture side ALREADY rightsized by O3a (720p ideal + 4 Mbps + motion
hint when screen present) — Robert's instinct, half shipped. Remaining now: raw camera channel
4 → ~2.5 Mbps, visual-evidence gated (saves disk + write bandwidth only; the exported PiP already
costs ~7.7 % of frame area so the file barely changes). Post-O4: encode the camera channel at its MAX
RENDER size exactly (default PiP 461 px wide @1080p; F4 resize ceiling × largest tier = 1280 px, so
720p capture stays transparent).
(d) CODEC LADDER, probed (medium): extend codecs.ts FALLBACK_CHAINS to hevc-hw → avc floor (av1
where hw encode exists), hardware-only above the floor, probed per browser, invisible; certification
tag says which rung ran; savings MEASURED on our own takes (expect 25-40 %). DEFAULT FILE STAYS AVC —
verified 2026-08-23: encode side is broad (Chrome 130+ hw, VideoToolbox, Safari) but real-world
DECODE of hevc is near-absent on Firefox/Edge (950k-session 2026 dataset) and av1 absent on pre-M3
Safari — a blind-shared non-avc file WILL fail for some recipients, and no invisible rule can probe
the recipient of a file. The rung flips where the recipient IS known: raw channels post-O4 (internal,
no recipient — free ~40 % disk) and the cloud-player milestone (we control playback → per-session
codec; av1+hevc pair covers 99.7 % of sessions).
GATES: (a) numbers in console + tag, probed back out of a real export · (b/c) size delta reported +
SSIM/visual-diff evidence of no loss + instant path untouched (logs) · (d) every rung's file plays in
current Chrome + Safari + Firefox (P1 rig) · export throughput ≥1× realtime holds · oracles green.
── HANDOFF 2026-08-23 (third session) — (a)(b)(c) SHIPPED AND MERGED.
   (a) RENDER HALF DONE: compose/bits.ts counts every encoded packet on the render path AND the
   packets the instant path copies (video/audio bytes, keyframe count and SHARE, achieved vs
   requested bitrate, mean keyframe vs mean delta) — one console line per export. The certification
   tag carries the codec rung (container/video/audio/GOP). Harness `npm run exp -- o11`.
   (b) GOP 2 s -> 5 s ON EVIDENCE. Screen-like content, 12 s take, sizes demuxed out of the file:
   1 s +41.6 % · 2 s baseline · 3 s -10.9 % · 5 s -23.8 % · 8 s -24.4 %; keyframes were 43.5 % of
   video bytes at the old default (229 KB per keyframe against a 5 KB delta). Motion content moves
   0.3 % across the whole ladder. PSNR vs the 2 s render 82-83 dB to 5 s, 65 dB at 8 s. 5 s beats 8 s
   because 8 s adds 0.6 pp and doubles a player's seek granularity. Reproduced on a second run
   (-22.4 %). OUTPUT cadence only — smart cut needs the CAPTURE encoder's, which is O4's.
   (c) CAMERA-WHEN-PiP DONE: raw camera channel 4 -> 2.5 Mbps when a screen is in the take. One camera
   stream recorded into two files at once: -29.3 % on disk (1,910,497 -> 1,350,135 B), +0.2 % on the
   exported file, PiP-region PSNR 52.1 dB. Camera-only takes keep 4 Mbps (they are captured at 1080p).
   (d) REMAINS, and still wants P1: the ladder only flips where the recipient is known.
── HANDOFF 2026-08-23 — (a) PARTIALLY DONE on the CAPTURE side, folded into O4 step 2 because owning
   the encoder makes the telemetry free. CompositorStats now counts, per take: video and audio bytes,
   keyframe count and the keyframe SHARE of video bytes, achieved vs requested bitrate, frames encoded
   vs dropped, peak encoder queue, and the per-frame cost split (paint / VideoFrame / encode call).
   Logged in one console line at stop and returned by `npm run exp -- o4step2`.
   FIRST NUMBERS (v2, 1080p synthetic, 8 Mbps requested): achieved 0.36-0.69 Mbps, keyframes 2.5-5.6 %
   of video bytes at a 2 s cadence. The 8 Mbps ceiling is nowhere near binding on this content, which
   is the first hard evidence for (b)'s premise — but synthetic canvas content compresses unrealistically
   well, so a REAL screen take has to be measured before GOP stretch is priced.
   STILL TO DO for (a): the same telemetry on the RENDER path (compose/pipeline.ts), and the codec rung
   in the certification tag. Both are independent of O4 and can be done in any session.

## O12. persistent-share — keep the screen share alive BETWEEN takes  [Robert-GATED — deferred 2026-08-25, Robert: "we will consider it later". Do NOT start without Robert's go.]
Why: the strongest possible fix for the Chrome share wedge (docs/SCREEN_WEDGE.md — the open
Chrome-side bug that Robert's rapid record/stop stress test reproduces). Every take today opens Chrome's
picker, creates a fresh SCK capture session, and tears it down at stop; the wedge accumulates with
that churn. Keeping ONE share across takes removes the picker from every take after the first,
removes the create/teardown cycles that aggravate Chrome, and makes every screen take start
instantly.
Cost, and why it is Robert's call, not an engineering one: the sharing indicator stays lit BETWEEN takes, and the app
would hold a screen claim while not recording — which cuts against two standing rules ("no idle
device access", "off is a real stop"). Shape if approved: opt-in ("keep sharing between takes"),
a visible affordance to drop the share at any time, share reused only if the surface is still live,
released on page hide as today.
GATES if built: zero picker on take 2+ · indicator/claim honestly presented in the UI · release
verified (zero live tracks after "stop sharing") · the wedge stress test (10 rapid cycles) green ·
oracles green.

═══ PHASE F — FEATURES (Robert-approved 2026-08-22; order below; F7 independent) ═══

## F1. mid-take-cuts — DONE 2026-08-23, merged (evidence in DECISIONS)
EditState.segments (optional KeptSegment[]) + piecewise output→recording mapping; video and preview
came free through channelSourceTimeAt, audio uses one mixer per (channel × span) with a ±3 ms fade
through zero at every real join. Split is an EDITOR concept (adjacent spans) and only a real gap is a
cut, so a split with nothing deleted keeps the instant path. Measured: sync delta 2.17 ms across 3
cuts, joint discontinuity 0.000 vs 0.168 baseline, no-op segment lists dropped.
Deferred to O5: smart-cut so cut exports are duration-independent (12 s take + 3 cuts = 1473 ms today).

## F2. zoom-pan — DONE 2026-08-23, merged (evidence in DECISIONS)
Robert confirmed the scope this session: TIMED zooms, not a static reframe.
Shipped: timeline/viewportTrack.ts — F4's camera track with a different subject, deliberately (the
roadmap said copy it, do not invent a second one). Fractions of the frame, keyframes on the RECORDING
timeline, a gesture writes the anchor/target PAIR that LANDS on the playhead. The viewport is a
TRANSFORM around the whole composition rather than a step in it: one setTransform in the export
(drawVideoFrame), one CSS transform on the stage, same arithmetic. Wheel zooms about the cursor
(zoomAround keeps the point under it fixed), drag pans when zoomed, a badge shows the factor and
resets, and the timeline marks each move.
MEASURED (`npm run exp -- f2`, viewport read off the camera PiP as a FIDUCIAL in decoded frames):
geometry error 2.2 / 2.2 / 1.6 px at 1080p across three sampled instants (band 10 px = the locator's
own 8 px blocks), including one BETWEEN the two moves, which is what proves the view holds instead of
drifting. Zoomed take: isDefaultEdit false, so the packet copy can never ship a video that ignores the
zoom. Untouched take: still instant, PiP still 3.2 px from its historical slot. Export cost +2.5 %.
PREVIEW (`node scripts/ui-shot.mjs --flow=zoom`): the stage scales by 1.82 against a badge of 1.8 and
a PiP that grows by exactly 1.82 — the same transform, measured on the prod build.
MAX ZOOM IS 2.5x AND THAT IS A CONSEQUENCE, NOT A TASTE: capture is capped at 1080p and the default
export is 1080p, so every zoom is an upscale. Past 2.5x a screen recording is mush, and the honest fix
is native-resolution capture (O6), not a bigger multiplier.
F2b DONE 2026-08-23: a zoom can be moved in time, and it moves AS A WHOLE. A zoom is not one
keyframe, it is a move — anchor and target ZOOM_MOVE_MS apart — so dragging either marker drags the
group and keeps its duration; leaving the anchor behind would stretch the ease, which is a different
edit. The group slides up to its neighbours and no further, so a drag can never reorder the track.
timeline/moveViewportKeyframe is pure with 6 unit tests; the dot carried pointer-events:none and 7 px
is not a drag target, so it has an invisible grip. MEASURED on the prod build
(`node scripts/ui-shot.mjs --flow=zoomdrag`): the pair sits at x=515 and 647, dragging the target
120 px lands them at 635.5 and 767.5 — both moved, gap unchanged. The rig needed fixing before the
product did: it dispatched pointermove on the window, and startDrag listens on the element via
pointer capture, which a synthetic pointer id never gets.
STILL DEFERRED: auto-zoom stays gated on P4's cursor metadata, which can generate exactly these
keyframes when it exists.

## F3. background-frame — DONE 2026-08-23, merged (evidence in DECISIONS)
Shipped: compose/background.ts (presets as DATA — a solid or a gradient angle plus stops — plus the
geometry, painted into the canvas by the export and into CSS by the editor, with the stage's <video>
positioned on the same contain box the compositor draws into) + EditState.background + a Frame control
in the timeline tools row (swatches · Flush/S/M/L · Shadow), screen takes only.
MEASURED by LOCATING the surface in decoded exported frames (`npm run exp -- f3`): inset M rounded
0.4 px off the pure geometry, inset L flush 0.0 px (band 3 px), the unframed default shows no inset
band at all, the shadowed case renders with a 22.6 px reach. Preview (`ui-shot --flow=frame`)
0.061/0.878 at M and 0.101/0.798 at L against the compositor's 0.060/0.880 and 0.100/0.800. Export
cost +4.4 % (gate <10 %) — and the harness needed a warm-up render first, or the framed case measured
21.8 % FASTER than the plain one.
The inset is a UNIFORM SCALE (a unit test refused "equal visual gap": under a uniform scale a 16:9
picture in a 16:9 frame always leaves margins in the ratio 16:9). No image wallpapers, deliberately.
DEFERRED: the background is a render-path EDIT, so the two live composites do not know about it —
a framed take cannot take the instant path, by design (see note 9).

## F4. movable-camera — DONE 2026-08-23, merged (evidence in DECISIONS)
Shipped: timeline/cameraTrack.ts (pure pose math in frame FRACTIONS, one function sampled by both the
export compositor and the editor stage) + EditState.camera keyframes on the RECORDING timeline +
drag/resize directly on the PiP + edit persistence (editsRepo, IndexedDB v2 — trims and F1 cuts now
survive a refresh too, which they never did). MEASURED, by locating the PiP in decoded exported frames:
drags at t=2 s and t=5 s land at (0.179,0.200) and (0.221,0.737)w0.342 against requests of (0.18,0.20)
and (0.22,0.74)w0.34 — ~2 px at 1080p, band 0.02. Samples at 1 s / 3.5 s / 8 s prove it holds instead of
drifting. Untouched take: isDefaultEdit true, instant packet copy, PiP within 0.001 of its historical
slot. Preview↔export parity: DOM 0.8667/0.8165/w0.2395 vs exported 0.867/0.819/w0.242. Reload drift 0.
Harness: `npm run exp -- f4`, `node scripts/ui-shot.mjs --flow=camera`.
DEFERRED: live-drag DURING recording (out of scope v1, revisit after O4 makes the live compositor
edit-aware); F2 will reuse the same track shape for a viewport.

## F5. silence-speed — (a) and (b) BOTH DONE 2026-08-23, merged (evidence in DECISIONS)
Robert: okay. Deterministic DSP only — NO transcript, NO ML, ever.
(a) DONE. timeline/silence.ts (pure: envelope -> quiet stretches -> padded cuts -> F1 segments) +
compose/analyzeSilence.ts (opens exactly the mixers the export would, over exactly the current edit;
code-split so the decoder loads only when the button is pressed) + a Tighten button, a proposal bar
and ghost spans on the timeline. The proposal is DATA until Apply, and any other edit discards it.
MEASURED against a known silence map (`npm run exp -- f5a`, 13 s): 100 % recall on gaps >=800 ms,
0 ms of the proposal inside speech, the deliberate 500 ms gap preserved, 2800 ms removed, and the
applied joins measure a 0.021 max sample step against a 0.116 baseline (joins QUIETER than the file —
F1's fade through zero). A take whose loud level is under 3x its own floor is refused honestly.
The RIG had to be fixed before it could be believed: the first run reported 760 ms of "speech" cut and
the detector was right — the answer key and the file disagreed about t=0 by the time it took to open a
write stream. Schedule the envelope AFTER the recorder is running.
(a-original spec) silence tightening: detect via the existing window-RMS analysis (threshold / min-gap
/ padding params) → a PROPOSED cut list the user previews and toggles; applying uses F1 machinery.
(b) DONE. KeptSegment.speed makes every mapping affine inside a span instead of an identity; video
came free (it is sampled through outputToRecordingMs per frame). compose/timeStretch.ts is a
streaming WSOLA — hold the tail the sound naturally continues into, search ±5 ms for the window that
best matches it, cross-fade — with the correlation on the MONO SUM so the channels cannot choose
different offsets, and SpeedSpanMixer stretches the SUM of a span's channels rather than each one
(separate searches would slide mic against system audio inside the span). Three rules worth keeping:
adjacent spans merge only at the SAME speed; `speed: 1` is stored as ABSENCE; and clampEditState used
to drop a lone whole-take span as "the same as no cuts", which discarded the edit — a test caught it.
A speed change makes isDefaultEdit false (note 9).
MEASURED IN THE EXPORTED FILE (`npm run exp -- f5b`): pitch 439.98 Hz against a 440 Hz source at 2×
= −0.08 cents, +0.20 cents at 3× (gate 10; a resampling regression reads ~1200, and the unsped export
measures 440.00 so the instrument is exact) · output length 9600 ms against 9600 of arithmetic ·
sync across both boundaries, three runs: every flash/click pair survives (11/11), maxAbs moves ≤2.7 ms
and the mean −8 to −25 ms, which is INSIDE the plain baseline's own spread in the same three runs
(93.7 / 113.6 / 155.6 ms) · cost 0.97-1.03× per second of output. UI verified on the prod build
(`node scripts/ui-shot.mjs --flow=speed`): stepped 1×/1.25×/1.5×/2×/3× acting on the clip under the
playhead, a badge on the sped clip, duration 0:04 → 0:03.
FIXED ALONG THE WAY, a live defect: the render's mix bus counted MIXERS, and F1 had made those
different from CHANNELS — one mic cut into three spans exported at 1/3 gain, 9.5 dB down, with the
loudness normalizer spending its makeup budget undoing it. busGainFor counts distinct channels now.
Also deleted a second copy of the mixer construction (analyzeSilence had its own, and F5b had just
made the two capable of disagreeing about how long a span lasts).
GATES (a): synthetic take with a known silence map: ≥95 % recall on gaps ≥800 ms, ZERO cuts inside
speech · joins click-free (fidelity) · proposal stays preview-only until applied.
GATES (b) — MET: pitch within 10 cents (measured −0.08 / +0.20) · sync bands hold at the boundaries
(pairs preserved, maxAbs ≤2.7 ms, mean shift inside the baseline's own spread) · export time impact
reported (0.97-1.03×).

## F6. pause-retake  [P1, after O4 — build segments on the new engine once, not twice]
Robert: yes. Pause/resume + retake-from-here.
Goal: the recording session gains segments: pause stops feeding recorders (devices STAY armed — no new
permission or idle-access surface), resume opens segment N+1 with its own epoch anchor; per-segment
durable blobs; the timeline concatenates segments; retake = drop the tail segment and re-record it.
Salvage covers every segment.
GATES: pause/resume take: lane-length equality bands hold per segment · cross-segment sync in oracle
bands (multi-segment matrix) · tab-kill mid-segment-2 salvages segment 1 fully + segment 2 partial ·
retake leaves prior segments intact · paused state visible + wake lock held while paused.

HANDOFF F6 — PAUSE/RESUME SHIPPED 2026-08-26 (THIRTEENTH SESSION). RETAKE IS NOT DONE and is scoped
below rather than half-built.
THE SEGMENT MACHINERY ALREADY EXISTED and that is why this is ~200 lines and not 600: since
2026-08-23 a kind can be SEVERAL non-overlapping ChannelRecordings, because setChannelActive(kind,
false/true) already closed a segment and opened a new one, and everything downstream resolves by id
and window (activeChannelsAt). What F6 adds is the one thing that path could not do: hold the take
WITHOUT RELEASING ANYTHING.
  · `pause()` closes every live channel's current segment and remembers its track. No track is
    stopped, no device released, the camera light stays on, the wake lock is held, and resume asks
    no permission and opens no picker. Deliberately NOT onTrackEnded — that path stops the track,
    emits 'channel-ended' to a UI that would show the channel as gone, and auto-stops the take when
    the last channel closes, every one of which is wrong for a pause.
  · `resume()` opens segment N+1 on the SAME tracks, through the same armChannel every channel uses.
  · THE PAUSE MUST NOT APPEAR IN THE TAKE, and the mechanism is one line: resume moves the SESSION
    EPOCH forward by the gap. Every startOffsetMs downstream is measured against that epoch, so
    segments already closed keep their offsets, the next one lands where the last ended, and the
    elapsed counter stops counting time nobody recorded. The alternative — one epoch, gap left in —
    would hand the user dead air to trim out of every paused take.
  · THE COMPOSITE IS CANCELLED at the pause, not merely marked. It is ONE continuous file whose
    timestamps are arrivals and it cannot represent a gap; leaving it running would also spend an
    encoder on the paused stretch. Cost stated plainly: a paused take exports through the RENDER
    (the same fallback a late join takes) and has no source-liveness tick while paused.
  · UI: a secondary pause/resume control in the control bar, and CaptureState gains 'paused'.
GATES, all through the real createCaptureSession (`npm run exp -- f6`), 5 s + 3 s pause + 5 s:
    two segments per channel ......... screen ×2 · camera ×2 · mic ×2
    THE SEAM ......................... screen 21 ms · camera 19 ms · mic 277 ms (band ±400)
    the take is what was RECORDED .... 10,058 ms against 10,000 ms of segments — the wall clock was
                                       13,000 ms, so the 3 s pause is genuinely absent
    nothing released ................. same track object recorded BOTH segments, live throughout,
                                       checked by IDENTITY and read BEFORE the pause (a track
                                       stopped and re-acquired is indistinguishable afterwards)
    paused state visible ............. recording → paused → recording → stopping → stopped
    every segment holds bytes ........ 6 segments, all non-empty
    a paused take exports ............ render, 4.56 MB, declining instant with "take has no composite"
  Plus: 419 tests · oracle PASS v2 (sync 57.3, inst 65.9, smartcut 63.6, tail 85) AND v1 (50.9, 45.2,
  render/34.9, tail 66) · armcancel green (cancel prompt, arms 411 ms, zero live tracks after stop).
NOT DONE, and named rather than implied:
  · RETAKE ("drop the tail segment and re-record it"). It is not a capture feature at all — it means
    RESUMING A FINISHED recording, which touches recovery, editsRepo and the editor's own state, and
    is a bigger job than pause/resume was. Pause/resume is the half a user reaches for first and it
    stands alone; retake should be its own task against this segment machinery.
  · The multi-segment ORACLE matrix (cross-segment sync in band). The seam is measured above as a
    timeline gap, which is the property the segments must have; what is not measured is A/V sync
    ACROSS a seam on a fiducial take. Worth doing before Robert leans on pause for long takes.
  · Tab-kill mid-segment-2 salvage. The manifest is rewritten at pause and at resume, so the
    ingredients are there; it is untested.

## F7. quality-slider — DONE 2026-08-23, merged (evidence in DECISIONS)
Three tiers before export (720p/1080p default/1440p) + size estimate + honest path label + persisted
choice. Default tier provably still instant (59-66 ms vs 1219-1641 ms render). Size estimate inside
±20 % twice over (√pixel-ratio scaling off the take's own composite size; linear scaling failed at
−40/+36 %). Source-native tier waits for O6.

## F7b. quality-slider v2 — DONE 2026-08-23, merged, WITH ONE GATE REPORTED UNMET (now task F7c)
SHIPPED: 540p · 720p · 1080p (default, instant) · 1440p. Steps are RESOLUTION; bitrate rungs at a
fixed resolution were measured and rejected (1080p: 2.77 MB at the 8 Mbps ceiling, 2.20 at 3 Mbps,
1.72 at 1.5 Mbps, with only 1.84 Mbps achieved at the top — those are quality levers, not size steps,
and O9 owns them). 900p was a fifth step, dropped because it is the one rung the estimator misses
(+29.0 % / +30.5 % against ±20 % on the others, twice). Two production-rig runs: -15.9/+12.1/-3.1/-11.9
and -15.5/+13.8/-3.3/-9.6; adjacent steps 31/47/32 % apart predicted; default step still instant at
82 and 86 ms against 1.6-2.2 s renders.
NOT MET, WITH NUMBERS -> F7c: ±20 % does not hold in general. The estimate is anchored on the
composite and the composite's encoder is not the export's — 0.97 vs 1.84 Mbps on the same text pixels,
so the prediction lands 47 % low at 1440p; and the size-vs-resolution shape is content-dependent (text
~linear, motion ~sqrt; composite keyframe share separates them 68.8 % vs 1.1 %). The default step's
number is EXACT (it is the file); everything else is marked "~".

## F7b (original spec, kept for the follow-on)  [Robert 2026-08-23]
Robert: slider before export with SIZES shown per step; "step is whatever optimal" — picked from
evidence. UI REFERENCE (Robert): "like claude code / chatgpt effort level" — a discrete stepped control
with a handful of NAMED levels, not a continuous slider; selecting a level shows its predicted MB.
Builds on F7's shipped slider + estimator; this is more rungs, not a new control.
Goal: the ladder becomes size-ordered steps mixing resolution AND bitrate (e.g. 720p-lean · 720p ·
1080p-lean · 1080p default · 1440p — final set from O11a's achieved-bitrate numbers), every step
labelled with its predicted MB (F7 estimator extended per-step: min(step ceiling, √pixel-scaled
source term)). STEP RULE (what "optimal" means): adjacent steps must differ by MORE than the
estimator's ±20 % error band (else the number shown is noise) and must be visually transparent at
intended viewing — steps failing either get dropped, however nice the round number.
Constraints: default step stays the instant packet-copy path, untouched (F7 frozen rule) · codec
stays invisible (O11d decides it, never the slider) · honest path label kept (re-encode steps say so).
GATES: measured size within ±20 % of the shown number on EVERY step, twice over · adjacent steps
differ ≥25 % predicted · default step still instant (logs) · lowest step visual-diff evidence ·
`node scripts/ui-shot.mjs --flow=quality` updated · oracles green.

## F7c. the size number, calibrated — SHIPPED 2026-08-25 (attempt 5). THE GATE IS MET.
THE COUNT THAT CLOSED IT, and it is a count and not a model: four consecutive GOPs of one encode
(`npm run exp -- f7c`, new gopSeries block) price a LATER GOP at 1.81× the FIRST on motion content
and 0.88× on screen —
             GOP1        GOP2        GOP3        GOP4     later/first
   motion  1,388,666   2,459,812   2,609,888   2,459,968     1.81
   screen    449,279     394,812     396,222     400,920     0.88
Put through a 12 s file (2.4 GOPs): mean/first = (1 + 1.4×1.81)/2.4 = 1.47, against the 1.44× uniform
shortfall attempt 4 could not explain. That was the whole discrepancy.
SO THE PROBE READS TWO CONSECUTIVE GOPS — 300 frames, ONE forward pass, forced keys at 0 and 150,
which is not a simulation of a file's first two GOPs but IS one — and estimateFromCalibration blends
them by the take's own length, COUNTING keyframes (a 6 s take at a 5 s cadence holds two, not 1.2).
MEASURED, `npm run exp -- o11 {"takeMs":24000}`, both contents, twice:
   run 1  screen +1.2 / −7.2 / −6.9 / −13.5 %      motion −2.8 / −0.4 / +2.1 / +0.1 %
   run 2  screen +2.1 / +0.3 / +6.0 / +0.7 %       motion −6.9 / −2.1 / −1.5 / −2.7 %
   the model it replaces, on those same takes: screen +104.6/+38.4/−15.1/−16.7, motion −71 to −81.
AND ON THE DEPLOYED BUILD, which is the gate that counts: a 16 s take's 540p export landed at 279 KB
against a shown ~315 KB (+12.9 %) and 1440p at 320 KB against ~350 KB (+9.4 %). The panel showed the
OLD model's ~4.1 MB and ~26.9 MB for those same two steps at the instant it opened, which is what a
user has been reading since the O4 flip — see task F7d.
TWO RIG DEFECTS FOUND ON THE WAY, and they invalidate the numbers of attempts 3 AND 4:
  (1) the probe's 150-frame window ran off the end of a 6 s take, so 40 % of every delta mean it ever
      reported was `Math.min(durationSec - 0.01, …)` — the same frozen instant, sixty times.
  (2) the o11 rig recorded its composite with startLiveComposite (v1) while production's default
      flipped to v2, so it had stopped scoring the estimator users actually get.
ONE MORE TRAP, PAID FOR: the first shipping shape encoded ONE window TWICE to get a second GOP. A
channel reader walks its decoder FORWARD, so replaying the same seconds seeks backwards and hands
back the same frame over and over — the "later GOP" was 150 duplicates and the probe read 84 % under.
Reading 300 consecutive frames once is both correct and cheaper.
SHIPPING SHAPE: src/core/compose/sizeProbe.ts (moved out of experimental), memory FLAT — the spike
held 150 ImageBitmaps ≈ 1.2 GB, it now holds one frame at a time and feeds every step's encoder as it
composes — encoder options taken from the render's own RENDER_ENCODER_OPTIONS, dynamic-imported from
QualityPanel so the panel still opens on the model's number and the measurement replaces it ~9 s
later. First paint unchanged (238,790 B). The DEFAULT step never goes through it: it is the composite
copied, so its number is the file. No scratch files: the probe muxes to a BufferTarget.

## F7c (attempt 4 and earlier, kept for the record) — [DONE, superseded by attempt 5 above]
ATTEMPT 4 (fifth session). The task named one suspect — "rate control has not settled in the short
encode, an unexplained factor of three". `npm run exp -- f7c` measured it instead: 180 frames behind
ONE keyframe, 45 warm-up frames discarded (without them the first bucket measures the RIG waking up),
mean delta bytes by DISTANCE FROM THE KEYFRAME:
           1-14    15-29    30-59   60-119  120-179   GOP mean   ratio
  screen   6386     2087     1127     2239     1715       2039     0.32
  motion   4775     4373     6043    10348    16356       9486     1.99
It is not a factor of three and not one direction: a 15-frame window is three times too EXPENSIVE on
screen content and twice too CHEAP on motion. The error FLIPS SIGN with content — attempt 1's tell
again — and no single correction can fix a sign flip, which kills the whole short-window-plus-
correction family rather than one member of it. (On screen the shape is worth naming: the encoder
spends 11-19 KB on each of the ~7 deltas after a keyframe, then collapses to ~200 B once the picture
holds still. The warm-up proves that is the ENCODER settling, not the content starting.)
So the window became the GOP — one window, 150 frames, exactly what the shipped 5 s cadence produces.
Scored on both rigs: screen −6.7 / −7.3 / −12.9 / −15.4 % (was −7 to −43), motion −39.9 / −36.2 /
−31.2 / −30.5 % (was −59 to −69). SCREEN IS INSIDE THE ±20 % GATE FOR THE FIRST TIME; motion is not,
so it is not shipped and the panel keeps F7's estimate. Probe cost went 1.7-2.5 s → 5.6-6.0 s.
ATTEMPT 5 HAS A COUNTABLE QUESTION, NOT A MODELLING ONE: motion is a UNIFORM 1.44× under at every
step, including 1080p where the probe draws the composed frame 1:1 and rescaling cannot be blamed.
One number, not a curve, means something countable is missing — a keyframe the render emits and the
model does not, a first GOP costing more than a middle one, or the audio term. Count them first.
WHY IT EXISTS: F7b's per-step size is predicted from the composite, and the composite's encoder is not
the export's encoder — 0.97 vs 1.84 Mbps on the same text pixels, so the prediction lands 47 % low at
1440p. The idea was to stop predicting and measure: encode a bit of the take at each step through the
export's own encoder and price the file from that.
THREE DESIGNS, ALL MEASURED AGAINST REAL RENDERED SIZES (12 s takes, screen-like and full-motion
content, `npm run exp -- o11`, which now scores any candidate automatically):
  1. key+delta pair sampled from the COMPOSITE            +136 % / −68 %
     Wrong pixels — a composite frame carries MediaRecorder's ringing on text (expensive to re-encode)
     and is already smoothed on motion (cheap). The error flips sign with content, which is the tell.
  2. key+delta pair composed from the RAW CHANNELS through the production drawVideoFrame  +128 % / −64 %
     Right pixels, wrong shape: one delta against a fresh keyframe is not what a file is made of.
  3. two 15-frame WINDOWS of consecutive composed frames  −7 to −43 % / −59 to −69 %
     Better on screen content, still far out on motion: the measured delta (4.8 KB at 1080p) is a
     THIRD of what the same content costs the real render (14-19 KB/frame).
NOT SHIPPED, deliberately: the panel keeps F7's estimate, whose error is smaller on the content that
matters and — the point — already known and written down. A probe 60 % out on one content type is not
an improvement on a model 20-45 % out on the other. The probe cost 1.7-2.5 s, also over its budget.
WHAT ATTEMPT 4 MUST HANDLE: the unexplained factor of three between a 30-frame encode and a 360-frame
one. Rate control has not settled in the short encode; find out what settles it (longer window? a
prime-the-encoder pass? reading the achieved rate from a longer sample?) and prove it on BOTH content
types before touching the panel.
GATES (unchanged): measured size within ±20 % of the shown number on EVERY step, on BOTH rigs, twice
over · panel opens in under 100 ms with the probe still running · zero orphan scratch files after 20
opens · default step still exact and still instant.

## F7d. the estimator's anchor moved — DONE 2026-08-25 (ELEVENTH session), and the re-anchor is REFUSED
THE TASK OFFERED TWO BRANCHES — "either re-anchor the model on the v2 composite's own achieved bitrate
or make the fallback say it is a rough guide" — and the measurement closes the first one. Scored on a
FRESH run of `npm run exp -- o11 {"takeMs":24000}` (24 s takes, every size demuxed back out of the
rendered file, composite recorded by the shipped v2 default), F7's model against the real file:
             540p     720p     900p    1080p    1440p
   screen   +86.4    +30.5     −4.5    −22.2    −21.0 %
   motion   −68.4    −77.1    −79.0    −75.6    −75.3 %
THE ERROR FLIPS SIGN WITH CONTENT — +86 % on screen at the small step, −79 % on motion — which is the
exact tell that killed probe attempts 1 and 4, and no single correction can fix a sign flip. So there
is nothing to re-anchor TO: the missing quantity is not a constant the composite got wrong, it is how
differently the two encoders price two kinds of content.
WHY, and it is a mechanism rather than a fudge factor (same run, `composite` block):
   screen   composite 3,592,504 B   1080p render 4,617,298 B   ratio 0.78   keyframes 51 % of bytes
   motion   composite 3,323,543 B   1080p render 13,612,016 B  ratio 0.24   keyframes 1.2 % of bytes
The capture encoder is REALTIME and must keep up; the render's is not. On motion it spends a quarter
of what the render spends on the same pixels, on screen four fifths — a 3.2x spread between two content
types, from one anchor. That is the whole of F7d.
THE PROBE, on the same takes, is the answer and it is confirmed a third time:
   screen  +0.3 / +0.5 / +4.0 / −1.1 %      motion  −4.8 / −0.6 / −0.2 / +0.4 %   (probe 5.25-5.28 s)
SO THE FALLBACK BECAME HONEST INSTEAD, and the defect that made this urgent was not the number:
  · THE PANEL PROMISED A CORRECTION IT COULD NOT MAKE. `measured` stayed null on any take the probe
    cannot run — an audio-only take (no video channel to compose), a take under two frames, a browser
    with no working VideoEncoder — so "Measuring the other sizes on this take — they settle in a few
    seconds" sat on screen FOREVER, over numbers nothing was going to settle. The probe now reports
    running / measured / unavailable, and the third one says: "Couldn't measure the sizes on this
    take, so they are rough guides — the file can come out several times bigger or smaller."
  · app/lib/sizeConfidence.ts (pure, 9 tests) owns the rule — exact / measured / provisional / rough —
    because it is the only part of that panel that can be wrong without anyone noticing.
  · The two notices are styled OPPOSITELY on purpose: "settling in a few seconds" recedes (opacity
    0.6), "this can be several times out" does not (--warn, full opacity).
FOUND WHILE PROVING IT ON A REAL BUILD, and worse than the thing the task named: on a take with NO
COMPOSITE the default step called itself "Instant" and the panel said "that size is the file, not a
guess" — while compose/choose.ts cannot packet-copy without a composite, so that export fully renders,
and the number shown was the 8 Mbps ceiling. Measured on the deployed-shape build: ~5.6 MB claimed as
the file for an audio-only take of a few hundred KB. The panel now asks the same question the export
ladder asks (`recording.composite`), and drops both the tag and the claim when the answer is no.
EVIDENCE, prod build in real Chrome (`node scripts/ui-shot.mjs --flow=roughsize`, new — it switches
the video chips OFF before the take, so the probe fails for a REAL reason rather than a mocked one):
  before: 1080p "~5.6 MB · INSTANT", hint "That size is the file, not a guess", no notice
  after : no INSTANT tag, "Re-renders the whole video at 1920x1080, so the size is an estimate",
          rough-guide notice present, and the false "the 1080p size is exact" clause dropped
  and the NORMAL take is unchanged: `--flow=quality` still reads 1080p 967 KB exact + INSTANT with
  the measuring notice while the probe runs.
ONE TRAP PAID FOR IN THE RIG, worth keeping: two chip clicks dispatched in the same task both read the
same prefs snapshot and the first is lost, so the flow toggles one input per task. A human cannot
produce that; a synthetic driver does it every time.
LEFT NAMED, NOT FIXED: with no video channel at all the fallback is still the 8 Mbps ceiling times the
duration, which is nonsense for a take that renders a near-static waveform. It is now labelled as a
rough guide, which is honest; making it a NUMBER would need the audio-only render measured, and this
session had no evidence for one. Do not invent a constant for it.

## F8. nle-scrub — frame-exact editor feel  [P2, after O4; Robert-approved]
Goal: keyframe-index-driven preview — scrub decodes the EXACT frame (WebCodecs random access) instead
of <video> seek granularity; filmstrip thumbnails from the keyframe index; GPU-rendered waveforms.
This is the "Final Cut timeline feel" the product statement names.
GATES: scrub shows the index-verified exact frame at 5 sampled times · timeline holds 60 fps with a
30-min take loaded (trace) · thumbnail/waveform generation budget reported · zero preview-parity
regression.
── HANDOFF 2026-08-25 (ELEVENTH session) — THE FILMSTRIP IS SHIPPED. The scrub half was refuted and
   fixed on 2026-08-25 (see the READY map); this is the half that survived the refutation, and it is
   the one a user actually sees: every VIDEO lane in the timeline now shows the take's own frames
   instead of a coloured rectangle standing for it.
SHIPPED:
  · core/compose/filmstrip.ts — decodes N frames through mediabunny's `samplesAtTimestamps` (RANDOM
    ACCESS, not compose/video.ts's forward walk: the reader decodes every frame between the instants
    it is asked for, which is right for a render and would decode 54,000 frames for a strip over a
    30-minute take) and stitches them into ONE image. Nothing in the export imports it and it imports
    no export code beyond the demuxer — instant, smart cut and the render are untouched (conflict
    rule: it changes none of the three).
  · app/lib/filmstripPlan.ts (pure, 7 tests) — the layout rule is a PITCH in CSS px, not a count, so
    a 30-minute take gets the same handful of thumbnails as a 10-second one and each stands for more
    time. MAX_THUMBS = 24 is a DECODE budget, not a look.
  · app/hooks/useFilmstrips.ts — dynamic-imported, width-bucketed (a window drag does not re-decode),
    ONE decoder at a time, every failure silent and total: no strip, and the lane looks exactly as it
    did before F8.
  · The colour language changes register over a picture: the kept span keeps its channel tint at 0.18
    so frames read through it, and a cut span goes DARK rather than merely fainter.
MEASURED on the deployed-shape build, real Chrome, `node scripts/ui-shot.mjs --flow=film` (new):
    screen 11/11 frames in 493-958 ms · camera 11/11 in 291-315 ms
  i.e. the GATE "thumbnail generation budget reported" is met, at about 45-85 ms per thumbnail
  against F8's own measured 65 ms per random-access decode.
ONE REGRESSION FOUND AND CLOSED BY THE SAME SHOT, and it is why this was verified in a browser rather
  than argued: .tl is a fixed 184 px and .tl__lanes already overflowed slightly at four channels, so
  two lanes growing 10 px each pushed Mic off the bottom. The lane is 30 px (not 34) and .tl__lanes
  tightens its gap to 5 px when any strip exists: 2x30 + 2x24 + 3x5 + 16 = 139 px against the old
  4x24 + 3x8 + 16 = 136. Both screenshots are in the session; the second shows all four lanes.
── AND THE AUDIO LANES TOO, same session. The filmstrip left the audio lanes as bare rectangles, which
   is the half of a timeline that says where somebody SPOKE.
SHIPPED: core/compose/lanewave.ts — one column per two device pixels, each an honest SAMPLE taken
   through `samplesAtTimestamps` rather than a summary of a scan, so the cost is set by how wide the
   lane is and not by how long the take is. Stitched into one image like the filmstrip, drawn over the
   channel's own tint (the opposite layering to the strip: there the picture is the background, here
   the colour is), pointer-events off so the lane still seeks.
MEASURED, prod build in real Chrome: 396-400 columns in 31-186 ms per channel, against the
   filmstrip's 255-545 ms — audio is much the cheaper half.
ONE DECISION IS PURE AND TESTED (compose/lanewave.ts waveScale, 7 tests) BECAUSE IT CAN BE WRONG
   WITHOUT LOOKING WRONG: what the lane is drawn AGAINST. The first version used the maximum and a
   real take killed it within a minute — a system-audio channel whose steady tone sits near 0.05
   opened with a 2.2 transient, so every other column drew at 2 % of the lane and the channel looked
   empty. The reference is a high PERCENTILE now and the few columns above it draw full height; the
   true maximum is still reported, because it is the honest number about the audio even when it is the
   wrong number to draw against. Screenshots of both versions are in the session.
F8 IS CLOSED 2026-08-26 (THIRTEENTH SESSION) — the 60 fps trace is measured, and the answer is that
  the timeline's cost does not depend on take length at all. `node scripts/ui-shot.mjs --flow=film
  --takeMs=N` now DRAGS the playhead across the whole timeline and samples rAF deltas while it moves
  (a still timeline repaints nothing and would score any amount of work as 60 fps). On the prod build
  in real Chrome:
        8 s take    120.5 fps median · 107.5 p95 · worst frame 9.4 ms
       40 s take    120.5 fps median · 108.7 p95 · worst frame 9.3 ms
      120 s take    120.5 fps median · 106.4 p95 · worst frame 9.4 ms
  FLAT ACROSS 15× THE LENGTH, at double the gate's 60 fps, with the worst single frame at 9.4 ms
  against a 16.7 ms budget. That is the design working rather than a coincidence: filmstripPlan caps
  thumbnails at 24 by PITCH so length changes what a thumbnail stands for and never how many there
  are, the waveform is ~400 columns set by lane WIDTH, and both are stitched into ONE background
  image per lane so the browser paints one layer however long the recording is.
  WHAT IS EXTRAPOLATED, said rather than hidden: 30 minutes is 15× beyond the longest cell measured.
  What is demonstrated is INDEPENDENCE of length over a 15× range, which is the property the constants
  predict; a 30-minute fixture would confirm it and would take 30 minutes to record.

═══ PHASE P — PORTS (Robert-approved direction 2026-08-22; P1/P2/P3/P5a may run parallel with O-phase) ═══

## P1. browser-breadth — Firefox + OS-aware capabilities + 3-engine oracle  [P2, medium, parallel-safe]
Why: Robert override "every capable browser". Verified 2026-08: Firefox 130+ has full desktop WebCodecs
incl. Opus AudioEncoder → our MEASURED audio path runs as-is; getDisplayMedia AUDIO is silently
ignored (tracking bug unassigned) → drop the channel with honest copy (Safari machinery exists);
no AAC encoder → export lands on the existing avc+opus / vp9+opus chains. Also: Chromium on WINDOWS
gives FULL system audio on monitor share (better than macOS tab-only) — the capability gate must
become engine×OS aware, not engine-only.
Goal: capabilities.ts engine×OS matrix (probe-first, UA-sniff last); Firefox mapped + QA'd; oracle
runner extended to Playwright's three engines (chromium/gecko headless gated; webkit best-effort on
synthetic).
GATES: real-Firefox take (screen+mic) → edit → export plays · per-platform capability copy honest ·
oracle green on chromium AND gecko · zero Chromium regression (existing bands).
── HANDOFF 2026-08-23 (fifth session) — CODE HALF DONE. The two remaining gates need downloads.
The thing that was wrong was not coverage but SHAPE: capabilities.ts asked what ENGINE this is, and
the answer depends on the OS too. Chromium on WINDOWS hands over the machine's audio for a monitor
share; the same Chromium on macOS and Linux only ever gives audio for a tab or window. Calling both
"Tab Audio" is wrong on one of them.
And Gecko is a case that reads like a bug report: Firefox ACCEPTS `audio: true` on getDisplayMedia
and returns video only — no error, no track. Offering the channel there records a silent take and
says nothing. Dropped, with copy that says what Firefox DOES rather than "not available".
SHIPPED: capabilities.ts consumes core/platform.ts instead of sniffing again, and gains
displayAudioScope ('none' | 'tab' | 'system'), engine, os and aacEncode. displayAudioScopeFor is pure
and the PROBE VETOES THE TABLE (no getDisplayMedia beats any row). aacEncode is advisory — Gecko has
no AAC encoder so exports land on the avc+opus / vp9+opus chains, but codecs.ts keeps probing,
because the encoder is a better authority than a table. channels.ts gained channelLabel() and its own
Firefox copy. browser-check.mjs gained Firefox (declared noCdp, so `--list` says WHY) and UA rows for
Firefox macOS/Windows/old and Chrome on Windows. 12 unit tests cover every cell.
EVIDENCE: spoofed Firefox 131 through the whole flow on the production build — engine reads `gecko`
and the channel list comes back ["Screen","Camera","Mic"], Tab Audio dropped by the matrix, all 8
gates PASS (export 1.5 MB 1920×1080 in 507 ms). docs/qa/firefox-ua.json. Real Chrome unregressed.
OPEN FOR ROBERT, one command each:
  (a) real Firefox needs Playwright's gecko driver — this runner speaks CDP, which Firefox does not:
      npm i -D playwright && npx playwright install firefox
      after which the oracle's third engine can be wired to it (and O11d unblocks).
  (b) the chromium × windows row CANNOT be spoofed, and that is CORRECT: navigator.userAgentData
      wins over the UA string, so a Windows UA on a Mac still reports macOS. Proven by unit test;
      closed for real only by running browser-check.mjs on Windows.

## P2. pwa-install — DONE 2026-08-23, merged (evidence in DECISIONS)
Manifest + generated icons (scripts/make-icons.mjs, no image dep) + minimal service worker (same-origin
GET only, range requests ignored, assets cache-first, document network-first, PROD-only registration) +
beforeinstallprompt affordance with remembered dismissal. Offline reload reaches the capture screen with
the network provably blocked; capture verified unaffected under a controlling worker.
Gate script: `node scripts/pwa-check.mjs`. Lighthouse itself not run (not installed).

## P3. ru-pack part 1 — DONE 2026-08-23, merged (2 gates await Robert hardware/network — see handoff)
Why: RU market ≈ 75 % Chromium (Chrome ~53 % + Yandex ~18 %, current Chromium base, verified 2026-08).
The real risks are infra and auth, not the engine.
Goal: Yandex Browser slot in the QA matrix + version-floor probe · reachability test of app + Supabase
from RU networks without VPN, documented with evidence · auth alternatives designed (Yandex ID / VK ID /
email-OTP) as an adapter behind the existing swappable cloud/auth seam — design + compiling stub now,
implementation when cloud provisioning lands · i18n approach DECIDED only (string extraction scheme),
no mass string churn yet (avoids F-phase conflicts). Aurora OS: explicitly out (no modern engine).
GATES: Yandex smoke capture→edit→export documented with version · reachability report · auth-alt
design in ARCH + stub compiles · zero behavior change for current users.
── HANDOFF 2026-08-23 (ee/p3) — DONE except the two gates that need hardware/network we don't have.
GATE 1 Yandex smoke — PARTIAL, honestly. Yandex Browser is NOT installed on the dev machine and
  installing it is a download, which is Robert's to run. So the gate was turned into ONE command:
  `node scripts/browser-check.mjs --browser=yandex --out=docs/qa/yandex.json` — it finds Yandex at
  both macOS app spellings, drives the PRODUCTION build through record→stop→editor→export in the
  real browser, and writes the row's evidence. What IS measured: Chrome 151.0.7922.170 PASS on all
  8 gates (4 channels, export 1.6 MB 1920×1080 in 508 ms, zero console errors), plus two UA-spoofed
  runs that prove OUR detection — a YaBrowser UA is named yandex (not chrome) with Chromium 128 read
  correctly, and a 2021 Yandex UA reports belowFloor:true while still being allowed to run. Those
  spoof runs prove nothing about Yandex's engine and docs/QA_MATRIX.md says so in the table.
GATE 2 reachability — report written (docs/RU_REACHABILITY.md) with a non-RU BASELINE measured
  (docs/qa/reachability-baseline.json: app 200, supabase-platform 307, google-oauth 200, yandex-id
  302, login.yandex 401, vk-id 200). The RU run itself is NOT done and cannot be faked from here —
  a VPN run measures the VPN. `node scripts/ru-reachability.mjs --label="<ISP>, no VPN" --supabase=<url>`
  from an RU connection closes it. Ranked finding: the engine is a non-issue (Chromium), the risks
  are Google-sign-in unreachability (#1), a platform-wide *.supabase.co order (#2, proven shape —
  India blocked it at Jio/Airtel/ACT on 2026-02-24), and Vercel shared-IP collateral blocks (#3,
  reports 2021→Jan 2026; *.vercel.app keeps working while custom domains die, so the custom domain
  we don't have yet is the exposure). Nothing threatens a RECORDING — capture/edit/export are local.
GATE 3 auth-alt design — BUILT, THEN REMOVED ON ROBERT INSTRUCTION 2026-08-23: "dont do auth alternatives
  for rus, just prepare it to work in yandex browser." The Yandex ID / VK ID / email-OTP adapter and
  the optional CloudProvider members are gone; the contract is exactly what it was. Consequence,
  recorded so it is a decision and not a surprise: where accounts.google.com is unreachable, cloud
  sharing is simply unavailable there — capture, edit and export are local and keep working, so the
  user still gets their file, just not a link. Do not re-add without Robert asking.
GATE 4 zero behavior change — held. typecheck clean · 181/181 tests (28 new) · oracle PASS
  (sync 36.2 ms, spur −52.4 dB, jump 0, tail 70 ms, export 5.04×) · first paint 225,689 B vs the
  ≤300 KB O7 gate. The below-floor notice is the only new UI and it is silent on every browser that
  passes the probes.
CAUGHT A BUG IN ITSELF: the first probe list included createSyncAccessHandle, which is
  [Exposed=DedicatedWorker] — invisible from the main thread — so Chrome 151 declared ITSELF
  unsupported and every user would have seen a "switch browsers" banner. browser-check.mjs caught it
  on its first run; platform.test.ts now fails if anything worker-only is re-added to the list.
i18n DECIDED, not started (docs/RU_REACHABILITY.md): flat typed catalog in src/app/i18n/en.ts, ~20-line
  t() and no library (first paint budget), lazy locale packs, Intl.PluralRules for RU's three forms,
  format.ts as the number/date seam, and EXTRACTION IS MANUAL-ON-TOUCH — a mass string rewrite now
  would collide with F2/F3/F4/F5, all of which are editing UI files.
Aurora OS: OUT, decided, not a backlog item — no modern engine ships on it.
STILL OPEN for Robert: (a) install Yandex Browser + run the one command above; (b) run the reachability
  probe from an RU connection without a VPN.

## P4. desktop-shell — Electron + native capture bridges  [P1 of phase, heavy, AFTER O4/O5]
Why: what no browser can give: FULL system audio on macOS (ScreenCaptureKit), global cursor/click
events recorded as a metadata track (→ true auto-zoom, upgrades F2), global hotkeys, tray, background
reliability, 4K60 headroom. Electron NOT Tauri: Tauri's macOS WKWebView would downgrade the engine to
Safari-grade exactly where it matters most; Electron = engine parity everywhere.
Goal: shell + a bridge contract feeding the O4 worker compositor: macOS ScreenCaptureKit (screen +
system audio + cursor data), Windows Graphics Capture + WASAPI loopback, Linux PipeWire. Cursor/click
event stream as a metadata track (schema in types.ts, compatible extension). Global hotkey record,
tray controls. Robert "maybe someday" (design for, do NOT build): cursor-EXCLUDED capture + vector cursor
re-render (sharp at any zoom, click ripples) — the metadata track schema must make it possible later. Distribution: notarized DMG (+ MAS later), Windows installer + winget, Flatpak.
GATES: macOS screen+system-audio+mic take → export, all lanes in sync bands · click-metadata aligns
to video ≤50 ms (oracle extension) · Windows equivalent with loopback audio · engine parity: same
oracle numbers inside the shell as in Chrome · fallback/watchdog behavior preserved.

## P5. ios-node — (a) ScreenCaptureKit spike NOW, (b) node build  [(a) light + TIME-SENSITIVE, (b) heavy after (a)]
Fact (verified from Apple docs metadata 2026-08-22): ScreenCaptureKit is on iOS 27.0 / iPadOS 27.0
(beta; ships ~Sept 2026) — "replaces ReplayKit for screen streaming, no broadcast extensions". This
retires the planned Broadcast-Upload-Extension architecture (50 MB cap, awkward picker) before it was
ever built. Safari 27 does NOT add getDisplayMedia on iPhone (checked) — native app remains the only
iPhone screen path.
(a) ee/p5-sck-spike — separate Xcode 27 project, real device, NO repo code. Answer with measurements:
    (1) capturesAudio (system/app audio) on iOS? (2) SCRecordingOutput straight-to-file on iOS?
    (3) AVCaptureSession camera CONCURRENT with SCStream — screen+camera PiP on phone (market first
    if yes)? (4) system-wide vs app-scoped + picker/background UX; (5) entitlements + review posture.
    Deliverable: VERDICT.md with numbers. GATES: all five answered with evidence.
(b) node: SCK → hardware h264 → mp4 file → the file IS a ChannelRecording blob → the SHARED engine
    (wrapped) edits/exports — never a separate editor (frozen). iOS 27+ only; older devices keep the
    Safari camera/mic node; NO ReplayKit legacy build, ever. The distributed-node role stays gated on
    the 2-device sync spike (ledger); the standalone recorder role is not.
GATES (b): record iPhone screen → edit → export ON DEVICE, file plays · capture timestamps within a
band vs wall clock (spike-informed) · TestFlight review pass · zero editor code duplicated.

## P6. android-node  [heavy, after P5b]
Why: NO mobile browser has getDisplayMedia (verified 2026-08, Chromium hides it on Android) —
MediaProjection is the only Android screen path. Android web already serves the camera/mic node + PWA.
Goal: MediaProjection screen + internal audio (Android 10+) + foreground service (Android 14 manifest
type) → h264 mp4 file → shared engine, same file-handoff shape as P5b. Distribution: Play + RuStore
(RU market) + direct APK.
GATES: screen+internal-audio take → shared-engine export plays · survives app-switch/background ·
RuStore + Play listing drafts · zero editor code duplicated.

## P7. hdr — capture→export HDR  [P3, after P4, heavy horizon; Robert-approved]
Why: SCK (macOS/iOS 27) and Windows Graphics Capture deliver HDR; nobody in the product class ships
HDR screen recording end-to-end.
Goal: HDR capture in the P4 shell → HDR10/HLG export with a tone-mapped SDR fallback; browsers stay
SDR until WebCodecs HDR hardens.
GATES: HDR take on an HDR display → file carries HDR metadata (probe evidence) and plays · SDR
fallback renders correctly · zero SDR-path regression.

## P8. safari-mic — sound survives a real Safari take  [P0 of the Safari lane; BLOCKED ON ROBERT ARTIFACT — do not code from theory]
Robert 2026-08-25, real Safari take: "mic sound got only couple second in middle and nothing else."
History that forbids guessing: the 2026-07 "fix" routed Apple WebKit audio through MediaRecorder
mp4/aac (session.ts isAppleWebKit gate, replacing the measured path that truncated at ~1 s) and was
verified only under a SPOOFED UA — which proves the code path runs, not that the sound survives.
Three capture "fixes" argued from code ordering have failed in the field in this project; this one
starts from evidence or not at all.
UNBLOCK = ONE artifact from Robert, either: (a) the exported file itself (ffprobe splits the question —
is the gap in the raw mic channel or manufactured by the export?), or (b) a Safari take with the
console open, copied out.
CANDIDATE CAUSES to test against the artifact, not before: Safari MediaRecorder timeslice chunking
(one blob at stop vs periodic chunks and how the durable writer orders them) · mediabunny misreading
Safari's mp4 sample tables at decode · a second consumer of the mic track (level meter / v1
composite Web Audio graph) silencing the recorder — Safari mutes an earlier mic consumer when a
later one attaches.
GATES: a real-Safari take (Robert's machine) plays full-length mic audio in the exported file ·
the fix names the mechanism with the artifact as evidence · Chromium mic path untouched (measured
path stays, tests green) · no spoofed-UA-only verification accepted.

═══ PHASE A — AI EXPORT (Robert 2026-08-24: "export for ai button is highest priority now"; max token
cutting; ONE FILE any AI understands, not human-watchable; AI2-AI7 are CANDIDATES reconsidered
only after Robert tests AI1's first real file; spec docs/AI_EXPORT.md) ═══

## AI1. export-for-ai — one file  [Robert priority, medium; READY]
Why: agents don't watch video — they sample frames and pay ~1 token per 750 pixels. Robert's bar:
ONE file that ANY AI understands (chat AIs included — no shell, no unzip), not human-watchable,
maximally token-cheap. The one-file format every major AI ingests natively as text+images is PDF;
zip needs a shell, markdown cannot carry images, base64-in-text explodes tokens, mp4 is the thing
agents cannot afford. Everything richer is AI2+ and must not creep into this task.
Goal: one "For AI" control in the export panel → `<take>-for-ai.pdf`:
  PAGE 1 — index, pure selectable text, ≤~200 tokens: duration, resolution, clock note, the event
  lines (v1 = capture facts only: channel on/off segments, stalls — all already in Recording, no
  new capture surface), and the keyframe list (timestamp + page number per line, so a paging agent
  descends selectively).
  THEN one pixel-delta keyframe per page: ≤1024 px full view, JPEG (PDF-native DCTDecode),
  timestamp caption as real text; a full-res crop of the changed region on the same page ONLY when
  that region is small (<~25 % of frame) — signal-derived, never a setting. Page dimensions equal
  the image: no pixel is padding, per-page cost predictable (~800 tokens at 1024×576).
  NO video track anywhere in the file. The ordinary MP4 stays a separate, unrelated export.
Design rules (decisions, recorded in the spec): the file FOLLOWS THE EDIT — keyframe and event
times remapped through the same output→recording mapping the render uses, cut content absent ·
all timestamps on the RECORDING EPOCH with clockOffsetMs declared on page 1 when the source
video's t=0 differs (P0-instant-sync's lesson: composite time is not recording time) · index is
signal-derived, never generated prose (the no-AI rule covers export-time summarization) ·
keyframe selection pure + unit-tested with a defined delta metric (share of changed pixels on a
downscaled luma diff) · THE CURSOR IS FILTERED BY THE DELTA'S OWN SHAPE, not by a knob (Robert raised
it: "it moves every frame but not very useful all the time"): tiny+moving+transient = cursor →
never a keyframe, logged as a low-rate pointer trail in the index text (v1's only interaction
signal while DOM events wait on AI2); tiny+stationary+blinking = caret → ignored; small but
PERSISTENT = real content (tooltip, menu, hover) → keyframe with the crop on it; large = content →
keyframes as normal. A change landing where the cursor last rested captions "change at cursor".
The FILTER ships unconditionally (a threshold); the TRAIL ships only if the rig proves the
detector reliable — report precision either way.
Implementation notes: tap composed output frames inside the machinery the render already iterates
(compose/render.ts loop, chosen via choose.ts) rather than building a second decode pipeline —
note 13: decode is the floor, nothing may decode the take twice · a minimal PDF writer is ours
and tiny (JPEG XObjects + built-in Helvetica text, a few hundred lines, zero deps) · write
through scratch (O1) so memory stays flat · src/core owns the builder, the app owns the button.
Constraints: no knobs in the UI · every other export path untouched, defaults unchanged · the
file must parse in strict readers (pdftotext/mutool clean) — "any AI" starts at "valid PDF".
GATES:
· any-AI ingestion proven: pdftotext extracts the full index text · the file opens in a real
  chat-AI upload (Robert's test) and page 1 answers "what is this recording" without descending
· token price printed at export: per-page (pixels/750 + text/4) and total, reported for a real take
· keyframe economy: a 60 s mostly-static synthetic take → ≤8 keyframe pages · a 10 s idle span
  emits 0 after its first · a motion burst concentrates keyframes inside it (distribution reported)
· cursor immunity (Robert's scenario, hard gate): a take that is ONLY a cursor wandering over a static
  screen → ≤1 keyframe after the first · a caret blinking in place → 0 · a tooltip appearing under
  the cursor → exactly 1, cropped on the tooltip · pointer-trail detector precision reported on the
  rig; the trail is dropped (not shipped degraded) if unreliable
· correctness by the existing rig: each of the oracle take's flashes appears in a keyframe whose
  caption time is within ±150 ms of the known flash time
· edit fidelity: a take with a cut → no keyframe from the cut span, times remapped (rig proof)
· cost: file build wall clock ≤1.5× the same take's full render (report ×)
· typecheck + tests + oracle green · instant-path logs unchanged.
ACCEPTANCE THAT UNBLOCKS THE REST OF PHASE A: Robert feeds a real take's file to an AI and reports.
AI2-AI7 stay frozen candidates until that report exists.
── HANDOFF 2026-08-24 (NINTH session, "go ai") — SHIPPED, ALL GATES GREEN.
SHIPPED, and src/core owns all of it (`src/core/ai/`, the app owns only the button):
   delta.ts    the metric — share of a 160×90 mean-luma grid whose cells moved by >10/255. Coarse on
               purpose: it kills codec noise (zero-mean inside a 12 px cell) and it is what separates
               a cursor (~8 cells) from a tooltip (~110) without a detector.
   select.ts   a PURE REDUCER over that metric, and the whole taxonomy. TWO diffs per sample: against
               the last emitted keyframe (how wrong the picture an agent already has is → the page
               decision) and against the previous sample (what is moving → cursor/caret). Pacing is
               derived from the take's own length (duration/60, clamped 0.5-15 s), so a long take
               cannot bankrupt the token budget and a held-back change is never lost — the reference
               has not advanced, so it fires at the next allowed instant.
   pdf.ts      a streaming PDF writer, ~330 lines, zero deps. JPEG XObjects (DCTDecode) + Helvetica.
               Image bytes go to disk as they are produced and only offsets are kept, so a 200-page
               file never sits in the heap; the small objects and the xref are written at close, which
               is also how the index can be page ONE while being authored LAST.
   indexText.ts  page 1, signal-derived only (durations, channel windows, cuts, speed, stalls,
               keyframe table with page numbers, pointer trail). No prose, ever — the no-AI rule.
   build.ts    the builder: composes the take at 4 samples/s through the SAME primitives the render
               uses (compose/video.ts readers + compose/layout.ts drawVideoFrame), measures, and keeps
               what changed. No second decode pipeline (note 13), no encode, no audio, no muxer.
   types.ts gains ExportResult.ai {pages, keyframes, approxTokens} so the result panel can say "3
               pages · ~2k tokens" instead of describing a PDF as a video.
MEASURED (`node scripts/ai-pdf-check.mjs`, full run, dev machine, artefact docs/qa/ai-export.pdf):
   economy      60 s take (still, 5 s motion burst, still) → 7 keyframes / 8 pages / ~5.6k tokens;
                6 of the 7 land inside the burst, longest gap 35 s, one page in the first 19 s.
   cursor       12 s of a cursor wandering a still page → 1 page (the first) out of 48 samples;
                47 samples classified cursor. Caret: 1 page. Tooltip: exactly 2 (first + tooltip),
                the tooltip page cropped, at 4.5 s.
   clock        every page's picture read BACK OUT of the PDF (JPEGs scanned from the bytes, the rig's
                fiducial timecode decoded off the picture): 20/20 pages, caption−picture median
                −577 ms with MAX DEVIATION 9 ms, and 0/20 pages differ from the channel decoded
                independently at the same instants. The median is the recording's own epoch mapping,
                not ours; the take's FIRST page is excluded and named — a canvas captureStream's first
                encoded frame holds what was painted before the recorder existed, confirmed identical
                on the channel itself, and every export path inherits it.
   edit         cut take: 0 of 20 pages come from the cut span, times remapped.
   cost         880 ms against the same take's full render at 1750 ms = 0.50× (gate ≤1.5×). In the
                real app a 2 s take: 219 ms, decode 99 / draw 84 / jpeg 16 — decode is still the floor.
   price        printed per page and in total at every export ([ai] console line): e.g. 788 + 1077
                tokens. 12 s fiducial take ~16.8k tokens vs ~283k for the same take as video frames.
   readers      an INDEPENDENT parser (src/experimental/tools/pdf-verify.mjs — goes through the xref
                like a reader, not by regex) AND Apple PDFKit both accept the file and extract the
                index text. pdftotext/mutool/qpdf are all absent on this machine and installing one is
                a download Robert owns, so PDFKit is the third-party reader; it agrees on the page count.
THE POINTER TRAIL SHIPS, and the number is why: 100 % of confident readings land within 5 % of the
   frame of the path the rig drew (median error 0.007 of the frame). The spec said drop it if the
   detector was unreliable; it is not. `POINTER_TRAIL_ENABLED` in build.ts is the switch if a real
   take ever disagrees.
TWO DELIBERATE DEVIATIONS FROM THE SPEC, both recorded as decisions:
   (a) the caption is a 22 pt BAND ABOVE the picture, not text drawn on it. On the image it cost no
       page area and covered the top ~20 px of the frame — which on a screen recording is the tab
       strip and the menu bar. 20 pt of page (~30 tokens) is the cheaper mistake.
   (b) the build runs ON THE MAIN THREAD with the decoder's own awaits as its yields, not in the
       export worker. It is a new path; the worker's one-shot fallback protocol is shaped for
       ExportResult+RenderStats and generalizing it is real surface on the certified export. The
       measured cost is half a render, so this is not a throughput decision — if Robert finds the UI janky
       on a long take, the worker is the obvious next move.
NOT DONE, and none of it is in AI1's scope: DOM events (AI2 needs a capture vehicle — Robert's choice),
   transcript (AI3, Robert-gated), a "change at cursor" caption on real takes (the code is in and unit
   tested; the rig's synthetic tooltip is the only case that has exercised it).
TOOLING: `npm run exp -- aiexport` (the rig alone) · `node scripts/ai-pdf-check.mjs` (the rig + both
   readers + the saved artefact; `--reuse=<report.json>` re-runs the reader gates with no recording).
── HANDOFF 2026-08-24 (same session, after Robert's FIRST REAL TAKE) — THE FILE WAS BOTH UNREADABLE AND
   TOO SPARSE, AND BOTH WERE CALIBRATION, NOT DESIGN. Robert exported a 97 s walkthrough of a real
   product UI and gave it to an agent TO RECREATE THE UI AND ITS ANIMATIONS. Two verdicts came back.
(1) "ai didnt understood its video" — the AI opened the PDF AND ASKED WHAT TO DO WITH IT. Page 1
   opened with machine facts (`clock: recording epoch`, `keyframes are pixel-delta selected`), which
   is a config dump, not a document. FIXED: the index now briefs its reader before it states
   anything — what the file is, that the pages after it are frames of ONE recording in time order,
   why the spacing is uneven, and what to do if it arrived with no instructions. Same line in the PDF
   /Title and /Subject and in every page caption ("INOUT screen recording - frame 19 at t=12.50s of
   97.27s"), so a page read alone still says what it belongs to. NOT a no-AI-rule violation: it
   describes the DOCUMENT, never the recording.
(2) "it losses way too much frames" — and the diff against the MP4 proved it: page 7 was t=12.25 s
   with the field empty, page 8 was t=17.75 s on a different tab, so the typing, the button turning
   active, the click and the tab transition were all inside one 5.5 s gap. THREE CAUSES, all fixed
   and all re-measured ON THAT SAME RECORDING (the rig gained a real-file mode: drop an mp4 in
   public/__*, `npm run exp -- aiexport '{"realOnly":true}'`; .gitignore keeps it out of the repo):
   · A TYPED WORD IS ~15 GRID CELLS and the content threshold was 36 — set that high only to keep a
     moving cursor out. The pointer is now MASKED OUT of the content metric (delta.pointerMask, cells
     around where the pointer is and where it was at the reference frame), so the threshold dropped
     to 17 cells and the cursor gate still passes: 1 page out of 96 samples of a wandering cursor.
   · AN ANIMATION BECAME ONE PAGE, because the pace allowed one page per 1.6 s. Motion now SUSPENDS
     the pace — while the picture keeps moving between looks, every look is a page — with two bounds
     that matter: a burst needs TWO consecutive moving samples (one change is an event, e.g. a
     tooltip, which keeps its close-up instead of being treated as animation) and it is capped at
     1.2 s (a scroll is not a transition).
   · 250 ms WAS THE FINEST ANYTHING COULD BE SEEN. Now 8 looks a second; the decoder was already
     walking every frame (note 13) so it costs one downscale and one diff more per second.
THE PAGE LIMIT WAS WRONG, AND ROBERT CAUGHT IT ("if pdf is limited than its fucked up format"). The
   budget had been capped at 96 frames on a recalled "readers reject a PDF past 100 pages". CHECKED
   2026-08-24 instead: Claude accepts 32 MB and 600 PAGES on 1M-context models — the 100-page figure
   is the 200k-context one — and Gemini accepts 50 MB / 1000 pages at a flat 258 tokens per page. The
   format was never the constraint; the binding cost is TOKENS (~800 per full-view page). So the
   budget became a spend decision derived from the take's own length: 2.5 frames per second, floor
   96, ceiling 300 (≈236k tokens, ≈11 MB), with a hard stop at 28 MB so a pathological recording
   degrades by stopping rather than by being rejected. On Robert's take the budget stopped binding at
   all — the content earned 165 frames of an allowed 243. LESSON, and it is note 10's shape again:
   the instrument was a REMEMBERED NUMBER. Verify a platform limit before designing around it.
   Frames are still SPENT by a pace derived from what is LEFT of the take and the budget, plus a
   burst credit that SHRINKS TO ZERO by the end — the first version of that controller spent Robert's
   take out at 84 s of 97 and the last thirteen seconds were not in the file at all.
MEASURED ON ROBERT'S OWN 97 s TAKE, before → after: 39 frames → 165 · worst gap 5.5 s → 2.9 s · median
   gap 2.5 s → 0.4 s · covered to 97.1 s of 97.3 · 167 pages · ~144k tokens · 6.5 MB · built in
   5.4 s (0.74× the same take's full render). The typing at 12.5 s is now several frames, mid-word,
   with the button caught mid-transition. Rebuilt file left for Robert at
   ~/Downloads/inout-20260824-183853-for-ai-v2.pdf.
GATE CHANGED, and it is worth knowing why: "60 s mostly-static take ≤8 pages" was DELETED. It passed
   by summarizing a motion burst into one page, which is exactly the defect Robert reported. What
   replaced it: the file fits what the readers ACTUALLY accept (≤600 pages, ≤30 MB) · the pages go
   where the motion is AND ARE DENSE THERE (≥12 inside a 5 s burst) · a REAL recording is covered end
   to end (median gap ≤1.5 s, worst ≤5 s, last frame past 90 % of the take). Everything else still
   passes unchanged, including all three cursor gates and the clock read back out of the file (max
   deviation 17 ms).
STILL OPEN, honestly: a 97 s take costs ~144k tokens — fine for a 1M-context model, tight for a 200k
   one, and 43k on Gemini's flat per-page pricing. The lever if that is ever too much is NOT fewer
   moments: it is cheaper pages — a contact sheet of 4-6 small frames per page for motion runs costs
   ~200 tokens per frame instead of ~800, because the page limit is generous and the token bill is
   what hurts. Not built; it is the obvious next move if Robert wants the same density for a quarter of
   the price.

## AI2. event-sidecar — the DOM events behind the pixels  [CANDIDATE — frozen until Robert tests
AI1's first real file; then also needs Robert vehicle decision below]
The original spec's core claim, and it needs a capture vehicle: a web page sees DOM events only in
its own tab — no browser API exposes the input events of a captured surface. Robert picks: (a)
companion extension (other tabs, not native apps) · (b) SDK snippet in the recorded page (the
"developers record their own product" market) · (c) wait for P4's desktop shell (global
cursor/clicks natively — richest, latest; P4 already specs a cursor/click metadata track, AI2 is
its browser-side sibling). Schema ready in docs/AI_EXPORT.md: click/key/scroll/nav/focus +
role/name/bbox; visible text in ordinary fields, count-only in password/sensitive fields. Events
land in events.jsonl and drive full-res crops around the click point.
GATES (once unblocked): sidecar click t vs the oracle's pixel flash inside the sync band · the
sensitive-field rule proven by test · AI1's bundle format only extended, never changed.

## AI3. transcript — VTT with timecodes  [CANDIDATE; Robert-GATED per the no-AI rule: ASR is a local model]
Silence/filler auto-stripped, original timestamps kept. Do not build without explicit per-instance
Robert approval.

## AI4. repro-bundle — console errors + failed requests, timestamped  [CANDIDATE; after AI2 — same vehicle]

## AI5. marker-key — press-to-mark during recording  [CANDIDATE; light; lands in the index's event lines]

## AI6. addressable-recordings — MCP server / agent pulls a take by reference  [CANDIDATE; after cloud]

## AI7. pii-redaction  [CANDIDATE; after AI2 — events name what is sensitive]
