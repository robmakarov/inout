node_modules/
dist/
.env
.env.*
!.env.example
*.local
.DS_Store
preview.log
.claude/settings.local.json
inout-*.mp4

# Robert recordings dropped in for a rig run — never committed.
public/__*

# the proto captures its own snapshots; a cache, not a source
proto/.app-capture.json
